"""GMEOW models — slice GMEOW Gender Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/gender
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class GenderEnum(StrEnum):
    genderAgender = "gmeow:genderAgender"
    genderBigender = "gmeow:genderBigender"
    genderDemiboy = "gmeow:genderDemiboy"
    genderDemigirl = "gmeow:genderDemigirl"
    genderGenderfluid = "gmeow:genderGenderfluid"
    genderGenderqueer = "gmeow:genderGenderqueer"
    genderMan = "gmeow:genderMan"
    genderNonBinary = "gmeow:genderNonBinary"
    genderQuestioning = "gmeow:genderQuestioning"
    genderTwoSpirit = "gmeow:genderTwoSpirit"
    genderWoman = "gmeow:genderWoman"


class GenderExpressionStyleEnum(StrEnum):
    expressionAndrogynous = "gmeow:expressionAndrogynous"
    expressionFeminine = "gmeow:expressionFeminine"
    expressionFluid = "gmeow:expressionFluid"
    expressionMasculine = "gmeow:expressionMasculine"
    expressionNeutral = "gmeow:expressionNeutral"


class SexAssignedAtBirthEnum(StrEnum):
    saabFemale = "gmeow:saabFemale"
    saabIntersex = "gmeow:saabIntersex"
    saabMale = "gmeow:saabMale"
    saabUnknown = "gmeow:saabUnknown"


class Gender(ConfiguredBaseModel):
    """Gender.

    A gender a person may identify with — a VALUE, never a gmeow:Person subclass. The set is
    open and culturally diverse; a gender not among the seed individuals is a FRESH
    gmeow:Gender individual carrying rdfs:label, not a new class. Pointed at by
    gmeow:genderValue.

    When to use:
        - Use as the open value vocabulary for the gender a gmeow:genderValue points at; mint a
          fresh individual with rdfs:label when no seeded value fits the culture or identity.

    When to avoid:
        - Avoid modelling a gender as a gmeow:Person subclass or a forced enum, and avoid treating
          the binary seeds as a default — every value is co-equal (Principle 9), so none is
          primary.

    How to use:
        - Reference a seeded gmeow:Gender individual or add a fresh one carrying rdfs:label and
          the GSSO/Homosaurus alignments; expose it only through a gmeow:GenderIdentity facet,
          never a flat literal.

    Examples:
        - ex:genderXochihua a gmeow:Gender ; rdfs:label "xochihua"@en .

    Usage:
        >>> from gmeow_models.gender import Gender
        >>> Gender.model_config["json_schema_extra"]["curie"]
        'gmeow:Gender'

    IRI:    https://blackcatinformatics.ca/gmeow/Gender
    CURIE:  gmeow:Gender

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Gender", "curie": "gmeow:Gender", "definitionDigest": "blake3:1a32ee49bfa9dd3cdbfa2a758239cdb95930699f9c5a998cef5f626baafcc865", "iri": "https://blackcatinformatics.ca/gmeow/Gender"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class IdentityFacet(ConfiguredBaseModel):
    """Identity Facet.

    A reified, SELF-ASSERTED claim about an aspect of a person's identity — an observation
    in the universal claim stack (observation-spine bridge): gender identity, gender
    expression, or (in the sexuality module) sexual/romantic orientation — mediating the
    person (facetSubject / observedFeature) and an open identity value. A gufo:Relator and a
    gmeow:Observation, the same idiom as gmeow:NameUsage. A person bears MANY co-equal
    facets (e.g. bigender); none is privileged, there is no preferred/primary marker, and a
    superseded facet is kept with gmeow:displayable false rather than deleted.
    Self-assertion (gmeow:selfAsserted) is the top authority; the optional validity period
    is carried by gmeow:validFrom/validUntil on the facet.

    When to use:
        - Use as the shared reification base whenever an identity claim must be first-class —
          carrying who asserted it, a validity period, a sensitivity level, and display control —
          rather than flattened into an attribute; the parent the concrete gender, expression, and
          orientation facets specialize.

    When to avoid:
        - Avoid typing an instance as a bare gmeow:IdentityFacet when a concrete subkind
          (GenderIdentity, GenderExpression, SexualOrientation) applies, and avoid using it for
          the recorded sex-assigned-at-birth datum, which is deliberately NOT a facet.

    How to use:
        - Mint the facet, link the person with gmeow:facetSubject and the asserting agent with
          gmeow:facetVantage, set gmeow:selfAsserted, point at the open value through the
          subkind's value property, and bound it with gmeow:validFrom/validUntil; mark a
          superseded facet gmeow:displayable false rather than deleting it.

    Examples:
        - ex:facet a gmeow:IdentityFacet ; gmeow:facetSubject ex:lillith ; gmeow:selfAsserted true
          ; gmeow:hasSensitivity gmeow:sensitivitySensitivePersonal .

    Usage:
        >>> from gmeow_models.gender import IdentityFacet
        >>> IdentityFacet.model_config["json_schema_extra"]["curie"]
        'gmeow:IdentityFacet'

    IRI:    https://blackcatinformatics.ca/gmeow/IdentityFacet
    CURIE:  gmeow:IdentityFacet

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/IdentityFacet", "curie": "gmeow:IdentityFacet", "definitionDigest": "blake3:c13643eeb288439e5726e761af167ed046a1650d5df8976c04037395ca9bf475", "iri": "https://blackcatinformatics.ca/gmeow/IdentityFacet"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    facetSubject: Person = Field(description="The person an identity facet (gender identity, gender expression, sexual/romantic orientation) is about — the observedFeature of the claim. Domain is gmeow:Observation so it applies to any observation subclass; the shape narrows to IdentityFacet instances.", alias="gmeow:facetSubject")
    facetVantage: Agent = Field(description="The agent asserting an identity facet — the vantage of the claim. When gmeow:selfAsserted is true, the facetVantage is the person themselves (Principle 9: self-assertion is top authority).", alias="gmeow:facetVantage")


class GenderExpression(IdentityFacet):
    """Gender Expression.

    A person's SELF-ASSERTED gender expression — how they present (a
    gmeow:GenderExpressionStyle value via gmeow:expressionValue). A distinct axis from
    gender identity: expression is never inferred from identity (or vice versa), nor from
    sex or address.

    When to use:
        - Use to record how a person presents — the expression axis — as a self-asserted facet
          pointing at an open gmeow:GenderExpressionStyle value, held separately from what they
          identify as.

    When to avoid:
        - Avoid conflating it with gender identity (gmeow:GenderIdentity) or reading either from
          the other, and avoid inferring it from sex-assigned-at-birth, pronouns, or honorifics.

    How to use:
        - Attach via gmeow:hasGenderExpression and set gmeow:expressionValue to a seeded or fresh
          gmeow:GenderExpressionStyle individual; keep it on its own facet so identity and
          expression remain orthogonal.

    Examples:
        - ex:ge a gmeow:GenderExpression ; gmeow:expressionValue gmeow:expressionAndrogynous ;
          gmeow:selfAsserted true .

    Usage:
        >>> from gmeow_models.gender import GenderExpression
        >>> GenderExpression.model_config["json_schema_extra"]["curie"]
        'gmeow:GenderExpression'

    IRI:    https://blackcatinformatics.ca/gmeow/GenderExpression
    CURIE:  gmeow:GenderExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GenderExpression", "curie": "gmeow:GenderExpression", "definitionDigest": "blake3:c1e15f7e0c92f38facc9aa0e64210a721dff102413691d38f108a568585fea31", "iri": "https://blackcatinformatics.ca/gmeow/GenderExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    expressionValue: GenderExpressionStyle = Field(description="The gmeow:GenderExpressionStyle value a gender-expression facet asserts (functional per facet). A predefined individual or a fresh one with rdfs:label; the single path to the value.", alias="gmeow:expressionValue")
    facetSubject: str = Field(description="The person an identity facet (gender identity, gender expression, sexual/romantic orientation) is about — the observedFeature of the claim. Domain is gmeow:Observation so it applies to any observation subclass; the shape narrows to IdentityFacet instances.", alias="gmeow:facetSubject")
    facetVantage: str = Field(description="The agent asserting an identity facet — the vantage of the claim. When gmeow:selfAsserted is true, the facetVantage is the person themselves (Principle 9: self-assertion is top authority).", alias="gmeow:facetVantage")


class GenderExpressionStyle(ConfiguredBaseModel):
    """Gender Expression Style.

    A style of gender expression — a VALUE pointed at by gmeow:expressionValue, never a
    subclass. Open: a style not seeded here is a fresh individual with rdfs:label.

    When to use:
        - Use as the open value vocabulary for the presentation style a gmeow:expressionValue
          points at; mint a fresh individual with rdfs:label when no seeded style fits.

    When to avoid:
        - Avoid modelling a style as a subclass or a forced enum, and avoid treating any seeded
          style as a default; the values are co-equal (Principle 9).

    How to use:
        - Reference a seeded gmeow:GenderExpressionStyle individual or add a fresh one with
          rdfs:label; expose it only through a gmeow:GenderExpression facet.

    Examples:
        - ex:expressionButch a gmeow:GenderExpressionStyle ; rdfs:label "butch"@en .

    Usage:
        >>> from gmeow_models.gender import GenderExpressionStyle
        >>> GenderExpressionStyle.model_config["json_schema_extra"]["curie"]
        'gmeow:GenderExpressionStyle'

    IRI:    https://blackcatinformatics.ca/gmeow/GenderExpressionStyle
    CURIE:  gmeow:GenderExpressionStyle

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GenderExpressionStyle", "curie": "gmeow:GenderExpressionStyle", "definitionDigest": "blake3:83e96d92f4f31e6b3fbccfde5254f9c31b4b35abf4760cb9dbfc375440027000", "iri": "https://blackcatinformatics.ca/gmeow/GenderExpressionStyle"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class GenderIdentity(IdentityFacet):
    """Gender Identity.

    A person's SELF-ASSERTED gender identity — what they are — as a reified facet pointing
    (via gmeow:genderValue) to an open gmeow:Gender value. Independent of
    pronouns/honorifics (address), of gender expression, and of sex-assigned-at-birth;
    nothing infers any of these from another. Multiple co-equal GenderIdentity facets
    express bigender/genderfluid/transition without privileging any.

    When to use:
        - Use to record what a person identifies as, as a self-asserted facet pointing at an open
          gmeow:Gender value; mint one facet per co-equal identity so bigender, genderfluid, or a
          transition are all expressible without privileging any.

    When to avoid:
        - Avoid for how a person presents (gmeow:GenderExpression), for how they are addressed
          (pronouns/honorifics in names), and for the recorded sex datum
          (gmeow:sexAssignedAtBirth); never infer one of these from another.

    How to use:
        - Attach via gmeow:hasGenderIdentity, set gmeow:genderValue to a seeded or freshly minted
          gmeow:Gender individual, and carry gmeow:hasDeterminacy when the identity is fuzzy or
          fluid; supersede a label with gmeow:displayable false, never deletion.

    Examples:
        - ex:gi a gmeow:GenderIdentity ; gmeow:genderValue gmeow:genderNonBinary ;
          gmeow:selfAsserted true ; gmeow:hasDeterminacy gmeow:determinacyFuzzy .

    Usage:
        >>> from gmeow_models.gender import GenderIdentity
        >>> GenderIdentity.model_config["json_schema_extra"]["curie"]
        'gmeow:GenderIdentity'

    IRI:    https://blackcatinformatics.ca/gmeow/GenderIdentity
    CURIE:  gmeow:GenderIdentity

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GenderIdentity", "curie": "gmeow:GenderIdentity", "definitionDigest": "blake3:b5e61fe00783d6fc1261589d8b8de47e897e0ee9058a800e271165f61ce2f94d", "iri": "https://blackcatinformatics.ca/gmeow/GenderIdentity"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    facetSubject: str = Field(description="The person an identity facet (gender identity, gender expression, sexual/romantic orientation) is about — the observedFeature of the claim. Domain is gmeow:Observation so it applies to any observation subclass; the shape narrows to IdentityFacet instances.", alias="gmeow:facetSubject")
    facetVantage: str = Field(description="The agent asserting an identity facet — the vantage of the claim. When gmeow:selfAsserted is true, the facetVantage is the person themselves (Principle 9: self-assertion is top authority).", alias="gmeow:facetVantage")
    genderValue: Gender = Field(description="The gmeow:Gender value a gender-identity facet asserts (functional PER FACET — one value each; multiplicity is expressed by multiple facets). The value is a gmeow:Gender individual: a predefined one, or — when none fits — a fresh individual carrying rdfs:label (the custom-PronounSet idiom). The single path to the gender value; there is deliberately no flat datatype shortcut.", alias="gmeow:genderValue")


class SexAssignedAtBirth(ConfiguredBaseModel):
    """Sex Assigned at Birth.

    A recorded sex-assigned-at-birth value pointed at by gmeow:sexAssignedAtBirth — a VALUE,
    never a subclass. Deliberately coarse and inclusive (incl. intersex); finer
    sex-characteristic detail is an optional free-text gmeow:intersexVariation note, not a
    clinical taxonomy.

    When to use:
        - Use as the deliberately coarse value vocabulary for the recorded sex-assigned-at-birth
          datum (female, male, intersex, unknown).

    When to avoid:
        - Avoid using it as gender identity/expression or as a clinical sex-characteristic
          taxonomy, and avoid adding finer medical values; use the free-text
          gmeow:intersexVariation note instead.

    How to use:
        - Reference a seeded gmeow:SexAssignedAtBirth individual from gmeow:sexAssignedAtBirth;
          treat the datum as sensitive personal data and keep it out of public projections.

    Examples:
        - ex:lillith gmeow:sexAssignedAtBirth gmeow:saabIntersex .

    Usage:
        >>> from gmeow_models.gender import SexAssignedAtBirth
        >>> SexAssignedAtBirth.model_config["json_schema_extra"]["curie"]
        'gmeow:SexAssignedAtBirth'

    IRI:    https://blackcatinformatics.ca/gmeow/SexAssignedAtBirth
    CURIE:  gmeow:SexAssignedAtBirth
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/sexassignedatbirth-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/SexAssignedAtBirth", "curie": "gmeow:SexAssignedAtBirth", "definitionDigest": "blake3:bba4b46c545a67fe3a6173b258611d7c660f3421475fd4988834629ce30447b7", "iri": "https://blackcatinformatics.ca/gmeow/SexAssignedAtBirth"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

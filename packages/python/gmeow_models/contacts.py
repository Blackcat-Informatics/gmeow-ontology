"""GMEOW models — slice GMEOW Contacts Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/contacts
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ContactPointTypeEnum(StrEnum):
    contactPointTypePersonal = "gmeow:contactPointTypePersonal"
    contactPointTypePersonalDomain = "gmeow:contactPointTypePersonalDomain"
    contactPointTypeSupport = "gmeow:contactPointTypeSupport"
    contactPointTypeWork = "gmeow:contactPointTypeWork"


class ContactPoint(ConfiguredBaseModel):
    """Contact Point.

    A means of reaching an agent: an email address, telephone number, postal address, or
    instant-messaging handle.

    When to use:
        - Use as the parent domain when a fact concerns a channel for reaching an agent rather
          than the agent itself — the anchor for contact-point usage, provider, and tenure, and
          the carrier of contact-data sensitivity.

    When to avoid:
        - Avoid typing a bare gmeow:ContactPoint when a more specific channel applies (use
          gmeow:EmailAddress, gmeow:TelephoneNumber, gmeow:PostalAddress, or
          gmeow:InstantMessageAddress) and avoid using it for the agent that owns the channel
          (that is gmeow:Agent).

    How to use:
        - Type the channel as its most specific subkind, link it to its owner with
          gmeow:hasContactPoint, and attach gmeow:hasSensitivity so the projection layer can
          withhold or coarsen it for the wrong consumer.

    Examples:
        - ex:lillithEmail a gmeow:ContactPoint ; gmeow:hasSensitivity
          gmeow:sensitivitySensitivePersonal .

    Usage:
        >>> from gmeow_models.contacts import ContactPoint
        >>> ContactPoint.model_config["json_schema_extra"]["curie"]
        'gmeow:ContactPoint'

    IRI:    https://blackcatinformatics.ca/gmeow/ContactPoint
    CURIE:  gmeow:ContactPoint

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ContactPoint", "curie": "gmeow:ContactPoint", "definitionDigest": "blake3:6791f4d2288423d42bfb097f3730063230ff610cc7b2c2badb9f7a487f222321", "iri": "https://blackcatinformatics.ca/gmeow/ContactPoint"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class InterpersonalRelationship(ConfiguredBaseModel):
    """Interpersonal Relationship.

    A reified standing relationship between agents (acquaintance, collaboration, …), able to
    bear its own time interval, confidence, and source evidence.

    When to use:
        - Use when a standing tie between agents must be first-class — carrying its own interval,
          confidence, or source evidence — beyond what the flat gmeow:hasMet/hasWorkedWith
          shortcuts can hold.

    When to avoid:
        - Avoid it for the bare symmetric fact (use the flat shortcuts) and avoid using it for a
          contact channel or an address tenure (those are gmeow:ContactPoint and
          gmeow:AddressTenure).

    How to use:
        - Mint the relator, bind its agents with gmeow:relationshipParty (at least one is required
          by axiom), and carry its span with gmeow:relationshipInterval; specialize to
          Professional or Acquaintance where the kind is known.

    Examples:
        - ex:tie a gmeow:InterpersonalRelationship ; gmeow:relationshipParty ex:lillith, ex:morgan
          .

    Usage:
        >>> from gmeow_models.contacts import InterpersonalRelationship
        >>> InterpersonalRelationship.model_config["json_schema_extra"]["curie"]
        'gmeow:InterpersonalRelationship'

    IRI:    https://blackcatinformatics.ca/gmeow/InterpersonalRelationship
    CURIE:  gmeow:InterpersonalRelationship

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InterpersonalRelationship", "curie": "gmeow:InterpersonalRelationship", "definitionDigest": "blake3:43e0132fa7499a888d98e28b73d64f37bc28c036d8d15dba94c6a61f9f1c3da4", "iri": "https://blackcatinformatics.ca/gmeow/InterpersonalRelationship"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    relationshipParty: list[Agent] | None = Field(default=None, description="An agent who is one of the parties to an interpersonal relationship. Non-functional: a relationship typically binds two parties (and is left open for group ties).", alias="gmeow:relationshipParty")


class PostalAddress(ContactPoint):
    """Postal Address.

    A contact point reachable by physical mail at a postal address. It is expressed in the
    postal/administrative topological reference frame (gmeow:referenceFramePostalAddress);
    its components below are the coordinate values along that frame's axes — the as-written
    surface form. The resolved, identifier-bearing geographic places are reached via
    gmeow:addressPlace and gmeow:containedInPlace.

    When to use:
        - Use for the as-written surface form of a mailing address — the bundle of street,
          locality, region, code, and country coordinate values expressed in the postal reference
          frame, before resolution to identifier-bearing places.

    When to avoid:
        - Avoid for the resolved geographic place an address denotes (use gmeow:addressPlace →
          gmeow:Place) and for coordinates/geometry, which belong to that place, not to the
          surface form.

    How to use:
        - Fill the component coordinate properties along the frame's axes, seam to the resolved
          hierarchy with gmeow:addressPlace, and mark gmeow:coarsenTo (e.g. to city level) so a
          public projection emits an enclosing locality rather than the exact premises.

    Examples:
        - ex:home a gmeow:PostalAddress ; gmeow:addressLocality "Calgary" ; gmeow:coarsenTo
          ex:cityLevel .

    Usage:
        >>> from gmeow_models.contacts import PostalAddress
        >>> PostalAddress.model_config["json_schema_extra"]["curie"]
        'gmeow:PostalAddress'

    IRI:    https://blackcatinformatics.ca/gmeow/PostalAddress
    CURIE:  gmeow:PostalAddress

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/PostalAddress", "curie": "gmeow:PostalAddress", "definitionDigest": "blake3:365ddf6875c3f40769c182227a4768adec340e5e3868f682f83df1fc4a4218bd", "iri": "https://blackcatinformatics.ca/gmeow/PostalAddress"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    postalAddressFrame: str = Field(description="The postal/administrative reference frame in which this address is expressed. Functional: an address is expressed in exactly one frame. The default is gmeow:referenceFramePostalAddress.", alias="gmeow:postalAddressFrame")

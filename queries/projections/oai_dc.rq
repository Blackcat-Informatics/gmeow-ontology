# Projection: GMEOW → pure oai_dc. GENERATED by `gmeow regenerate` (mappings) from mapping-dsl/ — DO NOT EDIT.
# Lossy and directional by design; drops: dcterms:created / modified / issued / valid / available / dateAccepted / dateCopyrighted / dateSubmitted all collapse to dc:date; dcterms:hasPart / isPartOf / references / isReferencedBy / requires / isRequiredBy / replaces / isReplacedBy / hasVersion / isVersionOf / conformsTo all collapse to dc:relation; dcterms:license / rightsHolder / accessRights all collapse to dc:rights; dcterms:spatial / temporal collapse to dc:coverage; GMEOW containedInPlace is the nearest flat spatial hook; GMEOW hasFormat lives on Manifestation, not Work; the projection traverses embodies; GMEOW has no flat publisher property; the projection traverses wasGeneratedBy→wasAssociatedWith and is lossy; all date refinements (created/modified/issued/available/accepted/submitted) collapse to dc:date; all date refinements collapse to dc:date; abstract/tableOfContents collapse to dc:description; all relation refinements collapse to dc:relation; spatial/temporal coverage refinements collapse to dc:coverage; bibliographicCitation has no direct 15-element mapping; falls to dc:relation; extent/medium collapse to dc:format; audience has no direct 15-element mapping; falls to dc:description; provenance falls to dc:source in the 15-element dumb-down; license/rightsHolder/accessRights all collapse to dc:rights.
PREFIX gmeow: <https://blackcatinformatics.ca/gmeow/>
PREFIX dc: <http://purl.org/dc/elements/1.1/>

CONSTRUCT {
    ?work dc:title ?title .
    ?work dc:creator ?author .
    ?work dc:contributor ?contrib .
    ?work dc:subject ?topic .
    ?work dc:description ?_final_desc .
    ?work dc:date ?date .
    ?work dc:identifier ?id .
    ?work dc:source ?src .
    ?work dc:language ?lang .
    ?work dc:relation ?rel .
    ?work dc:rights ?lic .
    ?work dc:coverage ?cov .
    ?work dc:format ?fmt .
    ?work dc:publisher ?pub .
    ?work dc:description ?val .
    ?work dc:description ?desc .
    ?work dc:rights ?rights .
}
WHERE {
    {
        ?work gmeow:title ?title .
    }
    UNION {
        ?work gmeow:hasAuthor ?author .
    }
    UNION {
        ?work gmeow:hasContributor ?contrib .
    }
    UNION {
        ?work gmeow:isAbout ?topic .
    }
    UNION {
        ?work gmeow:description ?desc .
        OPTIONAL {
            ?_lang gmeow:languageTag ?_intTag .
            FILTER(isLiteral(?desc) && LANG(?desc) = ?_intTag)
            ?_lang gmeow:bcp47Tag ?_extTag .
        }
        BIND ( IF(BOUND(?_extTag), STRLANG(STR(?desc), STR(?_extTag)), ?desc) AS ?_final_desc )
    }
    UNION {
        ?work gmeow:datePublished ?date .
    }
    UNION {
        ?work gmeow:identifier ?id .
    }
    UNION {
        ?work gmeow:sourceLocation ?src .
    }
    UNION {
        ?work gmeow:hasAppellation|gmeow:hasName|gmeow:hasTitle ?name .
        ?name gmeow:nameLanguage ?lang .
    }
    UNION {
        ?work gmeow:hasPart ?rel .
    }
    UNION {
        ?work gmeow:partOf ?rel .
    }
    UNION {
        ?work gmeow:hasLicense ?lic .
    }
    UNION {
        ?work gmeow:containedInPlace ?cov .
    }
    UNION {
        ?work gmeow:embodiedIn ?manif .
        ?manif gmeow:hasFormat ?fmt .
    }
    UNION {
        ?work gmeow:wasGeneratedBy ?act .
        ?act gmeow:wasAssociatedWith ?pub .
    }
    UNION {
        ?work gmeow:dateCreated ?date .
    }
    UNION {
        ?work gmeow:dateModified ?date .
    }
    UNION {
        ?work gmeow:dateAvailable ?date .
    }
    UNION {
        ?work gmeow:dateAccepted ?date .
    }
    UNION {
        ?work gmeow:dateSubmitted ?date .
    }
    UNION {
        ?work gmeow:abstract ?val .
    }
    UNION {
        ?work gmeow:tableOfContents ?val .
    }
    UNION {
        ?work gmeow:references ?rel .
    }
    UNION {
        ?work gmeow:isReferencedBy ?rel .
    }
    UNION {
        ?work gmeow:requires ?rel .
    }
    UNION {
        ?work gmeow:isRequiredBy ?rel .
    }
    UNION {
        ?work gmeow:supersedes ?rel .
    }
    UNION {
        ?work gmeow:supersededBy ?rel .
    }
    UNION {
        ?work gmeow:hasVersion ?rel .
    }
    UNION {
        ?work gmeow:versionOf ?rel .
    }
    UNION {
        ?work gmeow:conformsTo ?rel .
    }
    UNION {
        ?work gmeow:spatialCoverage ?cov .
    }
    UNION {
        ?work gmeow:temporalCoverage ?cov .
    }
    UNION {
        ?work gmeow:bibliographicCitation ?rel .
    }
    UNION {
        ?work gmeow:extent ?fmt .
    }
    UNION {
        ?work gmeow:medium ?fmt .
    }
    UNION {
        ?work gmeow:audience ?desc .
    }
    UNION {
        ?work gmeow:provenance ?src .
    }
    UNION {
        ?work gmeow:conditionsOfAccess ?rights .
    }
}

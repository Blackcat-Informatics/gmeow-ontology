"""GMEOW models — slice GMEOW Mathematics.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/math
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class Math_PValueAlternativeSidednessEnum(StrEnum):
    math_exactTail = "math:exactTail"
    math_greaterAlternative = "math:greaterAlternative"
    math_lessAlternative = "math:lessAlternative"
    math_midPTail = "math:midPTail"
    math_oneSidedAlternative = "math:oneSidedAlternative"
    math_twoSidedAlternative = "math:twoSidedAlternative"


class Math_AlgebraicStructure(ConfiguredBaseModel):
    """Algebraic Structure.

    A set equipped with operations satisfying stated axioms: a math:underlyingSet, one or
    more math:structureOperation, an identity/inverse where they exist, and the
    math:satisfiesAxiom laws it obeys. A group is not a labelled set — a math:Group asserted
    without an operation and axioms is ill-formed. The root of the algebraic hierarchy
    math:Magma … math:Field; substructures are related through math:substructureOf.

    Usage:
        >>> from gmeow_models.math import Math_AlgebraicStructure
        >>> Math_AlgebraicStructure.model_config["json_schema_extra"]["curie"]
        'math:AlgebraicStructure'

    IRI:    https://blackcatinformatics.ca/math/AlgebraicStructure
    CURIE:  math:AlgebraicStructure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/AlgebraicStructure", "curie": "math:AlgebraicStructure", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/AlgebraicStructure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    satisfiesAxiom: list[Any] = Field(min_length=1, description="Relates an algebraic structure to a math:Axiom it obeys — associativity, commutativity, distributivity. Domain math:AlgebraicStructure, range math:Axiom (stated in prose). The axiom denotes a logic: formula the logic: reasoner owns: math: declares that the structure satisfies the law, logic: reasons over it.", alias="math:satisfiesAxiom")
    structureOperation: list[Any] | None = Field(default=None, description="Names a binary (or n-ary) operation of an algebraic structure — the group operation, or a ring's addition and multiplication. Domain math:AlgebraicStructure, range math:Operation (stated in prose). A ring carries two such operations; a structure with none is ill-formed.", alias="math:structureOperation")
    underlyingSet: list[Any] | None = Field(default=None, description="Names the carrier set of an algebraic structure — the elements the operations act on. Domain math:AlgebraicStructure, range math:Set (stated in prose). Every math:AlgebraicStructure declares one; a structure without a carrier is ill-formed.", alias="math:underlyingSet")


class Math_AnalyticProperty(ConfiguredBaseModel):
    """Analytic Property.

    A qualitative analytic property a function may bear — non-affinity, convexity,
    boundedness, smoothness. An OPEN vocabulary of math:NamedIndividuals (never an owl:oneOf
    enumeration); a function declares one through math:hasAnalyticProperty. Every analytic
    property MUST resolve through math:definingLaw to a real first-order logic:Formula
    (math:nonAffinity → math:nonAffinityLaw, math:convexity → math:convexityLaw,
    math:boundedness → math:boundednessLaw) OR — for a genuinely higher-order property — to
    an honest logic:expressivenessBoundary record (math:smoothness →
    math:smoothnessBoundary). A bare analytic-property claim backed by no law and no
    boundary is ill-formed (math:UnbackedAnalyticProperty): an analytic property is a
    quantified statement about the function's values, never a flag.

    Usage:
        >>> from gmeow_models.math import Math_AnalyticProperty
        >>> Math_AnalyticProperty.model_config["json_schema_extra"]["curie"]
        'math:AnalyticProperty'

    IRI:    https://blackcatinformatics.ca/math/AnalyticProperty
    CURIE:  math:AnalyticProperty

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/AnalyticProperty", "curie": "math:AnalyticProperty", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/AnalyticProperty"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_ApplicationExpression(ConfiguredBaseModel):
    """Application Expression.

    The application of an operator to operands: the expression node an arithmetic operation
    lowers to. Its single operator is named through math:operator; the indexed argument-slot
    machinery that fixes operand order belongs to the mathematical-core surface this bedrock
    builds on, and is deliberately not minted here.

    Usage:
        >>> from gmeow_models.math import Math_ApplicationExpression
        >>> Math_ApplicationExpression.model_config["json_schema_extra"]["curie"]
        'math:ApplicationExpression'

    IRI:    https://blackcatinformatics.ca/math/ApplicationExpression
    CURIE:  math:ApplicationExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ApplicationExpression", "curie": "math:ApplicationExpression", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ApplicationExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    operator: Any = Field(description="The single operator a math:ApplicationExpression applies: relates an application to the math:Operation (for example a math:ArithmeticOperation individual) it invokes. Domain math:ApplicationExpression, range math:Operation (stated in prose).", alias="math:operator")


class Math_ApproximateValue(ConfiguredBaseModel):
    """Approximate Value.

    A finite approximation of an exact number: a value that names, through
    math:approximates, the exact math:Number it stands in for and, through
    math:approximationError, how far it may deviate. It carries the concrete numeric literal
    (math:quantityValue) in a declared floating-point datatype (math:numericDatatype,
    subsuming xsd:double / xsd:float). An approximate value and the exact number it
    approximates are distinct objects and are never conflated — this is the object that
    holds the decimal so the exact number need not.

    Usage:
        >>> from gmeow_models.math import Math_ApproximateValue
        >>> Math_ApproximateValue.model_config["json_schema_extra"]["curie"]
        'math:ApproximateValue'

    IRI:    https://blackcatinformatics.ca/math/ApproximateValue
    CURIE:  math:ApproximateValue

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ApproximateValue", "curie": "math:ApproximateValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ApproximateValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    approximates: list[Any] | None = Field(default=None, description="Names the exact number that an approximation stands in for. Domain math:ApproximateValue, range math:Number (stated in prose). The approximation and the exact number are distinct objects and are never conflated.", alias="math:approximates")
    approximationError: list[Any] = Field(min_length=1, description="The magnitude by which an approximation may deviate from the exact value it approximates — an absolute error bound. Domain math:ApproximateValue, range a numeric literal (stated in prose).", alias="math:approximationError")


class Math_ArgumentSlot(ConfiguredBaseModel):
    """Argument Slot.

    One indexed operand position of a math:ApplicationExpression or math:BindingExpression:
    it carries exactly one integer math:slotIndex and exactly one math:slotExpression.
    Operand order is carried by these slots, not by RDF list ordering — swapping two slots'
    expressions is a different expression, so non-commutative operators (matrix product,
    function composition, subtraction, the body of a binder) are order-safe. Slot indexes
    are unique within one application; strict canonical mode additionally requires them
    zero-based and contiguous. A slot missing its index or expression is ill-formed
    (math:MalformedArgumentSlot).

    Usage:
        >>> from gmeow_models.math import Math_ArgumentSlot
        >>> Math_ArgumentSlot.model_config["json_schema_extra"]["curie"]
        'math:ArgumentSlot'

    IRI:    https://blackcatinformatics.ca/math/ArgumentSlot
    CURIE:  math:ArgumentSlot
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/argumentslot-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ArgumentSlot", "curie": "math:ArgumentSlot", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ArgumentSlot"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    slotExpression: Any = Field(description="The math:MathematicalExpression filling one math:ArgumentSlot — the operand, or the body of a binder. Domain math:ArgumentSlot, range math:MathematicalExpression (stated in prose). Every slot names exactly one; a slot without an expression is ill-formed (math:MalformedArgumentSlot).", alias="math:slotExpression")
    slotIndex: int = Field(description="The integer position of a math:ArgumentSlot within its application — the operand order. Domain math:ArgumentSlot, range xsd:nonNegativeInteger (stated in prose). Unique within one application; strict canonical mode requires the indexes zero-based and contiguous. A slot without exactly one index is ill-formed (math:MalformedArgumentSlot).", alias="math:slotIndex")


class Math_ArithmeticOperation(ConfiguredBaseModel):
    """Arithmetic Operation.

    An arithmetic operation over numbers: addition, multiplication, exponentiation, and
    their inverses. A subclass of math:Operation; the concrete operators are named
    individuals (math:Addition, math:Multiplication, math:Exponentiation, math:Subtraction,
    math:Division, math:Root, math:Logarithm). Anchored externally to the OpenMath arith1
    and nums1 content dictionaries. An arithmetic expression applies one such operator
    inside a math:ApplicationExpression.

    Usage:
        >>> from gmeow_models.math import Math_ArithmeticOperation
        >>> Math_ArithmeticOperation.model_config["json_schema_extra"]["curie"]
        'math:ArithmeticOperation'

    IRI:    https://blackcatinformatics.ca/math/ArithmeticOperation
    CURIE:  math:ArithmeticOperation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ArithmeticOperation", "curie": "math:ArithmeticOperation", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ArithmeticOperation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    operatorCodomain: list[Any] | None = Field(default=None, description="The number system an operator's result lies in — the target of its signature. Domain math:ArithmeticOperation, range math:NumberSystem (stated in prose). Every math:ArithmeticOperation names exactly one; an operator without its codomain is unframed (math:UnframedOperator). The declared target may be wider than the operator's domain — a real even root or a real logarithm lands in ℂ, so math:Root and math:Logarithm frame math:realNumbers → math:complexNumbers honestly rather than pretending the result stays real.", alias="math:operatorCodomain")
    operatorDomain: list[Any] | None = Field(default=None, description="The number system an operator's operands are drawn from — the source of its signature. Domain math:ArithmeticOperation, range math:NumberSystem (stated in prose). Every math:ArithmeticOperation names exactly one; an operator without its domain is unframed (math:UnframedOperator). Distinct from math:domain (a function's or relation's source set): math:operatorDomain frames the operator symbol itself, over the number-system tower ℕ ⊂ ℤ ⊂ ℚ ⊂ ℝ ⊂ ℂ (and the signed extended real line ℝ\u{304}).", alias="math:operatorDomain")


class Math_AutomorphismGroup(ConfiguredBaseModel):
    """Automorphism Group.

    The group of all automorphisms of a structure under composition — the symmetries of that
    structure. A subclass of math:Group: a symmetry group IS an automorphism group, the
    identity that makes E8's math:WeylGroup and every symmetry expressible with the same
    machinery as det and a homomorphic-encryption scheme.

    Usage:
        >>> from gmeow_models.math import Math_AutomorphismGroup
        >>> Math_AutomorphismGroup.model_config["json_schema_extra"]["curie"]
        'math:AutomorphismGroup'

    IRI:    https://blackcatinformatics.ca/math/AutomorphismGroup
    CURIE:  math:AutomorphismGroup

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/AutomorphismGroup", "curie": "math:AutomorphismGroup", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/AutomorphismGroup"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    automorphismGroupOf: list[Any] = Field(min_length=1, description="Relates a math:AutomorphismGroup to the structure whose symmetries it collects — an automorphism group is always the symmetry group OF something. Domain math:AutomorphismGroup, range math:AlgebraicStructure or math:RootSystem (stated in prose). A math:WeylGroup math:automorphismGroupOf its root system: W(E8) is the automorphism group of the E8 root system. This is the invariant that makes a symmetry group anchored — an automorphism group declared without the structure it is the symmetry of is ill-formed.", alias="math:automorphismGroupOf")


class Math_BayesianResult(ConfiguredBaseModel):
    """Bayesian Result.

    A Bayesian inference result: the family of result objects whose semantics rest on a
    posterior obtained by updating a prior with a likelihood — a posterior distribution, a
    credible interval, a Bayes factor, a posterior probability, and the operational MCMC
    diagnostics. A subclass of math:MathematicalObject and one of the two inference-paradigm
    result families, held owl:disjointWith math:FrequentistResult so a single result is
    never read as both Bayesian and frequentist. The paradigm axis is a PARTIAL
    classification: only paradigm-exclusive kinds are placed under it, so a paradigm-neutral
    object (a math:Estimate, a math:Residual) is left free of either family rather than
    forced into one.

    When to use:
        - Use as the family for result objects whose semantics rest on a posterior from updating a
          prior with a likelihood: credible intervals, Bayes factors, and posterior probabilities.

    When to avoid:
        - Do not force a paradigm-neutral object such as a math:PredictionInterval or
          math:EffectSize into it; the axis is partial, only paradigm-exclusive kinds belong.

    How to use:
        - Type paradigm-exclusive Bayesian results under it; it is held owl:disjointWith
          math:FrequentistResult so no result is read as both.

    Examples:
        - exf:cri95 a math:CredibleInterval , math:BayesianResult .

    Usage:
        >>> from gmeow_models.math import Math_BayesianResult
        >>> Math_BayesianResult.model_config["json_schema_extra"]["curie"]
        'math:BayesianResult'

    IRI:    https://blackcatinformatics.ca/math/BayesianResult
    CURIE:  math:BayesianResult

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/BayesianResult", "curie": "math:BayesianResult", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/BayesianResult"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_BindingExpression(ConfiguredBaseModel):
    """Binding Expression.

    A binder: the expression node that binds a variable over a body. Application-shaped — it
    names its binder operator through math:operator (a math:Operation individual such as the
    differentiation, integration, summation, or limit binder), its body through indexed
    math:argumentSlot cells, and the math:VariableDeclaration it introduces through
    math:boundVariable. It binds math:VariableOccurrence use sites through
    math:bindsOccurrence, never glyphs, so nested and shadowed binders are checkable. The
    single binding form d/dx, ∂, ∫, ∑, and lim all specialize: a math:Derivative, a
    math:Limit, a math:Series, and a math:Integral are binding expressions, so no
    per-operator binder machinery is minted. A binder without a bound variable and a body is
    ill-formed (math:MalformedBindingExpression).

    Usage:
        >>> from gmeow_models.math import Math_BindingExpression
        >>> Math_BindingExpression.model_config["json_schema_extra"]["curie"]
        'math:BindingExpression'

    IRI:    https://blackcatinformatics.ca/math/BindingExpression
    CURIE:  math:BindingExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/BindingExpression", "curie": "math:BindingExpression", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/BindingExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    argumentSlot: list[Any] = Field(min_length=1, description="Relates a math:ApplicationExpression or math:BindingExpression to one math:ArgumentSlot carrying an indexed operand (or, for a binder, the body). Domain a math:ApplicationExpression or math:BindingExpression, range math:ArgumentSlot (stated in prose). Operand order is carried by the slots' math:slotIndex, never by RDF list ordering.", alias="math:argumentSlot")
    boundVariable: Any = Field(description="Relates a math:BindingExpression to the math:VariableDeclaration it introduces and binds — the variable of differentiation, integration, summation, or the limit. Domain math:BindingExpression, range math:VariableDeclaration (stated in prose). Every binder names exactly one bound variable's declaration; a binder without one is ill-formed (math:MalformedBindingExpression). This is the edge through which a binder introduces its variable's scoped identity, over which its bound math:VariableOccurrence use sites resolve.", alias="math:boundVariable")


class Math_CalibrationDiagnostic(ConfiguredBaseModel):
    """Calibration Diagnostic.

    A calibration diagnostic: the STATISTICAL sense of calibration — how well a fitted
    model's predicted probabilities match observed frequencies over held-out data, measured
    by a reliability curve, a Brier-score decomposition, or an expected-calibration-error
    binning. A subclass of math:ModelDiagnostic (a calibration diagnostic IS a model
    diagnostic), so it names the math:FittedModel whose probabilities it checks through
    math:diagnosticFor and the method it applies through math:diagnosticMethod. It is held
    owl:disjointWith math:StabilityCalibrationRecord — the TOPOLOGICAL sense, which
    calibrates a credence against a bottleneck stability bound over a
    math:PersistenceLifetime: the two share the word 'calibration' and nothing else, one
    calibrating a model's probabilities against data and the other a credence against a
    stability guarantee, so a consumer selects on the specific class, never on the shared
    token. Because both descend from math:MathematicalObject their disjointness is NOT
    entailed by their parents and is asserted directly; a node typed both is
    math:CalibrationSenseConflation.

    When to use:
        - Mint one for the STATISTICAL sense of calibration, how well a model predicted
          probabilities match observed frequencies over held-out data.

    When to avoid:
        - Do not use it for the topological calibration of a credence against a stability bound;
          that is math:StabilityCalibrationRecord, and typing a node both is a sense conflation.

    How to use:
        - As a math:ModelDiagnostic, name the math:FittedModel whose probabilities it checks
          through math:diagnosticFor and its method through math:diagnosticMethod, a reliability
          curve or expected-calibration-error binning.

    Examples:
        - exf:reliability a math:CalibrationDiagnostic ; math:diagnosticFor exf:logitFit ;
          math:diagnosticMethod exf:eceBinning .

    Usage:
        >>> from gmeow_models.math import Math_CalibrationDiagnostic
        >>> Math_CalibrationDiagnostic.model_config["json_schema_extra"]["curie"]
        'math:CalibrationDiagnostic'

    IRI:    https://blackcatinformatics.ca/math/CalibrationDiagnostic
    CURIE:  math:CalibrationDiagnostic

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/CalibrationDiagnostic", "curie": "math:CalibrationDiagnostic", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/CalibrationDiagnostic"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Chart(ConfiguredBaseModel):
    """Chart.

    A chart (coordinate patch) on a manifold: it names the region it covers through
    math:chartDomain, the math:CoordinateMap taking that region to coordinates through
    math:coordinateMap, and the target coordinate space (ℝⁿ or ℂⁿ) through
    math:targetCoordinateSpace. A chart missing its domain, coordinate map, or target space
    is ill-formed (math:UnderspecifiedChart). Its coordinate map is a homeomorphism onto its
    image, and the target space's dimension must match the manifold's
    (math:DimensionMismatch).

    Usage:
        >>> from gmeow_models.math import Math_Chart
        >>> Math_Chart.model_config["json_schema_extra"]["curie"]
        'math:Chart'

    IRI:    https://blackcatinformatics.ca/math/Chart
    CURIE:  math:Chart

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Chart", "curie": "math:Chart", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Chart"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    chartDomain: Any = Field(description="Relates a math:Chart to the region of the manifold it covers — an open subset, or the whole manifold for a global chart. Domain math:Chart, range a math:TopologicalSpace or math:OpenSet (stated in prose). Every chart names exactly one; a chart without it is ill-formed (math:UnderspecifiedChart).", alias="math:chartDomain")
    coordinateMap: Any = Field(description="Relates a math:Chart to its math:CoordinateMap — the homeomorphism assigning coordinates to points of its domain. Domain math:Chart, range math:CoordinateMap (stated in prose). Every chart names exactly one; a chart without it is ill-formed (math:UnderspecifiedChart).", alias="math:coordinateMap")
    targetCoordinateSpace: Any = Field(description="Relates a math:Chart to the coordinate space its map lands in — ℝⁿ for a real manifold, ℂⁿ for a complex one, carrying its math:spaceDimension. Domain math:Chart, range a math:Set or math:VectorSpace (stated in prose). Every chart names exactly one; its dimension must equal the manifold's (math:DimensionMismatch); a chart without it is ill-formed (math:UnderspecifiedChart).", alias="math:targetCoordinateSpace")


class Math_ClosedFormFunction(ConfiguredBaseModel):
    """Closed-Form Function.

    A function given by an explicit closed-form expression in one formal argument over
    tunable parameters: the general facility for stating a parameterized curve — for example
    T(x) = A·x⁻ᵖ − B·(1−x)⁻q + C — natively as a math:MathematicalExpression AST rather than
    as an opaque string. A subclass of math:Function, so it inherits the requirement to
    declare its math:domain and math:codomain (an unframed one is math:UnframedFunction). It
    additionally names its body through math:definingExpression (the expression AST) and its
    abstraction variable's declaration through math:formalArgument, and it distinguishes
    that formal argument from its fitted parameters, each declared through
    math:functionParameter — so in T(x) the argument x and the parameters A, B, C, p, q are
    separate declarations, not conflated. A closed form missing its body or its formal
    argument is ill-formed (math:UnboundClosedForm). It is a general facility supporting ANY
    curve, not a hardwired one; a concrete curve such as T(x) is an instance authored
    elsewhere.

    Usage:
        >>> from gmeow_models.math import Math_ClosedFormFunction
        >>> Math_ClosedFormFunction.model_config["json_schema_extra"]["curie"]
        'math:ClosedFormFunction'

    IRI:    https://blackcatinformatics.ca/math/ClosedFormFunction
    CURIE:  math:ClosedFormFunction

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ClosedFormFunction", "curie": "math:ClosedFormFunction", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ClosedFormFunction"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    definingExpression: list[Any] | None = Field(default=None, description="The body AST of a function given in closed form: relates a math:Function to the math:MathematicalExpression that is its explicit closed-form body. Domain math:Function, range math:MathematicalExpression (stated in prose). Every math:ClosedFormFunction names exactly one; a closed form without a body does not say what it computes and is ill-formed (math:UnboundClosedForm). The body is a structured expression tree, never a display string — the argument and the parameters enter it as math:VariableExpression leaves resolving to the function's math:formalArgument and math:functionParameters.", alias="math:definingExpression")
    formalArgument: list[Any] | None = Field(default=None, description="The abstraction variable of a closed-form function — the x in f(x): relates a math:ClosedFormFunction to the math:VariableDeclaration that is its formal argument. Domain math:ClosedFormFunction, range math:VariableDeclaration (typically a math:FreeVariableDeclaration, since the argument is free in the body; stated in prose). Every math:ClosedFormFunction names exactly one; a closed form without a formal argument does not say over which variable it abstracts and is ill-formed (math:UnboundClosedForm). It is the one declaration the function is a function OF, distinguished from the fitted math:functionParameters it is tuned by.", alias="math:formalArgument")


class Math_CompactSpace(ConfiguredBaseModel):
    """Compact Space.

    A compact topological space: one in which every open cover has a finite subcover. A
    subclass of math:TopologicalSpace that declares its compactness through math:isCompact —
    compactness is declared, not assumed (an undeclared compactness is ill-formed,
    math:UndeclaredTopologicalProperty). Unlike math:ConnectedSpace, whose defining law
    math:connectednessLaw is a first-order logic:Formula, compactness is genuinely
    second-order (it quantifies over open covers and appeals to finiteness) and so is backed
    not by a first-order law but by the honest recorded boundary math:compactnessBoundary
    (logic:expressivenessBoundary logic:SecondOrder, logic:preservationKind
    logic:Unsupported), referenced through math:preservesStructure. Compactness is the
    topological generalization of closed-and-bounded (Heine–Borel) and is preserved by
    continuous images.

    Usage:
        >>> from gmeow_models.math import Math_CompactSpace
        >>> Math_CompactSpace.model_config["json_schema_extra"]["curie"]
        'math:CompactSpace'

    IRI:    https://blackcatinformatics.ca/math/CompactSpace
    CURIE:  math:CompactSpace

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/CompactSpace", "curie": "math:CompactSpace", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/CompactSpace"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    isCompact: bool = Field(description="Declares whether a space is compact — every open cover has a finite subcover. Domain math:CompactSpace (or math:TopologicalSpace), range xsd:boolean (stated in prose). Compactness is declared, not assumed from the type (an undeclared compactness is ill-formed, math:UndeclaredTopologicalProperty). The declaration is backed not by a first-order law but by the honest recorded second-order boundary math:compactnessBoundary — compactness genuinely exceeds first-order logic (open covers plus finiteness), so the boundary is carried in the loss ledger (logic:preservationKind logic:Unsupported), never faked as a first-order logic:Formula.", alias="math:isCompact")


class Math_Compactification(ConfiguredBaseModel):
    """Compactification.

    A compactification: a structured record embedding an unbounded space in a bounded
    (compact) one as a dense subset, so the points the original space runs off toward are
    adjoined as a boundary at infinity. It names FOUR roles — the original unbounded space
    through math:originalSpace, the embedding/map through math:compactifyingMap, the
    resulting bounded space through math:compactifiedSpace, and the boundary added at
    infinity through math:boundaryAtInfinity. It is NOT any one construction — not the
    one-point (Alexandroff) versus Stone–Čech distinction per se — but the general
    structured record every such construction instantiates; a compactification missing any
    of the four roles is ill-formed (math:UnderspecifiedCompactification).

    Usage:
        >>> from gmeow_models.math import Math_Compactification
        >>> Math_Compactification.model_config["json_schema_extra"]["curie"]
        'math:Compactification'

    IRI:    https://blackcatinformatics.ca/math/Compactification
    CURIE:  math:Compactification

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Compactification", "curie": "math:Compactification", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Compactification"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    boundaryAtInfinity: list[Any] | None = Field(default=None, description="Relates a math:Compactification to the math:BoundaryAtInfinity it adjoins — the ideal points at infinity the original space approaches but never reaches. Domain math:Compactification, range math:BoundaryAtInfinity (stated in prose). Every compactification names exactly one; a compactification without it is ill-formed (math:UnderspecifiedCompactification).", alias="math:boundaryAtInfinity")
    compactifiedSpace: Any = Field(description="Relates a math:Compactification to the resulting bounded (compact) space — the image the embedding lands in together with the adjoined boundary at infinity. Domain math:Compactification, range a math:TopologicalSpace (a math:Interval for a compactified half-line) (stated in prose). Every compactification names exactly one; a compactification without it is ill-formed (math:UnderspecifiedCompactification).", alias="math:compactifiedSpace")
    compactifyingMap: Any = Field(description="Relates a math:Compactification to the embedding taking its original space into the compactified one — a math:Function (a math:CoordinateMap when it coordinatizes), angle-preserving for a math:ConformalCompactification. Domain math:Compactification, range math:Function (stated in prose). Every compactification names exactly one; a compactification without it is ill-formed (math:UnderspecifiedCompactification).", alias="math:compactifyingMap")
    originalSpace: Any = Field(description="Relates a math:Compactification to the original unbounded space it embeds — the space whose points at infinity the compactification adjoins as a boundary. Domain math:Compactification, range a math:TopologicalSpace or math:Manifold (stated in prose). Every compactification names exactly one; a compactification without it is ill-formed (math:UnderspecifiedCompactification).", alias="math:originalSpace")


class Math_Complement(ConfiguredBaseModel):
    """Complement.

    The complement construction: the elements of an ambient space not in S, under a declared
    notion of complement. A subclass of math:SetOperation that always names its ambient
    space through math:ambientSpace and which notion of complement it uses through
    math:complementSemantics (set-theoretic U ∖ S, orthogonal S⊥, complex-linear,
    topological, or quotient/cokernel) — a complement is relative to an ambient space under
    a semantics, so an unqualified complement (missing either) is ill-formed.

    Usage:
        >>> from gmeow_models.math import Math_Complement
        >>> Math_Complement.model_config["json_schema_extra"]["curie"]
        'math:Complement'

    IRI:    https://blackcatinformatics.ca/math/Complement
    CURIE:  math:Complement

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Complement", "curie": "math:Complement", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Complement"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    ambientSpace: Any = Field(description="Names the ambient space a math:Complement is taken relative to (the U in a complement of S within U). Domain math:Complement, range a math:Set, math:VectorSpace, math:TopologicalSpace, or math:FunctionSpace (stated in prose) — the ambient space generalizes the set-theoretic case to the orthogonal, complex-linear, topological, and quotient cases the math:complementSemantics distinguishes. A complement without an ambient space is ill-formed (math:UnqualifiedComplement). Generalizes the earlier set-only complement predicate: a complement is relative to an ambient space, and which space and which semantics must both be named.", alias="math:ambientSpace")
    complementSemantics: list[Any] | None = Field(default=None, description="Names which notion of complement a math:Complement uses: a math:ComplementSemantics individual — set-theoretic (U ∖ S), orthogonal (S⊥ in an inner-product space), complex-linear (a complementary subspace), topological (the closed complement of an open set), or quotient/cokernel. Domain math:Complement, range math:ComplementSemantics (stated in prose). 'The complement of X' without a named semantics is ambiguous and ill-formed (math:UnqualifiedComplement) — the flagship-adjacent 'complex complement' hazard the charter forbids.", alias="math:complementSemantics")


class Math_ConditionalIndependenceAssertion(ConfiguredBaseModel):
    """Conditional Independence Assertion.

    A conditional-independence assertion A ⫫ B | C: an independence assertion that
    additionally names the conditioning set it is relative to (math:independentGiven). A
    subclass of math:IndependenceAssertion. Like marginal independence it is a
    standpoint-scoped modeler's claim naming its model; distinct from marginal independence
    (a different, weaker-or-stronger relation), and distinct again from 'not modeled'. A
    conditional-independence assertion that fails to name its conditioning set is ill-formed
    (math:UnconditionedAssertion).

    Usage:
        >>> from gmeow_models.math import Math_ConditionalIndependenceAssertion
        >>> Math_ConditionalIndependenceAssertion.model_config["json_schema_extra"]["curie"]
        'math:ConditionalIndependenceAssertion'

    IRI:    https://blackcatinformatics.ca/math/ConditionalIndependenceAssertion
    CURIE:  math:ConditionalIndependenceAssertion

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ConditionalIndependenceAssertion", "curie": "math:ConditionalIndependenceAssertion", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ConditionalIndependenceAssertion"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    independentGiven: list[Any] = Field(min_length=1, description="Names the conditioning set of a conditional-independence assertion — the C in A ⫫ B | C. Domain math:ConditionalIndependenceAssertion, range the conditioning set (stated in prose). A conditional-independence assertion that fails to name its conditioning set is ill-formed (math:UnconditionedAssertion).", alias="math:independentGiven")


class Math_ConditionalProbability(ConfiguredBaseModel):
    """Conditional Probability.

    A conditional probability P(A | B): a probability object naming BOTH the event it is
    about and the conditioning event or context it is conditioned on (math:conditionalOn). A
    subclass of math:MathematicalObject. A conditional with an unnamed conditioning context
    is ill-formed (math:UnconditionedAssertion) — the conditioning context is never left
    implicit.

    Usage:
        >>> from gmeow_models.math import Math_ConditionalProbability
        >>> Math_ConditionalProbability.model_config["json_schema_extra"]["curie"]
        'math:ConditionalProbability'

    IRI:    https://blackcatinformatics.ca/math/ConditionalProbability
    CURIE:  math:ConditionalProbability

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ConditionalProbability", "curie": "math:ConditionalProbability", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ConditionalProbability"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conditionalOn: list[Any] = Field(min_length=1, description="Names the conditioning event or context of a conditional probability — the B in P(A | B). Domain math:ConditionalProbability, range a math:ProbabilityEvent or conditioning context (stated in prose). A conditional probability names one; a conditional with an unnamed conditioning context is ill-formed (math:UnconditionedAssertion).", alias="math:conditionalOn")


class Math_ConfidenceInterval(ConfiguredBaseModel):
    """Confidence Interval.

    A confidence interval: the frequentist interval estimate whose coverage is a property of
    the PROCEDURE under repeated sampling — an interval constructed so that, across
    replications of the study, a stated proportion of such intervals contain the true
    parameter. A subclass of math:FrequentistResult, so it is held owl:disjointWith the
    Bayesian math:CredibleInterval THROUGH the paradigm parents (a single node is never both
    — that conflation is math:IntervalKindConflation). It names its bounds through
    math:hasIntervalBound and its coverage through math:confidenceLevel (a framed
    math:ProbabilityValue over the sampling procedure); an interval missing its bounds or
    its level is ill-formed (math:IncompleteConfidenceInterval). It is NOT a statement of
    posterior probability that the parameter lies in the interval — that is the credible
    interval's semantics, which this class must never be read as.

    When to use:
        - Mint one for a frequentist interval whose coverage is a property of the PROCEDURE under
          repeated sampling.

    When to avoid:
        - Do not read it as the posterior probability that the parameter lies in the interval;
          that is a credible interval, and reading a confidence interval that way is the core
          misuse.

    How to use:
        - Name its bounds through math:hasIntervalBound and its coverage through
          math:confidenceLevel; missing either is math:IncompleteConfidenceInterval.

    Examples:
        - exf:ci95 a math:ConfidenceInterval ; math:confidenceLevel exf:p95 ;
          math:hasIntervalBound exf:lo , exf:hi .

    Usage:
        >>> from gmeow_models.math import Math_ConfidenceInterval
        >>> Math_ConfidenceInterval.model_config["json_schema_extra"]["curie"]
        'math:ConfidenceInterval'

    IRI:    https://blackcatinformatics.ca/math/ConfidenceInterval
    CURIE:  math:ConfidenceInterval

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ConfidenceInterval", "curie": "math:ConfidenceInterval", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ConfidenceInterval"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    confidenceLevel: list[Any] = Field(min_length=1, description="Relates a math:ConfidenceInterval (or math:ConfidenceRegion) to its coverage level — a framed math:ProbabilityValue (a 0.95, a 0.99) that is the proportion of such intervals, under repeated sampling, containing the true parameter. Domain a confidence interval or region, range math:ProbabilityValue (stated in prose). The coverage is a property of the PROCEDURE, never a posterior mass; a confidence interval naming no confidence level is ill-formed (math:IncompleteConfidenceInterval). Its counterpart on the Bayesian side is math:credibleMass, which the two must never be collapsed onto one another.", alias="math:confidenceLevel")
    hasIntervalBound: list[Any] = Field(min_length=2, description="Relates an interval (a math:ConfidenceInterval, math:CredibleInterval, math:PredictionInterval, or math:ToleranceInterval) to one of its bounds — a lower or an upper endpoint of the interval. Domain an interval kind, range the bound object (stated in prose). A two-sided interval names its lower AND its upper bound (two math:hasIntervalBound edges); a confidence interval that does not name its bounds is ill-formed (math:IncompleteConfidenceInterval).", alias="math:hasIntervalBound")


class Math_ConformalCompactification(ConfiguredBaseModel):
    """Conformal Compactification.

    A conformal (Penrose-style) compactification: the specialization whose
    math:compactifyingMap is angle-preserving (conformal), carrying a math:conformalFactor Ω
    that rescales the metric so the points at infinity sit at a finite conformal boundary. A
    subclass of math:Compactification; it additionally names its math:conformalFactor — a
    conformal compactification missing it is ill-formed
    (math:UnderspecifiedCompactification). This is the correct general home for embedding a
    math:LorentzianManifold's radial chart's infinity as a finite boundary: the conformal
    factor sends the unbounded metric distance to a finite rescaled one while preserving the
    causal (angle) structure the Lorentzian metric carries. The metric and its rescaling
    stay math-side; a spacetime's Penrose diagram is a downstream physics reading of this
    math object.

    Usage:
        >>> from gmeow_models.math import Math_ConformalCompactification
        >>> Math_ConformalCompactification.model_config["json_schema_extra"]["curie"]
        'math:ConformalCompactification'

    IRI:    https://blackcatinformatics.ca/math/ConformalCompactification
    CURIE:  math:ConformalCompactification

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ConformalCompactification", "curie": "math:ConformalCompactification", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ConformalCompactification"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conformalFactor: Any = Field(description="Relates a math:ConformalCompactification to its conformal rescaling factor Ω — a positive function that multiplies the metric so the unbounded metric distance becomes finite at the boundary while angles are preserved. Domain math:ConformalCompactification, range a positive math:Function or math:Quantity (an individual, never a bare literal — hence an owl:ObjectProperty, not a datatype property) (stated in prose). Every conformal compactification names exactly one; one without it is ill-formed (math:UnderspecifiedCompactification).", alias="math:conformalFactor")


class Math_ConnectedSpace(ConfiguredBaseModel):
    """Connected Space.

    A connected topological space: one that is not the union of two disjoint non-empty open
    sets. A subclass of math:TopologicalSpace that declares its connectedness through
    math:isConnected — connectedness is declared, not assumed (an undeclared connectedness
    is ill-formed, math:UndeclaredTopologicalProperty). The structure it preserves is the
    topology's connectedness, whose defining law is math:connectednessLaw (no splitting into
    two disjoint non-empty open sets covering the space), referenced through
    math:preservesStructure. Connectedness is preserved by continuous images (the
    intermediate value theorem is its avatar on ℝ).

    Usage:
        >>> from gmeow_models.math import Math_ConnectedSpace
        >>> Math_ConnectedSpace.model_config["json_schema_extra"]["curie"]
        'math:ConnectedSpace'

    IRI:    https://blackcatinformatics.ca/math/ConnectedSpace
    CURIE:  math:ConnectedSpace

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ConnectedSpace", "curie": "math:ConnectedSpace", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ConnectedSpace"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    isConnected: bool = Field(description="Declares whether a space is connected — not the union of two disjoint non-empty open sets (math:connectednessLaw). Domain math:ConnectedSpace (or math:TopologicalSpace), range xsd:boolean (stated in prose). The canonical checkable bearer of connectedness; connectedness is declared here, not assumed from the math:ConnectedSpace type (an undeclared connectedness is ill-formed, math:UndeclaredTopologicalProperty).", alias="math:isConnected")


class Math_ContinuousMap(ConfiguredBaseModel):
    """Continuous Map.

    A continuous map between topological spaces: a map under which the preimage of every
    open set is open. A subclass of math:Morphism (a structure-preserving map of spaces, its
    math:sourceObject and math:targetObject the two topological spaces), and it declares its
    continuity through math:isContinuous — continuity is declared, not assumed (an
    undeclared continuity is ill-formed, math:UndeclaredTopologicalProperty). The structure
    it preserves is the topology, whose defining law is math:continuityLaw (preimages of
    open sets are open), referenced through math:preservesStructure.

    Usage:
        >>> from gmeow_models.math import Math_ContinuousMap
        >>> Math_ContinuousMap.model_config["json_schema_extra"]["curie"]
        'math:ContinuousMap'

    IRI:    https://blackcatinformatics.ca/math/ContinuousMap
    CURIE:  math:ContinuousMap

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ContinuousMap", "curie": "math:ContinuousMap", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ContinuousMap"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    isContinuous: bool = Field(description="Declares whether a map is continuous — the preimage of every open set is open (math:continuityLaw). Domain math:ContinuousMap, range xsd:boolean (stated in prose). The canonical checkable bearer of continuity; continuity is declared here, not assumed from the math:ContinuousMap type (an undeclared continuity is ill-formed, math:UndeclaredTopologicalProperty).", alias="math:isContinuous")


class Math_Convergence(ConfiguredBaseModel):
    """Convergence.

    The convergence of a sequence or series: names what it converges to through
    math:convergesTo and the mode through math:convergenceMode (pointwise, uniform,
    absolute, conditional, in-measure, almost-everywhere, in-Lᵖ-norm, in-distribution).
    Convergence in which mode is a declared property, not an assumption — 'the series
    converges' is meaningless without the mode. A convergence without a mode is ill-formed
    (math:UnderspecifiedConvergence).

    Usage:
        >>> from gmeow_models.math import Math_Convergence
        >>> Math_Convergence.model_config["json_schema_extra"]["curie"]
        'math:Convergence'

    IRI:    https://blackcatinformatics.ca/math/Convergence
    CURIE:  math:Convergence

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Convergence", "curie": "math:Convergence", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Convergence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    convergenceMode: list[Any] | None = Field(default=None, description="Relates a math:Convergence to its math:ConvergenceMode — pointwise, uniform, absolute, conditional, in-measure, almost-everywhere, in-Lᵖ-norm, or in-distribution. Domain math:Convergence, range math:ConvergenceMode (stated in prose). The mode is required; convergence without a declared mode is ill-formed (math:UnderspecifiedConvergence).", alias="math:convergenceMode")
    convergesTo: list[Any] = Field(min_length=1, description="Relates a math:Convergence to the value or expression its sequence/series converges to — a math:Number, a limit function, or a distribution. Domain math:Convergence, range a math:MathematicalObject or value (stated in prose). Every convergence names what it converges to.", alias="math:convergesTo")


class Math_CredibleInterval(ConfiguredBaseModel):
    """Credible Interval.

    A credible interval: the Bayesian interval estimate whose semantics rest on a POSTERIOR
    — an interval carrying a stated posterior probability mass that the parameter lies
    within it, given the prior, the likelihood, and the data. A subclass of
    math:BayesianResult, so it is held owl:disjointWith the frequentist
    math:ConfidenceInterval THROUGH the paradigm parents (a single node is never both — that
    conflation is math:IntervalKindConflation: they share the word 'interval' and nothing
    else, frequentist coverage versus posterior mass). It names its posterior context
    through math:hasPosterior (the math:PosteriorDistribution it summarizes), its bounds
    through math:hasIntervalBound, and its mass through math:credibleMass (a framed
    math:ProbabilityValue over that posterior); a credible interval naming no posterior or
    no credible mass is ill-formed (math:IncompleteCredibleInterval).

    When to use:
        - Mint one for a Bayesian interval carrying a stated posterior probability mass that the
          parameter lies within it, given prior, likelihood, and data.

    When to avoid:
        - Do not read its mass as a procedure coverage or mint one with no posterior; a credible
          interval naming no posterior or no mass is ill-formed.

    How to use:
        - Name its posterior through math:hasPosterior, its bounds through math:hasIntervalBound,
          and its mass through math:credibleMass; missing the posterior or mass is
          math:IncompleteCredibleInterval.

    Examples:
        - exf:cri95 a math:CredibleInterval ; math:credibleMass exf:m95 ; math:hasIntervalBound
          exf:lo , exf:hi .

    Usage:
        >>> from gmeow_models.math import Math_CredibleInterval
        >>> Math_CredibleInterval.model_config["json_schema_extra"]["curie"]
        'math:CredibleInterval'

    IRI:    https://blackcatinformatics.ca/math/CredibleInterval
    CURIE:  math:CredibleInterval

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/CredibleInterval", "curie": "math:CredibleInterval", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/CredibleInterval"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    credibleMass: list[Any] = Field(min_length=1, description="Relates a math:CredibleInterval (or math:CredibleRegion) to its posterior mass — a framed math:ProbabilityValue (a 0.95) that is the posterior probability, given prior and data, that the parameter lies within the interval. Domain a credible interval or region, range math:ProbabilityValue (stated in prose). The mass is a POSTERIOR probability over the math:PosteriorDistribution the interval summarizes (math:hasPosterior), never a procedure's coverage; a credible interval naming no credible mass is ill-formed (math:IncompleteCredibleInterval). Its frequentist counterpart is math:confidenceLevel, held strictly apart from it.", alias="math:credibleMass")
    hasPosterior: list[Any] = Field(min_length=1, description="Relates a Bayesian update — or a Bayesian interval estimate (a math:CredibleInterval / math:CredibleRegion) that summarizes it — to the math:PosteriorDistribution it produces or is taken over. Domain a Bayesian update or a credible interval/region, range math:PosteriorDistribution (stated in prose). Names the posterior resulting from combining the prior and likelihood, and the posterior context a credible interval's math:credibleMass is a probability over; a credible interval naming no posterior is ill-formed (math:IncompleteCredibleInterval).", alias="math:hasPosterior")


class Math_Derivative(ConfiguredBaseModel):
    """Derivative.

    The derivative d/dx f of an expression as a first-class binding expression: it applies
    the math:differentiationBinder (math:operator), names what it differentiates through
    math:derivativeOf, the variable of differentiation through math:withRespectToVariable (a
    math:VariableDeclaration it binds), and its order through math:derivativeOrder. A
    derivative is a map of function spaces, not the string 'dy/dx'. A derivative missing
    what it differentiates, its variable, or its order is ill-formed
    (math:UnderspecifiedDerivative).

    Usage:
        >>> from gmeow_models.math import Math_Derivative
        >>> Math_Derivative.model_config["json_schema_extra"]["curie"]
        'math:Derivative'

    IRI:    https://blackcatinformatics.ca/math/Derivative
    CURIE:  math:Derivative

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Derivative", "curie": "math:Derivative", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Derivative"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    derivativeOf: Any = Field(description="Relates a math:Derivative to the math:MathematicalExpression (or math:Function) it differentiates. Domain math:Derivative, range math:MathematicalExpression (stated in prose). Every derivative names exactly one; a derivative without it is ill-formed (math:UnderspecifiedDerivative).", alias="math:derivativeOf")
    derivativeOrder: int = Field(description="The order of a math:Derivative — 1 for f′, 2 for f″, and so on. Domain math:Derivative, range xsd:positiveInteger (stated in prose). Every derivative names exactly one; a derivative without an order is ill-formed (math:UnderspecifiedDerivative).", alias="math:derivativeOrder")
    withRespectToVariable: Any = Field(description="Relates a math:Derivative to the math:VariableDeclaration it differentiates with respect to — the bound variable of the differentiation binder. Domain math:Derivative, range math:VariableDeclaration (stated in prose). Every derivative names exactly one; a derivative without it is ill-formed (math:UnderspecifiedDerivative).", alias="math:withRespectToVariable")


class Math_DerivedDimension(ConfiguredBaseModel):
    """Derived Dimension.

    A dimension expressed as a product of powers of the base dimensions — velocity L·T⁻¹,
    energy M·L²·T⁻², and so on. It records that product as a set of math:DimensionExponent
    cells through math:baseDimensionExponent (one edge per base dimension it involves), each
    naming a math:BaseDimension (math:exponentOfDimension) and an exact-rational power
    (math:exponentNumerator over math:exponentDenominator). The cells are an unordered set —
    a product commutes — so no list order is imposed. Its human-readable
    math:dimensionVector string is a computed projection of that structured set, never an
    independent source.

    Usage:
        >>> from gmeow_models.math import Math_DerivedDimension
        >>> Math_DerivedDimension.model_config["json_schema_extra"]["curie"]
        'math:DerivedDimension'

    IRI:    https://blackcatinformatics.ca/math/DerivedDimension
    CURIE:  math:DerivedDimension

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/DerivedDimension", "curie": "math:DerivedDimension", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/DerivedDimension"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    baseDimensionExponent: list[Any] | None = Field(default=None, description="Relates a math:DerivedDimension to one math:DimensionExponent cell of its product form over the base dimensions — one such edge per base dimension involved (an unordered set, since a product commutes). Domain math:DerivedDimension, range math:DimensionExponent (stated in prose). A base dimension carries no such edge — it is a generator, not a product.", alias="math:baseDimensionExponent")


class Math_DimensionExponent(ConfiguredBaseModel):
    """Dimension Exponent.

    One (base dimension, exact-rational power) cell of a derived dimension's product form:
    it names the math:BaseDimension it raises (math:exponentOfDimension) and the power, as
    an exact rational, through math:exponentNumerator over math:exponentDenominator
    (denominator non-zero). The cells named by a math:DerivedDimension's
    math:baseDimensionExponent edges together give its exponent vector over the seven base
    dimensions. Rational, not decimal, so equality of dimensions is exact.

    Usage:
        >>> from gmeow_models.math import Math_DimensionExponent
        >>> Math_DimensionExponent.model_config["json_schema_extra"]["curie"]
        'math:DimensionExponent'

    IRI:    https://blackcatinformatics.ca/math/DimensionExponent
    CURIE:  math:DimensionExponent

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/DimensionExponent", "curie": "math:DimensionExponent", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/DimensionExponent"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    exponentDenominator: int = Field(description="The denominator of the exact-rational power a math:DimensionExponent raises its base dimension to. Domain math:DimensionExponent, range xsd:integer (stated in prose), non-zero. Together with math:exponentNumerator it gives an exact math:RationalNumber power; a whole-number exponent uses denominator one.", alias="math:exponentDenominator")
    exponentNumerator: int = Field(description="The numerator of the exact-rational power a math:DimensionExponent raises its base dimension to. Domain math:DimensionExponent, range xsd:integer (stated in prose). Together with math:exponentDenominator it gives an exact math:RationalNumber power — ℚ, admitting fractional dimensions such as T^(−1/2) — never a floating-point or decimal approximation.", alias="math:exponentNumerator")
    exponentOfDimension: list[Any] | None = Field(default=None, description="Names the math:BaseDimension that a math:DimensionExponent cell raises to a power. Domain math:DimensionExponent, range math:BaseDimension only (stated in prose) — never a derived dimension, so the exponent vector is well-defined and acyclic by construction.", alias="math:exponentOfDimension")


class Math_DimensionalExpression(ConfiguredBaseModel):
    """Dimensional Expression.

    An expression whose operands must be dimensionally homogeneous — an addition, a
    subtraction, an equation, or a comparison, where every operand must share one dimension.
    Names its operands through math:homogeneousOperand (at least two). The homogeneity gate
    collects the dimensions of the operands and raises math:DimensionalInhomogeneity if they
    are not all equal. This is the first-class carrier of the homogeneity invariant; the
    full expression grammar (indexed argument slots) belongs to the mathematical-core
    surface this bedrock builds on, which will refine this class.

    Usage:
        >>> from gmeow_models.math import Math_DimensionalExpression
        >>> Math_DimensionalExpression.model_config["json_schema_extra"]["curie"]
        'math:DimensionalExpression'

    IRI:    https://blackcatinformatics.ca/math/DimensionalExpression
    CURIE:  math:DimensionalExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/DimensionalExpression", "curie": "math:DimensionalExpression", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/DimensionalExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    homogeneousOperand: list[Any] = Field(min_length=2, description="Relates a math:DimensionalExpression to one of the operands that must share its single dimension — a summand of an addition, a side of an equation, a term of a comparison. Domain math:DimensionalExpression, range a dimensioned object (a math:Quantity or another dimensioned expression) (stated in prose). An expression carries at least two; the homogeneity gate raises math:DimensionalInhomogeneity when their dimensions are not all equal.", alias="math:homogeneousOperand")


class Math_Distribution(ConfiguredBaseModel):
    """Distribution.

    A probability distribution: a law assigning probabilities over outcomes, named by its
    family through math:distributionFamily (normal, binomial, Poisson, …) and parameterized.
    A subclass of math:MathematicalObject; an R rnorm/dbinom/… call lifts to this object
    with its family and parameterization, distinct from the math:ProbabilityMeasure it
    induces.

    Usage:
        >>> from gmeow_models.math import Math_Distribution
        >>> Math_Distribution.model_config["json_schema_extra"]["curie"]
        'math:Distribution'

    IRI:    https://blackcatinformatics.ca/math/Distribution
    CURIE:  math:Distribution
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/distribution-class-584b5fe0c3e6

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Distribution", "curie": "math:Distribution", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Distribution"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    distributionFamily: list[Any] | None = Field(default=None, description="Relates a math:Distribution to the family it belongs to — normal, binomial, Poisson, and the rest. Domain math:Distribution, range the distribution family (stated in prose). A distribution names its family and its parameterization; missing the family it is underspecified.", alias="math:distributionFamily")


class Math_EffectSize(ConfiguredBaseModel):
    """Effect Size.

    An effect size: the standardized MAGNITUDE of a contrast — a Cohen's d, a correlation,
    an odds ratio, an R² — that answers 'how big is the effect' rather than 'is it
    distinguishable from zero'. A subclass of math:MathematicalObject, deliberately
    PARADIGM-NEUTRAL (under neither family) because an effect size is estimated in both
    paradigms. To be interpretable it identifies THREE things: the comparison it measures
    the magnitude of through math:effectSizeContrast — the SAME shared
    math:StatisticalContrast object an estimand targets through math:targetContrast, one
    modeled contrast, never a near-synonym — the math:MeasurementScale it is expressed on
    through math:effectSizeScale, and its reference frame through gmeow:hasReferenceFrame
    (the house frame predicate, the same edge a math:ProbabilityValue is framed by). An
    effect size missing its contrast, scale, or frame is an uninterpretable bare number
    (math:UnframedEffectSize).

    When to use:
        - Mint one for the standardized MAGNITUDE of a contrast, answering how big the effect is
          rather than whether it is distinguishable from zero.

    When to avoid:
        - Do not report an effect size as a bare number; missing its contrast, scale, or reference
          frame it is uninterpretable.

    How to use:
        - Identify its math:effectSizeContrast (the shared math:StatisticalContrast), its
          math:effectSizeScale, and its gmeow:hasReferenceFrame; missing any is
          math:UnframedEffectSize.

    Examples:
        - exf:cohensD a math:EffectSize ; math:effectSizeContrast exf:treatVsControl ;
          math:effectSizeScale exf:sdScale .

    Usage:
        >>> from gmeow_models.math import Math_EffectSize
        >>> Math_EffectSize.model_config["json_schema_extra"]["curie"]
        'math:EffectSize'

    IRI:    https://blackcatinformatics.ca/math/EffectSize
    CURIE:  math:EffectSize
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/effectsize-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/EffectSize", "curie": "math:EffectSize", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/EffectSize"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Embedding(ConfiguredBaseModel):
    """Embedding.

    An embedding: a map from a source object into a target latent space that preserves
    chosen structure. A subclass of math:Function naming its math:embeddingSource, its
    math:targetSpace, its math:embeddingFunction, and the math:embeddingModel that produced
    it. An embedding missing any of these is ill-formed (math:UnderspecifiedEmbedding).

    Usage:
        >>> from gmeow_models.math import Math_Embedding
        >>> Math_Embedding.model_config["json_schema_extra"]["curie"]
        'math:Embedding'

    IRI:    https://blackcatinformatics.ca/math/Embedding
    CURIE:  math:Embedding
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/embedding-class-0ced629946a3

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Embedding", "curie": "math:Embedding", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Embedding"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    embeddingFunction: list[Any] = Field(min_length=1, description="Relates a math:Embedding to the math:Function realizing the map from source elements to target vectors. Domain math:Embedding, range math:Function (stated in prose). The declared map; an embedding without it is ill-formed (math:UnderspecifiedEmbedding).", alias="math:embeddingFunction")
    embeddingModel: list[Any] = Field(min_length=1, description="Relates a math:Embedding to the math:LearnedModel that produced it. Domain math:Embedding, range math:LearnedModel (stated in prose). The declared model; an embedding without it is ill-formed (math:UnderspecifiedEmbedding).", alias="math:embeddingModel")
    embeddingSource: list[Any] = Field(min_length=1, description="Relates a math:Embedding to the object it embeds — a graph, a corpus, a set of entities. Domain math:Embedding, range a math:MathematicalObject or graph (stated in prose). The declared source; an embedding without it is ill-formed (math:UnderspecifiedEmbedding).", alias="math:embeddingSource")
    targetSpace: list[Any] = Field(min_length=1, description="Relates a math:Embedding to the math:LatentSpace it maps into. Domain math:Embedding, range math:LatentSpace or math:VectorSpace (stated in prose). The declared target; an embedding without it is ill-formed (math:UnderspecifiedEmbedding).", alias="math:targetSpace")


class Math_Estimand(ConfiguredBaseModel):
    """Estimand.

    An estimand: the target quantity a study is actually trying to learn — the thing an
    estimate speaks about, distinct from any coefficient inside a particular model. A
    subclass of math:MathematicalObject framed by the six coordinates that make it
    well-posed: the target population it refers to (math:targetPopulation), the outcome
    measured on that population (math:targetOutcome), the contrast between conditions it
    compares (math:targetContrast, a math:StatisticalContrast), the time or horizon it is
    defined at (math:targetTime), the intervention condition it conditions on
    (math:interventionCondition), and the missing-data policy under which it is defined
    (math:missingDataPolicy). This framing is load-bearing for causal inference, surveys,
    trials, and policy models, where the same estimand (an average treatment effect) can be
    targeted by different parameters under different models. An estimate names its estimand
    through math:estimatesEstimand; an estimand held apart from the model parameter is what
    keeps the target of inference from being read off a coefficient.

    When to use:
        - Mint one to fix the target quantity a study is actually trying to learn, distinct from
          any coefficient inside a particular model.

    When to avoid:
        - Do not read the estimand off a model parameter; the same average treatment effect can be
          targeted by different parameters under different models, so keep the target
          model-independent.

    How to use:
        - Frame it with all six coordinates: math:targetPopulation, math:targetOutcome,
          math:targetContrast, math:targetTime, math:interventionCondition, and
          math:missingDataPolicy; an estimate names it through math:estimatesEstimand.

    Examples:
        - exf:ate a math:Estimand ; math:targetPopulation exf:adultsCanada ; math:targetContrast
          exf:treatVsControl ; math:interventionCondition exf:drugArm .

    Usage:
        >>> from gmeow_models.math import Math_Estimand
        >>> Math_Estimand.model_config["json_schema_extra"]["curie"]
        'math:Estimand'

    IRI:    https://blackcatinformatics.ca/math/Estimand
    CURIE:  math:Estimand

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Estimand", "curie": "math:Estimand", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Estimand"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Estimate(ConfiguredBaseModel):
    """Estimate.

    An estimate: a produced value that estimates an estimand (math:estimatesEstimand) and,
    where relevant, a model parameter (math:estimatedParameter), carrying its value
    (math:estimateValue) and the estimator that produced it (math:estimator). A subclass of
    math:MathematicalObject and the structured result object of the process / result / claim
    separation — neither the analysis activity nor the held claim. The R broom tidy triple
    lifts model coefficients onto these objects.

    When to use:
        - Use to record a produced value that estimates an estimand — the structured result object
          carrying its value, its estimand, and the estimator that produced it.

    When to avoid:
        - Do not use it for the estimation activity (an math:InferenceRun) nor for the held claim
          about the value; an estimate that names no math:estimator is a
          math:UnderspecifiedEstimate.

    How to use:
        - Mint one per estimated value; link its target through math:estimatesEstimand, its
          producer through math:estimator, and its magnitude through math:estimateValue.

    Examples:
        - exf:betaHat a math:Estimate ; math:estimatesEstimand exf:ateEstimand ; math:estimator
          exf:olsEstimator ; math:estimateValue exf:betaValue .

    Usage:
        >>> from gmeow_models.math import Math_Estimate
        >>> Math_Estimate.model_config["json_schema_extra"]["curie"]
        'math:Estimate'

    IRI:    https://blackcatinformatics.ca/math/Estimate
    CURIE:  math:Estimate

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Estimate", "curie": "math:Estimate", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Estimate"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Filtration(ConfiguredBaseModel):
    """Filtration.

    A filtration (Kε): a monotone family of substructures of an ambient object indexed by a
    real-valued threshold ε, such that a smaller threshold's structure is contained in a
    larger one's — for thresholds ε₁ ≤ ε₂ the stage at ε₁ satisfies Kε₁ math:subsetOf Kε₂
    (the containment is the EXISTING transitive math:subsetOf, not a new order relation). A
    subclass of math:MathematicalObject. It names the object it filters through
    math:filtrationAmbient, the index quantity kind through math:filtrationIndexKind (the
    dimension the threshold ε ranges over — a distance/scale/density), and its stages
    through math:hasFiltrationStage, each a math:FiltrationStage pairing a threshold with
    the substructure present at it. The nesting law that ε₁ ≤ ε₂ entails containment is
    authored as the first-order logic:Formula math:filtrationMonotonicityLaw; the SHACL gate
    enforces only the structural presence of stages (a filtration naming no stage, or a
    stage missing its threshold or structure, is ill-formed, math:UnderspecifiedFiltration).
    The persistence of a feature across a filtration is a math:PersistenceLifetime.

    Usage:
        >>> from gmeow_models.math import Math_Filtration
        >>> Math_Filtration.model_config["json_schema_extra"]["curie"]
        'math:Filtration'

    IRI:    https://blackcatinformatics.ca/math/Filtration
    CURIE:  math:Filtration

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Filtration", "curie": "math:Filtration", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Filtration"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasFiltrationStage: list[Math_FiltrationStage] = Field(min_length=1, description="Relates a math:Filtration to one of its math:FiltrationStage cells — a (threshold, substructure) pair of the indexed family. Domain math:Filtration, range math:FiltrationStage (stated in prose). A filtration is given by its stages; one declaring no stage is ill-formed (math:UnderspecifiedFiltration).", alias="math:hasFiltrationStage")


class Math_FiltrationStage(ConfiguredBaseModel):
    """Filtration Stage.

    One stage of a math:Filtration: a (threshold, substructure) cell naming the real-valued
    threshold ε it sits at through math:filtrationThreshold (a gmeow:Quantity) and the
    substructure present at that threshold through math:stageStructure (a
    math:TopologicalSpace — the sub-space Kε of the ambient object at ε). A subclass of
    math:MathematicalObject; a filtration names its stages through math:hasFiltrationStage.
    Stages are ordered by their thresholds, and the monotonicity law
    math:filtrationMonotonicityLaw requires the structure at a lower threshold to be a
    math:subsetOf the structure at a higher one. A stage missing its threshold or its
    structure is ill-formed (math:UnderspecifiedFiltration).

    Usage:
        >>> from gmeow_models.math import Math_FiltrationStage
        >>> Math_FiltrationStage.model_config["json_schema_extra"]["curie"]
        'math:FiltrationStage'

    IRI:    https://blackcatinformatics.ca/math/FiltrationStage
    CURIE:  math:FiltrationStage

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FiltrationStage", "curie": "math:FiltrationStage", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FiltrationStage"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    filtrationThreshold: str = Field(description="Relates a math:FiltrationStage to the real-valued threshold ε it sits at — the index value of the stage in the filtration. Domain math:FiltrationStage, range gmeow:Quantity (stated in prose). Every stage names exactly one; a stage without its threshold is ill-formed (math:UnderspecifiedFiltration). The monotonicity law math:filtrationMonotonicityLaw orders stages by this threshold.", alias="math:filtrationThreshold")
    stageStructure: str = Field(description="Relates a math:FiltrationStage to the substructure present at its threshold — the sub-space Kε of the filtered object at ε. Domain math:FiltrationStage, range math:TopologicalSpace (stated in prose). Every stage names exactly one; a stage without its structure is ill-formed (math:UnderspecifiedFiltration). The structure at a lower threshold is a math:subsetOf the structure at a higher one (math:filtrationMonotonicityLaw).", alias="math:stageStructure")


class Math_FittedModel(ConfiguredBaseModel):
    """Fitted Model.

    A fitted model: a math:StatisticalModel bound to concrete data through math:fittedToData
    and specified by a math:modelFormula — the trained artifact an lm/glm/lmer fit produces,
    from which math:Estimate coefficients and math:Residual values are read. A fitted model
    missing its data or its specification is ill-formed (math:UnfittedModel). The R broom
    glance triple lifts a model's summary statistics onto this object.

    Usage:
        >>> from gmeow_models.math import Math_FittedModel
        >>> Math_FittedModel.model_config["json_schema_extra"]["curie"]
        'math:FittedModel'

    IRI:    https://blackcatinformatics.ca/math/FittedModel
    CURIE:  math:FittedModel

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FittedModel", "curie": "math:FittedModel", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FittedModel"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    fittedToData: list[Any] | None = Field(default=None, description="Relates a math:FittedModel to the math:DatasetMatrix it was fitted to. Domain math:FittedModel, range math:DatasetMatrix (stated in prose). Reserved for math:FittedModel — it is the data binding that distinguishes a fitted model from an unbound math:StatisticalModel, never attached to an estimate; a fitted model without it is ill-formed (math:UnfittedModel).", alias="math:fittedToData")
    modelFormula: list[Any] | None = Field(default=None, description="Relates a math:StatisticalModel or math:FittedModel to its math:ModelFormula specification. Domain a math:StatisticalModel, range math:ModelFormula (stated in prose). The specification is a math:BindingExpression AST, never a string; a fitted model without one is ill-formed (math:UnfittedModel).", alias="math:modelFormula")


class Math_FlagshipScenario(ConfiguredBaseModel):
    """Math_FlagshipScenario.

    Generated class projected from shapes; no authored definition (https://blackcatinformatics.ca/math/FlagshipScenario).

    Usage:
        >>> from gmeow_models.math import Math_FlagshipScenario
        >>> Math_FlagshipScenario.model_config["json_schema_extra"]["curie"]
        'math:FlagshipScenario'

    IRI:    https://blackcatinformatics.ca/math/FlagshipScenario
    CURIE:  math:FlagshipScenario

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FlagshipScenario", "curie": "math:FlagshipScenario", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FlagshipScenario"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_FormalVerificationResult(ConfiguredBaseModel):
    """Formal Verification Result.

    A formal verification result: the structured RESULT OBJECT of checking a math:Proof —
    the QED verdict a proof assistant returns. A subclass of math:MathematicalObject, NOT a
    gmeow:Observation: it is the result object of the process / result / claim separation,
    and the held verdict is a separate gmeow:Observation naming the checker as
    gmeow:vantage, the result through gmeow:observationResult, and the run through
    gmeow:wasGeneratedBy. None is typed as another; a result object roled as the claim is
    ill-formed (math:ResultRoleConflation), and a result not grounded by such an observation
    is ill-formed (math:UngroundedVerificationResult).

    Usage:
        >>> from gmeow_models.math import Math_FormalVerificationResult
        >>> Math_FormalVerificationResult.model_config["json_schema_extra"]["curie"]
        'math:FormalVerificationResult'

    IRI:    https://blackcatinformatics.ca/math/FormalVerificationResult
    CURIE:  math:FormalVerificationResult

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FormalVerificationResult", "curie": "math:FormalVerificationResult", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FormalVerificationResult"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_FrequentistResult(ConfiguredBaseModel):
    """Frequentist Result.

    A frequentist inference result: the family of result objects whose semantics rest on the
    behaviour of a procedure under repeated sampling — a p-value, a sampling distribution, a
    confidence interval, statistical power, and the Type I / Type II error rates. A subclass
    of math:MathematicalObject and one of the two inference-paradigm result families, held
    owl:disjointWith math:BayesianResult so a single result is never read as both
    frequentist and Bayesian. The paradigm axis is a PARTIAL classification: only
    paradigm-exclusive kinds are placed under it, so a paradigm-neutral object (a
    math:Estimate, a math:Residual) is left free of either family rather than forced into
    one.

    When to use:
        - Use as the family for result objects whose semantics rest on procedure behaviour under
          repeated sampling: p-values, sampling distributions, confidence intervals, power, and
          error rates.

    When to avoid:
        - Do not place a paradigm-neutral object such as a math:Estimate or math:Residual under
          it; the axis is partial, only paradigm-exclusive kinds belong.

    How to use:
        - Type paradigm-exclusive frequentist results under it; it is held owl:disjointWith
          math:BayesianResult so no result is read as both.

    Examples:
        - exf:p1 a math:PValue , math:FrequentistResult .

    Usage:
        >>> from gmeow_models.math import Math_FrequentistResult
        >>> Math_FrequentistResult.model_config["json_schema_extra"]["curie"]
        'math:FrequentistResult'

    IRI:    https://blackcatinformatics.ca/math/FrequentistResult
    CURIE:  math:FrequentistResult

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FrequentistResult", "curie": "math:FrequentistResult", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FrequentistResult"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Function(ConfiguredBaseModel):
    """Function.

    A function: a relation that assigns to each element of its domain exactly one element of
    its codomain. Every function declares its math:domain and math:codomain — a function
    without them is ill-formed — and may declare its math:image. Injectivity, surjectivity,
    and bijectivity are declared, checkable booleans (math:isInjective, math:isSurjective,
    math:isBijective), not assumed. Anchored externally to the OpenMath fns1 content
    dictionary.

    Usage:
        >>> from gmeow_models.math import Math_Function
        >>> Math_Function.model_config["json_schema_extra"]["curie"]
        'math:Function'

    IRI:    https://blackcatinformatics.ca/math/Function
    CURIE:  math:Function

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Function", "curie": "math:Function", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Function"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    codomain: list[Any] | None = Field(default=None, description="The target set of a function or relation — the set its outputs land in. Domain math:Relation, range math:Set (stated in prose). Every math:Function declares one; a function without a codomain is ill-formed. The codomain is the declared target, distinct from the math:image actually attained.", alias="math:codomain")
    domain: list[Any] | None = Field(default=None, description="The source set of a function or relation — the set its inputs are drawn from. Domain math:Relation, range math:Set (stated in prose). Every math:Function declares one; a function without a domain is ill-formed.", alias="math:domain")


class Math_FunctionPiece(ConfiguredBaseModel):
    """Function Piece.

    One piece of a math:PiecewiseFunction: a domain restriction paired with the
    sub-behaviour that holds on it. A subclass of math:MathematicalObject named by a
    piecewise function through math:hasPiece. It names EXACTLY ONE sub-domain through
    math:pieceDomain (a math:Interval — the sub-domain the piece governs), and, where the
    piece has an explicit closed form, its behaviour through math:pieceExpression (a
    math:MathematicalExpression); a piece whose behaviour is qualitative rather than
    symbolic carries instead a math:hasMonotonicity or a math:hasBound. A piece that names
    no math:pieceDomain does not say WHERE it applies and is ill-formed (the exactly-one
    math:pieceDomain gate). The piece is the case-split cell: the interval says over which
    points the cell holds, the expression or the qualitative marker says what the function
    does there.

    Usage:
        >>> from gmeow_models.math import Math_FunctionPiece
        >>> Math_FunctionPiece.model_config["json_schema_extra"]["curie"]
        'math:FunctionPiece'

    IRI:    https://blackcatinformatics.ca/math/FunctionPiece
    CURIE:  math:FunctionPiece

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/FunctionPiece", "curie": "math:FunctionPiece", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/FunctionPiece"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    pieceDomain: list[Any] | None = Field(default=None, description="Relates a math:FunctionPiece to the math:Interval it governs — the sub-domain over which the piece's behaviour holds. Domain math:FunctionPiece, range math:Interval (stated in prose). Each piece names exactly one piece-domain; a piece without it does not say where it applies and is ill-formed. The interval is a fully-formed math:Interval (both endpoints and both inclusions), so a half-open cell [0, 1) is distinguished from the closed cell [0, 1].", alias="math:pieceDomain")
    pieceExpression: list[Any] | None = Field(default=None, description="Relates a math:FunctionPiece to the math:MathematicalExpression giving its behaviour on its piece-domain — the closed form the function takes on that cell. Domain math:FunctionPiece, range math:MathematicalExpression (stated in prose). At most one; it is optional because a piece may carry qualitative analytic behaviour (math:hasMonotonicity, math:hasBound) instead of an explicit expression, but a piece never carries two conflicting expressions.", alias="math:pieceExpression")


class Math_GramMatrix(ConfiguredBaseModel):
    """Gram Matrix.

    A Gram matrix G: the math:SymmetricMatrix of a math:SymmetricBilinearForm in a
    math:Basis, Gᵢⱼ = B(eᵢ, eⱼ). A subclass of math:SymmetricMatrix that names the form it
    represents through math:representsForm and the basis it is taken in through
    math:inBasis. When the form is positive-definite the Gram matrix is positive-definite
    and the quadratic form xᵀGx is a squared norm; the affect-intensity norm √(xᵀGx) is
    grounded on such a Gram matrix of exact math:RationalValue entries.

    Usage:
        >>> from gmeow_models.math import Math_GramMatrix
        >>> Math_GramMatrix.model_config["json_schema_extra"]["curie"]
        'math:GramMatrix'

    IRI:    https://blackcatinformatics.ca/math/GramMatrix
    CURIE:  math:GramMatrix

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/GramMatrix", "curie": "math:GramMatrix", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/GramMatrix"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    inBasis: list[Any] | None = Field(default=None, description="Relates a math:GramMatrix or a math:Vector to the math:Basis its entries or coordinates are taken relative to. Domain a math:GramMatrix or math:Vector, range math:Basis (stated in prose). Coordinates and matrix entries are basis-relative; naming the basis makes the representation unambiguous.", alias="math:inBasis")
    representsForm: list[Any] | None = Field(default=None, description="Relates a math:GramMatrix to the math:SymmetricBilinearForm it is the matrix of in a basis (Gᵢⱼ = B(eᵢ, eⱼ)). Domain math:GramMatrix, range math:SymmetricBilinearForm (stated in prose). Paired with math:inBasis — the matrix represents THIS form in THAT basis.", alias="math:representsForm")


class Math_HomomorphicEncryptionScheme(ConfiguredBaseModel):
    """Homomorphic Encryption Scheme.

    An encryption scheme that is (partially or fully) a math:RingHomomorphism — computation
    on ciphertexts corresponds to computation on plaintexts. It names the operation it is
    math:homomorphicOver, its math:securityAssumption (a math:LatticeHardnessAssumption),
    and its math:noiseModel; its math:preservationLaw is Dec(E(a) ⊗ E(b)) = a ⊕ b as a
    logic: formula; and its encrypt/evaluate/decrypt steps are gmeow:Activity processes.
    Both a math:EncryptionScheme and a math:RingHomomorphism — the same map+law object as
    det and the Weyl action.

    Usage:
        >>> from gmeow_models.math import Math_HomomorphicEncryptionScheme
        >>> Math_HomomorphicEncryptionScheme.model_config["json_schema_extra"]["curie"]
        'math:HomomorphicEncryptionScheme'

    IRI:    https://blackcatinformatics.ca/math/HomomorphicEncryptionScheme
    CURIE:  math:HomomorphicEncryptionScheme

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/HomomorphicEncryptionScheme", "curie": "math:HomomorphicEncryptionScheme", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/HomomorphicEncryptionScheme"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    homomorphicOver: list[Any] | None = Field(default=None, description="Names the ring operation a homomorphic-encryption scheme carries from plaintext to ciphertext — the operation the homomorphic property holds for. Domain math:HomomorphicEncryptionScheme, range math:Operation (stated in prose). A refinement of math:preservedOperation: being homomorphic over an operation is preserving it.", alias="math:homomorphicOver")
    noiseModel: list[Any] = Field(min_length=1, description="Names the noise-growth model of a lattice-based scheme — noise accumulates with each homomorphic operation and must stay below a decryption threshold. Domain math:HomomorphicEncryptionScheme, range a noise-model object (stated in prose). A homomorphic scheme without a noise model is ill-formed.", alias="math:noiseModel")
    securityAssumption: list[Any] | None = Field(default=None, description="Names the hardness assumption a scheme's security rests on. Domain math:EncryptionScheme, range math:LatticeHardnessAssumption (stated in prose). A homomorphic-encryption scheme without a stated hardness assumption is ill-formed.", alias="math:securityAssumption")


class Math_Homomorphism(ConfiguredBaseModel):
    """Homomorphism.

    A structure-preserving map between algebraic structures of the same kind: it names,
    through math:preservedOperation, the operation it carries and, through
    math:preservationLaw, the logic: formula stating that carrying (φ(a·b) = φ(a)·φ(b)). A
    subclass of math:Morphism; its math:domain and math:codomain are
    math:AlgebraicStructure. math:kernel and math:image are first-class. A homomorphism
    asserted without a preserved operation and law is ill-formed.

    Usage:
        >>> from gmeow_models.math import Math_Homomorphism
        >>> Math_Homomorphism.model_config["json_schema_extra"]["curie"]
        'math:Homomorphism'

    IRI:    https://blackcatinformatics.ca/math/Homomorphism
    CURIE:  math:Homomorphism

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Homomorphism", "curie": "math:Homomorphism", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Homomorphism"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    preservationLaw: list[Any] = Field(min_length=1, description="Names the law a homomorphism (or a homomorphic-encryption scheme) preserves, as a logic: formula — φ(a·b) = φ(a)·φ(b), or Dec(E(a) ⊗ E(b)) = a ⊕ b. Domain math:Homomorphism, range a logic: formula (stated in prose). The value denotes a logic: formula (the denotation seam, a resource), never an opaque string: math: declares the law and logic: owns reasoning over it. A homomorphism without a preservation law is ill-formed.", alias="math:preservationLaw")
    preservedOperation: list[Any] | None = Field(default=None, description="Names an operation a homomorphism carries from domain to codomain (φ(a·b) = φ(a)·φ(b)). Domain math:Homomorphism, range math:Operation (stated in prose). A group homomorphism names one; a ring homomorphism names two (addition and multiplication). The order-agnostic refinement of math:preservesStructure. A homomorphism without a preserved operation is ill-formed.", alias="math:preservedOperation")


class Math_InferenceRun(ConfiguredBaseModel):
    """Inference Run.

    An inference run: the executed occurrence of an estimation or inference — the analysis
    activity that fits a model to data (math:fittedToData), applies an estimator, and
    produces result objects (a math:Estimate, a p-value) tied back through
    gmeow:wasGeneratedBy. A subclass of gmeow:Activity: the PROCESS half of the process /
    result / claim separation, distinct from the math:Estimate result objects it produces
    and from the gmeow:Observation that holds a claim about them from a vantage. Mirrors how
    math:IngestRun and math:PCAAnalysis subclass gmeow:Activity; it is held disjoint from
    gmeow:Observation, so a run typed as its own observation is ill-formed
    (math:ProcessObservationConflation).

    When to use:
        - Mint one for the executed occurrence of an estimation or inference, the analysis
          activity that fits a model and produces result objects.

    When to avoid:
        - Do not type a run as its own observation or as a result object; a run is the PROCESS,
          held disjoint from gmeow:Observation and distinct from the math:Estimate it produces.

    How to use:
        - Type it as a gmeow:Activity, tie its results back through gmeow:wasGeneratedBy, and name
          the plan it followed through math:usedAnalysisPlan.

    Examples:
        - exf:run1 a math:InferenceRun ; math:usedAnalysisPlan exf:plan1 .

    Usage:
        >>> from gmeow_models.math import Math_InferenceRun
        >>> Math_InferenceRun.model_config["json_schema_extra"]["curie"]
        'math:InferenceRun'

    IRI:    https://blackcatinformatics.ca/math/InferenceRun
    CURIE:  math:InferenceRun

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/InferenceRun", "curie": "math:InferenceRun", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/InferenceRun"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_IngestRun(ConfiguredBaseModel):
    """Ingest Run.

    A bridge run: the executed occurrence of the up-projection (put) leg of a
    logic:Correspondence that lifts an external artifact into the grounding layers,
    splitting mathematical/structural content into math: and computation into logic:. A
    subclass of gmeow:Activity — the process half of the process / result / claim separation
    — abstracting the three concrete bridges (math:RIngestRun, math:ONNXIngestRun,
    math:ProofIngestRun). It retains its source as a math:parseSource witness (the in-band
    mnemomorphic complement, marked logic:loadBearing, that discharges put after get equals
    identity), carries the process-layer in-band witness through logic:instantiatesSchema
    and logic:instantiatesPlan, and ties each produced math: object back through
    gmeow:wasGeneratedBy. A run that retains a source but lifts no structured math: codomain
    and enumerates no residue is ill-formed (math:UnliftableIngest).

    Usage:
        >>> from gmeow_models.math import Math_IngestRun
        >>> Math_IngestRun.model_config["json_schema_extra"]["curie"]
        'math:IngestRun'

    IRI:    https://blackcatinformatics.ca/math/IngestRun
    CURIE:  math:IngestRun

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/IngestRun", "curie": "math:IngestRun", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/IngestRun"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    instantiatesPlan: list[Any] = Field(min_length=1, description="Relates an executed action occurrence — a materialized logic:TransactionStep (a gmeow:Event minted per run position) — to the logic:Plan program it was executed under. Domain the occurrence, range logic:Plan (stated in prose). It is the plan-level companion of logic:instantiatesSchema: where that link records the reusable type an occurrence instantiates (Event to ActionSchema), this records the whole plan the occurrence realizes (Event to Plan). The pair is a load-bearing in-band recovery witness — the executed record carries, in band, a back-reference to both the schema and the plan it instantiates — so a correspondence's put leg can recover the planned skeleton from an execution record (the mnemomorphism's in-band complement at the process layer) rather than synthesizing a plausible plan. Without it the descriptive run and the prescriptive plan are only externally related; with it the planned portion round-trips.", alias="logic:instantiatesPlan")
    instantiatesSchema: list[Any] = Field(min_length=1, description="Relates a gmeow:Event (an action occurrence — a particular event executed along a particular path) to the logic:ActionSchema it instantiates. Domain gmeow:Event, range logic:ActionSchema (stated in prose). The occurrence is the same perdurant an intentional moment links to through gmeow:motivates; the schema is the reusable type it instantiates. The schema names what an action does; the path records that it was done and what changed.", alias="logic:instantiatesSchema")
    ingestCorrespondence: list[Any] | None = Field(default=None, description="Relates a math:IngestRun to the single logic:Correspondence node it carries — THE attachment point of the lift's law-spine. Domain math:IngestRun, range logic:Correspondence (stated in prose). Everything about the lift that is a law rather than a facet lives on that Correspondence: its logic:morphismClass (the ordered law-spine rung), logic:preservationKind, logic:mnemomorphic witness flag, logic:correspondenceRelation, logic:hasDeterminacy, and any logic:LawClaim with its logic:DischargeVerdict. math: never shadows those properties; it only points at the node that holds them, exactly as the language slice's lang:translationCorrespondence attaches a translation unit to its crossing. A bridge run carries exactly one.", alias="math:ingestCorrespondence")
    parseSource: list[Any] = Field(min_length=1, description="Relates a math:IngestRun to the in-band source witness it retains — the mnemomorphic complement that lets the up-projection (put) leg recover the source rather than synthesize a plausible one. Domain math:IngestRun; range the retained source witness, a structured node marked logic:loadBearing true and carrying enough of the source to recover what the lift did not map (never a bare droppable string — an opaque blob is the amnesic case), stated in prose. The witness is why 'retain the source' is a law (the Mnemomorphism gate), not a decoration.", alias="math:parseSource")


class Math_Integral(ConfiguredBaseModel):
    """Integral.

    An integral ∫_D f dμ as a first-class object: the application of a
    math:IntegrationOperator naming its integrand (math:integrand, a
    math:MeasurableFunction), its domain (math:integrationDomain, a math:MeasurableSpace or
    measurable set), and the measure it integrates against (math:withRespectTo, a
    math:Measure). It carries the dimension of its result through math:hasDimension; the
    homogeneity gate checks that this equals the integrand's dimension combined with the
    measure's. An expectation in the probability charter is an integral against a
    probability measure.

    Usage:
        >>> from gmeow_models.math import Math_Integral
        >>> Math_Integral.model_config["json_schema_extra"]["curie"]
        'math:Integral'

    IRI:    https://blackcatinformatics.ca/math/Integral
    CURIE:  math:Integral

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Integral", "curie": "math:Integral", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Integral"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    integrand: list[Any] | None = Field(default=None, description="Names the math:MeasurableFunction a math:Integral integrates — the f in ∫_D f dμ. Domain math:Integral, range math:MeasurableFunction (stated in prose). Every integral names one.", alias="math:integrand")
    integrationDomain: Any = Field(description="Names the domain a math:Integral integrates over — the D in ∫_D f dμ, a math:MeasurableSpace or measurable set. Domain math:Integral, range math:MeasurableSpace or math:Set (stated in prose). Every integral names one.", alias="math:integrationDomain")
    withRespectTo: list[Any] | None = Field(default=None, description="Names the math:Measure a math:Integral integrates against — the μ in ∫_D f dμ. Domain math:Integral, range math:Measure (stated in prose). Every integral names exactly one; it is the single measure property of an integral (an expectation integrates against a probability measure). The homogeneity gate combines the integrand's dimension with this measure's to check the integral's declared result dimension.", alias="math:withRespectTo")


class Math_Interval(ConfiguredBaseModel):
    """Interval.

    An ordered real interval: the intensional set { x ∈ ℝ̄ | lower ⋚ x ⋚ upper } of the
    points of the extended real line between two ordered endpoints. It names BOTH its
    endpoints — the lower through math:lowerEndpoint and the upper through
    math:upperEndpoint — and BOTH its endpoint inclusions — whether each end is included (a
    closed, square-bracket end) or excluded (an open, round-bracket end) — through
    math:lowerInclusion and math:upperInclusion. A bounded end names a finite numeric
    literal; an unbounded end names a pole (math:PositiveInfinity or math:NegativeInfinity)
    as its endpoint, and such an infinite end is always open (∞ is a limit point of ℝ̄,
    never a member). The inclusion is NEVER silently omitted: [0, 1) is the half-open unit
    interval and (0, 1) is the open one, and which is meant is data, not convention — an
    interval missing an endpoint or an inclusion is ill-formed
    (math:UnderspecifiedInterval). It is NOT the statistics math:ConfidenceInterval, which
    is a frequentist estimator carrying a coverage level and a point estimate, not an
    ordered subset of the line; a math:Interval is the pure order-theoretic set, the
    external anchor being the OpenMath interval1 content dictionary.

    Usage:
        >>> from gmeow_models.math import Math_Interval
        >>> Math_Interval.model_config["json_schema_extra"]["curie"]
        'math:Interval'

    IRI:    https://blackcatinformatics.ca/math/Interval
    CURIE:  math:Interval

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Interval", "curie": "math:Interval", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Interval"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    lowerEndpoint: Any = Field(description="The lower endpoint of a math:Interval — the left end of { x ∈ ℝ\u{304} | lower ⋚ x ⋚ upper }. Domain math:Interval, range a finite numeric literal (a bounded lower end) or the pole math:NegativeInfinity (an unbounded-below end) (stated in prose). Like math:totalMass and math:extendedRealValue, it is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite endpoint) and an individual (a pole): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. Every math:Interval names exactly one; an interval without it is ill-formed (math:UnderspecifiedInterval).", alias="math:lowerEndpoint")
    lowerInclusion: list[Any] | None = Field(default=None, description="Relates a math:Interval to the math:EndpointInclusion of its lower end — math:closedEndpoint (the lower endpoint is a member, the square bracket of [a, …) or math:openEndpoint (the lower endpoint is excluded, the round bracket of (a, …). Domain math:Interval, range math:EndpointInclusion (stated in prose). The lower inclusion is required and never silently omitted; an interval without it is ill-formed (math:UnderspecifiedInterval).", alias="math:lowerInclusion")
    upperEndpoint: Any = Field(description="The upper endpoint of a math:Interval — the right end of { x ∈ ℝ\u{304} | lower ⋚ x ⋚ upper }. Domain math:Interval, range a finite numeric literal (a bounded upper end) or the pole math:PositiveInfinity (an unbounded-above end) (stated in prose). Like math:totalMass and math:extendedRealValue, it is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite endpoint) and an individual (a pole): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. Every math:Interval names exactly one; an interval without it is ill-formed (math:UnderspecifiedInterval).", alias="math:upperEndpoint")
    upperInclusion: list[Any] | None = Field(default=None, description="Relates a math:Interval to the math:EndpointInclusion of its upper end — math:closedEndpoint (the upper endpoint is a member, the square bracket of …, b]) or math:openEndpoint (the upper endpoint is excluded, the round bracket of …, b). Domain math:Interval, range math:EndpointInclusion (stated in prose). The upper inclusion is required and never silently omitted; an interval without it is ill-formed (math:UnderspecifiedInterval).", alias="math:upperInclusion")


class Math_LieGroup(ConfiguredBaseModel):
    """Lie Group.

    A group that is also a smooth manifold, with smooth group operations (its group-ness
    stated in prose to keep the profile clean). It carries a math:RootSystem through
    math:hasRootSystem and a math:LieAlgebra as its infinitesimal structure. E8 is the
    248-dimensional exceptional simple Lie group.

    Usage:
        >>> from gmeow_models.math import Math_LieGroup
        >>> Math_LieGroup.model_config["json_schema_extra"]["curie"]
        'math:LieGroup'

    IRI:    https://blackcatinformatics.ca/math/LieGroup
    CURIE:  math:LieGroup

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/LieGroup", "curie": "math:LieGroup", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/LieGroup"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasRootSystem: list[Any] | None = Field(default=None, description="Relates a math:LieGroup or math:LieAlgebra to its math:RootSystem. Domain math:LieGroup or math:LieAlgebra, range math:RootSystem (stated in prose). A semisimple Lie group without a root system is ill-formed.", alias="math:hasRootSystem")


class Math_Limit(ConfiguredBaseModel):
    """Limit.

    The limit lim_{x→a} f(x) as a first-class binding expression: it applies the
    math:limitBinder (math:operator), names the expression whose limit is taken through
    math:limitOf, the limit point a through math:limitPoint, and — where it matters — the
    direction/mode through math:limitMode (from the left, from the right, or two-sided). A
    limit missing its expression or its limit point is ill-formed
    (math:UnderspecifiedLimit); the mode is optional because a two-sided limit has no
    direction.

    Usage:
        >>> from gmeow_models.math import Math_Limit
        >>> Math_Limit.model_config["json_schema_extra"]["curie"]
        'math:Limit'

    IRI:    https://blackcatinformatics.ca/math/Limit
    CURIE:  math:Limit

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Limit", "curie": "math:Limit", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Limit"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasLimitResult: list[Any] | None = Field(default=None, description="Relates a math:Limit to the math:LimitResult recording what it evaluates to — its outcome and, for a finite convergent limit, its value. Domain math:Limit, range math:LimitResult (stated in prose). Optional: a math:Limit may name only its expression, point, and mode and leave the evaluated result unstated; where the result is stated, it is a math:LimitResult, never a bare literal, so the outcome is explicit.", alias="math:hasLimitResult")
    limitOf: Any = Field(description="Relates a math:Limit to the math:MathematicalExpression whose limit is taken. Domain math:Limit, range math:MathematicalExpression (stated in prose). Every limit names exactly one; a limit without it is ill-formed (math:UnderspecifiedLimit).", alias="math:limitOf")
    limitPoint: Any = Field(description="Relates a math:Limit to the point its variable approaches — a math:Number, math:PositiveInfinity, or a point of a space. Domain math:Limit, range a math:Number or point (stated in prose). Every limit names exactly one; a limit without a point is ill-formed (math:UnderspecifiedLimit).", alias="math:limitPoint")


class Math_LimitResult(ConfiguredBaseModel):
    """Limit Result.

    The structured result of a math:Limit — what the limit evaluates to: its outcome through
    math:limitOutcome (one of math:convergesFinitely, math:divergesToPositiveInfinity,
    math:divergesToNegativeInfinity, or math:divergesWithoutLimit) and, for a finite
    convergent limit, its value through math:limitResultValue. It is NOT the limit
    expression itself (the math:Limit binding lim_{x→a} f(x), which names math:limitOf and
    math:limitPoint) — it is the evaluated result that limit runs to. A convergent result
    carries a finite value; a result diverging to a pole names that pole
    (math:PositiveInfinity or math:NegativeInfinity) as its value; a result diverging
    without a limit (an oscillating or otherwise non-existent limit) carries no value at
    all. A result missing its outcome is ill-formed (math:UnderspecifiedLimitResult), as is
    one whose value disagrees with the declared outcome.

    Usage:
        >>> from gmeow_models.math import Math_LimitResult
        >>> Math_LimitResult.model_config["json_schema_extra"]["curie"]
        'math:LimitResult'

    IRI:    https://blackcatinformatics.ca/math/LimitResult
    CURIE:  math:LimitResult

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/LimitResult", "curie": "math:LimitResult", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/LimitResult"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    limitOutcome: list[Any] | None = Field(default=None, description="Relates a math:LimitResult to its math:LimitOutcome — math:convergesFinitely, math:divergesToPositiveInfinity, math:divergesToNegativeInfinity, or math:divergesWithoutLimit. Domain math:LimitResult, range math:LimitOutcome (stated in prose). The outcome is required; a result without a declared outcome is ill-formed (math:UnderspecifiedLimitResult).", alias="math:limitOutcome")
    limitResultValue: Any | None = Field(default=None, description="The value a convergent or pole-divergent math:LimitResult evaluates to — a finite numeric literal for a math:convergesFinitely outcome, or the extended-real pole math:PositiveInfinity / math:NegativeInfinity for a math:divergesToPositiveInfinity / math:divergesToNegativeInfinity outcome. Domain math:LimitResult, range a finite numeric literal or a pole individual (stated in prose). Like math:totalMass and math:extendedRealValue, it is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite value) and an individual (a pole): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. Optional and absent on a math:divergesWithoutLimit result, which has no point of ℝ\u{304} to name; present and in agreement with the outcome otherwise (math:UnderspecifiedLimitResult).", alias="math:limitResultValue")


class Math_LogOddsValue(ConfiguredBaseModel):
    """Log-Odds Value.

    A log-odds (logit) value: log(p/(1−p)), a quantity on the whole real line (−∞, ∞). A
    subclass of gmeow:Quantity and a probability-scale transform of a probability, NOT a
    probability — held owl:disjointWith math:ProbabilityValue so a log-odds handed to a
    consumer expecting [0, 1] is a caught error (math:ProbabilityScaleConflation), not a
    silent one. It links back to the probability it transforms through a
    math:ProbabilityScaleTransform.

    Usage:
        >>> from gmeow_models.math import Math_LogOddsValue
        >>> Math_LogOddsValue.model_config["json_schema_extra"]["curie"]
        'math:LogOddsValue'

    IRI:    https://blackcatinformatics.ca/math/LogOddsValue
    CURIE:  math:LogOddsValue

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/LogOddsValue", "curie": "math:LogOddsValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/LogOddsValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Manifold(ConfiguredBaseModel):
    """Manifold.

    A manifold: a topological space locally homeomorphic to ℝⁿ, Hausdorff and
    second-countable, covered by a math:Atlas of charts. A subclass of
    math:TopologicalSpace. It declares both its dimension through math:manifoldDimension and
    its structure kind through math:manifoldStructureKind (topological, smooth, complex,
    Riemannian, Lorentzian) — a manifold missing either is ill-formed
    (math:UnderspecifiedManifold).

    Usage:
        >>> from gmeow_models.math import Math_Manifold
        >>> Math_Manifold.model_config["json_schema_extra"]["curie"]
        'math:Manifold'

    IRI:    https://blackcatinformatics.ca/math/Manifold
    CURIE:  math:Manifold

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Manifold", "curie": "math:Manifold", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Manifold"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    manifoldDimension: int = Field(description="The dimension of a math:Manifold — the n such that it is locally homeomorphic to ℝⁿ. Domain math:Manifold, range xsd:nonNegativeInteger (stated in prose). Every manifold declares exactly one; a manifold without a dimension is ill-formed (math:UnderspecifiedManifold). A chart's target space and a tangent space must match it (math:DimensionMismatch).", alias="math:manifoldDimension")
    manifoldStructureKind: list[Any] | None = Field(default=None, description="Relates a math:Manifold to the math:ManifoldStructureKind it carries — topological, smooth, complex, Riemannian, or Lorentzian. Domain math:Manifold, range math:ManifoldStructureKind (stated in prose). Every manifold declares exactly one; a manifold without a structure kind is ill-formed (math:UnderspecifiedManifold).", alias="math:manifoldStructureKind")


class Math_MathematicalConstant(ConfiguredBaseModel):
    """Mathematical Constant.

    A named mathematical constant: an exact number with a canonical identity, such as π, e,
    or the Euler–Mascheroni constant γ. A subclass of math:Number whose instances are exact
    individuals (math:isExact true) anchored to an external authority — a Wikidata QID and,
    where applicable, an OEIS or DLMF locator, recorded as a math: alignment. A constant is
    never given as a decimal literal; its decimal expansion belongs to a distinct
    math:ApproximateValue that math:approximates it.

    Usage:
        >>> from gmeow_models.math import Math_MathematicalConstant
        >>> Math_MathematicalConstant.model_config["json_schema_extra"]["curie"]
        'math:MathematicalConstant'

    IRI:    https://blackcatinformatics.ca/math/MathematicalConstant
    CURIE:  math:MathematicalConstant

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/MathematicalConstant", "curie": "math:MathematicalConstant", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/MathematicalConstant"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    isExact: list[str] | None = Field(default=None, description="Declares whether a number is an exact value (true) or the abstract value a math:ApproximateValue approximates. Domain math:Number, range xsd:boolean (stated in prose). A math:MathematicalConstant is always exact.", alias="math:isExact")
    quantityValue: list[Any] | None = Field(default=None, max_length=0, description="The concrete numeric literal a value carries — the floating-point or decimal expansion of a math:ApproximateValue, or the finite count of a cardinality. Domain math:ApproximateValue or math:Cardinality, range a numeric literal (stated in prose). A math:MathematicalConstant never carries this directly; its decimal belongs to a distinct math:ApproximateValue.", alias="math:quantityValue")


class Math_Measure(ConfiguredBaseModel):
    """Measure.

    A measure μ: a countably-additive assignment of non-negative extended-real mass to the
    measurable sets of a math:MeasurableSpace. Names the space it acts on through
    math:measureOn and its total mass through math:totalMass — which may be a finite
    non-negative number or math:PositiveInfinity (a σ-finite measure such as a
    math:LebesgueMeasure on the line, or a math:CountingMeasure on an infinite set, has
    infinite total mass). The general object every mass-bearing construction specializes: a
    math:ProbabilityMeasure is a measure of total mass one, and a math:Integral integrates a
    math:MeasurableFunction against a measure.

    Usage:
        >>> from gmeow_models.math import Math_Measure
        >>> Math_Measure.model_config["json_schema_extra"]["curie"]
        'math:Measure'

    IRI:    https://blackcatinformatics.ca/math/Measure
    CURIE:  math:Measure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Measure", "curie": "math:Measure", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Measure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    measureOn: list[Any] | None = Field(default=None, description="Names the math:MeasurableSpace a measure assigns mass over. Domain math:Measure, range math:MeasurableSpace (stated in prose). Every math:Measure declares one; a measure without its space is ill-formed.", alias="math:measureOn")
    totalMass: Any = Field(description="The total mass a measure assigns to its whole space — a finite non-negative number or the extended-real math:PositiveInfinity. Domain math:Measure, range a non-negative numeric literal or math:PositiveInfinity (stated in prose). It is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite mass) and an individual (math:PositiveInfinity): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. A math:ProbabilityMeasure has total mass one.", alias="math:totalMass")


class Math_MeasureEvaluation(ConfiguredBaseModel):
    """Measure Evaluation.

    The structured evaluation μ(A) of a measure on a named measurable subset, reified as a
    first-class object: it names the measure through math:evaluatedMeasure, the subset
    through math:measuredSubset (a math:MeasurableSet), and the resulting mass through
    math:measureResult (a finite non-negative number or math:PositiveInfinity, NEVER
    math:NegativeInfinity — a measure is non-negative). It is NOT the whole-space
    math:totalMass, which is the measure of the ENTIRE space μ(X): a measure evaluation is
    the measure of a NAMED subset A ⊆ X, so distinct subsets of one measure can be evaluated
    and compared side by side — the Lebesgue mass of a two-ball μ(B²) against that of a
    three-ball μ(B³), or the probability of one event against another. Naming all three of
    measure, subset, and result is what makes μ(A) a queryable datum rather than a display
    string; an evaluation missing any of the three is ill-formed
    (math:UnderspecifiedMeasureEvaluation).

    Usage:
        >>> from gmeow_models.math import Math_MeasureEvaluation
        >>> Math_MeasureEvaluation.model_config["json_schema_extra"]["curie"]
        'math:MeasureEvaluation'

    IRI:    https://blackcatinformatics.ca/math/MeasureEvaluation
    CURIE:  math:MeasureEvaluation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/MeasureEvaluation", "curie": "math:MeasureEvaluation", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/MeasureEvaluation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    evaluatedMeasure: list[Any] | None = Field(default=None, description="Names the math:Measure a math:MeasureEvaluation reads — the μ in μ(A). Domain math:MeasureEvaluation, range math:Measure (stated in prose). Every evaluation names exactly one; an evaluation without the measure it evaluates does not say whose mass it reports and is ill-formed (math:UnderspecifiedMeasureEvaluation).", alias="math:evaluatedMeasure")
    measureResult: Any = Field(description="The mass a math:MeasureEvaluation reads off — the value of μ(A), a finite non-negative number or the extended-real math:PositiveInfinity, NEVER math:NegativeInfinity because a measure is non-negative. Domain math:MeasureEvaluation, range a non-negative numeric literal or math:PositiveInfinity (stated in prose). Like math:totalMass, it is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite mass) and an individual (math:PositiveInfinity): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. Every evaluation names exactly one; an evaluation without its result is ill-formed (math:UnderspecifiedMeasureEvaluation), and a result of math:NegativeInfinity is a non-negativity violation (math:MalformedExtendedReal or math:UnderspecifiedMeasureEvaluation).", alias="math:measureResult")
    measuredSubset: list[Any] | None = Field(default=None, description="Names the math:MeasurableSet a math:MeasureEvaluation assigns mass to — the A in μ(A). Domain math:MeasureEvaluation, range math:MeasurableSet (stated in prose). Every evaluation names exactly one; naming the subset is what lets two evaluations of one measure over different subsets be compared, so an evaluation without its subset is ill-formed (math:UnderspecifiedMeasureEvaluation).", alias="math:measuredSubset")


class Math_MetricSignature(ConfiguredBaseModel):
    """Metric Signature.

    The signature (p, q) of a math:MetricTensor: the count of positive
    (math:signaturePositive) and negative (math:signatureNegative) eigenvalues of the
    metric. Structured, not an opaque string — Riemannian is (n, 0), Lorentzian is (1, n−1)
    or (n−1, 1), so 'exactly one timelike direction' is checkable against the manifold's
    structure kind. The sum p + q equals the manifold's dimension.

    Usage:
        >>> from gmeow_models.math import Math_MetricSignature
        >>> Math_MetricSignature.model_config["json_schema_extra"]["curie"]
        'math:MetricSignature'

    IRI:    https://blackcatinformatics.ca/math/MetricSignature
    CURIE:  math:MetricSignature
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/metricsignature-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/MetricSignature", "curie": "math:MetricSignature", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/MetricSignature"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_ModelDiagnostic(ConfiguredBaseModel):
    """Model Diagnostic.

    A model diagnostic: a first-class evaluation of a fitted model — a residual plot's
    summary, a Q–Q assessment, a heteroscedasticity or influence measure, a goodness-of-fit
    statistic. A subclass of math:MathematicalObject that names the math:FittedModel it
    evaluates through math:diagnosticFor and the diagnostic method it applies through
    math:diagnosticMethod, so 'what was checked, on which fit, by which method' is queryable
    rather than folded into a plot's caption. A diagnostic anchored to neither its fitted
    model nor its method is ill-formed (math:UnanchoredDiagnostic).

    When to use:
        - Mint one for a first-class evaluation of a fitted model, a residual summary, a Q-Q
          assessment, a heteroscedasticity or influence measure, or a goodness-of-fit statistic.

    When to avoid:
        - Do not leave a check buried in a plot caption; a diagnostic anchored to neither its fit
          nor its method is math:UnanchoredDiagnostic.

    How to use:
        - Name the math:FittedModel it evaluates through math:diagnosticFor and the method it
          applies through math:diagnosticMethod.

    Examples:
        - exf:qqPlot a math:ModelDiagnostic ; math:diagnosticFor exf:olsFit ;
          math:diagnosticMethod exf:qqAssessment .

    Usage:
        >>> from gmeow_models.math import Math_ModelDiagnostic
        >>> Math_ModelDiagnostic.model_config["json_schema_extra"]["curie"]
        'math:ModelDiagnostic'

    IRI:    https://blackcatinformatics.ca/math/ModelDiagnostic
    CURIE:  math:ModelDiagnostic

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ModelDiagnostic", "curie": "math:ModelDiagnostic", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ModelDiagnostic"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    diagnosticFor: list[Any] = Field(min_length=1, description="Relates a math:ModelDiagnostic (or the math:ModelComparison, math:InformationCriterion, math:CalibrationDiagnostic, and math:CrossValidationResult that specialize the diagnostic role over a fit) to the math:FittedModel it evaluates. Domain a model-evaluation object, range math:FittedModel (stated in prose). It is the anchoring edge that ties an evaluation to the fit it speaks about; a math:ModelDiagnostic naming no fitted model through it is ill-formed (math:UnanchoredDiagnostic). One shared edge for 'the fit this evaluation is about', never a near-synonym per diagnostic kind.", alias="math:diagnosticFor")
    diagnosticMethod: list[Any] = Field(min_length=1, description="Relates a math:ModelDiagnostic to the method it applies — a residual-analysis procedure, a goodness-of-fit test, an influence measure, a reliability-curve or expected-calibration-error binning for a math:CalibrationDiagnostic. Domain math:ModelDiagnostic, range the diagnostic method object (stated in prose). The method is a first-class object, not an opaque label, so a diagnostic's procedure is queryable alongside the fit it evaluates; a diagnostic naming no method is ill-formed (math:UnanchoredDiagnostic). Held apart from math:usesProofMethod (a proof's strategy) and math:estimator (an estimate's procedure): this is the method a diagnostic applies over a fit.", alias="math:diagnosticMethod")


class Math_ModelFormula(ConfiguredBaseModel):
    """Model Formula.

    A model formula: the structured specification a math:StatisticalModel relates a response
    to its terms by — the R y ~ x1 + x2 form lifted as a math:BindingExpression AST (the ~
    is a binder over indexed math:ArgumentSlot operands, never a string), so the response,
    terms, interactions, and offsets are inspectable structure. A subclass of
    math:MathematicalExpression; carried by math:modelFormula.

    Usage:
        >>> from gmeow_models.math import Math_ModelFormula
        >>> Math_ModelFormula.model_config["json_schema_extra"]["curie"]
        'math:ModelFormula'

    IRI:    https://blackcatinformatics.ca/math/ModelFormula
    CURIE:  math:ModelFormula
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/modelformula-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ModelFormula", "curie": "math:ModelFormula", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ModelFormula"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    argumentSlot: list[Any] | None = Field(default=None, description="Relates a math:ApplicationExpression or math:BindingExpression to one math:ArgumentSlot carrying an indexed operand (or, for a binder, the body). Domain a math:ApplicationExpression or math:BindingExpression, range math:ArgumentSlot (stated in prose). Operand order is carried by the slots' math:slotIndex, never by RDF list ordering.", alias="math:argumentSlot")


class Math_Norm(ConfiguredBaseModel):
    """Norm.

    A norm ‖·‖: the length function induced by a positive-definite
    math:SymmetricBilinearForm, ‖x‖ = √⟨x, x⟩ = √(xᵀGx). A subclass of
    math:MathematicalObject that names the form inducing it through math:inducedByForm. A
    norm is well-defined only when its form is positive-definite (a
    math:NonPositiveDefiniteNorm otherwise); it induces a metric math:Distance d(x, y) = ‖x
    − y‖.

    Usage:
        >>> from gmeow_models.math import Math_Norm
        >>> Math_Norm.model_config["json_schema_extra"]["curie"]
        'math:Norm'

    IRI:    https://blackcatinformatics.ca/math/Norm
    CURIE:  math:Norm
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/norm-class-453ee54729b0

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Norm", "curie": "math:Norm", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Norm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    inducedByForm: Any = Field(description="Relates a math:Norm to the math:SymmetricBilinearForm that induces it (‖x‖ = √⟨x, x⟩). Domain math:Norm, range math:SymmetricBilinearForm (stated in prose). The form must be positive-definite for the norm to be well-defined (math:NonPositiveDefiniteNorm otherwise).", alias="math:inducedByForm")


class Math_Number(ConfiguredBaseModel):
    """Number.

    A mathematical number: an abstract quantity that lives in a declared number system.
    Every math:Number situates itself in a math:NumberSystem through math:inNumberSystem —
    an unsituated number is ill-formed — and declares through math:isExact whether it is an
    exact value or the abstract counterpart that a math:ApproximateValue approximates. The
    five nested systems ℕ ⊂ ℤ ⊂ ℚ ⊂ ℝ ⊂ ℂ are its principal subclasses.

    Usage:
        >>> from gmeow_models.math import Math_Number
        >>> Math_Number.model_config["json_schema_extra"]["curie"]
        'math:Number'

    IRI:    https://blackcatinformatics.ca/math/Number
    CURIE:  math:Number

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Number", "curie": "math:Number", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Number"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    inNumberSystem: list[Any] | None = Field(default=None, description="Situates a number in the system it belongs to. Domain math:Number, range math:NumberSystem (stated in prose). Every math:Number declares exactly one — an unsituated number is ill-formed.", alias="math:inNumberSystem")


class Math_OddsValue(ConfiguredBaseModel):
    """Odds Value.

    An odds value: p/(1−p), a quantity in the open interval (0, ∞). A subclass of
    gmeow:Quantity and a probability-scale transform of a probability, NOT a probability —
    held owl:disjointWith math:ProbabilityValue so it can never be read as one
    (math:ProbabilityScaleConflation). It links back to the probability it transforms
    through a math:ProbabilityScaleTransform.

    Usage:
        >>> from gmeow_models.math import Math_OddsValue
        >>> Math_OddsValue.model_config["json_schema_extra"]["curie"]
        'math:OddsValue'

    IRI:    https://blackcatinformatics.ca/math/OddsValue
    CURIE:  math:OddsValue

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/OddsValue", "curie": "math:OddsValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/OddsValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_OrthogonalComplement(ConfiguredBaseModel):
    """Orthogonal Complement.

    The orthogonal complement S⊥ of a subspace S within an inner-product space — the vectors
    orthogonal to every vector of S under a declared inner product. A subclass of
    math:Complement that realizes the geometry charter's complement contract: it names its
    math:ambientSpace, sets math:complementSemantics math:orthogonalComplement, and
    additionally is math:definedByInnerProduct (the math:HermitianInnerProduct in the
    complex case), so 'the orthogonal complement' is never ambiguous. An orthogonal
    complement not naming its inner product is ill-formed
    (math:UnqualifiedOrthogonalComplement).

    Usage:
        >>> from gmeow_models.math import Math_OrthogonalComplement
        >>> Math_OrthogonalComplement.model_config["json_schema_extra"]["curie"]
        'math:OrthogonalComplement'

    IRI:    https://blackcatinformatics.ca/math/OrthogonalComplement
    CURIE:  math:OrthogonalComplement
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/orthogonalcomplement-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/OrthogonalComplement", "curie": "math:OrthogonalComplement", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/OrthogonalComplement"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    definedByInnerProduct: Any = Field(description="Relates a math:OrthogonalComplement to the inner product that defines orthogonality for it — the math:HermitianInnerProduct in the complex case. Domain math:OrthogonalComplement, range a math:HermitianInnerProduct or real inner-product structure (stated in prose). An orthogonal complement without a named inner product is ambiguous and ill-formed (math:UnqualifiedOrthogonalComplement).", alias="math:definedByInnerProduct")


class Math_PCAAnalysis(ConfiguredBaseModel):
    """PCA Analysis.

    A principal-component analysis: the analysis PROCESS that decomposes a centered, scaled
    input into orthogonal directions of maximal variance. A subclass of gmeow:Activity (an
    analysis is a process, distinct from its result objects and from any claim about them).
    It declares its math:analysisInput, its math:centeringPolicy and math:scalingPolicy, the
    math:CovarianceOperator it decomposes (math:covarianceOperator), the math:eigensolver
    used, and its outputs — math:principalComponent, math:loadingVector, math:scoreVector,
    math:explainedVarianceRatio, and its math:residualSubspace. A PCA missing any of these
    declared roles is ill-formed (math:IncompletePCAAnalysis).

    Usage:
        >>> from gmeow_models.math import Math_PCAAnalysis
        >>> Math_PCAAnalysis.model_config["json_schema_extra"]["curie"]
        'math:PCAAnalysis'

    IRI:    https://blackcatinformatics.ca/math/PCAAnalysis
    CURIE:  math:PCAAnalysis

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/PCAAnalysis", "curie": "math:PCAAnalysis", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/PCAAnalysis"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    analysisInput: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to the object it decomposes — a data matrix, a subspace, or a residual subspace. Domain math:PCAAnalysis, range a math:MathematicalObject (a matrix, math:Subspace, or math:OrthogonalComplement) (stated in prose). The declared input of the analysis; an analysis without its input is ill-formed (math:IncompletePCAAnalysis).", alias="math:analysisInput")
    centeringPolicy: list[Any] | None = Field(default=None, description="Relates a math:PCAAnalysis to its math:CenteringPolicy. Domain math:PCAAnalysis, range math:CenteringPolicy (stated in prose). Every PCA declares exactly one; the covariance it decomposes depends on it.", alias="math:centeringPolicy")
    covarianceOperator: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to the math:CovarianceOperator it decomposes. Domain math:PCAAnalysis, range math:CovarianceOperator (stated in prose). The operator whose spectral decomposition yields the principal components; a PCA without it is ill-formed (math:IncompletePCAAnalysis).", alias="math:covarianceOperator")
    eigensolver: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to the solver used to compute the decomposition — for example a full eigendecomposition or a randomized math:SingularValueDecomposition. Domain math:PCAAnalysis, range a math:Eigendecomposition, math:SingularValueDecomposition, or named solver (stated in prose). The declared method; an analysis without it is ill-formed (math:IncompletePCAAnalysis).", alias="math:eigensolver")
    explainedVarianceRatio: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to a math:ExplainedVariance it produces — the share of total variance a component accounts for. Domain math:PCAAnalysis, range math:ExplainedVariance (stated in prose). A declared output of the analysis.", alias="math:explainedVarianceRatio")
    loadingVector: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to a math:LoadingVector it produces — the coefficients of a component in the original features. Domain math:PCAAnalysis, range math:LoadingVector (stated in prose). A declared output of the analysis.", alias="math:loadingVector")
    principalComponent: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to one of the math:PrincipalComponent directions it produces. Domain math:PCAAnalysis, range math:PrincipalComponent (stated in prose). The declared component outputs; a PCA producing none is ill-formed (math:IncompletePCAAnalysis).", alias="math:principalComponent")
    residualSubspace: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to its math:ProjectionResidual or the residual subspace left after retaining the leading components — often a math:OrthogonalComplement. Domain math:PCAAnalysis, range a math:ProjectionResidual, math:Subspace, or math:OrthogonalComplement (stated in prose). A declared output; what the residual MEANS is a math:ResidualInterpretationClaim, not a property of the subspace.", alias="math:residualSubspace")
    scalingPolicy: list[Any] | None = Field(default=None, description="Relates a math:PCAAnalysis to its math:ScalingPolicy. Domain math:PCAAnalysis, range math:ScalingPolicy (stated in prose). Every PCA declares exactly one; standardizing to unit variance changes which directions carry the variance.", alias="math:scalingPolicy")
    scoreVector: list[Any] = Field(min_length=1, description="Relates a math:PCAAnalysis to a math:ScoreVector it produces — a data point's coordinates in the principal-component basis. Domain math:PCAAnalysis, range math:ScoreVector (stated in prose). A declared output of the analysis.", alias="math:scoreVector")


class Math_PValue(ConfiguredBaseModel):
    """P-Value.

    A p-value: P(T ≥ t | H0), the probability — framed by a null or sampling distribution —
    of a test statistic as or more extreme than the one observed, computed under the null
    hypothesis. A subclass of math:FrequentistResult; its magnitude is carried through
    math:pValue as a framed math:ProbabilityValue, never a bare literal. To be unambiguous
    it carries its test (its math:testStatistic, and/or the math:HypothesisTest whose
    math:pValue it is), its null hypothesis (math:nullHypothesis), its null distribution
    (math:nullDistribution), and its tail definition or sidedness (math:tailDefinition /
    math:alternativeSidedness). A p-value with a null and a statistic but no tail or
    sidedness is still ambiguous and ill-formed (math:IllFramedPValue), because whether the
    extremity is one-tailed, two-tailed, or a mid-p correction changes the value.

    When to use:
        - Mint one for the probability, under the null, of a statistic as or more extreme than
          observed, carrying its full test frame so it is unambiguous.

    When to avoid:
        - Do not read a p-value as the probability the null is true, and do not report one with a
          null and statistic but no tail or sidedness; both are misuses this class guards against.

    How to use:
        - Carry its math:testStatistic or math:HypothesisTest, its math:nullHypothesis, its
          math:nullDistribution, and its math:tailDefinition or math:alternativeSidedness; its
          magnitude is a framed math:pValue.

    Examples:
        - exf:p1 a math:PValue ; math:nullHypothesis exf:h0 ; math:nullDistribution exf:tNull ;
          math:alternativeSidedness math:twoSidedAlternative .

    Usage:
        >>> from gmeow_models.math import Math_PValue
        >>> Math_PValue.model_config["json_schema_extra"]["curie"]
        'math:PValue'

    IRI:    https://blackcatinformatics.ca/math/PValue
    CURIE:  math:PValue
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/pvalue-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/PValue", "curie": "math:PValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/PValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    alternativeSidedness: list[Math_PValueAlternativeSidednessEnum] | None = Field(default=None, description="Relates a math:PValue to its math:Sidedness — the enumerated value (one of the six math:Sidedness individuals) fixing whether the extremity is one-sided, two-sided, greater, less, exact, or mid-p. Domain math:PValue, range math:Sidedness (stated in prose). A closed value carried by sh:in; a p-value declaring neither a sidedness nor a math:tailDefinition is ill-framed (math:IllFramedPValue), and a value outside the six is not a lawful sidedness.", alias="math:alternativeSidedness")


class Math_PersistenceLifetime(ConfiguredBaseModel):
    """Persistence Lifetime.

    The birth–death interval of a single topological feature across a math:Filtration: the
    range of thresholds over which one math:HomologyGroup generator (a hole of some
    dimension) exists. A subclass of math:MathematicalObject. It names the filtration it is
    read over through math:overFiltration, the feature it tracks through
    math:persistenceFeature (a math:HomologyGroup generator), the threshold at which the
    feature appears through math:bornAt (a gmeow:Quantity), and the threshold at which it
    disappears through math:diesAt. Its persistence value is death − birth; a long-lived
    feature is signal, a short-lived one is noise. An ESSENTIAL feature never dies within
    the filtration, so its math:diesAt is the individual math:PositiveInfinity rather than a
    finite threshold — exactly as math:totalMass ranges over a finite value or
    math:PositiveInfinity, which is why math:diesAt is a bare rdf:Property. A lifetime
    missing its birth or death is ill-formed (math:UnderspecifiedPersistenceLifetime).

    Usage:
        >>> from gmeow_models.math import Math_PersistenceLifetime
        >>> Math_PersistenceLifetime.model_config["json_schema_extra"]["curie"]
        'math:PersistenceLifetime'

    IRI:    https://blackcatinformatics.ca/math/PersistenceLifetime
    CURIE:  math:PersistenceLifetime

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/PersistenceLifetime", "curie": "math:PersistenceLifetime", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/PersistenceLifetime"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    bornAt: str = Field(description="Relates a math:PersistenceLifetime to the threshold at which its feature appears in the filtration — the birth ε of the feature. Domain math:PersistenceLifetime, range gmeow:Quantity (stated in prose). Every lifetime names exactly one birth; a lifetime without it is ill-formed (math:UnderspecifiedPersistenceLifetime). Birth is always a finite threshold (a feature is born when it first appears); the death (math:diesAt) may be finite or math:PositiveInfinity.", alias="math:bornAt")
    diesAt: str = Field(description="Relates a math:PersistenceLifetime to the threshold at which its feature disappears — the death ε of the feature, or math:PositiveInfinity for an ESSENTIAL feature that never dies within the filtration. Domain math:PersistenceLifetime, range a gmeow:Quantity (the finite death threshold) or the individual math:PositiveInfinity (stated in prose). It is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a finite quantity individual and the extended-real math:PositiveInfinity — the same extended-real modelling math:totalMass uses, so the honest RDF supertype carries both without a DL contradiction. A lifetime missing its death entirely is ill-formed (math:UnderspecifiedPersistenceLifetime): even an essential feature must state math:PositiveInfinity rather than omit the death.", alias="math:diesAt")
    overFiltration: Math_Filtration = Field(description="Relates a math:PersistenceLifetime to the math:Filtration it is read over — the indexed family across which the feature is born and dies. Domain math:PersistenceLifetime, range math:Filtration (stated in prose). A lifetime is always relative to a filtration; its birth and death thresholds are index values of that filtration.", alias="math:overFiltration")
    persistenceFeature: str = Field(description="Relates a math:PersistenceLifetime to the topological feature it tracks — a math:HomologyGroup generator (a connected component, loop, or higher-dimensional void) whose birth and death the lifetime records. Domain math:PersistenceLifetime, range math:HomologyGroup (stated in prose). The feature whose survival across the filtration is the persistence signal.", alias="math:persistenceFeature")


class Math_PiecewiseFunction(ConfiguredBaseModel):
    """Piecewise Function.

    A function defined by pieces: a math:Function whose behaviour is given not by one closed
    form but by a family of math:FunctionPiece parts, each a sub-behaviour over its own
    domain restriction. A subclass of math:Function, so it inherits the requirement to
    declare its math:domain and math:codomain (an unframed one is math:UnframedFunction); it
    additionally names one or more pieces through math:hasPiece. A piecewise function
    declaring NO piece says nothing about what it computes and is ill-formed
    (math:UnderspecifiedPiecewiseFunction). The pieces partition (or cover) the domain; each
    piece names the sub-domain it governs through math:pieceDomain and, where it has an
    explicit form, its behaviour through math:pieceExpression — so the classic split f(x) =
    {x² for x < 0; e⁻ˣ for x ≥ 0} is data (two pieces over two intervals), not prose. It is
    not a math:ClosedFormFunction (one expression in one formal argument); it is the
    case-split generalization, and a piece may carry qualitative analytic behaviour
    (math:hasMonotonicity, math:hasBound) rather than a full expression.

    Usage:
        >>> from gmeow_models.math import Math_PiecewiseFunction
        >>> Math_PiecewiseFunction.model_config["json_schema_extra"]["curie"]
        'math:PiecewiseFunction'

    IRI:    https://blackcatinformatics.ca/math/PiecewiseFunction
    CURIE:  math:PiecewiseFunction

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/PiecewiseFunction", "curie": "math:PiecewiseFunction", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/PiecewiseFunction"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasPiece: list[Any] | None = Field(default=None, description="Relates a math:PiecewiseFunction to one of its math:FunctionPiece parts. Domain math:PiecewiseFunction, range math:FunctionPiece (stated in prose). A piecewise function names one or more pieces (≥ 1, unbounded — a function has at least one piece, and may have arbitrarily many); a piecewise function with no piece is ill-formed (math:UnderspecifiedPiecewiseFunction).", alias="math:hasPiece")


class Math_ProbabilityEvent(ConfiguredBaseModel):
    """Probability Event.

    A probability event: a measurable subset of a sample space — a MEASURABLE SET OF
    OUTCOMES, in the measure-theoretic sense, situated in a space through math:eventInSpace
    and assigned a framed probability through math:probabilityOfEvent. A subclass of
    math:Set. It is held owl:disjointWith logic:Event because a logic:Event is an occurrent
    (something that happens in time) whereas a probability event is a set of outcomes: a
    rain occurrence and the rain event-set in a forecast space are never conflated.
    Membership of an event in the space's algebra is math:eventInSpace against the space's
    math:eventSigmaAlgebra; there is NO separate EventSet class — the σ-algebra is the sole
    event algebra.

    Usage:
        >>> from gmeow_models.math import Math_ProbabilityEvent
        >>> Math_ProbabilityEvent.model_config["json_schema_extra"]["curie"]
        'math:ProbabilityEvent'

    IRI:    https://blackcatinformatics.ca/math/ProbabilityEvent
    CURIE:  math:ProbabilityEvent

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ProbabilityEvent", "curie": "math:ProbabilityEvent", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ProbabilityEvent"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_ProbabilityMeasure(ConfiguredBaseModel):
    """Probability Measure.

    A probability measure: a math:Measure of total mass one (math:totalMass 1). The
    probability bridge — the probability charter specializes this class rather than
    re-minting it, so a probability space is a math:MeasurableSpace carrying a probability
    measure and an expectation is a math:Integral against it. A probability measure that
    does not have total mass one is ill-formed (math:ProbabilityMeasureMassViolation).

    Usage:
        >>> from gmeow_models.math import Math_ProbabilityMeasure
        >>> Math_ProbabilityMeasure.model_config["json_schema_extra"]["curie"]
        'math:ProbabilityMeasure'

    IRI:    https://blackcatinformatics.ca/math/ProbabilityMeasure
    CURIE:  math:ProbabilityMeasure
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/probabilitymeasure-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ProbabilityMeasure", "curie": "math:ProbabilityMeasure", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ProbabilityMeasure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    totalMass: list[str] | None = Field(default=None, description="The total mass a measure assigns to its whole space — a finite non-negative number or the extended-real math:PositiveInfinity. Domain math:Measure, range a non-negative numeric literal or math:PositiveInfinity (stated in prose). It is an rdf:Property, not an owl:ObjectProperty or owl:DatatypeProperty, precisely because its range spans both a datatype literal (the finite mass) and an individual (math:PositiveInfinity): neither DL property kind admits both, so the honest RDF supertype carries the extended-real range without a DL contradiction. A math:ProbabilityMeasure has total mass one.", alias="math:totalMass")


class Math_ProbabilitySpace(ConfiguredBaseModel):
    """Probability Space.

    A probability space: the Kolmogorov triple (Ω, ℱ, P) made explicit as one object — a
    sample space Ω named through math:sampleSpace (a math:SampleSpace), an event σ-algebra ℱ
    named through math:eventSigmaAlgebra (a math:SigmaAlgebra), and a probability measure P
    named through math:probabilityMeasure (a math:ProbabilityMeasure). A subclass of
    math:MeasurableSpace carrying a probability measure — the probability charter
    SPECIALIZES the measure-theoretic bedrock rather than re-minting it. Each of the three
    components must be present, but any of them may be symbolic or generative (a
    math:SymbolicSampleSpace, a math:BorelSigmaAlgebra, a math:MeasureDefinitionExpression)
    rather than extensionally enumerated; a probability space missing any of the three
    components is ill-formed (math:IncompleteProbabilitySpace). The gate requires presence
    and structural completeness, never impossible enumeration.

    Usage:
        >>> from gmeow_models.math import Math_ProbabilitySpace
        >>> Math_ProbabilitySpace.model_config["json_schema_extra"]["curie"]
        'math:ProbabilitySpace'

    IRI:    https://blackcatinformatics.ca/math/ProbabilitySpace
    CURIE:  math:ProbabilitySpace

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ProbabilitySpace", "curie": "math:ProbabilitySpace", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ProbabilitySpace"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    eventSigmaAlgebra: list[Any] | None = Field(default=None, description="Names the event σ-algebra ℱ of a probability space — the algebra of measurable sets against which event membership is decided. Domain math:ProbabilitySpace, range math:SigmaAlgebra (stated in prose). Every math:ProbabilitySpace names one (extensional or symbolic, e.g. a math:BorelSigmaAlgebra); a space missing it is ill-formed (math:IncompleteProbabilitySpace). The σ-algebra is the sole event algebra — there is no separate EventSet class.", alias="math:eventSigmaAlgebra")
    probabilityMeasure: list[Any] | None = Field(default=None, description="Names the probability measure P of a probability space — the math:ProbabilityMeasure of total mass one assigning probabilities to the events of its σ-algebra. Domain math:ProbabilitySpace, range math:ProbabilityMeasure (stated in prose). Every math:ProbabilitySpace names one, possibly given by a math:MeasureDefinitionExpression; a space missing it is ill-formed (math:IncompleteProbabilitySpace).", alias="math:probabilityMeasure")
    sampleSpace: list[Any] | None = Field(default=None, description="Names the sample space Ω of a probability space. Domain math:ProbabilitySpace, range math:SampleSpace (stated in prose). Every math:ProbabilitySpace names one; the sample space may be symbolic or generative but never absent — a space missing it is ill-formed (math:IncompleteProbabilitySpace).", alias="math:sampleSpace")


class Math_ProbabilityValue(ConfiguredBaseModel):
    """Probability Value.

    A probability value: a quantity/result object that is ALWAYS in the closed unit interval
    [0, 1], carrying its magnitude and naming its probability frame or model. A subclass of
    gmeow:Quantity (a probability is math:dimensionless). Where exact, it is carried as a
    math:RationalValue with math:isExact true (the exact/approximate split — 1/3, not
    0.333); where empirical it may be an approximate value. The [0, 1] bound is a genuine
    hard fail enforced by SHACL and a native numeric check (math:ProbabilityOutOfBounds);
    odds and log-odds are NOT probabilities and are their own kinds (math:OddsValue,
    math:LogOddsValue), held disjoint from this class so a consumer seeing a
    math:ProbabilityValue may correctly assume a closed unit interval.

    Usage:
        >>> from gmeow_models.math import Math_ProbabilityValue
        >>> Math_ProbabilityValue.model_config["json_schema_extra"]["curie"]
        'math:ProbabilityValue'

    IRI:    https://blackcatinformatics.ca/math/ProbabilityValue
    CURIE:  math:ProbabilityValue

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ProbabilityValue", "curie": "math:ProbabilityValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ProbabilityValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_ProofCheckActivity(ConfiguredBaseModel):
    """Proof Check Activity.

    The occurrent RUN of checking a math:Proof — a proof-assistant execution (Coq, Lean,
    Isabelle, Metamath) that consumes a proof (math:usedProof). A subclass of
    gmeow:Activity: the PROCESS half of the process / result / claim separation. The engine
    it ran under is named as the gmeow:vantage of the grounding gmeow:Observation and
    through math:verifiedByEngine on the result object. It is NEVER typed as its own result
    object (a math:FormalVerificationResult) nor as the held claim (a gmeow:Observation);
    the run produces the result object (tied back through gmeow:wasGeneratedBy) and the
    observation grounds the verdict from the checker's vantage. A process typed as an
    observation is ill-formed (math:ProcessObservationConflation).

    Usage:
        >>> from gmeow_models.math import Math_ProofCheckActivity
        >>> Math_ProofCheckActivity.model_config["json_schema_extra"]["curie"]
        'math:ProofCheckActivity'

    IRI:    https://blackcatinformatics.ca/math/ProofCheckActivity
    CURIE:  math:ProofCheckActivity

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ProofCheckActivity", "curie": "math:ProofCheckActivity", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ProofCheckActivity"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Quantity(ConfiguredBaseModel):
    """Quantity.

    A dimensioned quantity: a value that carries a math:Dimension through math:hasDimension
    and, where measured, a unit (gmeow:unit, aligned to QUDT / OM 2) that realizes that
    dimension and a numeric magnitude (math:quantityValue). Every quantity carries a
    dimension — an undimensioned quantity is ill-formed (math:UndimensionedQuantity). Units
    live within a dimension; two quantities are comparable exactly when their dimensions are
    equal (math:commensurableWith).

    Usage:
        >>> from gmeow_models.math import Math_Quantity
        >>> Math_Quantity.model_config["json_schema_extra"]["curie"]
        'math:Quantity'

    IRI:    https://blackcatinformatics.ca/math/Quantity
    CURIE:  math:Quantity
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/quantity-class-a3ee3df2f961

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Quantity", "curie": "math:Quantity", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Quantity"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasDimension: list[Any] | None = Field(default=None, description="Names the math:Dimension a quantity, integral, or dimensioned object carries. Domain math:Quantity (or another dimensioned object such as a math:Integral), range math:Dimension (stated in prose). Every math:Quantity declares exactly one — an undimensioned quantity is ill-formed.", alias="math:hasDimension")


class Math_RandomVariable(ConfiguredBaseModel):
    """Random Variable.

    A random variable: a measurable map from a probability space's sample space into a
    measurable codomain, with a declared domain (math:randomVariableDomain) and codomain
    (math:randomVariableCodomain) and a probability-space or distribution context
    (math:hasDistribution). A subclass of math:MathematicalObject. Its dimensional
    constraints are CARRIED, not assumed — a random variable over height and its
    distribution's location parameter share height units — so a mis-dimensioned parameter is
    a caught error. A random variable without its domain, codomain, and a space or
    distribution context is ill-formed (math:UnscopedRandomVariable).

    Usage:
        >>> from gmeow_models.math import Math_RandomVariable
        >>> Math_RandomVariable.model_config["json_schema_extra"]["curie"]
        'math:RandomVariable'

    IRI:    https://blackcatinformatics.ca/math/RandomVariable
    CURIE:  math:RandomVariable

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/RandomVariable", "curie": "math:RandomVariable", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/RandomVariable"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    randomVariableCodomain: list[Any] = Field(min_length=1, description="Names the codomain of a random variable — the measurable space it maps into (the real line with its Borel algebra for a real-valued variable). Domain math:RandomVariable, range math:MeasurableSpace (stated in prose). A random variable without its codomain is ill-formed (math:UnscopedRandomVariable).", alias="math:randomVariableCodomain")
    randomVariableDomain: list[Any] = Field(min_length=1, description="Names the domain of a random variable — the measurable space (typically the sample space of a probability space) it is defined on. Domain math:RandomVariable, range math:MeasurableSpace or math:ProbabilitySpace (stated in prose). A random variable without its domain is ill-formed (math:UnscopedRandomVariable).", alias="math:randomVariableDomain")


class Math_RationalValue(ConfiguredBaseModel):
    """Rational Value.

    An exact rational carrier: a concrete p/q value held as a math:numerator and
    math:denominator integer pair (math:denominator ≠ 0). A subclass of
    math:MathematicalObject. It realizes the ℚ coordinate that the tower's
    math:RationalNumber describes abstractly, and — unlike math:ApproximateValue, which
    carries a decimal literal — it carries NO decimal: 1/4 is the pair (1, 4), never 0.25.
    Matrix entries and vector components of the exact numeric layer are math:RationalValue,
    so xᵀGx and the norm √(xᵀGx) are computed exactly.

    Usage:
        >>> from gmeow_models.math import Math_RationalValue
        >>> Math_RationalValue.model_config["json_schema_extra"]["curie"]
        'math:RationalValue'

    IRI:    https://blackcatinformatics.ca/math/RationalValue
    CURIE:  math:RationalValue

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/RationalValue", "curie": "math:RationalValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/RationalValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_ResidualInterpretationClaim(ConfiguredBaseModel):
    """Residual Interpretation Claim.

    The claim of what a math:ProjectionResidual, a math:PrincipalComponent, or a latent
    dimension MEANS — 'this direction encodes X'. A subclass of gmeow:Observation: semantic
    meaning read off geometry is inference from a standpoint, so it carries a gmeow:vantage
    (whose frame), a gmeow:observedFeature (which residual/component/dimension), and a
    gmeow:observationResult (the grounded conclusion, typically a logic:MetaLevelFormula),
    never an intrinsic property of the vector. A meaning asserted without its vantage is
    ill-formed (math:ResidualMeaningAsProperty); and no direct meaning property is minted,
    so a meaning is representable only through this claim.

    Usage:
        >>> from gmeow_models.math import Math_ResidualInterpretationClaim
        >>> Math_ResidualInterpretationClaim.model_config["json_schema_extra"]["curie"]
        'math:ResidualInterpretationClaim'

    IRI:    https://blackcatinformatics.ca/math/ResidualInterpretationClaim
    CURIE:  math:ResidualInterpretationClaim

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/ResidualInterpretationClaim", "curie": "math:ResidualInterpretationClaim", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/ResidualInterpretationClaim"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    observationResult: list[Any] = Field(min_length=1, description="The entity-valued result of an observation — a coordinate set, an instant, a quality value, a quantity, or another entity. For literal scalar readings (e.g. 22.5°C), the observationResult entity carries the literal via a datatype property (e.g. qudt:quantityValue or a module-specific scalar property). Non-functional: a single observation may yield several results in different frames or granularities, and competing result claims coexist rather than collapse.", alias="gmeow:observationResult")
    observedFeature: list[Any] = Field(min_length=1, description="The individual whose property or state is being observed — the feature of interest. The range is intentionally open (owl:Thing) because anything can be observed: a place, a person, an event, a document, a quality value, or a situation. SHACL shapes enforce closed-world constraints per observation kind.", alias="gmeow:observedFeature")
    vantage: list[Any] = Field(min_length=1, description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")


class Math_Ring(ConfiguredBaseModel):
    """Ring.

    A set with two operations — an abelian additive group and an associative multiplication
    that distributes over addition (∀a,b,c: a·(b+c) = a·b + a·c) — carried as two
    math:structureOperation with the distributivity axiom through math:satisfiesAxiom. A
    subclass of math:AlgebraicStructure; a math:CommutativeRing is a ring whose
    multiplication commutes.

    Usage:
        >>> from gmeow_models.math import Math_Ring
        >>> Math_Ring.model_config["json_schema_extra"]["curie"]
        'math:Ring'

    IRI:    https://blackcatinformatics.ca/math/Ring
    CURIE:  math:Ring

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Ring", "curie": "math:Ring", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Ring"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    satisfiesDistributivity: list[Any] = Field(min_length=1, description="Names the distributivity axiom a math:Ring satisfies — multiplication distributes over addition (a·(b+c) = a·b + a·c), the law that makes a two-operation structure a ring rather than two unrelated groups. Domain math:Ring, range a math:Axiom denoting a logic: formula (stated in prose). A refinement of math:satisfiesAxiom: a ring must name a distributivity law specifically, not merely some axiom. A ring without distributivity is ill-formed.", alias="math:satisfiesDistributivity")


class Math_RootSystem(ConfiguredBaseModel):
    """Root System.

    A finite configuration of vectors (roots) in a Euclidean space satisfying the
    root-system axioms — the combinatorial heart of a semisimple Lie group. It declares its
    math:rootSystemRank, a math:CartanMatrix, a math:DynkinDiagram, and a math:WeylGroup
    (the symmetry group of the roots), and may carry its math:hasSimpleRoot base and
    math:rootCount. E8's root system has 240 roots in rank 8.

    Usage:
        >>> from gmeow_models.math import Math_RootSystem
        >>> Math_RootSystem.model_config["json_schema_extra"]["curie"]
        'math:RootSystem'

    IRI:    https://blackcatinformatics.ca/math/RootSystem
    CURIE:  math:RootSystem

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/RootSystem", "curie": "math:RootSystem", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/RootSystem"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_Sequence(ConfiguredBaseModel):
    """Sequence.

    A sequence (aₙ): an indexed family, a math:Function whose math:domain is a subset of ℕ.
    It carries a math:Convergence through math:hasConvergence naming what it converges to
    and in which mode. A subclass of math:Function, so it declares its domain and codomain;
    a sequence asserted to converge without a math:Convergence naming the mode is ill-formed
    (math:UnderspecifiedConvergence).

    Usage:
        >>> from gmeow_models.math import Math_Sequence
        >>> Math_Sequence.model_config["json_schema_extra"]["curie"]
        'math:Sequence'

    IRI:    https://blackcatinformatics.ca/math/Sequence
    CURIE:  math:Sequence

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Sequence", "curie": "math:Sequence", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Sequence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasConvergence: list[Any] | None = Field(default=None, description="Relates a math:Series or math:Sequence to the math:Convergence describing what it converges to and in which mode. Domain a math:Series or math:Sequence, range math:Convergence (stated in prose). A series/sequence asserted to converge names its convergence; the mode is required (math:UnderspecifiedConvergence).", alias="math:hasConvergence")


class Math_Series(ConfiguredBaseModel):
    """Series.

    A series ∑ aₙ as a first-class binding expression: it applies the math:summationBinder
    (math:operator), names its general term through math:seriesTerm (the summand, a body
    indexed by the bound summation math:VariableDeclaration), and carries a math:Convergence
    through math:hasConvergence naming what it converges to and in which mode (absolute,
    conditional, …). A series asserted to converge without a math:Convergence naming the
    mode is ill-formed (math:UnderspecifiedConvergence).

    Usage:
        >>> from gmeow_models.math import Math_Series
        >>> Math_Series.model_config["json_schema_extra"]["curie"]
        'math:Series'

    IRI:    https://blackcatinformatics.ca/math/Series
    CURIE:  math:Series

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Series", "curie": "math:Series", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Series"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasConvergence: list[Any] | None = Field(default=None, description="Relates a math:Series or math:Sequence to the math:Convergence describing what it converges to and in which mode. Domain a math:Series or math:Sequence, range math:Convergence (stated in prose). A series/sequence asserted to converge names its convergence; the mode is required (math:UnderspecifiedConvergence).", alias="math:hasConvergence")
    seriesTerm: list[Any] = Field(min_length=1, description="Relates a math:Series to its general term aₙ — the summand, a math:MathematicalExpression indexed by the bound summation variable. Domain math:Series, range math:MathematicalExpression (stated in prose). Every series names its general term.", alias="math:seriesTerm")


class Math_Set(ConfiguredBaseModel):
    """Set.

    A mathematical set: a collection of distinct elements, given either extensionally (its
    members enumerated through math:hasElement — see math:FiniteSet) or intensionally (a
    defining condition through math:memberCondition — see math:SetBuilderExpression). A set
    is extensional or intensional, not silently both without a declared equality. Anchored
    externally to the OpenMath set1 content dictionary. The object-layer parent the bedrock
    builds on.

    Usage:
        >>> from gmeow_models.math import Math_Set
        >>> Math_Set.model_config["json_schema_extra"]["curie"]
        'math:Set'

    IRI:    https://blackcatinformatics.ca/math/Set
    CURIE:  math:Set

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Set", "curie": "math:Set", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Set"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_SetBuilderExpression(ConfiguredBaseModel):
    """Set-Builder Expression.

    A set given intensionally by a defining condition — the { x | φ(x) } form. Its condition
    is named through math:memberCondition and denotes a logic: formula, the denotation seam,
    never an opaque string. The intensional counterpart of the extensional math:FiniteSet.

    Usage:
        >>> from gmeow_models.math import Math_SetBuilderExpression
        >>> Math_SetBuilderExpression.model_config["json_schema_extra"]["curie"]
        'math:SetBuilderExpression'

    IRI:    https://blackcatinformatics.ca/math/SetBuilderExpression
    CURIE:  math:SetBuilderExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/SetBuilderExpression", "curie": "math:SetBuilderExpression", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/SetBuilderExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    memberCondition: str = Field(description="Names the defining condition of an intensional set — the φ in { x | φ(x) }. Domain math:Set (in practice a math:SetBuilderExpression), range a logic: formula (stated in prose). The value denotes a logic: formula, the denotation seam, and is never an opaque literal.", alias="math:memberCondition")


class Math_StabilityCalibrationRecord(ConfiguredBaseModel):
    """Stability Calibration Record.

    The derivation record that warrants a credence: it ties the persistence evidence, the
    derivation kind, and the stability guarantee to the logic:confidence a latent-meaning
    claim carries. A subclass of math:MathematicalObject naming its evidence through
    math:calibrationEvidence (a math:PersistenceLifetime), the kind of credence it produces
    through math:credenceDerivationKind (typically math:persistenceDerivedCredence), and the
    theorem underwriting the calibration through math:stabilityGuarantee
    (math:bottleneckStabilityTheorem — the bottleneck bound that a small perturbation of the
    underlying function moves the persistence, and hence the credence, by a bounded amount).
    The credence itself lands on the held claim, a math:ResidualInterpretationClaim carrying
    logic:confidence (never logic:probability); the claim references the record through
    gmeow:wasDerivedFrom, so a consumer can trace a confidence back to the filtration, the
    birth/death thresholds, and the guarantee that calibrated it. A record missing its
    evidence, derivation kind, or guarantee is ill-formed
    (math:UngroundedStabilityCalibration).

    Usage:
        >>> from gmeow_models.math import Math_StabilityCalibrationRecord
        >>> Math_StabilityCalibrationRecord.model_config["json_schema_extra"]["curie"]
        'math:StabilityCalibrationRecord'

    IRI:    https://blackcatinformatics.ca/math/StabilityCalibrationRecord
    CURIE:  math:StabilityCalibrationRecord

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/StabilityCalibrationRecord", "curie": "math:StabilityCalibrationRecord", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/StabilityCalibrationRecord"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    calibrationEvidence: list[Math_PersistenceLifetime] = Field(min_length=1, description="Relates a math:StabilityCalibrationRecord to the math:PersistenceLifetime whose persistence (death − birth) produced the calibrated credence — the evidence the confidence rests on. Domain math:StabilityCalibrationRecord, range math:PersistenceLifetime (stated in prose). Every record names at least one; a record with no evidence is an unwarranted number and is ill-formed (math:UngroundedStabilityCalibration).", alias="math:calibrationEvidence")
    credenceDerivationKind: str = Field(description="Relates a math:StabilityCalibrationRecord to the math:CredenceDerivationKind it produces — math:similarityDerivedCredence or math:persistenceDerivedCredence. Domain math:StabilityCalibrationRecord, range math:CredenceDerivationKind (stated in prose). Every record names exactly one, so which signal a calibrated logic:confidence rests on is queryable; a record without it is ill-formed (math:UngroundedStabilityCalibration).", alias="math:credenceDerivationKind")
    stabilityGuarantee: Math_Theorem = Field(description="Relates a math:StabilityCalibrationRecord to the math:Theorem underwriting its calibration — the stability result bounding how far the persistence, and hence the derived credence, can move under a perturbation of the underlying function (math:bottleneckStabilityTheorem, the 1-Lipschitz bottleneck bound). Domain math:StabilityCalibrationRecord, range math:Theorem (stated in prose). The warrant that turns a persistence-derived credence from a heuristic into a bounded quantity; a record without it is ill-formed (math:UngroundedStabilityCalibration).", alias="math:stabilityGuarantee")


class Math_StatisticalVariable(ConfiguredBaseModel):
    """Statistical Variable.

    A statistical variable: a measured or derived quantity recorded across the observation
    units of a sample — not merely a column name but an object carrying its
    math:VariableRole, its math:MeasurementScale, its math:UnitPolicy, its missing-value
    encoding (math:MissingValueCode / math:hasMissingnessIndicator), and, where derived, the
    math:DataTransformation that produced it. A subclass of math:MathematicalObject named by
    a math:Sample or math:DatasetMatrix through math:statisticalVariable. A variable whose
    values encode absence but names no math:MissingnessMechanism an analysis rests on is
    ill-formed (math:ImplicitMissingness).

    When to use:
        - Mint one for each measured or derived column, carrying its role, scale, unit policy, and
          missing-value encoding as first-class structure rather than a bare column name.

    When to avoid:
        - Do not reduce it to its datatype; a variable is not its storage format, and treating a
          nominal code as a number is exactly the confusion the carried math:MeasurementScale
          prevents.

    How to use:
        - Attach it to a math:Sample or math:DatasetMatrix through math:statisticalVariable, and
          hang its missing-value encoding off it through math:hasMissingnessIndicator and
          math:missingnessMechanism.

    Examples:
        - exf:income a math:StatisticalVariable ; math:hasMissingnessIndicator exf:incomeMissing ;
          math:missingnessMechanism exf:mar .

    Usage:
        >>> from gmeow_models.math import Math_StatisticalVariable
        >>> Math_StatisticalVariable.model_config["json_schema_extra"]["curie"]
        'math:StatisticalVariable'

    IRI:    https://blackcatinformatics.ca/math/StatisticalVariable
    CURIE:  math:StatisticalVariable
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/statisticalvariable-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/StatisticalVariable", "curie": "math:StatisticalVariable", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/StatisticalVariable"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_TangentSpace(ConfiguredBaseModel):
    """Tangent Space.

    The tangent space T_pM at a point p of a manifold M: the vector space of tangent vectors
    at that point. A subclass of math:VectorSpace; it names its base point through
    math:tangentSpaceAt and its manifold through math:tangentSpaceOf, and its
    math:spaceDimension equals the manifold's math:manifoldDimension (math:DimensionMismatch
    otherwise). Tensor fields are sections over the tangent bundle — the disjoint union of
    the tangent spaces.

    Usage:
        >>> from gmeow_models.math import Math_TangentSpace
        >>> Math_TangentSpace.model_config["json_schema_extra"]["curie"]
        'math:TangentSpace'

    IRI:    https://blackcatinformatics.ca/math/TangentSpace
    CURIE:  math:TangentSpace

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/TangentSpace", "curie": "math:TangentSpace", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/TangentSpace"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Math_TensorComputationGraph(ConfiguredBaseModel):
    """Tensor Computation Graph.

    A neural network's forward pass AS a mathematical object: an expression graph whose
    nodes are math:ApplicationExpressions over tensor operators (math:matrixProduct,
    math:tensorContraction, math:AttentionOperation as a bilinear form), whose leaves are
    math:WeightTensors in a math:ParameterSpace, and whose structure is the architecture. A
    subclass of math:MathematicalObject naming its math:computationNode(s); it reuses the
    expression AST wholesale (the nodes are math:ApplicationExpression with indexed
    math:ArgumentSlot operands), so the application/slot gates apply unchanged. A graph
    declaring no computation node is ill-formed (math:MalformedTensorComputationGraph). The
    AI's reflection on this structure is carried at the logic: metalevel, not in math:.

    Usage:
        >>> from gmeow_models.math import Math_TensorComputationGraph
        >>> Math_TensorComputationGraph.model_config["json_schema_extra"]["curie"]
        'math:TensorComputationGraph'

    IRI:    https://blackcatinformatics.ca/math/TensorComputationGraph
    CURIE:  math:TensorComputationGraph

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/TensorComputationGraph", "curie": "math:TensorComputationGraph", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/TensorComputationGraph"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    computationNode: list[Any] = Field(min_length=1, description="Relates a math:TensorComputationGraph to one of its nodes — a math:ApplicationExpression applying a tensor operator to indexed operands. Domain math:TensorComputationGraph, range math:ApplicationExpression (stated in prose). Reusing the expression AST means the node inherits the application/slot well-formedness gates; a graph with no computation node is ill-formed (math:MalformedTensorComputationGraph).", alias="math:computationNode")


class Math_Theorem(ConfiguredBaseModel):
    """Theorem.

    A convenience PROJECTION of a math:MathematicalStatement carrying math:statementRole
    math:roleTheorem — a flat type for consumers that want one, NEVER the canonical bearer
    of truth. The canonical carrier of theorem-hood is the role scoped by math:roleInTheory;
    a bare `a math:Theorem` must still carry a theory context and either a math:Proof or a
    declared external warrant, because the type alone is not a truth bit (a bare theorem
    with no theory context is math:UngroundedTheoremClaim).

    Usage:
        >>> from gmeow_models.math import Math_Theorem
        >>> Math_Theorem.model_config["json_schema_extra"]["curie"]
        'math:Theorem'

    IRI:    https://blackcatinformatics.ca/math/Theorem
    CURIE:  math:Theorem

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/Theorem", "curie": "math:Theorem", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/Theorem"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    roleInTheory: list[Any] = Field(min_length=1, description="Relates a math:MathematicalStatement to the named, versioned theory its math:statementRole holds in — the theory individual (an axiom system at a stated version, e.g. Euclidean geometry v2), stated in prose. Single-valued: a statement's role is scoped to one theory context, so 'this is a theorem' is always 'a theorem IN this theory version', never an unconditional global fact. This is precisely what makes theorem-hood theory-relative rather than a truth bit; a theorem claim with no theory context is ill-formed (math:UngroundedTheoremClaim).", alias="math:roleInTheory")


class Math_VariableOccurrence(ConfiguredBaseModel):
    """Variable Occurrence.

    A use site of a variable within an expression scope: it names the
    math:VariableDeclaration it refers to through math:declaredVariable and the scope it
    occurs in through math:occursInScope. A binder binds occurrences (math:bindsOccurrence),
    not glyphs, so an occurrence in the body of two nested binders that reuse a glyph
    resolves to exactly one of the two declarations. An occurrence that resolves to no
    declaration and is not marked free is ill-formed (math:UnscopedVariableOccurrence).

    Usage:
        >>> from gmeow_models.math import Math_VariableOccurrence
        >>> Math_VariableOccurrence.model_config["json_schema_extra"]["curie"]
        'math:VariableOccurrence'

    IRI:    https://blackcatinformatics.ca/math/VariableOccurrence
    CURIE:  math:VariableOccurrence
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/variableoccurrence-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/VariableOccurrence", "curie": "math:VariableOccurrence", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/VariableOccurrence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    declaredVariable: Any = Field(description="Relates a math:VariableOccurrence to the math:VariableDeclaration it refers to — the binding site or free declaration that gives the occurrence its identity. Domain math:VariableOccurrence, range math:VariableDeclaration (stated in prose). Every occurrence resolves to exactly one declaration; an occurrence resolving to none and not marked free is ill-formed (math:UnscopedVariableOccurrence).", alias="math:declaredVariable")


class Math_WeightTensor(ConfiguredBaseModel):
    """Weight Tensor.

    A weight tensor: the learned parameters of a math:NeuralLayer — a leaf of the
    math:TensorComputationGraph living in a declared math:ParameterSpace. A subclass of
    math:MathematicalObject naming its layer through math:weightOf and the parameter space
    it lives in through math:inParameterSpace. A weight tensor without a declared parameter
    space is ill-formed (math:UnframedWeightTensor).

    Usage:
        >>> from gmeow_models.math import Math_WeightTensor
        >>> Math_WeightTensor.model_config["json_schema_extra"]["curie"]
        'math:WeightTensor'

    IRI:    https://blackcatinformatics.ca/math/WeightTensor
    CURIE:  math:WeightTensor

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/math/WeightTensor", "curie": "math:WeightTensor", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/math/WeightTensor"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    inParameterSpace: list[Any] | None = Field(default=None, description="Relates a math:WeightTensor to the math:ParameterSpace it lives in. Domain math:WeightTensor, range math:ParameterSpace (stated in prose). Weights live in a declared parameter space; a weight tensor without one is ill-formed (math:UnframedWeightTensor).", alias="math:inParameterSpace")

"""GMEOW models — slice GMEOW Language.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/lang
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class GmnCompartmentEnum(StrEnum):
    gmnCompartmentNato = "gmeow:gmnCompartmentNato"
    gmnCompartmentPartner = "gmeow:gmnCompartmentPartner"


class GmnFixityEnum(StrEnum):
    gmnFixityBracketing = "gmeow:gmnFixityBracketing"
    gmnFixityInfix = "gmeow:gmnFixityInfix"
    gmnFixityPostfix = "gmeow:gmnFixityPostfix"
    gmnFixityPrefix = "gmeow:gmnFixityPrefix"


class GmnRingCriterionEnum(StrEnum):
    gmnCriterionAutomatedUnreviewed = "gmeow:gmnCriterionAutomatedUnreviewed"
    gmnCriterionHumanReviewed = "gmeow:gmnCriterionHumanReviewed"


class GmnRingLevelEnum(StrEnum):
    gmnLevelCore = "gmeow:gmnLevelCore"
    gmnLevelRestricted = "gmeow:gmnLevelRestricted"
    gmnLevelTrusted = "gmeow:gmnLevelTrusted"


class GmnSecurityRingEnum(StrEnum):
    gmnRingCore = "gmeow:gmnRingCore"
    gmnRingNato = "gmeow:gmnRingNato"
    gmnRingRestricted = "gmeow:gmnRingRestricted"
    gmnRingTrusted = "gmeow:gmnRingTrusted"


class GmnSigilRoleEnum(StrEnum):
    gmnSigilClaim = "gmeow:gmnSigilClaim"
    gmnSigilDefeater = "gmeow:gmnSigilDefeater"
    gmnSigilEvidence = "gmeow:gmnSigilEvidence"
    gmnSigilLangAst = "gmeow:gmnSigilLangAst"
    gmnSigilLogic = "gmeow:gmnSigilLogic"
    gmnSigilMath = "gmeow:gmnSigilMath"
    gmnSigilModal = "gmeow:gmnSigilModal"
    gmnSigilProcess = "gmeow:gmnSigilProcess"
    gmnSigilProof = "gmeow:gmnSigilProof"
    gmnSigilStandpoint = "gmeow:gmnSigilStandpoint"


class GmnCompaction(ConfiguredBaseModel):
    """GMN Compaction.

    A GMN-2 compaction record: a reified NEW claim about older claims — a
    gmeow:StandpointClaim with its own identity, its own standpoint, and its own confidence,
    whose content summarizes the source claims it names through gmeow:gmnCompacts.
    Compaction is lossy by nature: the compacted record is a shorter, coarser restatement
    produced under the GMN-2 usage level, and it NEVER overwrites its sources — every source
    claim survives, reachable through the gmeow:gmnCompacts provenance edges, so the
    compaction can always be audited against what it compacted.

    When to use:
        - Use to record a GMN-2 compaction — a provenance-linked, standpoint-held new claim that
          condenses older claims without destroying them.

    When to avoid:
        - Avoid treating a compaction as a replacement for its sources — it is an additional
          claim, and deleting or rewriting the compacted records fabricates certainty — and avoid
          a compaction that names no source through gmeow:gmnCompacts; an unsourced summary is
          unauditable.

    How to use:
        - Mint one compaction per summarization act, point gmeow:gmnCompacts at every source claim
          or record it compacts, and carry the compaction's own standpoint and confidence on the
          StandpointClaim layer; leave the sources untouched.

    Examples:
        - ex:cmp a gmeow:GmnCompaction ; gmeow:gmnCompacts ex:claim1 , ex:claim2 .

    Usage:
        >>> from gmeow_models.lang import GmnCompaction
        >>> GmnCompaction.model_config["json_schema_extra"]["curie"]
        'gmeow:GmnCompaction'

    IRI:    https://blackcatinformatics.ca/gmeow/GmnCompaction
    CURIE:  gmeow:GmnCompaction

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GmnCompaction", "curie": "gmeow:GmnCompaction", "definitionDigest": "blake3:f2d3869e83cf1c865db7602cdfd8436afa3ed5fd797a71e6be52bdf10975b227", "iri": "https://blackcatinformatics.ca/gmeow/GmnCompaction"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    gmnCompacts: list[Any] = Field(min_length=1, description="The provenance edge from a compaction to a source it compacts. Domain gmeow:GmnCompaction, range the source claims or records being compacted (stated in prose). One edge per source, every source named: the compaction is auditable exactly because nothing it summarizes is unreachable from it.", alias="gmeow:gmnCompacts")
    vantage: list[Any] = Field(min_length=1, description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")


class GmnDictionary(ConfiguredBaseModel):
    """GMN Dictionary.

    One version of the GMN alias table: the codebook-witnessed bijection between full GMEOW
    IRIs and their compact GMN-1 aliases that gmeow:gmnCorrNormalToGmn's mnemomorphic claim
    rests on. A dictionary carries its own gmeow:gmnDictionaryVersion (exactly one) and zero
    or more gmeow:gmnDictionaryEntry members, each binding one term to one alias string. The
    dictionary is a BIJECTION over its covered term set: injectivity (no two entries of one
    dictionary share an alias) is gated by lang:GmnDictionaryAliasInjectivityShape
    (lang:GmnDictionaryAliasCollision), never merely asserted in prose.

    When to use:
        - Use to make one version of the GMN dictionary/alias table a first-class, queryable
          object whose entries can be gated for injectivity — never a bare version string with no
          term-level structure behind it.

    When to avoid:
        - Avoid minting a dictionary without a gmeow:gmnDictionaryVersion (an unversioned alias
          table cannot be resolved) and avoid letting two entries of one dictionary alias the same
          string — that is the injectivity gate's business, never a hand-checked invariant.

    How to use:
        - Mint one gmeow:GmnDictionary per alias-table version, stamp its
          gmeow:gmnDictionaryVersion, and add one gmeow:gmnDictionaryEntry per aliased term; a
          codebook references its current dictionary through gmeow:references (the same flat-first
          linkage GmnCodebook already uses for the script and sigil roles).

    Examples:
        - gmeow:gmnDictV1 a gmeow:GmnDictionary ; gmeow:gmnDictionaryVersion "1" ;
          gmeow:gmnDictionaryEntry gmeow:gmnDictV1EntryEnvelope .

    Usage:
        >>> from gmeow_models.lang import GmnDictionary
        >>> GmnDictionary.model_config["json_schema_extra"]["curie"]
        'gmeow:GmnDictionary'

    IRI:    https://blackcatinformatics.ca/gmeow/GmnDictionary
    CURIE:  gmeow:GmnDictionary

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GmnDictionary", "curie": "gmeow:GmnDictionary", "definitionDigest": "blake3:e551c4f8310dd8ff8467e21ad8c57ec831af509ae7af2e60553f3bac5e2de66b", "iri": "https://blackcatinformatics.ca/gmeow/GmnDictionary"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class GmnEnvelope(ConfiguredBaseModel):
    """GMN Envelope.

    A GMN envelope: the attested carrier of a GMN payload — a gmeow:AttestationArtifact
    whose bytes are a GMN document plus its integrity and versioning header. An envelope
    reuses the attestation and versions vocabulary by reference rather than re-minting it:
    byte-exact identity is a gmeow:contentDigest, semantic fingerprints are
    gmeow:versionFingerprint literals, and the asserting standpoint is carried through the
    gmeow:accordingTo pattern. It declares its own dialect coordinates through
    gmeow:gmnSchemaVersion, gmeow:gmnDictionaryVersion, and gmeow:gmnGlyphTableVersion, its
    boundary ring through gmeow:gmnSecurityRing, and its losslessness judgment through
    gmeow:gmnEnvelopeCorrespondence — a logic:Correspondence, never a boolean flag.

    When to use:
        - Use for the attested, versioned, ring-classified carrier of a GMN document — the unit
          that crosses a serialization boundary.

    When to avoid:
        - Avoid carrying a losslessness boolean on the envelope — the judgment lives on the
          logic:Correspondence named by gmeow:gmnEnvelopeCorrespondence — and avoid re-minting
          digest, fingerprint, or standpoint properties that attestation and versions already
          provide.

    How to use:
        - Mint one envelope per serialized GMN payload; set gmeow:contentDigest for byte identity,
          the three gmeow:gmn*Version literals for its dialect coordinates, one
          gmeow:gmnSecurityRing, and point gmeow:gmnEnvelopeCorrespondence at the
          logic:Correspondence judging its crossing back to the normal form.

    Examples:
        - ex:env a gmeow:GmnEnvelope ; gmeow:gmnSchemaVersion "1" ; gmeow:gmnSecurityRing
          gmeow:gmnRingCore ; gmeow:gmnEnvelopeCorrespondence ex:corr .

    Usage:
        >>> from gmeow_models.lang import GmnEnvelope
        >>> GmnEnvelope.model_config["json_schema_extra"]["curie"]
        'gmeow:GmnEnvelope'

    IRI:    https://blackcatinformatics.ca/gmeow/GmnEnvelope
    CURIE:  gmeow:GmnEnvelope

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GmnEnvelope", "curie": "gmeow:GmnEnvelope", "definitionDigest": "blake3:ef5b099a07d69af262445ab5fff411f6b9690eb021e53eeea8368a1b2bd8701a", "iri": "https://blackcatinformatics.ca/gmeow/GmnEnvelope"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    accordingTo: list[Any] = Field(min_length=1, description="The standpoint / frame within which the annotated statement is asserted true — *according to whom*. DISTINCT from gmeow:wasAttributedTo (which source RECORDED the claim) and gmeow:confidence (how sure we are); a claim may carry all three with no inferential bridge among them. Its value is a gmeow:Agent (a polity, community, or organization IRI) or a gmeow:Standpoint individual (a frame that needs its own identity). When a statement is promoted to a reified gmeow:Observation, gmeow:accordingTo becomes the gmeow:vantage of the relator (vantage ⊑ accordingTo — documented, not axiomatised, because accordingTo is an AnnotationProperty and vantage is an ObjectProperty). Standpoint-TIME — when the frame held this position — rides gmeow:validFrom / gmeow:validUntil on the same statement in the light case, or a reified gmeow:StandpointTenure when the adoption / withdrawal is itself the fact of interest. The range is intentionally open. An unindexed statement is held in gmeow:unspecifiedStandpoint — unspecified, not universal; only an explicit gmeow:universalStandpoint index (or explicit quantification over all contexts of a stated type) propagates as universal.", alias="gmeow:accordingTo")
    contentDigest: list[Any] = Field(min_length=1, description="A content hash of an object's bytes (e.g. \"blake3:…\", \"sha256:…\", \"git:…\") — the reliable identity by content (two objects with the same bytes are the same object, regardless of path, name, or mtime). Domain-free: applies to creative works, source files, commits, distributions, and any other content-addressable artifact. Not functional: an object may carry digests under several algorithms.", alias="gmeow:contentDigest")
    gmnDictionaryVersion: list[Any] = Field(min_length=1, description="The alias-table (dictionary) version an envelope, codebook, or gmeow:GmnDictionary was serialized against — the coordinate of the IRI-to-alias mapping GMN compresses names with. Domain gmeow:GmnEnvelope, gmeow:GmnCodebook, or gmeow:GmnDictionary, range a string literal (stated in prose). Alias resolution is version-pinned: an alias is meaningless without the dictionary version that minted it, and an envelope carries exactly one (sh:maxCount 1 on lang:GmnEnvelopeContractShape) — versions are never interchangeable.", alias="gmeow:gmnDictionaryVersion")
    gmnEnvelopeCorrespondence: list[Any] | None = Field(default=None, description="Relates a GMN envelope to the single logic:Correspondence carrying its losslessness judgment — the law-spine of the envelope's crossing back to the narrow-waist normal form. Domain gmeow:GmnEnvelope, range logic:Correspondence (stated in prose). There is NO boolean lossless flag: whether the payload round-trips is the logic:preservationKind, logic:morphismClass, and logic:mnemomorphic witness on that Correspondence, exactly as lang:translationCorrespondence attaches a translation unit to its crossing.", alias="gmeow:gmnEnvelopeCorrespondence")
    gmnGlyphTableVersion: list[Any] = Field(min_length=1, description="The glyph-table version an envelope or codebook was serialized against — the coordinate of the projected glyph/codepoint table (the gmeow:gmnGlyph / gmeow:gmnCodepoints projection state). Domain gmeow:GmnEnvelope or gmeow:GmnCodebook, range a string literal (stated in prose).", alias="gmeow:gmnGlyphTableVersion")
    gmnSchemaVersion: list[Any] = Field(min_length=1, description="The GMN dialect schema version an envelope was serialized under — the major grammar/record-shape coordinate. Domain gmeow:GmnEnvelope, range a string literal (stated in prose). A reader accepts the current major plus the majors inside the version set's gmeow:gmnAcceptWindow, via migration.", alias="gmeow:gmnSchemaVersion")
    gmnSecurityRing: list[Any] | None = Field(default=None, description="Names the serialization boundary ring of a GMN envelope. Domain gmeow:GmnEnvelope, range gmeow:GmnSecurityRing (stated in prose). Exactly one per envelope: the ring is the admission class the serializer checked the payload against.", alias="gmeow:gmnSecurityRing")
    wasGeneratedBy: list[Any] = Field(min_length=1, description="Relates an entity to the activity that produced it — the generation link from an enduring product back to its occurrent cause. The performing agent is reached transitively through the activity's gmeow:wasAssociatedWith, keeping who-did-it separable from what-happened.", alias="gmeow:wasGeneratedBy")


class GmnSecurityRing(ConfiguredBaseModel):
    """GMN Security Ring.

    A deontic serialization boundary ring: a point in a Denning-style information-flow
    lattice saying where content serialized in a GMN envelope is permitted to flow. A ring
    factors into two orthogonal coordinates rather than a bare chain link: a classification
    level (gmeow:gmnRingLevel, an ordered gmeow:GmnRingLevel value) and a compartment/caveat
    set (gmeow:gmnRingCompartment, zero or more gmeow:GmnCompartment values — e.g. a
    NATO-eyes-only scope). The ring order gmeow:gmnRingWithin is DERIVED from these
    coordinates by product-order dominance — level dominance (gmeow:gmnRingLevelDominates)
    AND compartment containment (⊇) — never hand-chained; see gmeow:gmnRingWithin for the
    derivation rule. Rings are first-class individuals in an OPEN set: the three shipped
    defaults (gmeow:gmnRingCore / gmeow:gmnRingTrusted / gmeow:gmnRingRestricted) are
    members of the shipped gmeow:gmnRingPresetDefault grouping, and a fourth demonstrator
    (gmeow:gmnRingNato) shows that the SAME level with a DIFFERENT compartment set is a
    genuinely different ring, not a naming variant. Typed admission/exclusion criteria
    (gmeow:gmnRingAdmits / gmeow:gmnRingExcludes, over the open gmeow:GmnRingCriterion
    vocabulary) record what a ring's boundary checks, mirroring the rights slice's
    typed-value-over-relator discipline (gmeow:RightsAction referenced from gmeow:Permission
    / gmeow:Prohibition) rather than free text. Minted fresh after checking the rights and
    agreements vocabulary: gmeow:RightsStatement and its gmeow:Permission /
    gmeow:Prohibition / gmeow:Duty rule trio regulate content licensing — who may do what
    with an asset under what licence — not serialization boundaries, so the ring lattice is
    a new substrate rather than a graft onto ODRL-shaped deontics.

    When to use:
        - Use as the class of GMN serialization boundary rings — the factored, lattice-ordered
          admission classes an envelope declares so the flow rules of its payload are
          machine-checkable and derived, never hand-chained.

    When to avoid:
        - Avoid using a ring for licensing or usage rights (that is the gmeow:RightsStatement
          deontic layer); NEVER hand-assert a gmeow:gmnRingWithin edge — the order is DERIVED from
          gmeow:gmnRingLevel and gmeow:gmnRingCompartment, and a zero-authored-triples structural
          gate fails the build on one; and avoid an owl:oneOf closure — the ring set is open, the
          three defaults are a shipped preset, not the whole universe.

    How to use:
        - Mint a ring as an individual carrying exactly one gmeow:gmnRingLevel and zero or more
          gmeow:gmnRingCompartment values; classify each gmeow:GmnEnvelope with exactly one ring
          through gmeow:gmnSecurityRing; a serializer admits content into an envelope only when
          the content's ring equals the envelope's ring or is gmeow:gmnRingWithin it (the derived
          closure).

    Examples:
        - ex:envelope gmeow:gmnSecurityRing gmeow:gmnRingTrusted .

    Usage:
        >>> from gmeow_models.lang import GmnSecurityRing
        >>> GmnSecurityRing.model_config["json_schema_extra"]["curie"]
        'gmeow:GmnSecurityRing'

    IRI:    https://blackcatinformatics.ca/gmeow/GmnSecurityRing
    CURIE:  gmeow:GmnSecurityRing
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/gmnsecurityring-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/GmnSecurityRing", "curie": "gmeow:GmnSecurityRing", "definitionDigest": "blake3:062b8863c9feed5991996e8f6ee797b2a8bcb436e7d2dfcb43cc0bfc55cde34a", "iri": "https://blackcatinformatics.ca/gmeow/GmnSecurityRing"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    gmnRingCompartment: list[str] | None = Field(default=None, description="The compartment/caveat coordinate of a GMN security ring — the second axis gmeow:gmnRingWithin's derivation reads (⊇ containment). Non-functional: zero or more compartments per ring; an empty set is well-formed (the three default-preset rings carry none). Domain gmeow:GmnSecurityRing, range gmeow:GmnCompartment.", alias="gmeow:gmnRingCompartment")
    gmnRingLevel: str = Field(description="The classification-level coordinate of a GMN security ring — one of the two axes gmeow:gmnRingWithin's derivation reads. Functional: exactly one level per ring. Domain gmeow:GmnSecurityRing, range gmeow:GmnRingLevel.", alias="gmeow:gmnRingLevel")


class Lang_ComposedForm(ConfiguredBaseModel):
    """Composed Form.

    A composed form: a phrase, clause, sentence, paragraph, section, or text as a tree over
    indexed slots. Declares its level through lang:compositionLevel, its constituents
    through lang:formSlot, and its head where headedness is analyzed through lang:formHead.
    Constituent order is carried by lang:FormSlot individuals with integer lang:slotIndex,
    never by RDF list ordering — two encodings of one form must be one form. Code-switching
    is compositional: the composed form belongs to its matrix system while embedded
    constituents carry their own.

    Usage:
        >>> from gmeow_models.lang import Lang_ComposedForm
        >>> Lang_ComposedForm.model_config["json_schema_extra"]["curie"]
        'lang:ComposedForm'

    IRI:    https://blackcatinformatics.ca/lang/ComposedForm
    CURIE:  lang:ComposedForm

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/ComposedForm", "curie": "lang:ComposedForm", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/ComposedForm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_Denotation(ConfiguredBaseModel):
    """Denotation.

    A meaning assignment as a reified record — the Peircean triad made structural (sign,
    object, and the interpreting context that connects them), never a bare triple from form
    to target. Reification is what makes disagreement, revision, and ambiguity
    representable. Every denotation names its form (lang:denotedForm, a non-surface form),
    its kind (lang:denotationKind), its target (lang:denotationTarget), and its context
    (lang:denotationContext); lang:viaSense routes it through a sense where the head lexeme
    is ambiguous. Disjoint from lang:Form and lang:Sense — the reference corner of the Frege
    triangle is not a form and not a sense.

    Usage:
        >>> from gmeow_models.lang import Lang_Denotation
        >>> Lang_Denotation.model_config["json_schema_extra"]["curie"]
        'lang:Denotation'

    IRI:    https://blackcatinformatics.ca/lang/Denotation
    CURIE:  lang:Denotation
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/denotation-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Denotation", "curie": "lang:Denotation", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Denotation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_FeatureValue(ConfiguredBaseModel):
    """Feature Value.

    A morphological feature value — Plur, Sing, Past, Pres, Nom, Acc. Its instances are
    named individuals belonging to a lang:FeatureInventory through lang:inFeatureInventory.
    A lang:MorphFeature names its value (or, for disjunctive underspecification, several)
    through lang:featureValue, an inventory individual rather than a bare string.

    Usage:
        >>> from gmeow_models.lang import Lang_FeatureValue
        >>> Lang_FeatureValue.model_config["json_schema_extra"]["curie"]
        'lang:FeatureValue'

    IRI:    https://blackcatinformatics.ca/lang/FeatureValue
    CURIE:  lang:FeatureValue
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/featurevalue-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/FeatureValue", "curie": "lang:FeatureValue", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/FeatureValue"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_Form(ConfiguredBaseModel):
    """Form.

    The abstract root of the form AST: a form that is meant to be analyzed, denoted,
    translated, matched, or projected is a structured object, never an opaque string. Every
    lang:Form names exactly one sign system through lang:inSignSystem — an unsituated form
    is ill-formed. Its identity is computed over structural content alone (sign system,
    stratum, features, slot structure with constituent keys) and is independent of encoding,
    script, casing, and rendering. The strata that specialize it — surface forms, morphemes
    and morphs, lexemes and word forms, orthographic and syntactic words, composed forms,
    and covert forms — each have a distinct identity criterion.

    Usage:
        >>> from gmeow_models.lang import Lang_Form
        >>> Lang_Form.model_config["json_schema_extra"]["curie"]
        'lang:Form'

    IRI:    https://blackcatinformatics.ca/lang/Form
    CURIE:  lang:Form

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Form", "curie": "lang:Form", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Form"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    inSignSystem: list[Any] | None = Field(default=None, description="Situates a form in the sign system it belongs to. Domain lang:Form, range lang:SignSystem (stated in prose). Every lang:Form declares exactly one — an unsituated form is ill-formed. Code-switching is compositional: a composed form belongs to its matrix system while embedded constituents carry their own.", alias="lang:inSignSystem")


class Lang_FormSlot(ConfiguredBaseModel):
    """Form Slot.

    A slot in a composed form: carries an integer lang:slotIndex (constituent order), the
    constituent form (lang:slotForm), optionally a grammatical role (lang:slotRole, a
    lang:FormRole individual), and — where a dependency analysis is present — a dependency
    relation (lang:dependencyRelation) and a head reference among sibling slots
    (lang:dependsOn). Slot indexes are unique per composed form; strict canonical mode
    requires them zero-based and contiguous. Word order and grammatical function are
    identity-bearing.

    Usage:
        >>> from gmeow_models.lang import Lang_FormSlot
        >>> Lang_FormSlot.model_config["json_schema_extra"]["curie"]
        'lang:FormSlot'

    IRI:    https://blackcatinformatics.ca/lang/FormSlot
    CURIE:  lang:FormSlot
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/formslot-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/FormSlot", "curie": "lang:FormSlot", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/FormSlot"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_GmnImportedPlane(ConfiguredBaseModel):
    """GMN Imported Plane.

    An external notation or gloss standard imported verbatim into the GMN symbology
    inventory — the IPA chart, the Leipzig Glossing Rules — as a first-class lang:SignSystem
    the codebook references. An imported plane is imported by reference, never re-minted, so
    it must carry both its citation (gmeow:cites, typed by a gmeow:CitationAct) and its
    version (dcterms:hasVersion): a plane without a citation or a version is an unattributed
    or unpinned import, and the alias table it stands for is meaningless without the edition
    it was drawn from.

    Usage:
        >>> from gmeow_models.lang import Lang_GmnImportedPlane
        >>> Lang_GmnImportedPlane.model_config["json_schema_extra"]["curie"]
        'lang:GmnImportedPlane'

    IRI:    https://blackcatinformatics.ca/lang/GmnImportedPlane
    CURIE:  lang:GmnImportedPlane

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/GmnImportedPlane", "curie": "lang:GmnImportedPlane", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/GmnImportedPlane"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    cites: list[Any] = Field(min_length=1, description="Relates an entity to a creative work it cites — the flat 80%-case shortcut. Non-functional: an entity may cite many works. Promote to a gmeow:CitationAct node when the intent, selector, provenance, or standpoint of the citation must be recorded.", alias="gmeow:cites")
    hasVersion: list[Any] = Field(min_length=1, alias="http://purl.org/dc/terms/hasVersion")


class Lang_Grapheme(ConfiguredBaseModel):
    """Grapheme.

    A grapheme: a member character of a script's repertoire — a Latin letter, a Cyrillic
    letter, a Han character. Declares the script it belongs to through lang:inScript; the
    owning script lists it through lang:hasGrapheme. A grapheme not in its script's
    repertoire is ill-formed. Below the morph in the stratification; a repertoire element
    rather than a sign-system-scoped form.

    Usage:
        >>> from gmeow_models.lang import Lang_Grapheme
        >>> Lang_Grapheme.model_config["json_schema_extra"]["curie"]
        'lang:Grapheme'

    IRI:    https://blackcatinformatics.ca/lang/Grapheme
    CURIE:  lang:Grapheme

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Grapheme", "curie": "lang:Grapheme", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Grapheme"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_InterpretationAct(ConfiguredBaseModel):
    """Interpretation Act.

    The act of assigning meaning to a form — a gmeow:Activity with an agent or method (a
    person, an NLP engine, a compositional rule run), inputs (the form through
    lang:interpretedForm, the context, an anchor through lang:anchorsIndexical where deixis
    needs resolving), and provenance. Its results are lang:Reading objects through
    lang:producedReading. This is the process of the observation spine's
    process/result/claim separation, and it is NEVER typed as a gmeow:Observation: the act
    is disjoint from the claim about its readings.

    Usage:
        >>> from gmeow_models.lang import Lang_InterpretationAct
        >>> Lang_InterpretationAct.model_config["json_schema_extra"]["curie"]
        'lang:InterpretationAct'

    IRI:    https://blackcatinformatics.ca/lang/InterpretationAct
    CURIE:  lang:InterpretationAct

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/InterpretationAct", "curie": "lang:InterpretationAct", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/InterpretationAct"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_MorphFeature(ConfiguredBaseModel):
    """Morph Feature.

    A typed morphological feature-value pair — Number=Plur, Tense=Past — carrying a key
    drawn from a feature inventory (lang:featureKey, a lang:FeatureKey individual), a value
    drawn from that inventory (lang:featureValue, a lang:FeatureValue individual), and
    optionally a layer (lang:featureLayer, as in the UD Number[psor] convention).
    Morphological content is carried this way, never as an unparsed feature string; the
    inventory aligns to Universal Dependencies' universal features and extends past them
    where a language demands it.

    Usage:
        >>> from gmeow_models.lang import Lang_MorphFeature
        >>> Lang_MorphFeature.model_config["json_schema_extra"]["curie"]
        'lang:MorphFeature'

    IRI:    https://blackcatinformatics.ca/lang/MorphFeature
    CURIE:  lang:MorphFeature
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/morphfeature-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/MorphFeature", "curie": "lang:MorphFeature", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/MorphFeature"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    featureKey: list[Any] | None = Field(default=None, description="Names the key of a morphological feature. Domain lang:MorphFeature, range lang:FeatureKey (stated in prose). An inventory individual, never a bare string.", alias="lang:featureKey")
    featureValue: list[Any] | None = Field(default=None, description="Names the value (or, for disjunctive underspecification, values) of a morphological feature. Domain lang:MorphFeature, range lang:FeatureValue (stated in prose). An inventory individual, never a bare string; a value set is unordered.", alias="lang:featureValue")


class Lang_Paraphrase(ConfiguredBaseModel):
    """Paraphrase.

    A form-to-form crossing within one sign system under a declared sameness kind — a
    restatement, a plain-language rewrite, a definitional gloss. Names the form it
    paraphrases through lang:paraphraseOf, the sameness it claims to preserve through
    lang:paraphraseSamenessKind (a lang:ParaphraseSamenessKind individual — same denotation,
    same sense, or same force and content), and, where the crossing is judged, a
    preservation kind through lang:paraphrasePreservation, which carries a
    logic:preservationKind. Sameness is never a bare assertion: a paraphrase claim is
    vantage-held, so that a paraphrase-correctness claim is a gmeow:Observation from a
    gmeow:vantage rather than a groundless equivalence. Unlike translation, both forms
    belong to one sign system; unlike rendering, both sides are forms rather than content
    and form.

    Usage:
        >>> from gmeow_models.lang import Lang_Paraphrase
        >>> Lang_Paraphrase.model_config["json_schema_extra"]["curie"]
        'lang:Paraphrase'

    IRI:    https://blackcatinformatics.ca/lang/Paraphrase
    CURIE:  lang:Paraphrase

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Paraphrase", "curie": "lang:Paraphrase", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Paraphrase"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    paraphraseSamenessKind: list[Any] | None = Field(default=None, description="Names the kind of sameness a paraphrase claims to preserve. Domain lang:Paraphrase, range lang:ParaphraseSamenessKind (stated in prose). The kind fixes which corner of the Frege triangle is held constant; a paraphrase that declares no sameness kind is ill-formed, because an undeclared sameness is an equivalence smuggled in without saying what it preserves.", alias="lang:paraphraseSamenessKind")


class Lang_ProjectionEmission(ConfiguredBaseModel):
    """Projection Emission.

    A recorded emission of a lang: structure to an external target — OntoLex-Lemon, CoNLL-U,
    EBNF, TEI, NIF, SemAF/AMR, BCP-47, and their siblings — carrying its honest per-emission
    preservation judgment. Every projection out of the canonical lang: core is lossy or
    exact, but never silent: an emission names the target it lowers to through
    lang:projectionTargetName, the lang: structure it projects through lang:projectsSource,
    its logic:preservationKind, and — where it drops anything — the constructs it cannot
    carry through lang:unsupportedConstruct. Instances live in the projection corpus graph
    (boxABox in that graph), so the loss ledger is queryable alongside every other GMEOW
    lowering rather than hidden in generator code.

    Usage:
        >>> from gmeow_models.lang import Lang_ProjectionEmission
        >>> Lang_ProjectionEmission.model_config["json_schema_extra"]["curie"]
        'lang:ProjectionEmission'

    IRI:    https://blackcatinformatics.ca/lang/ProjectionEmission
    CURIE:  lang:ProjectionEmission

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/ProjectionEmission", "curie": "lang:ProjectionEmission", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/ProjectionEmission"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    projectionTargetName: list[Any] = Field(min_length=1, description="Names the external target a lang:ProjectionEmission lowers to — OntoLex-Lemon, CoNLL-U, EBNF, TEI, NIF, SemAF/AMR, BCP-47, and the like. Domain lang:ProjectionEmission, range xsd:string (stated in prose). The target is the addressee of the loss judgment: what preservation kind and unsupported constructs mean is fixed relative to it.", alias="lang:projectionTargetName")
    preservationKind: list[Any] = Field(min_length=1, description="Relates a projection (or projection-report entry) to the logic:PreservationKind it claims: the machine-readable preservation polarity in the loss ledger. Lets a report say not just 'dropped modality' but, e.g., 'OWL-EL is a logic:SoundUnderApproximation for subclass entailments under profile X; PTIME'. The projections conformance category checks that each emitted projection matches its declared preservation kind.", alias="logic:preservationKind")


class Lang_Rendering(ConfiguredBaseModel):
    """Rendering.

    A reified record of realizing content as a form: a content object (a lang:Form, a
    lang:Denotation, a composed form, a document node) rendered into a concrete lang:Form or
    lang:SurfaceForm under a declared kind and convention. Names the content it renders
    through lang:renderedContent, the form it produces through lang:renderingForm, its kind
    through lang:renderingKind (a lang:RenderingKind individual), the convention governing
    it through lang:renderingConvention, and — where the crossing is judged — a preservation
    kind through lang:renderingPreservation, which carries a logic:preservationKind rather
    than a lang: preservation enum. Rendering is the facet layer over a logic:Correspondence
    whose two sides are content and form; serializing the ontology to Turtle, lifting prose
    to a docs page, and transliterating a name are all renderings distinguished by kind,
    never a subclass ladder.

    Usage:
        >>> from gmeow_models.lang import Lang_Rendering
        >>> Lang_Rendering.model_config["json_schema_extra"]["curie"]
        'lang:Rendering'

    IRI:    https://blackcatinformatics.ca/lang/Rendering
    CURIE:  lang:Rendering

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Rendering", "curie": "lang:Rendering", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Rendering"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    renderedContent: list[Any] = Field(min_length=1, description="Relates a lang:Rendering to the content it realizes — a lang:Form, a lang:Denotation, a composed form, or a document node. Domain lang:Rendering, range a content resource (stated in prose). The content side of the rendering's crossing; the form side is lang:renderingForm.", alias="lang:renderedContent")
    renderingConvention: list[Any] = Field(min_length=1, description="Names the convention governing a rendering — a lang:Grammar, a lang:Orthography, or a gmeow:TransliterationScheme that fixes how content becomes form. Domain lang:Rendering, range a convention resource (stated in prose). The convention licenses the surface; it does not own the rendered form's identity.", alias="lang:renderingConvention")
    renderingKind: list[Any] | None = Field(default=None, description="Names the kind of a rendering. Domain lang:Rendering, range lang:RenderingKind (stated in prose). A facet only; the preservation and morphism class of the crossing live on the linked logic:Correspondence.", alias="lang:renderingKind")
    renderingPreservation: list[Any] = Field(min_length=1, description="Relates a lang:Rendering to the preservation judgment of its crossing, carried as a logic:preservationKind on the rendering's logic:Correspondence. Domain lang:Rendering, range logic:PreservationKind (stated in prose). Preservation is never a lang: enum: this property is the facet handle onto the shared logic: judgment, not a second grade.", alias="lang:renderingPreservation")


class Lang_Sense(ConfiguredBaseModel):
    """Sense.

    A sense: a lexicalized or constructed way of meaning, attached to a lexeme or a composed
    form through lang:senseOf — the middle corner of the Frege triangle. The morning star
    and the evening star are two forms with two senses that reach one referent. A sense is
    what WordNet synsets, dictionary sense numbers, and OntoLex LexicalSenses approximate,
    and what translation tries to preserve. Disjoint from lang:Form and lang:Denotation:
    sense identity never implies co-reference and form identity never implies sense
    identity. Sense-to-sense relations (hypernymy, meronymy, antonymy) are reasoned in
    logic: over the logic:Type a sense denotes, or aligned to WordNet-RDF/lexinfo — never a
    second is-a graph forked inside lang:.

    Usage:
        >>> from gmeow_models.lang import Lang_Sense
        >>> Lang_Sense.model_config["json_schema_extra"]["curie"]
        'lang:Sense'

    IRI:    https://blackcatinformatics.ca/lang/Sense
    CURIE:  lang:Sense

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/Sense", "curie": "lang:Sense", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/Sense"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_SurfaceAnchor(ConfiguredBaseModel):
    """Surface Anchor.

    A stand-off anchor locating a surface form in a source: names the source through
    lang:anchorSource, the span through lang:anchorStart and lang:anchorEnd, and the offset
    space through lang:offsetSpace (byte, codepoint, or grapheme cluster). Surface-stratum
    material excluded from form identity but included in surface identity; the anchor is
    what makes the prose-hash discipline a lookup rather than a recomputation.

    Usage:
        >>> from gmeow_models.lang import Lang_SurfaceAnchor
        >>> Lang_SurfaceAnchor.model_config["json_schema_extra"]["curie"]
        'lang:SurfaceAnchor'

    IRI:    https://blackcatinformatics.ca/lang/SurfaceAnchor
    CURIE:  lang:SurfaceAnchor
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/surfaceanchor-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/SurfaceAnchor", "curie": "lang:SurfaceAnchor", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/SurfaceAnchor"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    anchorEnd: list[int] = Field(min_length=1, description="The end offset of an anchor's span, measured in its declared offset space. Domain lang:SurfaceAnchor, range xsd:integer (stated in prose).", alias="lang:anchorEnd")
    anchorSource: list[Any] = Field(min_length=1, description="Names the source document or blob an anchor points into. Domain lang:SurfaceAnchor, range a resource (stated in prose).", alias="lang:anchorSource")
    anchorStart: list[int] = Field(min_length=1, description="The start offset of an anchor's span, measured in its declared offset space. Domain lang:SurfaceAnchor, range xsd:integer (stated in prose).", alias="lang:anchorStart")
    offsetSpace: list[str] = Field(min_length=1, description="Names the unit an anchor's offsets are measured in. Domain lang:SurfaceAnchor, range lang:OffsetSpace (stated in prose).", alias="lang:offsetSpace")


class Lang_SurfaceForm(ConfiguredBaseModel):
    """Surface Form.

    A concrete realization of a form: text (through lang:surfaceText) with a declared script
    (lang:inScript), encoding (lang:encoding), Unicode normalization
    (lang:unicodeNormalization), and collation locale (lang:collationLocale). The only
    stratum where byte identity means anything: two surface forms with different
    normalization are different surface forms of possibly the same form. A surface either
    realizes an analyzed form through lang:realizes or is typed lang:UnanalyzedProse — never
    silently either — and declares how far it has been analyzed through lang:analysisLevel.
    It may carry a lang:SurfaceAnchor locating it in a source document. Re-encoding or
    re-normalizing a text creates new surface forms but no new forms.

    Usage:
        >>> from gmeow_models.lang import Lang_SurfaceForm
        >>> Lang_SurfaceForm.model_config["json_schema_extra"]["curie"]
        'lang:SurfaceForm'

    IRI:    https://blackcatinformatics.ca/lang/SurfaceForm
    CURIE:  lang:SurfaceForm

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/SurfaceForm", "curie": "lang:SurfaceForm", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/SurfaceForm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    analysisLevel: str = Field(description="Names how far a surface form has been analyzed. Domain lang:SurfaceForm, range lang:AnalysisLevel (stated in prose). Exactly one per surface, and the surface's structure must match the declared level.", alias="lang:analysisLevel")


class Lang_TranslationUnit(ConfiguredBaseModel):
    """Translation Unit.

    A reified source-form-to-target-form pairing — the atom of a translation. Names its
    source form through lang:translationSource, its target form through
    lang:translationTarget, and the sibling unit it aligns with through lang:translationOf,
    and it carries the law-spine of the crossing by pointing — through
    lang:translationCorrespondence — at exactly one logic:Correspondence node. That
    Correspondence, not the unit, holds the logic:preservationKind, the logic:morphismClass,
    the logic:correspondenceRelation, the logic:mnemomorphic witness enumerating any
    untranslatable residue, the logic:hasDeterminacy, and any logic:hasLawClaim. A unit
    whose target is inadequate is marked through lang:translationGap; the enumerated residue
    itself lives on the carried Correspondence's mnemomorphic witness, never on the unit.

    Usage:
        >>> from gmeow_models.lang import Lang_TranslationUnit
        >>> Lang_TranslationUnit.model_config["json_schema_extra"]["curie"]
        'lang:TranslationUnit'

    IRI:    https://blackcatinformatics.ca/lang/TranslationUnit
    CURIE:  lang:TranslationUnit

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/TranslationUnit", "curie": "lang:TranslationUnit", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/TranslationUnit"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Lang_TransliterationMap(ConfiguredBaseModel):
    """Transliteration Map.

    A form-to-form map across orthographies of one sign system — Hepburn Japanese to Latin,
    Cyrillic Serbian to Latin Serbian. Names its source and target orthographies through
    lang:transliterationSourceOrthography and lang:transliterationTargetOrthography and the
    naming scheme it grafts through lang:transliterationScheme, which references a
    gmeow:TransliterationScheme individual (the FnO-backed romanization scheme in the core
    language slice). Transliteration is a rendering-layer map that changes the script
    surface, not form identity: two orthographic surfaces linked by a transliteration map
    may realize one and the same form. Its preservation is judged on the carried
    logic:Correspondence, never restated as a lang: kind.

    Usage:
        >>> from gmeow_models.lang import Lang_TransliterationMap
        >>> Lang_TransliterationMap.model_config["json_schema_extra"]["curie"]
        'lang:TransliterationMap'

    IRI:    https://blackcatinformatics.ca/lang/TransliterationMap
    CURIE:  lang:TransliterationMap

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/lang/TransliterationMap", "curie": "lang:TransliterationMap", "definitionDigest": "", "iri": "https://blackcatinformatics.ca/lang/TransliterationMap"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    transliterationScheme: list[Any] = Field(min_length=1, description="Relates a lang:TransliterationMap to the named romanization or transliteration scheme it grafts — a gmeow:TransliterationScheme individual in the core language slice, whose FnO transform the projection layer resolves. Domain lang:TransliterationMap, range gmeow:TransliterationScheme (stated in prose). The scheme names the convention; the map's preservation is judged on its logic:Correspondence.", alias="lang:transliterationScheme")
    transliterationSourceOrthography: list[Any] = Field(min_length=1, description="Relates a lang:TransliterationMap to the orthography its input surfaces are written in. Domain lang:TransliterationMap, range lang:Orthography (stated in prose). The source side of the orthography-to-orthography map; the target is lang:transliterationTargetOrthography.", alias="lang:transliterationSourceOrthography")
    transliterationTargetOrthography: list[Any] = Field(min_length=1, description="Relates a lang:TransliterationMap to the orthography its output surfaces are written in. Domain lang:TransliterationMap, range lang:Orthography (stated in prose). The target side of the orthography-to-orthography map; the source is lang:transliterationSourceOrthography.", alias="lang:transliterationTargetOrthography")

"""GMEOW models — slice GMEOW Cognition Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/cognition
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class KnowledgeLevelEnum(StrEnum):
    knowledgeAware = "gmeow:knowledgeAware"
    knowledgeKnowsAbout = "gmeow:knowledgeKnowsAbout"
    knowledgeMastered = "gmeow:knowledgeMastered"
    knowledgeUnderstands = "gmeow:knowledgeUnderstands"


class ProficiencyScaleEnum(StrEnum):
    scaleACTFL = "gmeow:scaleACTFL"
    scaleAssessed = "gmeow:scaleAssessed"
    scaleBloomRevised = "gmeow:scaleBloomRevised"
    scaleCEFR = "gmeow:scaleCEFR"
    scaleDreyfus = "gmeow:scaleDreyfus"
    scaleILR = "gmeow:scaleILR"
    scaleKnowledgeDepth = "gmeow:scaleKnowledgeDepth"
    scaleNIH = "gmeow:scaleNIH"
    scaleSOLO = "gmeow:scaleSOLO"
    scaleSelfReported = "gmeow:scaleSelfReported"


class KnowledgeProficiency(ConfiguredBaseModel):
    """Knowledge Proficiency.

    A reified, leveled depth-of-knowledge of an agent toward a subject — the gufo:Relator
    binding {agent} × {subject} × {level on a scale} × {interval}, mirroring expertise's
    gmeow:SkillProficiency. Founded on the gmeow:CognitiveState mode (documentation, not an
    axiom — Principle 12); the mode and this relator are DIFFERENT individuals, never
    double-typed. Flat-first: the four-level knowledge spectrum (gmeow:isAwareOf …
    gmeow:hasMastered) is the cheap surface; promote to a KnowledgeProficiency when level,
    scale, temporal scope, or standpoint matters. Suppression not deletion (Principle 10):
    lapsed knowledge is recorded as a closed interval / gmeow:displayable false, never
    deleted. Boundaries, documented WITHOUT axiom bridges (Principle 9): cognition (knowing,
    objectual) ⊥ epistemics (believing, propositional); cognition ⊥ expertise (knowing ⊥
    can-do — a skill is defeasible evidence for gmeow:knowsAbout, never a subproperty
    entailment).

    When to use:
        - Use when a depth-of-knowledge fact needs its own level, rating scale, temporal scope, or
          standpoint to be first-class — the reify-on-demand promotion of the flat knowledge
          spectrum (Principle 4), and the construct that records lapsed knowledge by closing its
          interval rather than deleting it (Principle 10).

    When to avoid:
        - Avoid for the 80% case where an agent simply knows a subject to some depth — assert the
          flat gmeow:isAwareOf … gmeow:hasMastered spectrum and promote only when context must be
          first-class; avoid double-typing it with the gmeow:CognitiveState mode (different
          individuals, Principle 12); avoid it for the can-do axis (that is expertise's
          gmeow:SkillProficiency).

    How to use:
        - Mint one KnowledgeProficiency per (agent, subject, level, scale) tuple, binding the five
          roles (gmeow:knowledgeProficiencyAgent / …Subject / …Level / …Scale / …Interval); read
          gmeow:pairsWith from the flat spectrum properties to surface the promotion path in gmeow
          describe, and carry contested or attributed depth on the statement layer via
          gmeow:accordingTo.

    Examples:
        - ex:lillithPythonKP a gmeow:KnowledgeProficiency ; gmeow:knowledgeProficiencyAgent
          ex:lillith ; gmeow:knowledgeProficiencySubject ex:python ;
          gmeow:knowledgeProficiencyLevel gmeow:knowledgeUnderstands ;
          gmeow:knowledgeProficiencyScale gmeow:scaleKnowledgeDepth .

    Usage:
        >>> from gmeow_models.cognition import KnowledgeProficiency
        >>> KnowledgeProficiency.model_config["json_schema_extra"]["curie"]
        'gmeow:KnowledgeProficiency'

    IRI:    https://blackcatinformatics.ca/gmeow/KnowledgeProficiency
    CURIE:  gmeow:KnowledgeProficiency

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/KnowledgeProficiency", "curie": "gmeow:KnowledgeProficiency", "definitionDigest": "blake3:39f6de4ed630556bde02642869c6eb3cd9ffcd96f182d7ce30490b2c8cc91ced", "iri": "https://blackcatinformatics.ca/gmeow/KnowledgeProficiency"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    knowledgeProficiencyAgent: list[Agent] | None = Field(default=None, description="The agent whose depth of knowledge a knowledge-proficiency expresses. Functional — constitutive of the proficiency.", alias="gmeow:knowledgeProficiencyAgent")
    knowledgeProficiencyLevel: list[KnowledgeLevelEnum] | None = Field(default=None, description="The attained ordinal depth of a knowledge-proficiency (a gmeow:KnowledgeLevel: knowledgeAware ≺ knowledgeKnowsAbout ≺ knowledgeUnderstands ≺ knowledgeMastered). Functional.", alias="gmeow:knowledgeProficiencyLevel")
    knowledgeProficiencyScale: list[ProficiencyScaleEnum] | None = Field(default=None, description="The framework a knowledge-proficiency level is measured on (the native gmeow:KnowledgeLevel scale, or Bloom's revised / SOLO / Dreyfus as alternates) — a gmeow:ProficiencyScale value (kernel). Functional.", alias="gmeow:knowledgeProficiencyScale")
    knowledgeProficiencySubject: list[Entity] | None = Field(default=None, description="The subject a knowledge-proficiency concerns (open range gmeow:Entity: a topic, person, place, or event). Functional — constitutive.", alias="gmeow:knowledgeProficiencySubject")

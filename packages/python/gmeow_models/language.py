"""GMEOW models — slice GMEOW Language Core Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/language
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class TransliterationSchemeEnum(StrEnum):
    schemeBGNPCGN = "gmeow:schemeBGNPCGN"
    schemeHepburn = "gmeow:schemeHepburn"
    schemeIAST = "gmeow:schemeIAST"
    schemeIPA = "gmeow:schemeIPA"
    schemeISO15919 = "gmeow:schemeISO15919"
    schemeISO233 = "gmeow:schemeISO233"
    schemeKunreiShiki = "gmeow:schemeKunreiShiki"
    schemeMcCuneReischauer = "gmeow:schemeMcCuneReischauer"
    schemeNihonShiki = "gmeow:schemeNihonShiki"
    schemePinyin = "gmeow:schemePinyin"
    schemeRevisedRomanization = "gmeow:schemeRevisedRomanization"
    schemeWadeGiles = "gmeow:schemeWadeGiles"


class Language(ConfiguredBaseModel):
    """Language.

    A language as a first-class information object with a self-minted IRI: a system of
    signs/symbols and rules used for communication or computation. Registry-INDEPENDENT —
    BCP-47/ISO 639/Glottolog/Wikidata are optional alignments (gmeow:languageCode,
    gmeow:authorityLink, skos:exactMatch), never identity, so a code-less conlang or
    AI-generated language is fully first-class. Origin, modality and status are open value
    vocabularies; the scripts it is written in are bound co-equally via lang:Orthography
    (lang:orthographyFor + lang:usesScript); its endonym/exonym names are gmeow:Appellation.

    When to use:
        - Use to mint a language as a first-class IRI rather than a flat code literal — the
          registry-independent target of gmeow:writtenInLanguage, so a code-less conlang or
          AI-generated language is fully modelled (no privileged language, Principle 9). The
          framework's own @x-gmeow-* private-use tags ride lang:carrierTag on the three carrier
          varieties (lang:gmeowEnglish/gmeowFrench/gmeowMandarin), not on every language.

    When to avoid:
        - Avoid flattening a language into an inLanguage code string on the content (that is the
          anti-pattern gmeow:writtenInLanguage replaces) and avoid treating a registry code as
          identity; codes are alignments via gmeow:languageCode, not the IRI.

    How to use:
        - Mint the Language with a self-minted IRI, give it endonym/exonym gmeow:Appellation
          names, bind its scripts co-equally through lang:Orthography (each a lang:orthographyFor
          the language, lang:usesScript a lang:Script), and assert registry coreference with
          skos:exactMatch plus gmeow:languageCode rather than encoding identity in a code.

    Examples:
        - ex:langTlhingan a gmeow:Language ; gmeow:languageCode "tlh" ; skos:exactMatch
          <http://www.wikidata.org/entity/Q10134> .

    Usage:
        >>> from gmeow_models.language import Language
        >>> Language.model_config["json_schema_extra"]["curie"]
        'gmeow:Language'

    IRI:    https://blackcatinformatics.ca/gmeow/Language
    CURIE:  gmeow:Language

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Language", "curie": "gmeow:Language", "definitionDigest": "blake3:a272b61120b25fe900f05121e8a5d7363ca4096ef8afbfbf99c56b575421fec7", "iri": "https://blackcatinformatics.ca/gmeow/Language"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

"""GMEOW models — slice GMEOW Lifecycle Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/lifecycle
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class EntityExistence(ConfiguredBaseModel):
    """Entity Existence.

    The reified, time-scoped fact that an entity existed over an interval — a gufo:Situation
    that carries its period via gmeow:duringInterval (inherited from TimeScopedRelation).
    Use when the existence claim is contested (two sources disagree on when a place ceased
    to exist), when evidence must be attached, or when standpoint indexing is needed.
    Flat-first: the common case uses gmeow:hasCreationEvent / gmeow:hasDestructionEvent /
    gmeow:existenceInterval directly on the entity.

    When to use:
        - Use the reified form when the existence claim itself must carry standpoint, confidence,
          evidence, or contestation — two sources disagreeing on when a place ceased to exist, or
          a dated founding needing a citation.

    When to avoid:
        - Avoid it for the ordinary case — flat gmeow:hasCreationEvent / gmeow:hasDestructionEvent
          / gmeow:existenceInterval on the entity carry it more cheaply; promote to this relator
          only when the claim needs to be argued about.

    How to use:
        - Mint a gmeow:EntityExistence, attach the subject with gmeow:existenceEntity, the
          bounding events with gmeow:existenceCreationEvent/existenceDestructionEvent, the period
          via the inherited gmeow:duringInterval, and standpoint/evidence via the statement layer.

    Examples:
        - ex:troyExistence a gmeow:EntityExistence ; gmeow:existenceEntity ex:troy ;
          gmeow:existenceDestructionEvent ex:sack ; gmeow:accordingTo ex:homer .

    Usage:
        >>> from gmeow_models.lifecycle import EntityExistence
        >>> EntityExistence.model_config["json_schema_extra"]["curie"]
        'gmeow:EntityExistence'

    IRI:    https://blackcatinformatics.ca/gmeow/EntityExistence
    CURIE:  gmeow:EntityExistence

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/EntityExistence", "curie": "gmeow:EntityExistence", "definitionDigest": "blake3:01dc79108dc7a4c3b081d2176c8403ea7d1391f8c6b46115040a8dd6e3d47a3e", "iri": "https://blackcatinformatics.ca/gmeow/EntityExistence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    duringInterval: Any = Field(description="Relates a time-scoped relation to the interval over which it holds.", alias="gmeow:duringInterval")
    existenceEntity: Entity = Field(description="The entity whose existence is recorded by this EntityExistence. Functional: an EntityExistence concerns exactly one entity (constitutive of the situation's identity).", alias="gmeow:existenceEntity")

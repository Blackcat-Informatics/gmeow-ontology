# Projection: GMEOW → pure loinc. GENERATED by `gmeow regenerate` (mappings) from mapping-dsl/ — DO NOT EDIT.
# Lossy and directional by design; drops: the general sensory observation is narrowed to a laboratory test; non-lab sensory observations lose their specific semantics; the GMEOW observable-property profile is flattened to a LOINC component; method metadata outside the lab-test scope is dropped; unit, determinacy, granularity, and reference frame are dropped; only the numeric result value survives; the general sensory observation is narrowed to a clinical finding / diagnosis interpretation; the generic observed feature is narrowed to a patient in clinical contexts.
PREFIX gmeow: <https://blackcatinformatics.ca/gmeow/>
PREFIX loinc: <http://loinc.org/rdf/>
PREFIX snomed: <http://snomed.info/id/>

CONSTRUCT {
    ?obs a loinc:LabTest .
    ?prop a loinc:Component .
    ?method a loinc:Method .
    ?qty a loinc:Quantity .
    ?obs a snomed:404684003 .
    ?feature a snomed:116154003 .
}
WHERE {
    {
        ?obs a gmeow:SensoryObservation .
    }
    UNION {
        ?prop a gmeow:ObservableProperty .
    }
    UNION {
        ?method a gmeow:ObservationMethod .
    }
    UNION {
        ?qty a gmeow:ScalarQuantity .
    }
    UNION {
        ?obs a gmeow:SensoryObservation .
        ?obs gmeow:observedFeature ?feature .
    }
}

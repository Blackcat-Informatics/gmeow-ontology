"""GMEOW models — slice GMEOW Lexicon Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/lexicon
Tier:       extension
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class DerivationKindEnum(StrEnum):
    derivationAffixation = "gmeow:derivationAffixation"
    derivationBackFormation = "gmeow:derivationBackFormation"
    derivationBorrowing = "gmeow:derivationBorrowing"
    derivationCalque = "gmeow:derivationCalque"
    derivationClipping = "gmeow:derivationClipping"
    derivationCompounding = "gmeow:derivationCompounding"
    derivationFolkEtymology = "gmeow:derivationFolkEtymology"
    derivationInheritance = "gmeow:derivationInheritance"
    derivationReanalysis = "gmeow:derivationReanalysis"
    derivationReconstruction = "gmeow:derivationReconstruction"
    derivationSemanticShift = "gmeow:derivationSemanticShift"
    derivationSoundChange = "gmeow:derivationSoundChange"
    derivationSpellingChange = "gmeow:derivationSpellingChange"
    derivationUnknownOrigin = "gmeow:derivationUnknownOrigin"


class LexicalFormTypeEnum(StrEnum):
    formNormalized = "gmeow:formNormalized"
    formReconstructed = "gmeow:formReconstructed"
    formRendered = "gmeow:formRendered"
    formSigned = "gmeow:formSigned"
    formSpoken = "gmeow:formSpoken"
    formTranslated = "gmeow:formTranslated"
    formTransliterated = "gmeow:formTransliterated"
    formWritten = "gmeow:formWritten"


class EtymologicalDerivation(ConfiguredBaseModel):
    """Etymological Derivation.

    A provenance-rich claim about historical derivation between lexical items or forms. Not
    a flat property — a full relator carrying standpoint (accordingTo), confidence,
    determinacy, and temporal scope (Principle 9). Multiple derivations for the same target
    coexist without privilege; a superseded derivation is suppressed with displayable false,
    never erased (Principle 10). Follows the LanguageState / KinRelationship pattern as an
    Observation + Relator.

    When to use:
        - Use for a historical-derivation claim between lexical items or forms — borrowing,
          inheritance, semantic shift — that must carry standpoint, confidence, and temporal scope
          rather than a flat origin string.

    When to avoid:
        - Avoid recording etymology as a single flat property or text blob, and avoid deleting a
          discredited derivation — competing derivations coexist, and a superseded one is
          suppressed with gmeow:displayable false (Principle 10).

    How to use:
        - Mint the relator, name source/target/kind (all required by the someValuesFrom axioms),
          attach supporting attestations via gmeow:derivationEvidence, and carry standpoint and
          confidence on the observation spine.

    Examples:
        - ex:etym1 a gmeow:EtymologicalDerivation ; gmeow:etymonSource ex:oldEnglishWaeter ;
          gmeow:derivationTarget ex:wordWater ; gmeow:derivationKind gmeow:derivationInheritance .

    Usage:
        >>> from gmeow_models.lexicon import EtymologicalDerivation
        >>> EtymologicalDerivation.model_config["json_schema_extra"]["curie"]
        'gmeow:EtymologicalDerivation'

    IRI:    https://blackcatinformatics.ca/gmeow/EtymologicalDerivation
    CURIE:  gmeow:EtymologicalDerivation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/EtymologicalDerivation", "curie": "gmeow:EtymologicalDerivation", "definitionDigest": "blake3:332735ef7dcd30f7d277d00692aa29e5f4cd2cfd04811517b8f96df3066b8650", "iri": "https://blackcatinformatics.ca/gmeow/EtymologicalDerivation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    derivationKind: list[DerivationKindEnum] | None = Field(default=None, description="The kind(s) of etymological derivation claimed — borrowing, calque, semantic shift, sound change, etc. NON-FUNCTIONAL: a single derivation may be classified as both borrowing and reanalysis by different analytical standpoints (Principle 9).", alias="gmeow:derivationKind")
    derivationTarget: list[str] | None = Field(default=None, description="Within gmeow:EtymologicalDerivation, values are node references constrained to gmeow:InformationObject. The descendant lexical item or form that is derived from the source. Functional per relator: one target per EtymologicalDerivation.", alias="gmeow:derivationTarget")
    etymonSource: list[str] | None = Field(default=None, description="Within gmeow:EtymologicalDerivation, values are node references constrained to gmeow:InformationObject. The etymon — the ancestor lexical item or form from which the target is derived. Functional per relator: one source per EtymologicalDerivation (a derivation with multiple sources is modeled as multiple derivations). Distinct from gmeow:derivationSource (creative-works), which names the source work of a CreativeDerivation.", alias="gmeow:etymonSource")


class UsageAttestation(ConfiguredBaseModel):
    """Usage Attestation.

    An evidence node recording that a lexical form, reading, inscription, symbol,
    construction, or usage was observed in a source or context. Evidence, not truth: an
    attestation records that something was seen, not that a proposed interpretation is
    correct (Principle 12). Follows the LanguageState / KinRelationship pattern as an
    Observation + Relator. Mediates between an attested form, a source, a context, and
    optionally a time interval.

    When to use:
        - Use to record that a form was OBSERVED in a source or context — an evidence node, not a
          truth claim about whether an interpretation is correct.

    When to avoid:
        - Avoid using it to assert that an analysis is correct (it records observation, not truth)
          and avoid flattening it to a bare property — the relator must carry source, context, and
          standpoint.

    How to use:
        - Mint the relator, name the form with gmeow:attestedForm and the source with
          gmeow:attestedInSource (both required by the someValuesFrom axiom), add
          context/carrier/interval as available, and carry confidence and standpoint on the
          observation spine.

    Examples:
        - ex:att1 a gmeow:UsageAttestation ; gmeow:attestedForm ex:waterWritten ;
          gmeow:attestedInSource ex:oed .

    Usage:
        >>> from gmeow_models.lexicon import UsageAttestation
        >>> UsageAttestation.model_config["json_schema_extra"]["curie"]
        'gmeow:UsageAttestation'

    IRI:    https://blackcatinformatics.ca/gmeow/UsageAttestation
    CURIE:  gmeow:UsageAttestation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/UsageAttestation", "curie": "gmeow:UsageAttestation", "definitionDigest": "blake3:a9c2f67257ee996f44a27bff3ba5c41290eb0a89bbd93fca9fd717834088c1ce", "iri": "https://blackcatinformatics.ca/gmeow/UsageAttestation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    attestedForm: list[str] | None = Field(default=None, description="Within gmeow:UsageAttestation, values are node references constrained to gmeow:LexicalForm. The lexical form that a usage attestation observes. Functional per relator: one attested form per UsageAttestation.", alias="gmeow:attestedForm")
    attestedInSource: list[str] | None = Field(default=None, description="Within gmeow:UsageAttestation, values are node references constrained to gmeow:CreativeWork. The bibliographic or documentary source in which the attested form was observed. NON-FUNCTIONAL: an attestation may be supported by multiple sources, and competing source attributions coexist (Principle 9). Targets a CreativeWork (the retired Source Kind was greenfield-refactored in source/claim refactor).", alias="gmeow:attestedInSource")

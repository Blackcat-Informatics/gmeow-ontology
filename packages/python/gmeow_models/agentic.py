"""GMEOW models — slice GMEOW Agentic Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/agentic
Tier:       extension
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ToolCall(ConfiguredBaseModel):
    """Tool Call.

    One invocation of a tool by an agent: the tool agent (gmeow:usedTool), the requesting
    model invocation when known (gmeow:calledByInvocation), and the verbatim
    arguments/result payloads. The agent's actions join the same provenance graph as its
    claims — an entity the call produced links back via gmeow:wasGeneratedBy (no forward
    output property, Principle 5), so 'which tool, called by which invocation, with what
    arguments?' is answerable after the fact. The ModelInvocation idiom one level down.

    Examples:
        - An assistant's MCP store_claim call: the ToolCall records the tool agent, the verbatim
          JSON arguments, and the stored claim's id as the result payload; the claim itself links
          back with gmeow:wasGeneratedBy.

    Usage:
        >>> from gmeow_models.agentic import ToolCall
        >>> ToolCall.model_config["json_schema_extra"]["curie"]
        'gmeow:ToolCall'

    IRI:    https://blackcatinformatics.ca/gmeow/ToolCall
    CURIE:  gmeow:ToolCall

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ToolCall", "curie": "gmeow:ToolCall", "definitionDigest": "blake3:ce6f90f93fcbbf8d1f1be0e80c4eb35845e405cd3a3652c8dcd2498a05b691e0", "iri": "https://blackcatinformatics.ca/gmeow/ToolCall"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    calledByInvocation: list[str] | None = Field(default=None, description="The model invocation that requested this tool call. Functional: one requesting invocation per call. Optional: a recording harness may not expose the invocation — a ToolCall without one is still auditable provenance.", alias="gmeow:calledByInvocation")
    toolArguments: list[str] | None = Field(default=None, description="The verbatim arguments payload presented to the tool (typically JSON), byte-faithful. For large payloads store a content digest literal instead ('blake3:…' — the gmeow:contentDigest convention): the digest is the value, resolvable from a content-addressed store. Functional: one arguments record per call.", alias="gmeow:toolArguments")
    toolResult: list[str] | None = Field(default=None, description="The verbatim result payload the tool returned, byte-faithful — the PAYLOAD of the event, never a forward entity link: an entity the call produced links back via gmeow:wasGeneratedBy (Principle 5). For large payloads store a content digest literal instead ('blake3:…'), as gmeow:toolArguments. Functional: one result record per call.", alias="gmeow:toolResult")
    usedTool: list[SoftwareAgent] | None = Field(default=None, description="The tool that was called — a SoftwareAgent (an MCP tool, a search service, a code runner). Deliberately no Tool subclass: tool-ness is the role the agent plays in this call event, not an identity. Functional: one tool per call; a multi-tool turn is several ToolCalls.", alias="gmeow:usedTool")

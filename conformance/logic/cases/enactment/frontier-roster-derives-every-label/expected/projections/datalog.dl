% GENERATED by `gmeow logic compile` — DO NOT EDIT.
% Datalog projection of the canonical logic: program.

% === Ground facts (axioms) ===
label("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", 'frontier label: blocked on capability or resource', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", 'Derives logic:FrontierBlockedCapabilityOrResource for a frontier entry whose step axis is waiting and whose action is the blocked step of an operational capability gap. The gap is an operational one — a capability the deployment does not have — and never a reasoning-profile gap, so this rule is also where the non-conflation is enforced by construction rather than by a reviewer noticing.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "c14n12", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "c14n31", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "c14n32", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "c14n10", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "https://blackcatinformatics.ca/logic/ruleFrontierBlockedCapability", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", 'frontier label: blocked on an unmet precondition', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", 'Derives logic:FrontierBlockedPrecondition for a frontier entry whose step axis is pending — the step belongs to the enactment and its preconditions have not been evaluated as met, which is exactly the position the step axis mints for an unmet precondition. Positive Horn, and disjoint from every other rule by the step axis alone. The label matters because its remedy differs from every neighbouring obstruction: a precondition is remediable by changing the world, where a missing capability is not, and telling an operator to wait when the answer is to change the world is the failure this separation prevents.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", "c14n11", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", "c14n33", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", "https://blackcatinformatics.ca/logic/ruleFrontierBlockedPrecondition", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", 'frontier label: cancelled', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", 'Derives logic:FrontierCancelled for a frontier entry whose step and enactment axes are both cancelled and whose commitment knowledge was never evaluated — the quiescent cancellation, where stopping by decision settles the matter because no external effect was ever in question. The knowledge position is the load-bearing conjunct: it is what distinguishes this from cancellation with in-flight effects, and reading a bare cancelled position as quiescent is exactly how an outstanding exchange gets closed on paper while remaining open at the provider.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "c14n34", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "c14n39", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "c14n47", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "c14n18", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "https://blackcatinformatics.ca/logic/ruleFrontierCancelled", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", 'frontier label: cancelled with in-flight effects', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", 'Derives logic:FrontierCancelledWithInFlightEffects for a frontier entry whose step and enactment axes are both cancelled while its commitment knowledge remains undetermined. Cancellation is a decision about the occurrence and it does not resolve knowledge about the world, so the outstanding exchange still demands reconciliation; the sibling entry that positions that exchange reads reconciliation-required. It differs from plain cancelled on the commitment-knowledge axis and on nothing else, which is exactly why the two cannot share a slot — and it is arguably the hardest real position an operator meets, which a bare cancelled label hides.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "c14n17", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "c14n21", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "c14n44", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "c14n16", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "https://blackcatinformatics.ca/logic/ruleFrontierCancelledWithInFlightEffects", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", 'frontier label: compensation eligible', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", 'Derives logic:FrontierCompensationEligible for a frontier entry whose effect axis sits at receipted — a forward effect is known to have committed, so a counteraction is now permissible. Eligibility is all this derives: it asserts no compensation attempt, no compensation outcome, and no claim that compensating will succeed, because compensation is a new action with its own possibility of failing.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", "c14n20", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", "c14n13", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", "https://blackcatinformatics.ca/logic/ruleFrontierCompensationEligible", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", 'frontier label: compensation failed with the forward effect committed', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", 'Derives logic:FrontierCompensationFailedForwardCommitted for a frontier entry whose exchange sits in its compensating leg, whose step axis is failed, whose commitment knowledge is still supported, and whose enactment is still running. Read together those four positions say the whole thing: a compensating action was launched, the step carrying it terminally failed, the forward effect it was launched against remains committed, and the occurrence has not given up. The last position is what separates this from terminally-failed — the enactment is still running because escalation is a continuation, and escalation rather than retry is the only move this position licenses.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "c14n22", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "c14n25", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "c14n45", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "c14n5", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "c14n46", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "https://blackcatinformatics.ca/logic/ruleFrontierCompensationFailedForwardCommitted", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", 'frontier label: compensation required', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", 'Derives logic:FrontierCompensationRequired for a frontier entry whose effect axis sits at reconciled, whose commitment knowledge is supported, and whose enactment axis is failed — a probe settled that the forward effect committed, and the occurrence it belonged to will not complete, so the committed effect has to be counteracted for the run to reach a consistent terminal position. Three axes carry this and none of them could alone: the effect position says the question was answered, the knowledge value says which way, and the enactment position is what turns permission into obligation. That is precisely what separates it from compensation-eligible, where declining costs nothing.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "c14n27", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "c14n30", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "c14n6", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "c14n14", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "https://blackcatinformatics.ca/logic/ruleFrontierCompensationRequired", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierCompleted", 'frontier label: completed', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierCompleted", "Derives logic:FrontierCompleted for a frontier entry whose step axis is completed — the step carried out its prescribed work and reached a terminal position, so nothing further is owed on it. The guard reads that one axis and stops there on purpose: the label asserts nothing about whether the enactment's goal is met, which is the goal-evaluation axis, and nothing about whether an external effect the step dispatched actually committed, which is the commitment-knowledge axis carried by the exchange's own entry.", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierCompleted", "c14n4", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierCompleted", "c14n0", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierCompleted", "https://blackcatinformatics.ca/logic/ruleFrontierCompleted", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", 'frontier label: mutually exclusive with another candidate', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", 'Derives logic:FrontierMutuallyExclusive for a frontier entry that logic:entryExcludes another entry. This is the one guard in the set that reads no axis, and deliberately: exclusivity is a relation between two candidates and not a position either of them occupies, so putting it on an axis would be the conflation the seven-axis discipline exists to refuse. It becomes visible only when local frontiers are glued, which is also why the withheld candidate carries no readiness position — being ready is a claim about a frontier on which the excluding candidate was not taken.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", "c14n15", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", "c14n26", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", "https://blackcatinformatics.ca/logic/ruleFrontierMutuallyExclusive", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", 'frontier label: ready but approval required', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", 'Derives logic:FrontierReadyApprovalRequired for a frontier entry whose step axis is ready and whose approval axis is created but not yet detached. Readiness and approval are separate axes, and this rule is where that separation earns its keep: the same entry is simultaneously ready to run and gated, which a single blended state could only express by minting a combined value for every such pairing.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", "c14n23", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", "c14n3", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", "c14n7", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", "https://blackcatinformatics.ca/logic/ruleFrontierReadyApprovalRequired", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", 'frontier label: ready and authorized', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", 'Derives logic:FrontierReadyAuthorized for a frontier entry whose step axis is ready and whose approval axis is null — the policy already authorizes the effect, so no operator gate stands between the action and its dispatch. Positive Horn. This is the rule that makes policy-maximal automation a derivation rather than a habit: an entry is authorized because its axis tuple says so, not because nobody remembered to gate it.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", "c14n24", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", "c14n28", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", "c14n2", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", "https://blackcatinformatics.ca/logic/ruleFrontierReadyAuthorized", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", 'frontier label: reconciliation foreclosed', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", 'Derives logic:FrontierReconciliationImpossible for a frontier entry whose effect axis sits at abandoned — the exchange was given up without an authoritative determination because no reconciliation semantics remain available. The effect axis mints exactly one position for that, so the guard needs nothing else. The label is the honest counterpart of reconciliation-required: there, probing is still available and the frontier names it; here it is foreclosed, and naming another probe would send an operator after evidence that cannot be obtained.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", "c14n42", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", "c14n19", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", "https://blackcatinformatics.ca/logic/ruleFrontierReconciliationImpossible", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", 'frontier label: reconciliation required', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", 'Derives logic:FrontierReconciliationRequired for a frontier entry whose effect axis sits at attempted — an effect was dispatched and no receipt settled it. This is the no-blind-retry law stated positively: rather than only forbidding a retry, the frontier NAMES the one action that is licensed from an undetermined position, so an operator reading it is told what to do instead of merely what not to do.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", "c14n1", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", "c14n8", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", "https://blackcatinformatics.ca/logic/ruleFrontierReconciliationRequired", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", 'frontier label: skipped with a typed reason', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "Derives logic:FrontierSkippedWithTypedReason for a frontier entry whose step axis is skipped and which carries a logic:skipReason. The typed reason is a guard and not a decoration: the label's whole content is that the omission was deliberate and attributable, so an entry claiming a skip without one derives nothing rather than deriving a label it cannot support — which is the correct outcome, because an untyped skip is indistinguishable from an oversight and the frontier must not present the two alike.", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "c14n29", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "c14n35", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "c14n41", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "https://blackcatinformatics.ca/logic/ruleFrontierSkippedWithTypedReason", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", 'frontier label: terminally failed', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "Derives logic:FrontierTerminallyFailed for a frontier entry whose step axis is failed and whose enactment axis is failed — the step attempted its work and failed, and the occurrence stopped because no continuation is available, which is the enactment axis's own definition of failure. Both conjuncts are needed: a failed step inside a still-running enactment has a continuation somewhere, and the enactment position is what says there is none. That is also what separates it from compensation-failed-forward-committed, where the run is still going because escalation remains open.", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "c14n38", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "c14n9", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "c14n40", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "https://blackcatinformatics.ca/logic/ruleFrontierTerminallyFailed", "default").
label("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", 'frontier label: waiting on an event, a time or an input', "default").
definition("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", 'Derives logic:FrontierWaitingOnEventTimeOrInput for a frontier entry whose step axis is waiting and which names, through logic:entryAwaits, the thing it is blocked pending. The awaited witness is what makes the label a derivation rather than a shrug: the step axis says THAT the step waits, and only the witness says what would end the wait. It is also what keeps this rule disjoint from the capability-gap rule, which shares the waiting position — a step blocked by a capability the deployment does not have awaits nothing, because what is absent does not arrive, and its remedy is a capability-gap proposal rather than patience.', "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", "c14n36", "default").
body("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", "c14n37", "default").
head("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", "c14n43", "default").
provenance("https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", "https://blackcatinformatics.ca/logic/ruleFrontierWaitingOnEventTimeOrInput", "default").

% === Rules ===
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierBlockedCapabilityOrResource", ?C) :-
    entryAction(?entry, ?step, ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepWaiting", ?C),
    gapBlockedStep(?gap, ?step, ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierBlockedPrecondition", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepPending", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCancelled", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EnactmentCancelled", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/InfoNotEvaluated", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepCancelled", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCancelledWithInFlightEffects", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EnactmentCancelled", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/InfoUndetermined", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepCancelled", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCompensationEligible", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EffectReceipted", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCompensationFailedForwardCommitted", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EffectCompensating", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EnactmentRunning", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/InfoSupported", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepFailed", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCompensationRequired", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EffectReconciled", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EnactmentFailed", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/InfoSupported", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierCompleted", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepCompleted", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierMutuallyExclusive", ?C) :-
    entryExcludes(?entry, ?other, ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierReadyApprovalRequired", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/ApprovalCreated", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepReady", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierReadyAuthorized", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/ApprovalNull", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepReady", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierReconciliationImpossible", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EffectAbandoned", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierReconciliationRequired", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EffectAttempted", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierSkippedWithTypedReason", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepSkipped", ?C),
    skipReason(?entry, ?reason, ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierTerminallyFailed", ?C) :-
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/EnactmentFailed", ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepFailed", ?C).
entryLabel(?entry, "https://blackcatinformatics.ca/logic/FrontierWaitingOnEventTimeOrInput", ?C) :-
    entryAwaits(?entry, ?awaited, ?C),
    entryAxisWitness(?entry, "https://blackcatinformatics.ca/logic/StepWaiting", ?C).

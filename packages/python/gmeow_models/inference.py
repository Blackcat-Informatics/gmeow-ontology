"""GMEOW models — slice GMEOW Inference Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/inference
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class AcceptanceStatusEnum(StrEnum):
    acceptanceIn = "gmeow:acceptanceIn"
    acceptanceOut = "gmeow:acceptanceOut"
    acceptanceUndecided = "gmeow:acceptanceUndecided"


class AttackKindEnum(StrEnum):
    attackRebut = "gmeow:attackRebut"
    attackUndercut = "gmeow:attackUndercut"
    attackUndermine = "gmeow:attackUndermine"


class InferenceModeEnum(StrEnum):
    modeAbduction = "gmeow:modeAbduction"
    modeAnalogical = "gmeow:modeAnalogical"
    modeDeduction = "gmeow:modeDeduction"
    modeInduction = "gmeow:modeInduction"


class Analogy(ConfiguredBaseModel):
    """Analogy.

    A reified analogical mapping — the source-to-target structure-mapping (Gentner) that
    warrants an analogical inference: it mediates a source domain (gmeow:analogicalSource),
    a target domain (gmeow:analogicalTarget), and the element-pair correspondences
    (gmeow:hasCorrespondence) holding between them, scored by structural consistency
    (gmeow:systematicity, solver-layer). The warrant of an analogical
    gmeow:InferenceCommitment points at an Analogy. Rival mappings compete; best-analogy
    selection is solver-layer like IBE, never an asserted isBest bit.

    Usage:
        >>> from gmeow_models.inference import Analogy
        >>> Analogy.model_config["json_schema_extra"]["curie"]
        'gmeow:Analogy'

    IRI:    https://blackcatinformatics.ca/gmeow/Analogy
    CURIE:  gmeow:Analogy

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Analogy", "curie": "gmeow:Analogy", "definitionDigest": "blake3:d3dee3a06f1e6b59cdb1d5ea00b9cfa78b35267501e748c0929513725a9a9969", "iri": "https://blackcatinformatics.ca/gmeow/Analogy"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    analogicalSource: list[Any] = Field(min_length=1, description="The source (base) domain of a gmeow:Analogy — the familiar structure whose relations are mapped onto the target. NOT functional.", alias="gmeow:analogicalSource")
    analogicalTarget: list[Any] = Field(min_length=1, description="The target domain of a gmeow:Analogy — the less-familiar structure that inherits relations from the source via the correspondences. NOT functional.", alias="gmeow:analogicalTarget")
    hasCorrespondence: list[Any] = Field(min_length=1, description="Relates a gmeow:Analogy to one of its element-pair gmeow:Correspondence relations — the reified mapped pairs whose structural consistency is the analogy's gmeow:systematicity. NOT functional: an analogy holds many correspondences.", alias="gmeow:hasCorrespondence")


class Argument(ConfiguredBaseModel):
    """Argument.

    A node in an argumentation framework — the attackable, supportable unit a named
    acceptability semantics is evaluated over (Dung / ASPIC+). An Argument wraps at most one
    Toulmin step (gmeow:argumentInferenceStep -> gmeow:InferenceCommitment), rests on the
    premises it actually uses (gmeow:hasPremiseUse) via the rule it applies
    (gmeow:hasInferenceApplication), and establishes a conclusion
    (gmeow:argumentConclusion). It bears the typed gmeow:Attack and the accruing
    gmeow:Support relations toward other arguments, and is the proper domain the epistemics
    gmeow:attacksClaim / gmeow:supportsClaim hooks pair with. A relator, not a process (the
    gmeow:Commitment idiom); whether it survives is the solver-layer
    gmeow:ArgumentEvaluation, never an asserted accepted bit (Principle 12).

    Usage:
        >>> from gmeow_models.inference import Argument
        >>> Argument.model_config["json_schema_extra"]["curie"]
        'gmeow:Argument'

    IRI:    https://blackcatinformatics.ca/gmeow/Argument
    CURIE:  gmeow:Argument
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/argument-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Argument", "curie": "gmeow:Argument", "definitionDigest": "blake3:3c617821316f781be5117fc1a1fed52c21d8fe39ffec409b293247b084af1436", "iri": "https://blackcatinformatics.ca/gmeow/Argument"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    argumentConclusion: Any = Field(description="The conclusion a gmeow:Argument establishes — the claim it concludes for. Functional: one argument, one conclusion (an argument concluding two things is two arguments).", alias="gmeow:argumentConclusion")
    argumentInferenceStep: Any | None = Field(default=None, description="Relates a gmeow:Argument to the single Toulmin step (gmeow:InferenceCommitment) it wraps — the premises x conclusion x warrant structure the argument is built on. Functional: one Argument wraps at most one step; a multi-step chain is a chain of Arguments linked by gmeow:Support.", alias="gmeow:argumentInferenceStep")
    hasInferenceApplication: list[InferenceApplication] | None = Field(default=None, description="Relates a gmeow:Argument to a gmeow:InferenceApplication (rule + substitution) by which it derives its conclusion from its premise-uses. NOT functional: an argument's derivation may chain several rule-applications.", alias="gmeow:hasInferenceApplication")
    hasPremiseUse: list[PremiseUse] | None = Field(default=None, description="Relates a gmeow:Argument to a gmeow:PremiseUse it rests on — the premises it actually bases on (available != used). NOT functional: an argument may use several premises.", alias="gmeow:hasPremiseUse")


class ArgumentEvaluation(ConfiguredBaseModel):
    """Argument Evaluation.

    A reified, solver-set verdict on a gmeow:Argument — the gmeow:AcceptanceStatus it
    carries (gmeow:acceptanceStatus) under a named acceptability semantics
    (gmeow:underSemantics -> logic:ArgumentationSemantics), together with the extension it
    belongs to (gmeow:extensionMember). The argument-graph evaluation is COMPUTED, never
    asserted (Principle 12): there is no accepted / isBest bit, only the engine-written
    status; under a credulous (preferred) semantics several evaluations of one argument
    coexist, one per extension (Principle 9). Reifies the assessment so its semantics,
    status, and extension travel as one auditable bundle.

    Usage:
        >>> from gmeow_models.inference import ArgumentEvaluation
        >>> ArgumentEvaluation.model_config["json_schema_extra"]["curie"]
        'gmeow:ArgumentEvaluation'

    IRI:    https://blackcatinformatics.ca/gmeow/ArgumentEvaluation
    CURIE:  gmeow:ArgumentEvaluation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ArgumentEvaluation", "curie": "gmeow:ArgumentEvaluation", "definitionDigest": "blake3:87ce285711d3519e438bc6444e0cbd3fdbf212ed4d2346ef882d42e4f1f92876", "iri": "https://blackcatinformatics.ca/gmeow/ArgumentEvaluation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    acceptanceStatus: list[AcceptanceStatusEnum] | None = Field(default=None, description="The Dung labelling status a gmeow:ArgumentEvaluation assigns its argument — gmeow:acceptanceIn / gmeow:acceptanceOut / gmeow:acceptanceUndecided. Functional: one evaluation, one status. A solver-set value (Principle 12): the engine writes it, and there is no asserted accepted / isBest bit.", alias="gmeow:acceptanceStatus")
    evaluatesArgument: list[Argument] | None = Field(default=None, description="Relates a gmeow:ArgumentEvaluation to the gmeow:Argument it assesses. Functional: one evaluation assesses one argument under one semantics; a credulous semantics yields several evaluations of the same argument, one per extension.", alias="gmeow:evaluatesArgument")
    underSemantics: Any = Field(description="The named acceptability semantics under which a gmeow:ArgumentEvaluation is computed — logic:GroundedArgumentation, logic:PreferredArgumentation, logic:NoArgumentation, or logic:PolicySpecificArgumentation. Functional: one evaluation, one semantics. Coupling to logic: is by reference (no asserted triples into logic:).", alias="gmeow:underSemantics")


class Attack(ConfiguredBaseModel):
    """Attack.

    A reified, TYPED attack between arguments — a structural conflict that names WHAT it
    attacks and HOW. It relates an attacking structure (gmeow:attackSource) to a target
    (gmeow:attackTarget, a gmeow:AttackTarget) under a kind (gmeow:attackKind, a
    gmeow:AttackKind): gmeow:attackUndermine attacks a gmeow:PremiseUse,
    gmeow:attackUndercut attacks a gmeow:InferenceApplication (the warrant),
    gmeow:attackRebut attacks the conclusion gmeow:StandpointClaim. The attack is the
    structural relation, never the adjudicated outcome — which argument survives is the
    solver-layer gmeow:ArgumentEvaluation. Self-attack is forbidden by SHACL, not OWL (the
    gmeow:competesWith discipline — doctrine in SHACL, keeping the DL profile clean).

    Usage:
        >>> from gmeow_models.inference import Attack
        >>> Attack.model_config["json_schema_extra"]["curie"]
        'gmeow:Attack'

    IRI:    https://blackcatinformatics.ca/gmeow/Attack
    CURIE:  gmeow:Attack

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Attack", "curie": "gmeow:Attack", "definitionDigest": "blake3:50d81701c0c95687fd1b3e55e34b4a5ff63ed2d40e9fe41b993e8faba0f40a38", "iri": "https://blackcatinformatics.ca/gmeow/Attack"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    attackKind: list[AttackKindEnum] | None = Field(default=None, description="The kind of a typed gmeow:Attack — gmeow:attackUndermine, gmeow:attackUndercut, or gmeow:attackRebut. Functional: an attack has exactly one kind (SHACL enforces it). The kind and the gmeow:attackTarget agree: undermine targets a gmeow:PremiseUse, undercut a gmeow:InferenceApplication, rebut a gmeow:StandpointClaim.", alias="gmeow:attackKind")
    attackSource: list[Argument] | None = Field(default=None, description="The attacking structure of a typed gmeow:Attack — the gmeow:Argument (or other claim structure) mounting the attack. NOT functional.", alias="gmeow:attackSource")
    attackTarget: Any = Field(description="What a typed gmeow:Attack targets — a gmeow:PremiseUse (undermine), a gmeow:InferenceApplication (undercut), or a conclusion gmeow:StandpointClaim (rebut). Functional: an attack names exactly one target (SHACL enforces it), agreeing with its gmeow:attackKind.", alias="gmeow:attackTarget")


class Correspondence(ConfiguredBaseModel):
    """Correspondence.

    A single mapped element-pair within an gmeow:Analogy — the reified relation that one
    source element (gmeow:correspondingSource) plays the same structural role as one target
    element (gmeow:correspondingTarget). Reached from its analogy via
    gmeow:hasCorrespondence. The atom of structure-mapping: an analogy's systematicity is
    the structural consistency of its set of Correspondences.

    Usage:
        >>> from gmeow_models.inference import Correspondence
        >>> Correspondence.model_config["json_schema_extra"]["curie"]
        'gmeow:Correspondence'

    IRI:    https://blackcatinformatics.ca/gmeow/Correspondence
    CURIE:  gmeow:Correspondence
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/correspondence-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Correspondence", "curie": "gmeow:Correspondence", "definitionDigest": "blake3:02a346c6c68559e8c0d03c2432e0000071b854fd27ea2c4e20dd598ba720ee98", "iri": "https://blackcatinformatics.ca/gmeow/Correspondence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    correspondingSource: Any = Field(description="The source-domain element of a gmeow:Correspondence — one half of a mapped pair. Functional: a correspondence maps exactly one source element to one target element.", alias="gmeow:correspondingSource")
    correspondingTarget: Any = Field(description="The target-domain element of a gmeow:Correspondence — the other half of a mapped pair. Functional: exactly one target element per correspondence.", alias="gmeow:correspondingTarget")


class InferenceApplication(ConfiguredBaseModel):
    """Inference Application.

    A reified application of an inference rule within a gmeow:Argument — the rule that
    licenses the step (gmeow:appliedRule, by reference at a logic: rule / proof-trace)
    together with the substitution that instantiates it (gmeow:appliedSubstitution). The
    target of an undercutting gmeow:Attack: granting the premises, an undercut denies that
    THIS rule-application supports the conclusion. Rule-applicability stays defeasible even
    where the deductive step is contract-local indefeasible (design/LOGIC-FOUNDATION.md).

    Usage:
        >>> from gmeow_models.inference import InferenceApplication
        >>> InferenceApplication.model_config["json_schema_extra"]["curie"]
        'gmeow:InferenceApplication'

    IRI:    https://blackcatinformatics.ca/gmeow/InferenceApplication
    CURIE:  gmeow:InferenceApplication

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InferenceApplication", "curie": "gmeow:InferenceApplication", "definitionDigest": "blake3:c834687f185e7baf8a3f70afffdfe4a9337aaced67807d5f925d5c5e5ebdfdec", "iri": "https://blackcatinformatics.ca/gmeow/InferenceApplication"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    appliedRule: Any = Field(description="The inference rule a gmeow:InferenceApplication applies — the licence for the step. Functional: one application applies one rule.", alias="gmeow:appliedRule")
    appliedSubstitution: list[str] | None = Field(default=None, description="The substitution that instantiates a gmeow:InferenceApplication's rule — the variable bindings under which the rule fires, serialised as text. The logical form stays in logic: (by reference); this records, at the epistemic layer, which binding the agent used. NOT functional.", alias="gmeow:appliedSubstitution")


class InferenceCommitment(ConfiguredBaseModel):
    """Inference Commitment.

    The endurant, structured argument relation an inference instantiates — the reified
    Toulmin/Peircean commitment mediating its premises (gmeow:premise), its single
    conclusion (gmeow:conclusion, a gmeow:StandpointClaim), its warrant (gmeow:warrant), and
    its mode (gmeow:inferenceModeOf). The argumentation layer wraps it in a gmeow:Argument
    (gmeow:argumentInferenceStep) to bear the typed gmeow:Attack and accruing gmeow:Support
    relations. A relator, not a process (the gmeow:Commitment idiom): the occurrent
    reasoning episode that bears it is the gmeow:InferenceProcess reached by
    gmeow:hasInferenceCommitment. Reifying is the on-demand tier; the flat 80% case hangs
    gmeow:inferredFrom + gmeow:inferenceMode directly on the conclusion-claim. Retained as
    audit when its conclusion is suppressed (belief revision), never erased (Principle 10).

    Usage:
        >>> from gmeow_models.inference import InferenceCommitment
        >>> InferenceCommitment.model_config["json_schema_extra"]["curie"]
        'gmeow:InferenceCommitment'

    IRI:    https://blackcatinformatics.ca/gmeow/InferenceCommitment
    CURIE:  gmeow:InferenceCommitment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InferenceCommitment", "curie": "gmeow:InferenceCommitment", "definitionDigest": "blake3:0971ecf5c88bd160511f4ff75cfbf51ecba92a3ef898a898ed2caa561ee8d94e", "iri": "https://blackcatinformatics.ca/gmeow/InferenceCommitment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conclusion: list[StandpointClaim] | None = Field(default=None, description="The single conclusion of a reified gmeow:InferenceCommitment — the gmeow:StandpointClaim the argument establishes (vantage = the reasoner, observedFeature = the concluded proposition). Functional: one commitment, one conclusion; an argument with two conclusions is two commitments (SHACL enforces exactly one).", alias="gmeow:conclusion")
    inferenceModeOf: Any | None = Field(default=None, description="The Peircean mode of a reified gmeow:InferenceCommitment — a gmeow:InferenceMode value. Functional: a commitment has exactly one mode (a step that is both inductive and abductive is two commitments). The reified counterpart of the flat gmeow:inferenceMode; the value scopes the abduction / analogy SHACL shapes.", alias="gmeow:inferenceModeOf")
    premise: list[Any] = Field(min_length=1, description="A premise of a reified gmeow:InferenceCommitment — an input from which its conclusion is drawn. NOT functional: an argument has one or more premises (SHACL requires at least one, and that no premise equals the conclusion).", alias="gmeow:premise")


class PremiseUse(ConfiguredBaseModel):
    """Premise Use.

    A reified USE of a premise within a gmeow:Argument — the basing fact that this argument
    actually rests on this premise (gmeow:premiseUsed), distinct from the merely AVAILABLE
    premises of the underlying gmeow:InferenceCommitment (available != used). The target of
    an undermining gmeow:Attack, which denies the used premise. Reached from its argument
    via gmeow:hasPremiseUse.

    Usage:
        >>> from gmeow_models.inference import PremiseUse
        >>> PremiseUse.model_config["json_schema_extra"]["curie"]
        'gmeow:PremiseUse'

    IRI:    https://blackcatinformatics.ca/gmeow/PremiseUse
    CURIE:  gmeow:PremiseUse

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/PremiseUse", "curie": "gmeow:PremiseUse", "definitionDigest": "blake3:b2f9098e62255fccd3fe7cf3413f587e7b45fa76b69ceb759cc043e74ecc2cce", "iri": "https://blackcatinformatics.ca/gmeow/PremiseUse"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    premiseUsed: Any = Field(description="The premise a gmeow:PremiseUse bases on — the single premise this use actually rests on (the basing fact). Functional: one premise-use names one used premise.", alias="gmeow:premiseUsed")


class Support(ConfiguredBaseModel):
    """Support.

    A reified, ACCRUING support relation in bipolar argumentation — an instance relating a
    supporting structure (gmeow:supportSource) to the gmeow:Argument it supports
    (gmeow:supportTarget). Each independent support is its own Support instance, so support
    ACCRUES: several independent supports for one conclusion combine rather than counting
    once (Principle 9; design/LOGIC-FOUNDATION.md). The support pole of bipolar
    argumentation; the attack pole is gmeow:Attack.

    Usage:
        >>> from gmeow_models.inference import Support
        >>> Support.model_config["json_schema_extra"]["curie"]
        'gmeow:Support'

    IRI:    https://blackcatinformatics.ca/gmeow/Support
    CURIE:  gmeow:Support

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Support", "curie": "gmeow:Support", "definitionDigest": "blake3:18f47df8498bd9fab016d2c4b39e49153fd05c91cd41476cdea9a214c9673377", "iri": "https://blackcatinformatics.ca/gmeow/Support"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    supportSource: Any = Field(description="The supporting structure of a gmeow:Support — a gmeow:SupportSource (another gmeow:Argument or a gmeow:StandpointClaim that lends support). NOT functional: support accrues from several sources.", alias="gmeow:supportSource")
    supportTarget: list[Argument] | None = Field(default=None, description="The gmeow:Argument a gmeow:Support supports. Functional: one Support instance supports one argument; independent supports for the same argument are separate Support instances, so support accrues (Principle 9).", alias="gmeow:supportTarget")

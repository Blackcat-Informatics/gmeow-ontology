"""GMEOW models — slice GMEOW Inquiry Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/inquiry
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class QuestionTypeEnum(StrEnum):
    typeAlternative = "gmeow:typeAlternative"
    typeHow = "gmeow:typeHow"
    typePolar = "gmeow:typePolar"
    typeWh = "gmeow:typeWh"
    typeWhy = "gmeow:typeWhy"


class InquiryTenure(ConfiguredBaseModel):
    """Inquiry Tenure.

    The reified, time-scoped fact that an agent held an open question over an interval — an
    inquiry opened in January and closed by an accepted answer in June is an
    opened-then-closed tenure, never a deletion (Principle 10). The StandpointTenure idiom
    applied to inquiry: use only when the opening / closing of an inquiry is itself worth
    modelling; the common case is a bare gmeow:asks with validFrom / validUntil on the
    statement. Carries its interval via gmeow:duringInterval (temporal module); a closed
    tenure is kept with gmeow:displayable false.

    When to use:
        - Use as the reified half of the gmeow:asks ↔ gmeow:InquiryTenure pair: when the opening,
          closing, or reopening of an agent's open question over an interval is itself the fact of
          interest, so the inquiry can be opened and closed rather than mutated.

    When to avoid:
        - Avoid for the 80% case where an agent simply holds a question open (use the flat
          gmeow:asks with validFrom / validUntil) and never delete a closed inquiry — close it and
          set gmeow:displayable false, reopening by suppressing the prior accepted answer
          (Principle 10).

    How to use:
        - Mint one InquiryTenure per (agent, question) holding, bind gmeow:tenureInquirer and
          gmeow:tenureQuestion, scope it with gmeow:duringInterval (temporal module); record
          closure by an accepted gmeow:answers-claim and suppression with gmeow:displayable false
          rather than removing the triple.

    Examples:
        - ex:keySearch a gmeow:InquiryTenure ; gmeow:tenureInquirer ex:lillith ;
          gmeow:tenureQuestion ex:whereIsTheKey ; gmeow:duringInterval ex:firstHalfYear .

    Usage:
        >>> from gmeow_models.inquiry import InquiryTenure
        >>> InquiryTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:InquiryTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/InquiryTenure
    CURIE:  gmeow:InquiryTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InquiryTenure", "curie": "gmeow:InquiryTenure", "definitionDigest": "blake3:b3c08c4105adc83b3861a8da615a8f59ad4b211970d21bf7d377db6e601327d2", "iri": "https://blackcatinformatics.ca/gmeow/InquiryTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    tenureQuestion: list[str] | None = Field(default=None, description="Within gmeow:InquiryTenure, values are node references constrained to gmeow:Question. The question the tenure records the agent as holding open over its interval. Functional: one tenure, one question; an agent holding five questions open bears five tenures. Sharing is at the Question: two agents inquiring into the same question hold distinct tenures pointing at one Question individual.", alias="gmeow:tenureQuestion")

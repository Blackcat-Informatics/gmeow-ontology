"""GMEOW models — slice GMEOW Spatial Aggregation Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/aggregation
Tier:       extension
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class AggregationFunctionEnum(StrEnum):
    aggAverage = "gmeow:aggAverage"
    aggCentroid = "gmeow:aggCentroid"
    aggCount = "gmeow:aggCount"
    aggDensity = "gmeow:aggDensity"
    aggMaximum = "gmeow:aggMaximum"
    aggMinimum = "gmeow:aggMinimum"
    aggSum = "gmeow:aggSum"


class SpatialAggregation(ConfiguredBaseModel):
    """Spatial Aggregation.

    A measurement that aggregates entities located within a spatial region, yielding a
    scalar result such as a count, density, or average. The aggregation region is the
    observedFeature; the result is a ScalarQuantity. The actual computation is performed by
    the solver layer (Principle 12), not materialised as asserted triples in the OWL core.

    Usage:
        >>> from gmeow_models.aggregation import SpatialAggregation
        >>> SpatialAggregation.model_config["json_schema_extra"]["curie"]
        'gmeow:SpatialAggregation'

    IRI:    https://blackcatinformatics.ca/gmeow/SpatialAggregation
    CURIE:  gmeow:SpatialAggregation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/SpatialAggregation", "curie": "gmeow:SpatialAggregation", "definitionDigest": "blake3:b92974e0df80d55be62761c5cd5301519df58761941cd4ad6cd2c1ae60be52f1", "iri": "https://blackcatinformatics.ca/gmeow/SpatialAggregation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    aggregationFunction: AggregationFunctionEnum = Field(description="The statistical function applied to the entities in the aggregation region — count, sum, average, density, minimum, maximum, or centroid.", alias="gmeow:aggregationFunction")
    minimumPopulation: int | None = Field(default=None, description="The minimum population size (k) required for the aggregation result to be disclosed — the k-anonymity parameter. A result failing this check is suppressed at projection time (coarsen or withhold, Principle 10), never deleted.", alias="gmeow:minimumPopulation")
    observationResult: Any = Field(description="The entity-valued result of an observation — a coordinate set, an instant, a quality value, a quantity, or another entity. For literal scalar readings (e.g. 22.5°C), the observationResult entity carries the literal via a datatype property (e.g. qudt:quantityValue or a module-specific scalar property). Non-functional: a single observation may yield several results in different frames or granularities, and competing result claims coexist rather than collapse.", alias="gmeow:observationResult")
    observedFeature: Place = Field(description="The individual whose property or state is being observed — the feature of interest. The range is intentionally open (owl:Thing) because anything can be observed: a place, a person, an event, a document, a quality value, or a situation. SHACL shapes enforce closed-world constraints per observation kind.", alias="gmeow:observedFeature")
    vantage: Any = Field(description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")

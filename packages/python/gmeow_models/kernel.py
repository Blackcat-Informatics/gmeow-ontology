"""GMEOW models — slice GMEOW Core Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/kernel
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class Entity(ConfiguredBaseModel):
    """Entity.

    Anything in the GMEOW universe of discourse that persists in time and can bear
    properties: an agent, a document, a contact point, and so on.

    When to use:
        - Use as the universal domain when a property, facet, or claim must attach to anything
          that persists and can bear properties — the anchor for the domain-free facets
          (granularity, determinacy, sensitivity, consumer eligibility) that are defined
          Entity-wide.

    When to avoid:
        - Avoid typing an instance as a bare gmeow:Entity when a more specific sortal applies;
          reserve the unqualified type for genuinely category-neutral resources, and never use it
          for occurrents (those are gufo:Event, not endurants).

    How to use:
        - Type each instance with its most specific sortal (Agent, InformationObject,
          PhysicalObject, SocialObject) and let it inherit Entity; attach the universal facets
          here because they range over the whole sortal spine.

    Examples:
        - ex:thing a gmeow:Entity ; gmeow:hasSensitivity gmeow:sensitivityPublic ;
          gmeow:hasGranularity ex:cityLevel .

    Usage:
        >>> from gmeow_models.kernel import Entity
        >>> Entity.model_config["json_schema_extra"]["curie"]
        'gmeow:Entity'

    IRI:    https://blackcatinformatics.ca/gmeow/Entity
    CURIE:  gmeow:Entity

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Entity", "curie": "gmeow:Entity", "definitionDigest": "blake3:1b82a3478fbd36a974c2abc9cc5d07ca44fb892e482a3a0732a3ba07169ff5f4", "iri": "https://blackcatinformatics.ca/gmeow/Entity"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class PhysicalObject(Entity):
    """Physical Object.

    An entity that is a material thing occupying space and time — a device, instrument,
    vehicle, building, or natural body. Distinct from Agent (which acts and bears
    responsibility) and InformationObject (which carries information content). The sortal
    for pose-bearing, sensor-hosting, and spatially-located things that are not agents.

    When to use:
        - Use for a material thing that occupies space and time and is not itself an agent — a
          device, instrument, vehicle, building, sensor host, or natural body that bears a pose or
          a spatial location.

    When to avoid:
        - Avoid for acting participants (Agent), for content carriers whose materiality is
          incidental (InformationObject), and for convention-sustained things (SocialObject); the
          three are disjoint with PhysicalObject by design.

    How to use:
        - Type the artifact as PhysicalObject and hang pose, sensor, and spatial-location
          relations on it; keep agency and information roles on separate co-located entities
          rather than overloading the physical sortal.

    Examples:
        - ex:rover a gmeow:PhysicalObject ; gmeow:hasPose ex:roverPose .

    Usage:
        >>> from gmeow_models.kernel import PhysicalObject
        >>> PhysicalObject.model_config["json_schema_extra"]["curie"]
        'gmeow:PhysicalObject'

    IRI:    https://blackcatinformatics.ca/gmeow/PhysicalObject
    CURIE:  gmeow:PhysicalObject

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/PhysicalObject", "curie": "gmeow:PhysicalObject", "definitionDigest": "blake3:fa0309bd8510d9c019ebe14e6514da5b507c14725cfd7fbaa50d2c198b93087e", "iri": "https://blackcatinformatics.ca/gmeow/PhysicalObject"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

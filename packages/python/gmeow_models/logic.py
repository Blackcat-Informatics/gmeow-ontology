"""GMEOW models — slice GMEOW Logic.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/logic
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from datetime import datetime

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class Logic_ActionSchema(ConfiguredBaseModel):
    """Action Schema.

    A named precondition / effect / invariant template that a transaction program may
    invoke: the reusable unit of state change. Distinct from the concrete transaction
    program that instantiates it, so a reusable schema is never conflated with a single
    state-changing run. Punned (OWL 2 DL metamodelling): it is simultaneously the IR node
    kind AND the class whose instances are specific reusable action schemas — an action
    occurrence is a gmeow:Event that instantiates one such schema through
    logic:instantiatesSchema. The structure means–end reasoning reads is declared on a
    schema by the eight action-theory facets: logic:precondition, logic:effect,
    logic:invariant, logic:capability, logic:nondeterministicOutcome, logic:observation,
    logic:compensation, and logic:actionResource.

    Usage:
        >>> from gmeow_models.logic import Logic_ActionSchema
        >>> Logic_ActionSchema.model_config["json_schema_extra"]["curie"]
        'logic:ActionSchema'

    IRI:    https://blackcatinformatics.ca/logic/ActionSchema
    CURIE:  logic:ActionSchema

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/ActionSchema", "curie": "logic:ActionSchema", "definitionDigest": "blake3:6b4ffaac50897b65c48330d0f995a4100900e313430c22708d73403f02c58b2e", "iri": "https://blackcatinformatics.ca/logic/ActionSchema"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    capability: list[str] = Field(min_length=1, description="Relates a logic:ActionSchema to what an agent must be able to do to execute it — the executional precondition on the actor rather than on the state. Domain logic:ActionSchema (stated in prose). One of the eight action-theory facets, distinct from logic:precondition (a precondition on the situation, not the agent).", alias="logic:capability")
    precondition: list[str] = Field(min_length=1, description="Relates a logic:ActionSchema to the situation that must hold for the action to apply — the gate on the elementary state transition the schema grounds. Domain logic:ActionSchema (stated in prose, matching the module's domain/range convention). One of the eight action-theory facets means–end reasoning reads.", alias="logic:precondition")


class Logic_ChaseAcceptanceWitness(ConfiguredBaseModel):
    """Chase Acceptance Witness.

    The acceptance record that an existential-rule chase materialization terminated under a
    weak-acyclicity certificate, executed natively by
    gmeow_logic::materialize::materialize_existential_rules, which surfaces the
    ChaseAdmission termination certificate as a first-class gmeow:Finding. It carries,
    through logic:carriesTerminationCertificate, the certificate evidence. The flagship
    producer runs the native chase and pins that the surfaced Finding is non-empty and that
    the derivation carries an explain trace passing gmeow_logic::explain::assert_faithful;
    this witness is the ontological twin the thin logic:ChaseAcceptanceWitnessShape gates.

    Usage:
        >>> from gmeow_models.logic import Logic_ChaseAcceptanceWitness
        >>> Logic_ChaseAcceptanceWitness.model_config["json_schema_extra"]["curie"]
        'logic:ChaseAcceptanceWitness'

    IRI:    https://blackcatinformatics.ca/logic/ChaseAcceptanceWitness
    CURIE:  logic:ChaseAcceptanceWitness

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/ChaseAcceptanceWitness", "curie": "logic:ChaseAcceptanceWitness", "definitionDigest": "blake3:2b2128fa1233cd3c71ad905857e897902dfe608b0625958298d73557b71f7fcf", "iri": "https://blackcatinformatics.ca/logic/ChaseAcceptanceWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    carriesTerminationCertificate: list[str] = Field(min_length=1, description="Relates a logic:ChaseAcceptanceWitness to the weak-acyclicity termination-certificate evidence (xsd:string, the ChaseAdmission::WeaklyAcyclic evidence summary) that admits its chase. Domain logic:ChaseAcceptanceWitness (stated in prose). A chase acceptance record with no certificate reports a closure that may never terminate.", alias="logic:carriesTerminationCertificate")


class Logic_ClosureAcceptanceWitness(ConfiguredBaseModel):
    """Closure Acceptance Witness.

    The acceptance record that an EL→RL→DL reasoning closure
    (gmeow_logic::reason::reason_all) genuinely entailed new atoms: it names, through
    logic:closureEntailedAtom, at least one atom the closure derived beyond its asserted
    EDB. The flagship producer runs the native closure and pins that a designated entailed
    atom is present and absent from the EDB; this witness is the ontological twin the thin
    logic:ClosureAcceptanceWitnessShape gates.

    Usage:
        >>> from gmeow_models.logic import Logic_ClosureAcceptanceWitness
        >>> Logic_ClosureAcceptanceWitness.model_config["json_schema_extra"]["curie"]
        'logic:ClosureAcceptanceWitness'

    IRI:    https://blackcatinformatics.ca/logic/ClosureAcceptanceWitness
    CURIE:  logic:ClosureAcceptanceWitness

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/ClosureAcceptanceWitness", "curie": "logic:ClosureAcceptanceWitness", "definitionDigest": "blake3:78ed1233f30b73ba2f31c59a4911e80043b01c5412adf6a447d5b40e3e1326a9", "iri": "https://blackcatinformatics.ca/logic/ClosureAcceptanceWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    closureEntailedAtom: list[str] = Field(min_length=1, description="Relates a logic:ClosureAcceptanceWitness to a serialized atom (xsd:string, e.g. `A subClassOf C`) that the EL→RL→DL closure entails but the asserted EDB does not contain. Domain logic:ClosureAcceptanceWitness (stated in prose). At least one is required: a closure witness that entails nothing beyond its input demonstrates no reasoning.", alias="logic:closureEntailedAtom")


class Logic_Collection(ConfiguredBaseModel):
    """Collection.

    A plurality individuated by its members, where the members are themselves the relevant
    units of the whole — a deck of cards, a committee, a playlist. The counterpart of
    gufo:Collection; members relate to it via logic:memberOf rather than logic:properPartOf.
    Contrast logic:Quantity, which is individuated by constitution rather than by member
    count.

    Usage:
        >>> from gmeow_models.logic import Logic_Collection
        >>> Logic_Collection.model_config["json_schema_extra"]["curie"]
        'logic:Collection'

    IRI:    https://blackcatinformatics.ca/logic/Collection
    CURIE:  logic:Collection
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/collection-class-c3c093723155

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Collection", "curie": "logic:Collection", "definitionDigest": "blake3:7960e4b9e0ca375ef884521bf89bcbb6b49e79b409e10081168742b06b93ca63", "iri": "https://blackcatinformatics.ca/logic/Collection"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_Conjecture(ConfiguredBaseModel):
    """Conjecture.

    A named, standpoint-scoped candidate formula under active epistemic test: the dynamic,
    testable specialization of the logic:FormalizationCandidate spine (rdfs:subClassOf
    logic:FormalizationCandidate). Because it subclasses the candidate, a conjecture carries
    all eight universal candidate carriers (logic:candidateSourceHash,
    logic:candidateExtractionProvenance, logic:candidateScope, logic:candidateContract,
    logic:candidateCategory, logic:candidateLifecycle, logic:candidateProjectionBehavior,
    logic:candidateSemanticRisk); what specializes it is that its positive/negative cases
    are ENGINE-PRODUCED (via a scoped reasoning run recorded through
    logic:verdictProvenance) rather than reviewer-asserted. Its verdict is always scoped to
    a single reified standpoint (logic:conjectureStandpoint, REQUIRED — Principle 9 refuses
    a global-false verdict), and it carries an EPISTEMIC lifecycle
    (logic:conjectureLifecycleState: open / corroborated / refuted-in-standpoint /
    withdrawn) ORTHOGONAL to the GOVERNANCE lifecycle it inherits from the candidate
    (logic:candidateLifecycle). Conclusiveness reuses the existing logic:DischargeVerdict
    value class (logic:conjectureDischargeVerdict), never a freshly minted notion: a
    conclusive independence carries logic:ObligationDischarged, a budget-exhausted or
    fragment-gap run carries logic:ObligationUnknown. The conjectured formula is tested
    symmetrically against its constructed strong negation as two INDEPENDENT legs — support
    for the formula (the standpoint's KB entails it) and support for its negation (the KB
    refutes it, decided by the inconsistency of asserting the formula) — so a KB that
    entails BOTH the formula AND its negation from one standpoint yields the Belnap glut
    logic:InfoBoth, a within-standpoint contradiction LOCALIZED to the candidate proposition
    (a genuine refutation still carrying a concrete logic:ContradictionWitness), whereas a
    KB contradictory for reasons UNRELATED to the candidate cannot host a meaningful test at
    all. A refutation names a concrete logic:ContradictionWitness, and the two symmetric
    promotion legs feed a corroborated formula back as a canonical-axiom candidate
    (logic:conjecturePromotionCandidate) and a robustly-refuted formula forward as a
    logic:NonEntailmentObligation candidate (logic:antiConjectureObligationCandidate)
    (design/LOGIC-FOUNDATION.md, the conjecture-and-refutation section).

    Usage:
        >>> from gmeow_models.logic import Logic_Conjecture
        >>> Logic_Conjecture.model_config["json_schema_extra"]["curie"]
        'logic:Conjecture'

    IRI:    https://blackcatinformatics.ca/logic/Conjecture
    CURIE:  logic:Conjecture
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/conjecture-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Conjecture", "curie": "logic:Conjecture", "definitionDigest": "blake3:027b4255be74b3d8d984c3aaf2f89749cff75c51db9af44d28a3debf90fb0364", "iri": "https://blackcatinformatics.ca/logic/Conjecture"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conjectureFormula: list[str] = Field(min_length=1, description="The canonical, alpha-normalized identity of the conjectured formula: the content_key string of the full first-order AST (the alpha-normalized content identity from the full-FOL formula AST), so two conjectures that name the same formula up to bound-variable renaming share one key and cannot be double-counted. Single-valued, xsd:string. Domain logic:Conjecture.", alias="logic:conjectureFormula")
    conjectureLifecycleState: list[str] = Field(min_length=1, description="Relates a logic:Conjecture to its current EPISTEMIC logic:ConjectureLifecycleState (open / corroborated / refuted-in-standpoint / withdrawn). Single-valued and REQUIRED. Orthogonal to logic:candidateLifecycle (the GOVERNANCE state inherited from the candidate spine): a conjecture may be governance-accepted yet epistemically open, so the two are never collapsed (Principle 9). Domain logic:Conjecture, range logic:ConjectureLifecycleState.", alias="logic:conjectureLifecycleState")
    conjectureStandpoint: list[str] = Field(min_length=1, description="Relates a logic:Conjecture to the reified standpoint its verdict is scoped to — a REQUIRED, single-valued IRI (a gmeow:Standpoint, stated in prose). Standpoint scoping here is REIFICATION, never a named graph: the verdict is 'the formula is refuted FROM THIS standpoint', never a global-false claim (Principle 9). A conjecture with no standpoint has an ill-scoped verdict and is a hard governance error (verify.conjecture-standpoint-scoped). Single-valued. Domain logic:Conjecture.", alias="logic:conjectureStandpoint")
    verdictProvenance: list[str] = Field(min_length=1, description="Relates a logic:Conjecture to the logic:VerdictProvenanceKind recording whether its cases were engine-produced or reviewer-asserted — the one genuinely-new axis the conjecture library adds over the candidate spine. Single-valued and REQUIRED. Domain logic:Conjecture, range logic:VerdictProvenanceKind.", alias="logic:verdictProvenance")


class Logic_ContradictionWitness(ConfiguredBaseModel):
    """Contradiction Witness.

    The RDF shape of the runtime refutation witness a conjecture test emits when it refutes
    the formula: the concrete counterexample that forced an individual to owl:Nothing. It
    names the offending individual (logic:witnessIndividual), the world IRI the
    contradiction was found in (logic:witnessWorld), and the set of premises that jointly
    entailed the contradiction (logic:witnessPremise, one serialized triple each,
    multi-valued). It is an ABox artifact — a produced fact about a specific reasoning run,
    not a constraint — and is present exactly when its conjecture is
    logic:ConjectureRefutedInStandpoint (design/LOGIC-FOUNDATION.md, the
    conjecture-and-refutation section).

    Usage:
        >>> from gmeow_models.logic import Logic_ContradictionWitness
        >>> Logic_ContradictionWitness.model_config["json_schema_extra"]["curie"]
        'logic:ContradictionWitness'

    IRI:    https://blackcatinformatics.ca/logic/ContradictionWitness
    CURIE:  logic:ContradictionWitness
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/contradictionwitness-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/ContradictionWitness", "curie": "logic:ContradictionWitness", "definitionDigest": "blake3:2eaf7879e1ec03695f1690c3ecf12f97538e01cf5f1d3caa09e18f86a4098253", "iri": "https://blackcatinformatics.ca/logic/ContradictionWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    witnessIndividual: list[str] = Field(min_length=1, description="The individual a logic:ContradictionWitness records as forced to owl:Nothing — the entity whose type became unsatisfiable under the conjectured formula, identifying exactly where the contradiction bit. Single-valued, xsd:string (the individual's IRI as a string). Domain logic:ContradictionWitness.", alias="logic:witnessIndividual")
    witnessPremise: list[str] = Field(min_length=1, description="A premise that participated in the contradiction a logic:ContradictionWitness records — one serialized triple (subject predicate object) per value, MULTI-valued because a refutation typically rests on several jointly-inconsistent facts. Together the premises are the minimal derivation the reader can replay to see why the formula is refuted from the standpoint. Multi-valued, xsd:string. Domain logic:ContradictionWitness.", alias="logic:witnessPremise")
    witnessWorld: list[str] = Field(min_length=1, description="The world IRI the logic:ContradictionWitness was found in — the modal/context world within the conjecture's standpoint where the contradiction was realized, so the refutation stays world-scoped rather than asserted across all worlds. Single-valued, xsd:string (the world IRI as a string). Domain logic:ContradictionWitness.", alias="logic:witnessWorld")


class Logic_CounterfactualAcceptanceWitness(ConfiguredBaseModel):
    """Counterfactual Acceptance Witness.

    The acceptance record that a Stratum-C counterfactual world was constructed and resolved
    to a deterministic outcome, executed natively by
    gmeow_logic::counterfactual::construct_and_resolve over the entrenchment ordering. It
    names, through logic:selectedByEntrenchment, the outcome the entrenchment ranking
    selected. The flagship producer runs the native resolution and pins the deterministic
    selected binding; this witness is the ontological twin the thin
    logic:CounterfactualAcceptanceWitnessShape gates.

    Usage:
        >>> from gmeow_models.logic import Logic_CounterfactualAcceptanceWitness
        >>> Logic_CounterfactualAcceptanceWitness.model_config["json_schema_extra"]["curie"]
        'logic:CounterfactualAcceptanceWitness'

    IRI:    https://blackcatinformatics.ca/logic/CounterfactualAcceptanceWitness
    CURIE:  logic:CounterfactualAcceptanceWitness

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/CounterfactualAcceptanceWitness", "curie": "logic:CounterfactualAcceptanceWitness", "definitionDigest": "blake3:e69cfaa5a7accf98604b03a0964ebcd41e4e4e51644b633d9ccbb3305d53e537", "iri": "https://blackcatinformatics.ca/logic/CounterfactualAcceptanceWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    selectedByEntrenchment: list[str] = Field(min_length=1, description="Relates a logic:CounterfactualAcceptanceWitness to the outcome (an IRI) the entrenchment ranking deterministically selected when resolving the counterfactual antecedent. Domain logic:CounterfactualAcceptanceWitness (stated in prose). Its absence marks an unentrenched, arbitrary counterfactual: an outcome chosen without a recorded ordering is a silent guess.", alias="logic:selectedByEntrenchment")


class Logic_Endurant(ConfiguredBaseModel):
    """Endurant.

    A continuant: an individual that persists through time and is wholly present at each
    moment of its existence, enduring through change rather than unfolding through it. The
    counterpart of gufo:Endurant and the bearer side of the endurant/perdurant divide;
    contrast logic:Perdurant, which has temporal parts. Substantials and aspects are both
    endurants.

    Usage:
        >>> from gmeow_models.logic import Logic_Endurant
        >>> Logic_Endurant.model_config["json_schema_extra"]["curie"]
        'logic:Endurant'

    IRI:    https://blackcatinformatics.ca/logic/Endurant
    CURIE:  logic:Endurant

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Endurant", "curie": "logic:Endurant", "definitionDigest": "blake3:b6332962441fd08cf7bbc5b13ee65e65d1b749331b3cc2293f2e11a66f7db314", "iri": "https://blackcatinformatics.ca/logic/Endurant"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_Event(ConfiguredBaseModel):
    """Event.

    A perdurant occurrence that happens in time and is composed of temporal parts: an
    action, a process, a happening that unfolds and is complete once it has occurred. Tokens
    it as an occurrence rather than an endurant; in the type-level world strata (risk,
    teleology, norms) the no-occurrence gate keeps reasoning over Event *types* without
    entailing any Event instance. Unitary: a proper temporal part of an event is not itself
    that event, and a completed (logic:Closed) event cannot vary — it bears no time-varying
    logic:Fluent (the change-asymmetry, logic:OccurrentChangeAsymmetry). An event is
    constituted by the process it is made of (logic:occurrentConstituent).

    Usage:
        >>> from gmeow_models.logic import Logic_Event
        >>> Logic_Event.model_config["json_schema_extra"]["curie"]
        'logic:Event'

    IRI:    https://blackcatinformatics.ca/logic/Event
    CURIE:  logic:Event
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/event-class-042aff927d4d

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Event", "curie": "logic:Event", "definitionDigest": "blake3:2f073cd0f7506eff335f4be38b8985e7838aa2e4baa82564388721c30025fdf8", "iri": "https://blackcatinformatics.ca/logic/Event"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_ExistentialChaseDemonstrand(ConfiguredBaseModel):
    """Existential Chase Demonstrand.

    A narrow, self-describing existential obligation: a class each of whose instances
    carries the C ⊑ ∃p.D obligation `ExistentialChaseDemonstrand ⊑
    ∃demonstratesChaseWitness.ChaseWitnessLeaf`, which the native existential chase MUST
    discharge by inventing a fresh Skolem witness (a gmeow:InventedWitness) for every
    instance that asserts no logic:demonstratesChaseWitness edge. It is deliberately narrow
    — ONLY the two seeded demonstrand individuals inhabit it — so the demonstration dogfoods
    the chase without cascading global inference over a widely-instantiated class. This is
    the native chase's own defining law made concrete and on-topic: an existential
    obligation entails a Skolem witness, and running the chase over this class is the
    minimal proof that GMEOW's own existential materializer actually fires.

    When to use:
        - Use as the minimal, self-describing demonstrator of the native existential chase — the
          on-topic C ⊑ ∃p.D obligation whose discharge mints the Skolem witnesses that dogfood
          GMEOW's own existential reasoning.

    When to avoid:
        - Avoid widening it (its whole point is that ONLY the two seeded demonstrand individuals
          inhabit it, so the invented nulls drive no cascading inference) and avoid adding an
          owl:disjointWith or a functional/cardinality axiom to it or its logic:ChaseWitnessLeaf —
          a demonstrand whose invented null is forced into a contradiction stops demonstrating the
          chase and starts breaking it.

    How to use:
        - Seed at least two named individuals of this class with NO asserted
          logic:demonstratesChaseWitness edge and run the native existential chase; observe that
          it invents one distinct gmeow:InventedWitness (Skolem null) per instance to satisfy the
          ∃demonstratesChaseWitness.ChaseWitnessLeaf obligation, each recorded in the reasoned
          graph and its diagnostics projection.

    Examples:
        - logic:existentialChaseDemonstrandAlpha a logic:ExistentialChaseDemonstrand .

    Usage:
        >>> from gmeow_models.logic import Logic_ExistentialChaseDemonstrand
        >>> Logic_ExistentialChaseDemonstrand.model_config["json_schema_extra"]["curie"]
        'logic:ExistentialChaseDemonstrand'

    IRI:    https://blackcatinformatics.ca/logic/ExistentialChaseDemonstrand
    CURIE:  logic:ExistentialChaseDemonstrand

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/ExistentialChaseDemonstrand", "curie": "logic:ExistentialChaseDemonstrand", "definitionDigest": "blake3:8df1e3a8408b2fa84b62012df64498e26ef07fdcfb3c7f402205dfd59c2b47cf", "iri": "https://blackcatinformatics.ca/logic/ExistentialChaseDemonstrand"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    demonstratesChaseWitness: list[str] | None = Field(default=None, description="Relates a logic:ExistentialChaseDemonstrand to the logic:ChaseWitnessLeaf witness that discharges its existential obligation — the `p` in the demonstrator's C ⊑ ∃p.D law. Domain logic:ExistentialChaseDemonstrand, range logic:ChaseWitnessLeaf (stated in prose). When a demonstrand instance asserts no such edge, the native existential chase invents a fresh Skolem witness (a gmeow:InventedWitness) and materializes the demonstratesChaseWitness edge to it; the property is the object-property head `p` whose someValuesFrom obligation makes the chase fire — an object property, never a datatype property, so its value is an invented individual, not a literal.", alias="logic:demonstratesChaseWitness")


class Logic_FormalizationCandidate(ConfiguredBaseModel):
    """Formalization Candidate.

    A typed, reviewable proposal to turn a prose constraint into a formal axiom: the
    governed unit of the prose-to-axiom transition. It carries its source-text hash
    (logic:candidateSourceHash), extraction provenance
    (logic:candidateExtractionProvenance), intended scope (logic:candidateScope) and
    reasoning contract (logic:candidateContract, ME1), its logic:FormalizationCategory, its
    logic:CandidateLifecycleState and reviewer decision (logic:reviewedBy), a positive
    competency case and a negative countermodel (logic:candidatePositiveCase /
    logic:candidateNegativeCase), any required logic:NonEntailmentObligation
    (logic:candidateNonEntailment), its projection behaviour
    (logic:candidateProjectionBehavior) and its logic:SemanticRiskClass. An LLM extraction
    always enters as a candidate, never directly as an accepted axiom; only a candidate
    promoted through review becomes canonical (design/LOGIC-FOUNDATION.md, §Typed
    formalization governance).

    Usage:
        >>> from gmeow_models.logic import Logic_FormalizationCandidate
        >>> Logic_FormalizationCandidate.model_config["json_schema_extra"]["curie"]
        'logic:FormalizationCandidate'

    IRI:    https://blackcatinformatics.ca/logic/FormalizationCandidate
    CURIE:  logic:FormalizationCandidate

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/FormalizationCandidate", "curie": "logic:FormalizationCandidate", "definitionDigest": "blake3:afae6da04872a8e42c38b5676813933a4948c731c92ebc0ec756624dea94e7a1", "iri": "https://blackcatinformatics.ca/logic/FormalizationCandidate"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    candidateCategory: list[str] = Field(min_length=1, description="Relates a logic:FormalizationCandidate to the logic:FormalizationCategory recording what kind of force its prose carried. Single-valued and REQUIRED: a candidate with no category resolving to the closed set is a hard governance error, never silently bucketed. Domain logic:FormalizationCandidate, range logic:FormalizationCategory.", alias="logic:candidateCategory")
    candidateContract: list[str] = Field(min_length=1, description="Relates a logic:FormalizationCandidate to the logic:ReasoningContract (ME1) its proposed axiom would be interpreted under — the semantics that make 'sound' meaningful for it. Single-valued. Domain logic:FormalizationCandidate, range logic:ReasoningContract.", alias="logic:candidateContract")
    candidateExtractionProvenance: list[str] = Field(min_length=1, description="The provenance of a candidate's extraction — what produced it (an extractor identifier, run, or model invocation reference). Records that an automated extraction is exactly that, so it enters at logic:CandidateProposed and earns no canonical force without review. Single-valued, xsd:string. Domain logic:FormalizationCandidate.", alias="logic:candidateExtractionProvenance")
    candidateLifecycle: list[str] = Field(min_length=1, description="Relates a logic:FormalizationCandidate to its current logic:CandidateLifecycleState. Single-valued. logic:CandidateAccepted is the canonical state and is legitimate only with a recorded logic:reviewedBy decision. Domain logic:FormalizationCandidate, range logic:CandidateLifecycleState.", alias="logic:candidateLifecycle")
    candidateProjectionBehavior: list[str] = Field(min_length=1, description="Relates a logic:FormalizationCandidate to the logic:PreservationKind its axiom would carry under projection — how faithfully it survives down-projection to OWL/Datalog/SHACL. Reuses the loss-ledger preservation polarity rather than minting a parallel note. Single-valued. Domain logic:FormalizationCandidate, range logic:PreservationKind.", alias="logic:candidateProjectionBehavior")
    candidateScope: list[str] = Field(min_length=1, description="The intended scope of a candidate's axiom — the terms, slice, or context it is meant to constrain. Read together with logic:candidateContract (the reasoning semantics it is interpreted under). Single-valued, xsd:string. Domain logic:FormalizationCandidate.", alias="logic:candidateScope")
    candidateSemanticRisk: list[str] = Field(min_length=1, description="Relates a logic:FormalizationCandidate to its logic:SemanticRiskClass — how far a wrong formalization would propagate, which sets how strict the reviewer gate must be. Single-valued. Domain logic:FormalizationCandidate, range logic:SemanticRiskClass.", alias="logic:candidateSemanticRisk")
    candidateSourceField: list[str] | None = Field(default=None, description="Names which annotation field of the logic:candidateFormalizes term a candidate harvested — a logic:ProseField from the closed set (logic:ProseFieldDefinition, logic:ProseFieldUseWhen, logic:ProseFieldAvoidWhen). Together with logic:candidateFormalizes it pins the harvested axiom to the exact source triple. Co-present with logic:candidateFormalizes: a candidate carrying one of the pair without the other is a hard governance error (verify.harvest-link-paired), never a half-link. Single-valued. Domain logic:FormalizationCandidate, range logic:ProseField.", alias="logic:candidateSourceField")
    candidateSourceHash: list[str] = Field(min_length=1, description="The content hash of the source text a logic:FormalizationCandidate was extracted from: the anchor that ties the candidate back to the exact prose it formalizes and detects drift if that prose changes. Single-valued, xsd:string. Domain logic:FormalizationCandidate.", alias="logic:candidateSourceHash")


class Logic_Formula(ConfiguredBaseModel):
    """Formula.

    A first-order formula node: the reified AST node a non-Horn logic: statement compiles
    into. A logic:Formula is a quantifier (carrying logic:forall or logic:exists over a body
    and one or more logic:quantifiedVariable bindings), a connective (logic:and, logic:or,
    logic:not, logic:antecedent with logic:consequent, or logic:iff over sub-formulae), or
    an atomic predication (logic:relation over ordered logic:argument terms). It is the
    unified full-FOL core of which Horn+NAF derivation rules are a recognized sub-fragment;
    the canonical RDF 1.2 serialization round-trips it losslessly, while weaker projections
    carry any formula they cannot express as flagged unsupported residue.

    When to use:
        - Use for first-order quantifier or connective structure that cannot live uniquely as a
          Horn fact or rule.

    When to avoid:
        - Do not encode a trivially Horn binary fact as a top-level formula; place it in the axiom
          or rule collection.

    How to use:
        - Choose one constructor family, link its sub-formulae, and place every argument or
          binding in an indexed logic:TermCarrier.

    Examples:
        - The tree for every person knows someone is a logic:forall node whose body is a
          logic:exists node containing a logic:relation atom.

    Usage:
        >>> from gmeow_models.logic import Logic_Formula
        >>> Logic_Formula.model_config["json_schema_extra"]["curie"]
        'logic:Formula'

    IRI:    https://blackcatinformatics.ca/logic/Formula
    CURIE:  logic:Formula

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Formula", "curie": "logic:Formula", "definitionDigest": "blake3:c848f62ee31ae4ec362bde22cc72732a617ee7216b88bf3a858d67b9b4c14a3d", "iri": "https://blackcatinformatics.ca/logic/Formula"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_FreshnessGuard(ConfiguredBaseModel):
    """Freshness Guard.

    A valid-time currency guard on an action-schema precondition. It declares that the datum
    supporting a logic:precondition (via logic:guardsPrecondition) is relied upon only when
    it passes TWO INDEPENDENT valid-time axes. (1) AGE HORIZON: the datum's age — the span
    from its logic:datumRecordedAt to the probe's logic:decisionTime — is within a declared
    logic:freshnessHorizon (an xsd:duration ceiling on how OLD the value may be). (2)
    EXPLICIT VALID-TIME WINDOW: when the guard declares a logic:freshnessWindow, the probe's
    logic:decisionTime must fall inside the closed absolute interval
    [logic:freshnessWindowStart, logic:freshnessWindowEnd] — the datum is authoritative only
    WITHIN that named bounding interval (e.g. 'the current episode'), regardless of how
    recently it was recorded. The two axes are ORTHOGONAL and neither implies the other: a
    datum can pass the age horizon yet fall outside the window, or vice versa. Each axis is
    OPTIONAL and imposes no constraint when absent (a guard may carry horizon only, window
    only, both, or neither). Outside either bound the datum is undetermined, not
    stale-but-used: the action gate yields logic:GateUndetermined rather than admitting on
    an out-of-window / out-of-date value or denying on a value that merely aged or is
    off-episode. This is GMEOW's native, canonical generalization of a decision-rule
    'currency' annotation (max age → horizon) AND its 'time_window' annotation (an explicit
    named bounding interval → window) — the valid-time freshness discipline the typed
    context algebra applies, executed at the action gate.

    Usage:
        >>> from gmeow_models.logic import Logic_FreshnessGuard
        >>> Logic_FreshnessGuard.model_config["json_schema_extra"]["curie"]
        'logic:FreshnessGuard'

    IRI:    https://blackcatinformatics.ca/logic/FreshnessGuard
    CURIE:  logic:FreshnessGuard
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/freshnessguard-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/FreshnessGuard", "curie": "logic:FreshnessGuard", "definitionDigest": "blake3:1794bf0f6d4c2f5468efd8409dca5374ef2c6d9d516530ac372ddbcf8cf74536", "iri": "https://blackcatinformatics.ca/logic/FreshnessGuard"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    freshnessHorizon: list[str] | None = Field(default=None, description="The maximum admissible age of a guarded precondition datum, as an xsd:duration measured from the datum's logic:datumRecordedAt to the probe's logic:decisionTime. Domain logic:FreshnessGuard, range xsd:duration (stated in prose). Only fixed-length designators are admissible (weeks / days / hours / minutes / seconds); a nominal-length designator (years / months, which are not fixed spans) is a hard error, because the age comparison would be ill-defined.", alias="logic:freshnessHorizon")
    freshnessWindowEnd: list[datetime] | None = Field(default=None, description="The inclusive upper bound of a logic:FreshnessGuard's explicit valid-time logic:freshnessWindow, as an xsd:dateTime. Domain logic:FreshnessGuard, range xsd:dateTime (stated in prose). Together with logic:freshnessWindowStart it defines the closed absolute interval [start, end] within which the guarded datum is authoritative; a probe's logic:decisionTime after this end falls outside the window and yields logic:GateUndetermined. This axis is DISTINCT from the age ceiling logic:freshnessHorizon — it bounds WHEN the decision may be taken, not how OLD the datum may be. Required exactly once when a window is declared (paired with logic:freshnessWindowStart); a declared window with a missing bound, a malformed xsd:dateTime, or end before start is a hard error. When no window is declared this bound is absent and the window axis imposes no constraint.", alias="logic:freshnessWindowEnd")
    freshnessWindowStart: list[datetime] | None = Field(default=None, description="The inclusive lower bound of a logic:FreshnessGuard's explicit valid-time logic:freshnessWindow, as an xsd:dateTime. Domain logic:FreshnessGuard, range xsd:dateTime (stated in prose). Together with logic:freshnessWindowEnd it defines the closed absolute interval [start, end] within which the guarded datum is authoritative; a probe's logic:decisionTime before this start falls outside the window and yields logic:GateUndetermined. This axis is DISTINCT from the age ceiling logic:freshnessHorizon — it bounds WHEN the decision may be taken, not how OLD the datum may be. Required exactly once when a window is declared (paired with logic:freshnessWindowEnd); a declared window with a missing bound, a malformed xsd:dateTime, or end before start is a hard error. When no window is declared this bound is absent and the window axis imposes no constraint.", alias="logic:freshnessWindowStart")
    guardsPrecondition: list[str] = Field(min_length=1, description="Relates a logic:FreshnessGuard to the logic:precondition situation it guards — the situation whose supporting datum must be current for the gate to admit. Domain logic:FreshnessGuard, range the guarded precondition situation (stated in prose).", alias="logic:guardsPrecondition")


class Logic_FunctionTerm(ConfiguredBaseModel):
    """Function Term.

    A compound function-term application node: a single reified function symbol
    (logic:functionSymbol, a logic:Type individual) applied to a non-empty, ordered list of
    logic:argument term-carriers, denoting a first-order term such as cons(H, T) or s(X). It
    mirrors the atomic-predication pattern (logic:relation over ordered logic:argument), but
    predicates a FUNCTION symbol producing a term rather than a relation producing a truth
    value; reifying the symbol as a logic:Type individual keeps the object level
    first-order, so there is no function-variable. An argument carrier may itself carry
    logic:termApplication, so a nested term such as cons(H, cons(1, nil)) round-trips. It is
    not itself a logic:Formula: it is a term, referenced from a carrier's
    logic:termApplication, never asserted as a sentence.

    When to use:
        - Use when an argument is itself a compound term built by applying a function symbol to
          sub-terms, rather than an IRI, variable, literal, or sequence marker.

    When to avoid:
        - Do not use a function term for a nullary constant; a 0-ary function symbol is an
          individual and is carried by logic:termIri, so an empty-argument function term would
          give one constant two spellings.

    How to use:
        - Reify the function symbol as a logic:Type individual on logic:functionSymbol, place each
          operand in a zero-based logic:argument term-carrier, and reference the whole node from
          an argument carrier's logic:termApplication.

    Examples:
        - cons(H, cons(1, nil)) is a logic:FunctionTerm whose logic:functionSymbol is ex:cons and
          whose second logic:argument carries logic:termApplication onto the inner cons(1, nil)
          function term.

    Usage:
        >>> from gmeow_models.logic import Logic_FunctionTerm
        >>> Logic_FunctionTerm.model_config["json_schema_extra"]["curie"]
        'logic:FunctionTerm'

    IRI:    https://blackcatinformatics.ca/logic/FunctionTerm
    CURIE:  logic:FunctionTerm

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/FunctionTerm", "curie": "logic:FunctionTerm", "definitionDigest": "blake3:45ced004e2e444c5551f2b15ac292eaf46f7467696da780b037a6faf295ea950", "iri": "https://blackcatinformatics.ca/logic/FunctionTerm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_FunctionalComplex(ConfiguredBaseModel):
    """Functional Complex.

    A whole whose parts play distinct functional roles — a car (engine, wheels, chassis), an
    organism (organs), an organization (departments): a complex object, not a mere
    collection of like members. The counterpart of gufo:FunctionalComplex; its parts relate
    to it via logic:properPartOf (mereological parthood) rather than logic:memberOf, and
    each part contributes a different function. Stated in prose, not as a subclass edge.
    Contrast logic:Collection (uniform members) and logic:Quantity (homogeneous stuff).

    Usage:
        >>> from gmeow_models.logic import Logic_FunctionalComplex
        >>> Logic_FunctionalComplex.model_config["json_schema_extra"]["curie"]
        'logic:FunctionalComplex'

    IRI:    https://blackcatinformatics.ca/logic/FunctionalComplex
    CURIE:  logic:FunctionalComplex

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/FunctionalComplex", "curie": "logic:FunctionalComplex", "definitionDigest": "blake3:d99808cc719243faa2e626cfc98d65cac46d4b57841ee74530b372d60e201918", "iri": "https://blackcatinformatics.ca/logic/FunctionalComplex"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Logic_GoalEvaluation(ConfiguredBaseModel):
    """Goal Evaluation.

    The reified, vantage-indexed record of whether a goal is met: one logic:GoalEvaluation
    per (goal, situation or world it is judged against, evaluator holding the judgment, time
    of judgment, criterion applied), identified by that tuple of subjects and attributed
    through gmeow:accordingTo. Its outcome is factored into independent axes rather than one
    collapsed label — logic:satisfactionStatus, logic:feasibilityStatus,
    logic:lifecycleStatus, logic:goalEvaluationStatus, and the logic:satisfactionDegree
    quantity — each varying independently. The binary gmeow:satisfiedBy edge is the
    conclusive projection of this record: an edge is generated from each evaluation whose
    satisfaction status is logic:Satisfied and whose goal-evaluation status is
    logic:GoalEvaluationCompleted, and a directly authored gmeow:satisfiedBy edge
    deterministically expands into a default such evaluation under the asserting vantage, so
    the flat and reified records never disagree. Two evaluators who disagree are two
    coexisting evaluations, each retained rather than reconciled (Principle 9).

    Usage:
        >>> from gmeow_models.logic import Logic_GoalEvaluation
        >>> Logic_GoalEvaluation.model_config["json_schema_extra"]["curie"]
        'logic:GoalEvaluation'

    IRI:    https://blackcatinformatics.ca/logic/GoalEvaluation
    CURIE:  logic:GoalEvaluation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/GoalEvaluation", "curie": "logic:GoalEvaluation", "definitionDigest": "blake3:0bfcec2fd13001cb425249def14701be6b76809dd9b15fcf843ef1482bdacd63", "iri": "https://blackcatinformatics.ca/logic/GoalEvaluation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    evaluatesGoal: list[Goal] = Field(min_length=1, description="Relates a logic:GoalEvaluation to the gmeow:Goal it judges. Domain logic:GoalEvaluation, range gmeow:Goal (stated in prose). One of the subjects in the identifying tuple of an evaluation (goal, situation/world, evaluator, time, criterion).", alias="logic:evaluatesGoal")
    feasibilityStatus: list[str] = Field(min_length=1, description="Relates a logic:GoalEvaluation to the logic:FeasibilityStatus it carries — the feasibility axis of the factored outcome. Domain logic:GoalEvaluation, range logic:FeasibilityStatus (stated in prose). Varies independently of satisfaction, lifecycle, and goal-evaluation status.", alias="logic:feasibilityStatus")
    goalEvaluationStatus: list[str] = Field(min_length=1, description="Relates a logic:GoalEvaluation to the logic:GoalEvaluationStatus it carries — whether the evaluation is conclusive. Domain logic:GoalEvaluation, range logic:GoalEvaluationStatus (stated in prose). Together with satisfaction status logic:Satisfied, the value logic:GoalEvaluationCompleted is the condition under which a gmeow:satisfiedBy edge is generated. DISTINCT from logic:resultEvaluation, which carries the engine-computation logic:EvaluationStatus of a logic:ReasoningResult.", alias="logic:goalEvaluationStatus")
    lifecycleStatus: list[str] = Field(min_length=1, description="Relates a logic:GoalEvaluation to the logic:LifecycleStatus it carries — the lifecycle axis of the factored outcome. Domain logic:GoalEvaluation, range logic:LifecycleStatus (stated in prose). Varies independently of satisfaction, feasibility, and goal-evaluation status.", alias="logic:lifecycleStatus")
    satisfactionStatus: list[str] = Field(min_length=1, description="Relates a logic:GoalEvaluation to the logic:SatisfactionStatus it carries — the satisfaction axis of the factored outcome. Domain logic:GoalEvaluation, range logic:SatisfactionStatus (stated in prose). Varies independently of feasibility, lifecycle, and goal-evaluation status.", alias="logic:satisfactionStatus")


class Logic_McpActionSchema(ConfiguredBaseModel):
    """MCP Action Schema.

    A logic:ActionSchema that grounds an MCP (Model Context Protocol) tool action — the
    opt-in marker subtype that brings an action schema under the stricter MCP-action
    governance profile (logic:McpActionPolicyShape). Beyond the facets every action schema
    carries it must always declare a logic:compensation (rollback) because an MCP tool that
    mutates external state must be recoverable: the dogfood case where the canonical process
    model governs the agent's own tool calls. A plain logic:ActionSchema is NOT required to
    carry compensation; typing it logic:McpActionSchema is the explicit declaration that the
    governance profile applies.

    Usage:
        >>> from gmeow_models.logic import Logic_McpActionSchema
        >>> Logic_McpActionSchema.model_config["json_schema_extra"]["curie"]
        'logic:McpActionSchema'

    IRI:    https://blackcatinformatics.ca/logic/McpActionSchema
    CURIE:  logic:McpActionSchema

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/McpActionSchema", "curie": "logic:McpActionSchema", "definitionDigest": "blake3:1a0b870e35d207d4861c19820fc3597752eeb13c8680c1c2de707a72191c38ec", "iri": "https://blackcatinformatics.ca/logic/McpActionSchema"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    capability: list[str] = Field(min_length=1, description="Relates a logic:ActionSchema to what an agent must be able to do to execute it — the executional precondition on the actor rather than on the state. Domain logic:ActionSchema (stated in prose). One of the eight action-theory facets, distinct from logic:precondition (a precondition on the situation, not the agent).", alias="logic:capability")
    compensation: list[str] = Field(min_length=1, description="Relates a logic:ActionSchema to the action that undoes this one — the basis for recoverable transactions. Tied to the realized outcome, not the schema as a whole: when the outcome set is nondeterministic each branch names the compensation that recovers from IT, so recovery matches what actually happened rather than applying one generic undo across every branch. Domain logic:ActionSchema (stated in prose). One of the eight action-theory facets.", alias="logic:compensation")
    precondition: list[str] = Field(min_length=1, description="Relates a logic:ActionSchema to the situation that must hold for the action to apply — the gate on the elementary state transition the schema grounds. Domain logic:ActionSchema (stated in prose, matching the module's domain/range convention). One of the eight action-theory facets means–end reasoning reads.", alias="logic:precondition")


class Logic_NonEntailmentObligation(ConfiguredBaseModel):
    """Non-Entailment Obligation.

    A first-class, executable record of a conclusion the foundation must NEVER draw: a
    deliberate non-assertion the engine is held to, not a comment the axioms happen to
    respect. It names the forbidden predicate (logic:obligationForbiddenPredicate), the
    logic:DischargeCondition under which absence-of-proof is conclusively checkable, and the
    logic:DischargeVerdict the check returns. Because the canonical logic is semi-decidable,
    an obligation is conclusively discharged only within a stated fragment or bound; outside
    it the verdict is logic:ObligationUnknown, carried forward, never 'proved absent'. The
    two standing obligations are 'intent must not be derived from structural deception' and
    'counterpart must not become transitive' (design/LOGIC-FOUNDATION.md, §Typed
    formalization governance).

    Usage:
        >>> from gmeow_models.logic import Logic_NonEntailmentObligation
        >>> Logic_NonEntailmentObligation.model_config["json_schema_extra"]["curie"]
        'logic:NonEntailmentObligation'

    IRI:    https://blackcatinformatics.ca/logic/NonEntailmentObligation
    CURIE:  logic:NonEntailmentObligation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/NonEntailmentObligation", "curie": "logic:NonEntailmentObligation", "definitionDigest": "blake3:a4cd210539d18ced802c879a123eae5abb63e1f769df4464564009f96dacdf5b", "iri": "https://blackcatinformatics.ca/logic/NonEntailmentObligation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    obligationDischargeCondition: list[str] = Field(min_length=1, description="Relates a logic:NonEntailmentObligation to the logic:DischargeCondition under which its absence-of-proof is conclusively checkable. An obligation declaring a condition the engine does not wire (anything other than logic:DischargeSyntacticReachability or logic:DischargeFiniteClosure) is a hard error, never a silent pass. Domain logic:NonEntailmentObligation, range logic:DischargeCondition.", alias="logic:obligationDischargeCondition")
    obligationForbiddenPredicate: list[str] = Field(min_length=1, description="The IRI of the predicate a logic:NonEntailmentObligation forbids the closure from ever containing (and forbids any rule head from producing). The check reads this IRI, then either proves it unreachable over the rule strata (logic:DischargeSyntacticReachability) or absent from the materialized closure (logic:DischargeFiniteClosure). Single-valued, xsd:anyURI. Domain logic:NonEntailmentObligation.", alias="logic:obligationForbiddenPredicate")


class Logic_NotificationWaitSchema(ConfiguredBaseModel):
    """Notification Wait Schema.

    A logic:ActionSchema whose completion the engine learns of ONLY by being externally
    told: it does not fire until a logic:ExternalSignal it logic:awaitsSignal obtains in the
    state — a manual-notification or callback-driven completion, the
    prescriptive-descriptive epistemic boundary where what the engine knows of the real
    world is limited to what it is informed of. The opt-in marker subtype that brings an
    action schema under the wait-well-formedness profile (logic:NotificationWaitShape, which
    requires a logic:awaitsSignal). At the action gate a wait whose signal has not obtained
    is neither admitted nor denied but logic:GateUndetermined, carrying a
    logic:awaitingSignal witness — the wait is pending (the engine has not been told), a
    judgment withheld rather than a failure; in a plan a wait step blocks the path until the
    signal is received.

    Usage:
        >>> from gmeow_models.logic import Logic_NotificationWaitSchema
        >>> Logic_NotificationWaitSchema.model_config["json_schema_extra"]["curie"]
        'logic:NotificationWaitSchema'

    IRI:    https://blackcatinformatics.ca/logic/NotificationWaitSchema
    CURIE:  logic:NotificationWaitSchema

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/NotificationWaitSchema", "curie": "logic:NotificationWaitSchema", "definitionDigest": "blake3:76b3cdb20ccaf020fb443ae3c8c6899989015d29c24ef609ae97facb4e09a62a", "iri": "https://blackcatinformatics.ca/logic/NotificationWaitSchema"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    awaitsSignal: list[str] = Field(min_length=1, description="Relates a logic:NotificationWaitSchema to the logic:ExternalSignal situation whose arrival its completion waits on. Domain logic:NotificationWaitSchema, range a logic:ExternalSignal situation (stated in prose). The wait fires only once this signal obtains in the state; until then it is pending (logic:awaitingSignal), never silently completed.", alias="logic:awaitsSignal")


class Logic_Plan(ConfiguredBaseModel):
    """Plan.

    A transaction program (a logic:TransactionProgram-kind node) whose primitive operations
    invoke action schemas and whose success criterion is a goal expression. As a program it
    carries the structure a decomposed goal alone cannot — ordering and serial composition,
    guards, branching, concurrency, loops, and fallback — all in the operators of the
    transaction layer. A POLICY is a plan whose branches are conditioned on what an earlier
    action's observation reveals rather than fixed in advance; this is a reading of a plan,
    NOT a separate class. Plan adoption and revision are recorded as an intention is: a plan
    is held over an interval and revised by suppression, never mutated. Because an action
    schema may carry a nondeterministic outcome set, plan success is quantified over those
    outcomes through logic:planSuccessMode rather than left to the bare executional-success
    reading. On the foundational spine a logic:Plan is a gmeow:SocialObject (a description,
    not a physical thing), grounded in prose per the logic-core convention rather than by a
    subclass edge; a process surface that specializes logic:Plan therefore also asserts its
    own gmeow:SocialObject or gmeow:InformationObject sort directly, since the prose
    grounding licenses no DL inference of it.

    Usage:
        >>> from gmeow_models.logic import Logic_Plan
        >>> Logic_Plan.model_config["json_schema_extra"]["curie"]
        'logic:Plan'

    IRI:    https://blackcatinformatics.ca/logic/Plan
    CURIE:  logic:Plan

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/Plan", "curie": "logic:Plan", "definitionDigest": "blake3:98f6eb7fdf9ec82c89c39d0b1ef08e2c566182922eecc0b5a8eca312f9b22d46", "iri": "https://blackcatinformatics.ca/logic/Plan"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    planGoal: list[Goal] = Field(min_length=1, description="Relates a logic:Plan to the gmeow:Goal that is its success criterion — the goal the plan's steps, invoking action schemas, are arranged to reach. Domain logic:Plan, range gmeow:Goal (stated in prose). The plan's success against this goal is quantified over nondeterministic outcomes through logic:planSuccessMode.", alias="logic:planGoal")
    planSuccessMode: list[str] = Field(min_length=1, description="Relates a logic:Plan to the logic:PlanSuccessMode quantifying its success over the nondeterministic outcomes of its action schemas. Domain logic:Plan, range logic:PlanSuccessMode (stated in prose). The quantification exists because the bare executional-success reading of the transaction layer would count one favourable path as success; this declares whether some, every, or every fair execution must reach the goal.", alias="logic:planSuccessMode")


class Logic_RecoveryCase(ConfiguredBaseModel):
    """Recovery Case.

    A first-class executable query-class case that decides whether a logic:Correspondence
    genuinely recovers its source. Its logic:recoveryTransform is an ordered universally
    quantified source-to-view logic:Formula. The native correspondence executor instantiates
    the complete source pattern, computes the declared view, derives the candidate inverse,
    and compares the recovered source graph with the original. A recovery case is neutral
    evidence rather than a positive assertion: it may discharge a genuine section/retraction
    or deliberately refute a lossy mapping by retaining a source distinction absent from the
    view. Every logic:mnemomorphic true correspondence that claims logic:SectionLaw requires
    executable complete recovery evidence: an atomic one-triple leg may synthesize its
    complete case, while a composite leg requires an authored logic:RecoveryCase. A
    structural inverse or a declared boolean alone is never proof
    (design/LOGIC-CORRESPONDENCE.md, genuine recovery cases).

    Usage:
        >>> from gmeow_models.logic import Logic_RecoveryCase
        >>> Logic_RecoveryCase.model_config["json_schema_extra"]["curie"]
        'logic:RecoveryCase'

    IRI:    https://blackcatinformatics.ca/logic/RecoveryCase
    CURIE:  logic:RecoveryCase
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/recoverycase-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/RecoveryCase", "curie": "logic:RecoveryCase", "definitionDigest": "blake3:a470144826e84170305c997ae6018bc98b8ecdcf685837d15fd17b3bea2a0abd", "iri": "https://blackcatinformatics.ca/logic/RecoveryCase"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    recoveryTransform: list[Logic_Formula] = Field(min_length=1, description="The exactly-one ordered source-to-view logic:Formula executed for a logic:RecoveryCase. The supported discharge fragment is a universally quantified implication whose antecedent and consequent are positive conjunctions of binary RDF atoms: the antecedent is the complete source graph pattern and the consequent is the projected view pattern. The order is semantic — reversing the implication changes which information must be recovered — while conjunction order remains immaterial under the formula's canonical identity. Domain logic:RecoveryCase, range logic:Formula.", alias="logic:recoveryTransform")


class Logic_RefutationAcceptanceWitness(ConfiguredBaseModel):
    """Refutation Acceptance Witness.

    The acceptance record that a symmetric φ/¬φ conjecture test resolved to a refutation
    carrying a concrete contradiction witness, executed natively by
    gmeow_logic::conjecture::conjecture_test. It names, through
    logic:namesContradictionWitness, the logic:ContradictionWitness the refutation rests on.
    The flagship producer runs the native test and pins that a refutation case yields
    Some(witness) while a corroboration case yields None; this witness is the ontological
    twin the thin logic:RefutationAcceptanceWitnessShape gates.

    Usage:
        >>> from gmeow_models.logic import Logic_RefutationAcceptanceWitness
        >>> Logic_RefutationAcceptanceWitness.model_config["json_schema_extra"]["curie"]
        'logic:RefutationAcceptanceWitness'

    IRI:    https://blackcatinformatics.ca/logic/RefutationAcceptanceWitness
    CURIE:  logic:RefutationAcceptanceWitness

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/RefutationAcceptanceWitness", "curie": "logic:RefutationAcceptanceWitness", "definitionDigest": "blake3:83f0f4ba96e7f5258d30212a85fc6d5573a90e14c63f77c0bcb595706d514777", "iri": "https://blackcatinformatics.ca/logic/RefutationAcceptanceWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    namesContradictionWitness: list[Logic_ContradictionWitness] = Field(min_length=1, description="Relates a logic:RefutationAcceptanceWitness to the concrete logic:ContradictionWitness (an IRI) its refutation rests on — the individual forced to owl:Nothing and the jointly-inconsistent premises. Domain logic:RefutationAcceptanceWitness (stated in prose). A refutation naming no witness is an unreplayable assertion, not a proof.", alias="logic:namesContradictionWitness")


class Logic_SectionAcceptanceWitness(ConfiguredBaseModel):
    """Section Acceptance Witness.

    The acceptance record that a correspondence get/put leg pair discharges its section law
    (put ∘ get = identity), executed natively by
    gmeow_logic::correspondence_exec::leg_pair_verdict. It names, through
    logic:dischargesSectionObligation, the section obligation the executed round-trip
    discharges. The flagship producer runs the native discharge and pins that a genuine
    inverse yields DischargeVerdict::ObligationDischarged while a broken section does not;
    this witness is the ontological twin the thin logic:SectionAcceptanceWitnessShape gates.

    Usage:
        >>> from gmeow_models.logic import Logic_SectionAcceptanceWitness
        >>> Logic_SectionAcceptanceWitness.model_config["json_schema_extra"]["curie"]
        'logic:SectionAcceptanceWitness'

    IRI:    https://blackcatinformatics.ca/logic/SectionAcceptanceWitness
    CURIE:  logic:SectionAcceptanceWitness

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/SectionAcceptanceWitness", "curie": "logic:SectionAcceptanceWitness", "definitionDigest": "blake3:46dc7e2e882ff951d7bb7ef0ec55c3228e99ea8189fd2612a3059f017e1a16ce", "iri": "https://blackcatinformatics.ca/logic/SectionAcceptanceWitness"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    dischargesSectionObligation: list[str] = Field(min_length=1, description="Relates a logic:SectionAcceptanceWitness to the section obligation (an IRI) its executed get/put round-trip discharges. Domain logic:SectionAcceptanceWitness (stated in prose). Exactly the edge whose absence makes the discharge unfalsifiable: a section witness that names no obligation asserts a round-trip without saying which law it satisfies.", alias="logic:dischargesSectionObligation")


class Logic_TermCarrier(ConfiguredBaseModel):
    """Term Carrier.

    An ordered slot in an atomic formula's logic:argument list, a function term's
    logic:argument list, or a quantifier's logic:quantifiedVariable block. It carries
    exactly one zero-based logic:termIndex and exactly one value kind: logic:termIri,
    logic:termVariable, logic:termLiteral, logic:termSequenceMarker, or
    logic:termApplication (a nested compound function term). It is not the term value
    itself; it is the RDF node that preserves the value's kind and position independently of
    triple order.

    When to use:
        - Use for each ordered atom argument and each ordered quantifier binding.

    When to avoid:
        - Do not place two value kinds on one carrier or infer its position from Turtle
          serialization order.

    How to use:
        - Assign one contiguous zero-based index and one of the four exclusive term-value
          predicates.

    Examples:
        - An atom's second argument can be represented by a carrier with logic:termIndex 1 and
          logic:termVariable 'object'.

    Usage:
        >>> from gmeow_models.logic import Logic_TermCarrier
        >>> Logic_TermCarrier.model_config["json_schema_extra"]["curie"]
        'logic:TermCarrier'

    IRI:    https://blackcatinformatics.ca/logic/TermCarrier
    CURIE:  logic:TermCarrier

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/logic/TermCarrier", "curie": "logic:TermCarrier", "definitionDigest": "blake3:1d27c546e48579e8e984c6aa84e450469e745f0dac75e557b230f4b99e5c80f9", "iri": "https://blackcatinformatics.ca/logic/TermCarrier"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

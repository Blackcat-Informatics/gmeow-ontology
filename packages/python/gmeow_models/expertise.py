"""GMEOW models — slice GMEOW Expertise Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/expertise
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ProficiencyLevelEnum(StrEnum):
    assessedBeginner = "gmeow:assessedBeginner"
    assessedCompetent = "gmeow:assessedCompetent"
    assessedExpert = "gmeow:assessedExpert"
    cefrA1 = "gmeow:cefrA1"
    cefrA2 = "gmeow:cefrA2"
    cefrB1 = "gmeow:cefrB1"
    cefrB2 = "gmeow:cefrB2"
    cefrC1 = "gmeow:cefrC1"
    cefrC2 = "gmeow:cefrC2"
    dreyfusAdvancedBeginner = "gmeow:dreyfusAdvancedBeginner"
    dreyfusCompetent = "gmeow:dreyfusCompetent"
    dreyfusExpert = "gmeow:dreyfusExpert"
    dreyfusNovice = "gmeow:dreyfusNovice"
    dreyfusProficient = "gmeow:dreyfusProficient"
    levelHeritage = "gmeow:levelHeritage"
    levelNative = "gmeow:levelNative"
    nihAdvanced = "gmeow:nihAdvanced"
    nihBeginner = "gmeow:nihBeginner"
    nihExpert = "gmeow:nihExpert"
    nihIntermediate = "gmeow:nihIntermediate"


class ProficiencyScaleEnum(StrEnum):
    scaleACTFL = "gmeow:scaleACTFL"
    scaleAssessed = "gmeow:scaleAssessed"
    scaleBloomRevised = "gmeow:scaleBloomRevised"
    scaleCEFR = "gmeow:scaleCEFR"
    scaleDreyfus = "gmeow:scaleDreyfus"
    scaleILR = "gmeow:scaleILR"
    scaleKnowledgeDepth = "gmeow:scaleKnowledgeDepth"
    scaleNIH = "gmeow:scaleNIH"
    scaleSOLO = "gmeow:scaleSOLO"
    scaleSelfReported = "gmeow:scaleSelfReported"


class Credential(ConfiguredBaseModel):
    """Credential.

    An educational or occupational credential held by an agent — a degree, certification,
    badge, license, or other attestation of qualification. Its issuer is
    gmeow:credentialIssuer, its subject matter is gmeow:credentialFor, and its verification
    is borne by a gmeow:Attestation (Principle 4).

    When to use:
        - Use for a degree, certification, badge, or license that qualifies an agent in a skill or
          occupation — the credential carries the qualification content, an agent holds it via
          gmeow:holdsCredential.

    When to avoid:
        - Avoid for the signed verification envelope (that is a gmeow:Attestation, attestation.ttl
          — expertise supplies the content, attestation the proof) and for the bare skill or
          occupation a credential certifies (gmeow:Skill, gmeow:Occupation).

    How to use:
        - Mint the credential, name its gmeow:credentialIssuer and gmeow:credentialFor, link the
          holder with gmeow:holdsCredential, and route verification through a gmeow:Attestation
          rather than re-modelling the signature here (Principle 4).

    Examples:
        - ex:rnLicense a gmeow:Credential ; gmeow:credentialIssuer ex:nursingBoard ;
          gmeow:credentialFor ex:nursing .

    Usage:
        >>> from gmeow_models.expertise import Credential
        >>> Credential.model_config["json_schema_extra"]["curie"]
        'gmeow:Credential'

    IRI:    https://blackcatinformatics.ca/gmeow/Credential
    CURIE:  gmeow:Credential

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Credential", "curie": "gmeow:Credential", "definitionDigest": "blake3:72cfa447fe2969d9e00d6a6fa8a1020f2bb66677bda7754ace8f77f7c0c10228", "iri": "https://blackcatinformatics.ca/gmeow/Credential"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    credentialIssuer: list[Organization] | None = Field(default=None, description="The organization that issued a credential. Functional per credential: one issuer is constitutive of the credential's identity (a different issuer is a different credential, even for the same subject matter).", alias="gmeow:credentialIssuer")


class SkillProficiency(ConfiguredBaseModel):
    """Skill Proficiency.

    A reified, leveled proficiency of an agent in a skill — the gufo:Relator binding {agent}
    × {skill} × {level on a scale} × {interval}, mirroring languages'
    gmeow:LanguageProficiency. Mint one per (agent, skill) so per-skill levels coexist; the
    flat gmeow:hasSkill relation states that an agent has a skill, this relator adds the
    level and the scale.

    When to use:
        - Use to promote a flat gmeow:hasSkill to a leveled claim — when the attained level, the
          scale it is rated on, the temporal scope, or a standpoint (who avows or attributes it)
          must become first-class; contested levels then coexist as separate standpoint-indexed
          relators (Principle 9).

    When to avoid:
        - Avoid for the 80% case where merely having the skill is enough (assert flat
          gmeow:hasSkill and reify on demand) and for language ratings specifically (use
          gmeow:LanguageProficiency, which specializes the same machinery).

    How to use:
        - Mint one relator per (agent, skill), bind the functional roles
          gmeow:skillProficiencyAgent / gmeow:skillProficiencyOf / gmeow:skillProficiencyLevel /
          gmeow:skillProficiencyScale and the temporal gmeow:skillProficiencyInterval; carry an
          avowed-versus-attributed distinction through the statement layer's gmeow:accordingTo,
          and route any endorsement through a gmeow:Attestation over the relator.

    Examples:
        - ex:aliceWeldingProf a gmeow:SkillProficiency ; gmeow:skillProficiencyAgent ex:alice ;
          gmeow:skillProficiencyOf ex:welding ; gmeow:skillProficiencyLevel gmeow:dreyfusExpert .

    Usage:
        >>> from gmeow_models.expertise import SkillProficiency
        >>> SkillProficiency.model_config["json_schema_extra"]["curie"]
        'gmeow:SkillProficiency'

    IRI:    https://blackcatinformatics.ca/gmeow/SkillProficiency
    CURIE:  gmeow:SkillProficiency

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/SkillProficiency", "curie": "gmeow:SkillProficiency", "definitionDigest": "blake3:9640ab96c86f64ebe140e9927c878cba45174b7dbfe331de8734589a140637ed", "iri": "https://blackcatinformatics.ca/gmeow/SkillProficiency"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    skillProficiencyAgent: list[Agent] | None = Field(default=None, description="The agent whose proficiency a skill-proficiency expresses. Functional — constitutive of the proficiency.", alias="gmeow:skillProficiencyAgent")
    skillProficiencyLevel: list[ProficiencyLevelEnum] | None = Field(default=None, description="The attained level of a skill-proficiency (a gmeow:ProficiencyLevel value, e.g. dreyfusExpert, assessedCompetent). Functional.", alias="gmeow:skillProficiencyLevel")
    skillProficiencyOf: list[str] | None = Field(default=None, description="Within gmeow:SkillProficiency, values are node references constrained to gmeow:Skill. The skill a skill-proficiency concerns. Functional — constitutive.", alias="gmeow:skillProficiencyOf")
    skillProficiencyScale: list[ProficiencyScaleEnum] | None = Field(default=None, description="The framework/scale a skill-proficiency level is measured on (Dreyfus, NIH, assessed, self-reported) — a gmeow:ProficiencyScale value. Functional.", alias="gmeow:skillProficiencyScale")

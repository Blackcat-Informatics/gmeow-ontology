"""GMEOW models — slice GMEOW Notation and Symbolic Systems Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/notation
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class NotationUsageRoleEnum(StrEnum):
    notationRoleCipher = "gmeow:notationRoleCipher"
    notationRoleCommunication = "gmeow:notationRoleCommunication"
    notationRoleEncoding = "gmeow:notationRoleEncoding"
    notationRoleExpression = "gmeow:notationRoleExpression"
    notationRoleRepresentation = "gmeow:notationRoleRepresentation"
    notationRoleShorthand = "gmeow:notationRoleShorthand"
    notationRoleTranscription = "gmeow:notationRoleTranscription"


class ProjectionLossEnum(StrEnum):
    lossDropsDynamics = "gmeow:lossDropsDynamics"
    lossDropsInstrumentation = "gmeow:lossDropsInstrumentation"
    lossDropsMicrotiming = "gmeow:lossDropsMicrotiming"
    lossDropsPerformerCount = "gmeow:lossDropsPerformerCount"
    lossDropsSpatialSoundContext = "gmeow:lossDropsSpatialSoundContext"
    lossDropsSpectralDerivation = "gmeow:lossDropsSpectralDerivation"
    lossDropsTacet = "gmeow:lossDropsTacet"
    lossDropsTimbre = "gmeow:lossDropsTimbre"
    lossDropsTraversalConstraints = "gmeow:lossDropsTraversalConstraints"
    lossDropsTuningFrame = "gmeow:lossDropsTuningFrame"
    lossQuantizesPitchTo12Edo = "gmeow:lossQuantizesPitchTo12Edo"
    lossQuantizesTimeToRationalGrid = "gmeow:lossQuantizesTimeToRationalGrid"
    lossSymbolizesContinuousTrajectory = "gmeow:lossSymbolizesContinuousTrajectory"


class SymbolicSystemKindEnum(StrEnum):
    symbolicKindCommunicationConvention = "gmeow:symbolicKindCommunicationConvention"
    symbolicKindCryptographic = "gmeow:symbolicKindCryptographic"
    symbolicKindEmoji = "gmeow:symbolicKindEmoji"
    symbolicKindEncoding = "gmeow:symbolicKindEncoding"
    symbolicKindGesture = "gmeow:symbolicKindGesture"
    symbolicKindMathematical = "gmeow:symbolicKindMathematical"
    symbolicKindMusical = "gmeow:symbolicKindMusical"
    symbolicKindPlatformConvention = "gmeow:symbolicKindPlatformConvention"
    symbolicKindStenographic = "gmeow:symbolicKindStenographic"
    symbolicKindTranscription = "gmeow:symbolicKindTranscription"


class NotationProjectionProfile(ConfiguredBaseModel):
    """Notation Projection Profile.

    A closed descriptor schema for the lossy projection from frame-relative content to one
    notation system. Declares representable parameters, incurred losses, and the FnO
    projection function that performs the render (Principles 4, 11, 12). RETAINED under the
    notation graft (Principle 19), not superseded by lang:Rendering: a projection profile IS
    the rendering convention for a notation crossing — the reusable, per-notation-system
    ledger of representable parameters, declared losses, and the FnO render — and
    lang:Rendering holds none of that parameter-level ledger or the executable FnO binding.
    A concrete render of canonical content through this profile is a lang:Rendering whose
    lang:renderingKind is lang:renderingNotation, whose lang:renderingConvention names this
    profile, and whose lang:renderingPreservation carries a logic:PreservationKind judging
    the crossing; the profile's declared-loss ledger is the fine-grained parameter account
    under that coarse preservation kind. No notation-local preservation enum or display-kind
    ladder is minted.

    When to use:
        - Use to declare, per notation system, exactly which canonical parameters its surface form
          can carry and which it drops — the explicit ledger that makes a notation's lossiness
          queryable rather than implicit (every notation is a lossy render, never the canonical
          object).

    When to avoid:
        - Avoid using it as the rendered output itself or as canonical data; the profile DESCRIBES
          the projection (its representable parameters, declared losses, and FnO function), it
          does not hold the projected surface form.

    How to use:
        - Mint one profile per notation system, name the system with gmeow:notationSystemOf,
          enumerate gmeow:representableParameter and gmeow:declaredLoss so the completeness gate
          sees every parameter as represented or accounted-for, and bind the render via
          gmeow:projectionFunction. To record a concrete render, author a lang:Rendering that
          points at this profile through lang:renderingConvention, fixes lang:renderingKind
          lang:renderingNotation, names the produced surface through lang:renderingForm, and
          carries a logic:PreservationKind through lang:renderingPreservation — reusing the shared
          lang: rendering facets rather than a notation-local ladder.

    Examples:
        - ex:staffProfile a gmeow:NotationProjectionProfile ; gmeow:notationSystemOf
          ex:westernStaff ; gmeow:declaredLoss ex:lossMicrotiming .

    Usage:
        >>> from gmeow_models.notation import NotationProjectionProfile
        >>> NotationProjectionProfile.model_config["json_schema_extra"]["curie"]
        'gmeow:NotationProjectionProfile'

    IRI:    https://blackcatinformatics.ca/gmeow/NotationProjectionProfile
    CURIE:  gmeow:NotationProjectionProfile

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/NotationProjectionProfile", "curie": "gmeow:NotationProjectionProfile", "definitionDigest": "blake3:fd7c7879b2a9cbe9b7954dd4fce42f17f7ca61222ee4dba80e531df133ae54b7", "iri": "https://blackcatinformatics.ca/gmeow/NotationProjectionProfile"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    notationSystemOf: list[NotationSystem] | None = Field(default=None, description="The notation system that this projection profile describes. Functional: one notation system per projection profile.", alias="gmeow:notationSystemOf")
    projectionFunction: list[str] = Field(min_length=1, description="An FnO function reference that renders canonical content to this notation system (Principle 12).", alias="gmeow:projectionFunction")


class NotationSystem(ConfiguredBaseModel):
    """Notation System.

    A structured symbolic system with defined rules for representing information in a
    specific domain — mathematical notation, musical notation, stenography, phonetic
    transcription, or cryptographic encoding. A lang:SignSystem of lang:notationalKind (the
    notation graft, Principle 19): distinguished from a language not by a subclass ladder
    but by its lang:signSystemKind, which is lang:notationalKind rather than
    lang:naturalLanguageKind / lang:formalLanguageKind. Lacks independent generative
    syntax/semantics; parasitic on or encoding for an existing language or domain (Principle
    9).

    When to use:
        - Use for a structured, rule-governed symbol system that renders content of an existing
          language or domain into a surface form — IPA, Western staff notation, Morse, a
          stenographic system — every one of which is a lossy projection of underlying
          frame-relative content.

    When to avoid:
        - Avoid for a system with independent generative syntax and semantics (that is
          gmeow:Language — a natural, formal, or programming language, per lang:signSystemKind)
          and for loose conventions that do not rise to a defined notation; reserve the
          projection-profile machinery for declaring the losses, not this bare type.

    How to use:
        - Type the system as NotationSystem, tag its domain with gmeow:notationSystemKind, link
          the language or domain it serves via gmeow:notationSystemFor, and attach a
          gmeow:NotationProjectionProfile to declare what the surface form represents and loses.

    Examples:
        - ex:ipa a gmeow:NotationSystem ; gmeow:notationSystemKind gmeow:symbolicKindTranscription
          .

    Usage:
        >>> from gmeow_models.notation import NotationSystem
        >>> NotationSystem.model_config["json_schema_extra"]["curie"]
        'gmeow:NotationSystem'

    IRI:    https://blackcatinformatics.ca/gmeow/NotationSystem
    CURIE:  gmeow:NotationSystem

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/NotationSystem", "curie": "gmeow:NotationSystem", "definitionDigest": "blake3:1417d30084960eaaa1b325aefe2f05da311495b4b149031e38f8f8c0ad4707b6", "iri": "https://blackcatinformatics.ca/gmeow/NotationSystem"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    smuflCodepoint: list[str] | None = Field(default=None, description="A Unicode codepoint reference in the Standard Music Font Layout (SMuFL) specification that identifies a glyph used by this notation system. Multiple codepoints may be asserted as multiple triples.", alias="gmeow:smuflCodepoint")


class NotationSystemUsage(ConfiguredBaseModel):
    """Notation System Usage.

    A reified, role- and period-scoped use of a notation system by an entity — an
    observation in the universal claim stack (observation-spine bridge): the gufo:Relator
    binding {entity} × {notation system} × {role} × {period}, mirroring names' NameUsage
    relator (the language side's script binding is carried by lang:Orthography). Lets a
    language use IPA for phonetic transcription, a musical work use Western staff notation,
    or a text use Morse code for encoding, each co-equally and standpoint-scoped.

    When to use:
        - Use when a notation system's use by an entity must be first-class — carrying role,
          period, standpoint, or confidence — so a language using IPA, a work using staff
          notation, and a text using Morse coexist co-equally without any one being privileged
          (the reify-on-demand half of the flat hasNotationSystem).

    When to avoid:
        - Avoid when a plain unscoped link suffices (use the flat gmeow:hasNotationSystem) and
          avoid stuffing the canonical content itself into the relator; the usage records THAT a
          notation is used, not the rendered surface form, which is a separate lossy projection.

    How to use:
        - Mint the relator and fill its four functional roles — gmeow:notationUsageTarget,
          gmeow:notationUsageNotationSystem, gmeow:notationUsageRole, gmeow:notationUsageInterval
          — then attach standpoint and confidence from the observation spine; pairs with
          gmeow:hasNotationSystem as the flat shortcut.

    Examples:
        - ex:u a gmeow:NotationSystemUsage ; gmeow:notationUsageTarget ex:english ;
          gmeow:notationUsageNotationSystem ex:ipa ; gmeow:notationUsageRole
          gmeow:notationRoleTranscription ; gmeow:notationUsageInterval ex:since1888 .

    Usage:
        >>> from gmeow_models.notation import NotationSystemUsage
        >>> NotationSystemUsage.model_config["json_schema_extra"]["curie"]
        'gmeow:NotationSystemUsage'

    IRI:    https://blackcatinformatics.ca/gmeow/NotationSystemUsage
    CURIE:  gmeow:NotationSystemUsage

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/NotationSystemUsage", "curie": "gmeow:NotationSystemUsage", "definitionDigest": "blake3:c74db86230e47d8b88d990d0e01a01eafa0b62796e29c62eca142437b14e0089", "iri": "https://blackcatinformatics.ca/gmeow/NotationSystemUsage"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    notationUsageInterval: list[TimeInterval] = Field(min_length=1, description="The time interval over which the notation usage is asserted to hold. Functional per relator: one interval per NotationSystemUsage.", alias="gmeow:notationUsageInterval")
    notationUsageNotationSystem: list[NotationSystem] = Field(min_length=1, description="The notation system being used. Functional per relator: one notation system per NotationSystemUsage.", alias="gmeow:notationUsageNotationSystem")
    notationUsageRole: list[str] = Field(min_length=1, description="Within gmeow:NotationSystemUsage, values are node references constrained to gmeow:NotationUsageRole. The role the notation plays in this usage — transcription, encoding, representation, communication, expression, shorthand, cipher. Functional per relator: one role per NotationSystemUsage.", alias="gmeow:notationUsageRole")
    notationUsageTarget: list[Entity] = Field(min_length=1, description="The entity that uses the notation system — a language, lexical form, expression, work, or information object. Functional per relator: one target per NotationSystemUsage.", alias="gmeow:notationUsageTarget")

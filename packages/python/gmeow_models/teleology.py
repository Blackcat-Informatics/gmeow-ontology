"""GMEOW models — slice GMEOW Teleology Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/teleology
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class Commitment(ConfiguredBaseModel):
    """Commitment.

    A social commitment — an agent committed toward at least one other agent to bring about
    a goal (UFO-C: social moment). A relator, not a mode: it mediates the committed agent
    (gmeow:committedAgent) and the beneficiaries (gmeow:commitmentBeneficiary), who must be
    distinct (the logic:commitmentBeneficiaryDistinctness axiom, projected to SHACL). A
    covenant, a contract promise, and a service-level agreement are Commitments; the
    agreements and contracts modules carry the document and the legal wrapper, this class
    carries the teleological bond itself.

    When to use:
        - Use for the social commitment grade — an agent bound TOWARD at least one distinct other
          agent to bring about a goal (a covenant, a contract promise, a service-level agreement),
          where the bond itself is first-class.

    When to avoid:
        - Avoid for intrinsic modes that hold no one accountable (use gmeow:Desire or
          gmeow:Intention) and for the document or legal wrapper (those live in the agreements and
          contracts modules); the beneficiary must be distinct from the committed agent.

    How to use:
        - Mint one Commitment per direction — model mutual promises as two Commitments so
          withdrawal, breach, and tenure stay independent; bind gmeow:committedAgent, one or more
          gmeow:commitmentBeneficiary, and the gmeow:intentionGoal, and carry the period on the
          relator itself.

    Examples:
        - ex:slaPromise a gmeow:Commitment ; gmeow:committedAgent ex:vendor ;
          gmeow:commitmentBeneficiary ex:client ; gmeow:intentionGoal ex:ninetyNinePercentUptime .

    Usage:
        >>> from gmeow_models.teleology import Commitment
        >>> Commitment.model_config["json_schema_extra"]["curie"]
        'gmeow:Commitment'

    IRI:    https://blackcatinformatics.ca/gmeow/Commitment
    CURIE:  gmeow:Commitment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Commitment", "curie": "gmeow:Commitment", "definitionDigest": "blake3:169b14bbb4993968283a0459f7e6bd687908a84bc9ebba62c02350732ac2687a", "iri": "https://blackcatinformatics.ca/gmeow/Commitment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    commitmentBeneficiary: list[str] = Field(min_length=1, description="An agent toward whom the commitment is made. NOT functional: a commitment may be made toward several beneficiaries at once. Must be distinct from the committed agent (the logic:commitmentBeneficiaryDistinctness axiom, projected to SHACL); a resolution one makes purely to oneself is an Intention, not a Commitment.", alias="gmeow:commitmentBeneficiary")
    committedAgent: list[str] = Field(min_length=1, description="The agent bound by a commitment — the one who has committed. Functional: one commitment, one committed agent; mutual promises are two Commitments, one in each direction, which keeps withdrawal, breach, and tenure independent per direction.", alias="gmeow:committedAgent")
    intentionGoal: list[str] = Field(min_length=1, description="The goal at which a desire, intention, or commitment aims — its propositional content. Functional: one mode, one goal; an agent pursuing five goals bears five modes. Sharing is at the Goal: two agents committed to the same outcome hold distinct modes aiming at one Goal individual.", alias="gmeow:intentionGoal")


class Desire(ConfiguredBaseModel):
    """Desire.

    An intentional mode of wanting without commitment — the agent would welcome the goal's
    satisfaction but has not committed to pursuing it (UFO-C: desire). Inheres in exactly
    one agent (gmeow:intentBearer). Promote a Desire to an Intention by minting the
    Intention and suppressing the Desire's tenure — never by mutation (Principle 10).

    When to use:
        - Use for the lowest commitment grade — the agent wants the goal satisfied but has not
          settled on pursuing it; the wanting that has not yet become an Intention.

    When to avoid:
        - Avoid once the agent has committed internally (use gmeow:Intention) or toward another
          agent (use gmeow:Commitment); do not mutate a Desire into an Intention — mint the
          Intention and suppress the Desire's tenure (Principle 10).

    How to use:
        - Mint a Desire, set gmeow:intentBearer (one agent) and gmeow:intentionGoal (one Goal);
          promote by minting the higher-grade moment and closing the Desire's
          gmeow:IntentionTenure rather than deleting it.

    Examples:
        - ex:wishForRain a gmeow:Desire ; gmeow:intentBearer ex:lillith ; gmeow:intentionGoal
          ex:droughtEnds .

    Usage:
        >>> from gmeow_models.teleology import Desire
        >>> Desire.model_config["json_schema_extra"]["curie"]
        'gmeow:Desire'

    IRI:    https://blackcatinformatics.ca/gmeow/Desire
    CURIE:  gmeow:Desire

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Desire", "curie": "gmeow:Desire", "definitionDigest": "blake3:e26d9c4953228699dc220c2299964730eb8d74f3794dc35c57eb1e8fccb26c01", "iri": "https://blackcatinformatics.ca/gmeow/Desire"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    intentBearer: Any = Field(description="The agent in whom a desire or intention inheres. Functional: an intrinsic mode has exactly one bearer (gUFO inherence, asserted with GMEOW's own property by Principle 5 — gufo:inheresIn is the alignment target, not the asserted axiom). Two agents wanting the same goal are two modes sharing one gmeow:intentionGoal.", alias="gmeow:intentBearer")
    intentionGoal: Any = Field(description="The goal at which a desire, intention, or commitment aims — its propositional content. Functional: one mode, one goal; an agent pursuing five goals bears five modes. Sharing is at the Goal: two agents committed to the same outcome hold distinct modes aiming at one Goal individual.", alias="gmeow:intentionGoal")


class Goal(ConfiguredBaseModel):
    """Goal.

    A described state of affairs that an agent may want, intend, or commit to bring about —
    the propositional content of the intentional modes, existing by social convention like
    every description (a gmeow:SocialObject). A goal asserts nothing and prescribes nothing
    by itself: it is satisfied by situations (gmeow:satisfiedBy), held by agents
    (gmeow:hasGoal or the reified modes), and partly defined by its named shadow
    (gmeow:counterGoal). Whether a given situation satisfies the goal is a vantage-indexed
    claim (Principle 9).

    When to use:
        - Use to name the propositional content an agent may want, intend, or commit to — the
          shared target that the intentional modes and the flat gmeow:hasGoal point at, and the
          thing a situation can satisfy.

    When to avoid:
        - Avoid for the agent's holding of the goal (that is gmeow:hasGoal or a
          Desire/Intention/Commitment) and for prescriptive force — a Goal prescribes nothing;
          obligatory conduct arrives via the norms extension, never on the Goal itself.

    How to use:
        - Mint one Goal individual and share it across every agent and mode aiming at it; attach
          gmeow:counterGoal for constitutive opposition and gmeow:satisfiedBy for achieving
          situations, recording disputed satisfaction as coexisting gmeow:accordingTo claims
          rather than a global verdict.

    Examples:
        - ex:reachOrbit a gmeow:Goal ; gmeow:counterGoal ex:scrubLaunch ; gmeow:satisfiedBy
          ex:firstStableOrbit .

    Usage:
        >>> from gmeow_models.teleology import Goal
        >>> Goal.model_config["json_schema_extra"]["curie"]
        'gmeow:Goal'

    IRI:    https://blackcatinformatics.ca/gmeow/Goal
    CURIE:  gmeow:Goal

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Goal", "curie": "gmeow:Goal", "definitionDigest": "blake3:42f4b966630ee4d6062978ad67d1782bee6fee14087f176b18a324db2fb6e430", "iri": "https://blackcatinformatics.ca/gmeow/Goal"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Intention(ConfiguredBaseModel):
    """Intention.

    An intentional mode of internal commitment — the agent has settled on pursuing the goal
    (UFO-C: intention), though no other agent holds them to it. Inheres in exactly one agent
    (gmeow:intentBearer); aims at exactly one goal (gmeow:intentionGoal); may motivate
    events (gmeow:motivates). The social grade — committed TOWARD someone — is
    gmeow:Commitment.

    When to use:
        - Use when an agent has internally settled on pursuing a goal but no other agent holds
          them to it — the middle commitment grade, and the natural source of gmeow:motivates on
          the events the agent undertakes.

    When to avoid:
        - Avoid for mere wanting (use gmeow:Desire) and for resolutions made TOWARD another agent
          (use gmeow:Commitment, a relator); a resolution made purely to oneself is an Intention,
          not a Commitment.

    How to use:
        - Mint an Intention with one gmeow:intentBearer and one gmeow:intentionGoal; link the
          actions it explains with gmeow:motivates, and record adoption or abandonment over time
          as a gmeow:IntentionTenure rather than mutating the mode.

    Examples:
        - ex:planToShip a gmeow:Intention ; gmeow:intentBearer ex:lillith ; gmeow:intentionGoal
          ex:reachOrbit ; gmeow:motivates ex:launchPrep .

    Usage:
        >>> from gmeow_models.teleology import Intention
        >>> Intention.model_config["json_schema_extra"]["curie"]
        'gmeow:Intention'

    IRI:    https://blackcatinformatics.ca/gmeow/Intention
    CURIE:  gmeow:Intention

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Intention", "curie": "gmeow:Intention", "definitionDigest": "blake3:3515ea31c2e5dee83d6be19269a4f034364096971596ae5605d8859ccd8d76f8", "iri": "https://blackcatinformatics.ca/gmeow/Intention"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    intentBearer: Any = Field(description="The agent in whom a desire or intention inheres. Functional: an intrinsic mode has exactly one bearer (gUFO inherence, asserted with GMEOW's own property by Principle 5 — gufo:inheresIn is the alignment target, not the asserted axiom). Two agents wanting the same goal are two modes sharing one gmeow:intentionGoal.", alias="gmeow:intentBearer")
    intentionGoal: Any = Field(description="The goal at which a desire, intention, or commitment aims — its propositional content. Functional: one mode, one goal; an agent pursuing five goals bears five modes. Sharing is at the Goal: two agents committed to the same outcome hold distinct modes aiming at one Goal individual.", alias="gmeow:intentionGoal")


class IntentionTenure(ConfiguredBaseModel):
    """Intention Tenure.

    The reified, time-scoped fact that an agent held an intentional mode over an interval —
    a goal adopted in January and abandoned in June is an opened-then-closed tenure, never a
    deletion (Principle 10). The StandpointTenure idiom applied to teleology: use only when
    adoption / revision is itself worth modelling; the common case is a bare gmeow:hasGoal
    with validFrom / validUntil on the statement. Carries its interval via
    gmeow:duringInterval (temporal module); a withdrawn tenure is kept with
    gmeow:displayable false.

    When to use:
        - Use as the reified half of the gmeow:hasGoal ↔ gmeow:IntentionTenure pair: when
          adoption, revision, or abandonment of an intrinsic mode over an interval is itself the
          fact of interest, so the tenure can be opened and closed rather than mutated.

    When to avoid:
        - Avoid for the 80% case where an agent simply holds a goal (use the flat gmeow:hasGoal
          with validFrom / validUntil) and for commitment tenure (a Commitment relator carries its
          own period by construction); never delete an abandoned tenure — close it and set
          gmeow:displayable false (Principle 10).

    How to use:
        - Mint one IntentionTenure per (agent, mode) holding, bind gmeow:tenureAgent and
          gmeow:tenureIntention, scope it with gmeow:duringInterval; record withdrawal by closing
          the interval and suppressing with gmeow:displayable false rather than removing the
          triple.

    Examples:
        - ex:resolveTenure a gmeow:IntentionTenure ; gmeow:tenureAgent ex:lillith ;
          gmeow:tenureIntention ex:planToShip ; gmeow:duringInterval ex:firstHalfYear .

    Usage:
        >>> from gmeow_models.teleology import IntentionTenure
        >>> IntentionTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:IntentionTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/IntentionTenure
    CURIE:  gmeow:IntentionTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/IntentionTenure", "curie": "gmeow:IntentionTenure", "definitionDigest": "blake3:4581dcc8f9099ff2c8328d7176c180441a1c8f9185e8127c2efefae57e7b20bb", "iri": "https://blackcatinformatics.ca/gmeow/IntentionTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    tenureAgent: Any = Field(description="The agent whose intentional mode an intention-tenure records. Functional: one tenure, one agent.", alias="gmeow:tenureAgent")
    tenureIntention: Any = Field(description="The desire or intention the tenure records the agent as holding over its interval. Functional: one tenure, one mode. Commitment tenure is carried by the Commitment relator's own temporal scope (relators are time-indexed by construction); the tenure class exists for the intrinsic modes, whose adoption and abandonment would otherwise be invisible.", alias="gmeow:tenureIntention")

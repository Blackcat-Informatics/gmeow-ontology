"""GMEOW models — slice GMEOW Concepts Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/concepts
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ConceptCategorization(ConfiguredBaseModel):
    """Concept Categorization.

    A standpoint-indexed claim that an entity is categorized under a concept — the reified
    form of gmeow:instanceOfConcept. The vantage is the agent or standpoint making the
    categorization; the observedFeature is the categorized entity; the observationResult is
    the gmeow:Concept. Typicality, confidence, and accordingTo are reused from the
    standpoint and provenance layers.

    When to use:
        - Use when a categorization is perspectival, graded, time-scoped, or must carry provenance
          — the promotion target from the flat gmeow:instanceOfConcept shortcut.

    When to avoid:
        - Avoid for the flat case where an entity is simply under a concept with no need for
          vantage or typicality; do not mint both a flat shortcut and a reified claim for the same
          holding without reason (Principle 4).

    How to use:
        - Mint one ConceptCategorization per (vantage, entity, concept) tuple; bind the entity via
          gmeow:observedFeature and the concept via gmeow:observationResult; add gmeow:typicality
          for graded membership and gmeow:accordingTo / gmeow:confidence from the
          statement/provenance layers.

    Examples:
        - ex:whaleCategorization a gmeow:ConceptCategorization ; gmeow:vantage ex:marineBiologist
          ; gmeow:observedFeature ex:whale ; gmeow:observationResult ex:mammal ; gmeow:typicality
          "0.95"^^xsd:decimal .

    Usage:
        >>> from gmeow_models.concepts import ConceptCategorization
        >>> ConceptCategorization.model_config["json_schema_extra"]["curie"]
        'gmeow:ConceptCategorization'

    IRI:    https://blackcatinformatics.ca/gmeow/ConceptCategorization
    CURIE:  gmeow:ConceptCategorization

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConceptCategorization", "curie": "gmeow:ConceptCategorization", "definitionDigest": "blake3:6e769c27fd5223bc6b798f4b213573467a063fcafbdd22aacccb4c0e8381df1d", "iri": "https://blackcatinformatics.ca/gmeow/ConceptCategorization"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    observationResult: list[str] | None = Field(default=None, description="The entity-valued result of an observation — a coordinate set, an instant, a quality value, a quantity, or another entity. For literal scalar readings (e.g. 22.5°C), the observationResult entity carries the literal via a datatype property (e.g. qudt:quantityValue or a module-specific scalar property). Non-functional: a single observation may yield several results in different frames or granularities, and competing result claims coexist rather than collapse.", alias="gmeow:observationResult")
    observedFeature: list[Entity] | None = Field(default=None, description="The individual whose property or state is being observed — the feature of interest. The range is intentionally open (owl:Thing) because anything can be observed: a place, a person, an event, a document, a quality value, or a situation. SHACL shapes enforce closed-world constraints per observation kind.", alias="gmeow:observedFeature")
    typicality: list[float] | None = Field(default=None, ge=0, le=1, description="A graded degree of membership in [0,1] for a prototype-theoretic concept, attached to a reified gmeow:ConceptCategorization. Non-functional: multiple sources may report typicality, and those reports coexist (Principle 9). Solver-layer prototype-theoretic typicality; it is not a reasoner entailment.", alias="gmeow:typicality")


class ConceptTenure(ConfiguredBaseModel):
    """Concept Tenure.

    The reified, time-scoped fact that a concept had a particular intension over an interval
    — conceptual change made first-class. When an agent community revises the definition or
    applicability of a concept, the old intension is retained as a closed tenure suppressed
    with gmeow:displayable false, never deleted (Principle 10). Carries its scope via
    gmeow:duringInterval (temporal module) and links to the concept via
    gmeow:conceptHoldsFor.

    When to use:
        - Use when the change of a concept's intension, usage, or applicability over time is
          itself the fact of interest — conceptual drift, definition revision, or paradigm shift.

    When to avoid:
        - Avoid for timeless concept hierarchies and for the lightweight case where a bare
          categorization claim with validFrom/validUntil suffices; reify on demand (Principle 4).

    How to use:
        - Mint one ConceptTenure per concept-intension interval; bind the concept with
          gmeow:conceptHoldsFor and the interval with gmeow:duringInterval. On revision, close the
          interval with gmeow:endedAtTime and set gmeow:displayable false on the tenure; mint a
          new tenure for the revised intension.

    Examples:
        - ex:mammalTenure a gmeow:ConceptTenure ; gmeow:conceptHoldsFor ex:mammal ;
          gmeow:duringInterval ex:preDnaEra .

    Usage:
        >>> from gmeow_models.concepts import ConceptTenure
        >>> ConceptTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:ConceptTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/ConceptTenure
    CURIE:  gmeow:ConceptTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConceptTenure", "curie": "gmeow:ConceptTenure", "definitionDigest": "blake3:fae6bfbe75be239a384de1f6ec160ae2c77b249bd36ab4c95fb95ba4cd6b369a", "iri": "https://blackcatinformatics.ca/gmeow/ConceptTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conceptHoldsFor: list[str] | None = Field(default=None, description="Relates a gmeow:ConceptTenure to the gmeow:Concept whose intension it time-scopes. Functional: one tenure records the intension of exactly one concept; a concept may have many tenures over time.", alias="gmeow:conceptHoldsFor")
    duringInterval: list[TimeInterval] | None = Field(default=None, description="Relates a time-scoped relation to the interval over which it holds.", alias="gmeow:duringInterval")

"""GMEOW models — slice GMEOW AI Claim Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/ai
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ContradictionKindEnum(StrEnum):
    contradictionKindFactual = "gmeow:contradictionKindFactual"
    contradictionKindFraming = "gmeow:contradictionKindFraming"
    contradictionKindNumeric = "gmeow:contradictionKindNumeric"
    contradictionKindTemporal = "gmeow:contradictionKindTemporal"


class MemoryKindEnum(StrEnum):
    memoryKindEpisodic = "gmeow:memoryKindEpisodic"
    memoryKindProcedural = "gmeow:memoryKindProcedural"
    memoryKindSemantic = "gmeow:memoryKindSemantic"
    memoryKindWorking = "gmeow:memoryKindWorking"


class PromptRoleEnum(StrEnum):
    promptRoleAssistant = "gmeow:promptRoleAssistant"
    promptRoleSystem = "gmeow:promptRoleSystem"
    promptRoleTool = "gmeow:promptRoleTool"
    promptRoleUser = "gmeow:promptRoleUser"


class SupportPolarityEnum(StrEnum):
    polarityNeutral = "gmeow:polarityNeutral"
    polarityRefutes = "gmeow:polarityRefutes"
    polaritySupports = "gmeow:polaritySupports"


class Chunk(ConfiguredBaseModel):
    """Chunk.

    A contiguous segment of a source information object, produced by a chunking step for
    retrieval — the unit a retrieval returns, a model reads, and an evidence span pins into.
    Located in its source by gmeow:chunkOf + gmeow:spanStart/gmeow:spanEnd and
    content-addressed via the EXISTING gmeow:contentDigest (sources slice), so 'which exact
    bytes did the model see?' is answerable after the fact.

    When to use:
        - Use for a retrieval-sized segment cut from a larger source — the unit a retriever
          returns, a model reads, and an evidence span pins into; the genuinely missing
          information-object kind in the Source → Chunk → EvidenceSpan → claim spine.

    When to avoid:
        - Avoid typing the whole source document as a Chunk (the source is a plain
          gmeow:InformationObject) and avoid re-chunking by re-pointing — a new segmentation mints
          fresh Chunk individuals, since gmeow:chunkOf is functional.

    How to use:
        - Type the segment gmeow:Chunk, locate it with gmeow:chunkOf +
          gmeow:spanStart/gmeow:spanEnd, content-address it with the existing gmeow:contentDigest,
          and let evidence spans pin into it via gmeow:spanOfChunk.

    Examples:
        - ex:c1 a gmeow:Chunk ; gmeow:chunkOf ex:doc ; gmeow:spanStart 0 ; gmeow:spanEnd 512 ;
          gmeow:contentDigest ex:sha256 .

    Usage:
        >>> from gmeow_models.ai import Chunk
        >>> Chunk.model_config["json_schema_extra"]["curie"]
        'gmeow:Chunk'

    IRI:    https://blackcatinformatics.ca/gmeow/Chunk
    CURIE:  gmeow:Chunk

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Chunk", "curie": "gmeow:Chunk", "definitionDigest": "blake3:fcb1b2bd3997e9af18efb3d251e3b15f52529b4908a4aa50eb714d1221d06e39", "iri": "https://blackcatinformatics.ca/gmeow/Chunk"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    chunkOf: str = Field(description="The source information object this chunk segments. Functional: a chunk is cut from exactly one source; re-chunking produces new Chunk individuals, never a re-pointed one.", alias="gmeow:chunkOf")
    spanEnd: str = Field(description="The ending character offset (unicode code points, zero-based, exclusive) of a span. Pairs with gmeow:spanStart.", alias="gmeow:spanEnd")
    spanStart: str = Field(description="The starting character offset (unicode code points, zero-based, inclusive) of a span — a Chunk within its source, or an EvidenceSpan within a chunk. Domain-free: one span idiom for the retrieval substrate and the evidence layer. The TYPED sharpening of the citations slice's gmeow:selectorTextPosition (which remains the home of non-numeric position locators); mechanical AIS checking (EvidenceSpan audit machinery) needs integers, not prose.", alias="gmeow:spanStart")


class Contradiction(ConfiguredBaseModel):
    """Contradiction.

    A reified conflict between two or more claim nodes — surfaced, attributed
    (gmeow:detectedBy), kind-classified (gmeow:contradictionKind), and NEVER resolved by
    rank. Conflicting claims coexist accordingTo their standpoints (P9); a standpoint may
    mark one refuted via the EXISTING gmeow:claimModality, but no winner machinery exists
    anywhere in the graph. Detection is itself contestable: two detectors disagreeing are
    two Contradiction individuals.

    When to use:
        - Use to SURFACE a conflict between two or more claim nodes as a first-class, attributed,
          kind-classified relator — making disagreement queryable without choosing a winner (P9).

    When to avoid:
        - Avoid using it to resolve or rank the conflict (no winner machinery exists — conflicting
          claims coexist accordingTo their standpoints, and a standpoint marks one refuted via
          gmeow:claimModality) and avoid treating detection as settled; rival detectors are
          separate Contradiction individuals.

    How to use:
        - Mint a gmeow:Contradiction, bind at least two claim nodes with gmeow:contradictsClaim,
          attribute it with gmeow:detectedBy, classify it with gmeow:contradictionKind; leave
          resolution to standpoint-level gmeow:claimModality rather than deletion (P10).

    Examples:
        - ex:conflict1 a gmeow:Contradiction ; gmeow:contradictsClaim ex:claimA, ex:claimB ;
          gmeow:detectedBy ex:nliJudge ; gmeow:contradictionKind gmeow:contradictionKindFactual .

    Usage:
        >>> from gmeow_models.ai import Contradiction
        >>> Contradiction.model_config["json_schema_extra"]["curie"]
        'gmeow:Contradiction'

    IRI:    https://blackcatinformatics.ca/gmeow/Contradiction
    CURIE:  gmeow:Contradiction

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Contradiction", "curie": "gmeow:Contradiction", "definitionDigest": "blake3:7fead3cd5c0264e2111b0cf8c1fe65975d93455fd1c1b3fc4ab8d94ae51c16c1", "iri": "https://blackcatinformatics.ca/gmeow/Contradiction"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    contradictsClaim: list[str] = Field(min_length=2, description="Relates a contradiction to one of the claim nodes in conflict — at least two per contradiction (SHACL; the OWL existential documents the relator's relata for the reasoner). The range is intentionally open: a claim node is a gmeow:StandpointClaim, another Observation, or an RDF 1.2 statement reifier, and a range assertion would leak one form's typing onto the others (the tenurePosition precedent, standpoint slice).", alias="gmeow:contradictsClaim")


class MemoryItem(ConfiguredBaseModel):
    """Memory Item.

    An observation/claim in the role of an agent's remembered belief (gmeow:memoryOf the
    remembering agent; gmeow:memoryKind the cognitive register). A gufo:Role on the
    UNIVERSAL claim construct — any claim form can be remembered, and being remembered is
    contingent, never an essence. Belief revision is supersession with gmeow:displayable
    false (P10), never deletion: what an agent believed WHEN is a query, not an archaeology
    project. The substrate under the gmeow client's Memory and the MCP store/recall/revise
    triad (P14).

    When to use:
        - Use to cast any observation/claim into the contingent ROLE of an agent's remembered
          belief — the substrate under the gmeow client's Memory and the MCP store/recall/revise
          triad (P14).

    When to avoid:
        - Avoid making memory an essence or a separate claim copy (it is a gufo:Role on the
          universal claim construct — the same claim may be one agent's memory and another's
          input) and never revise by deletion; supersede with gmeow:displayable false so 'what was
          believed when' stays queryable (P10).

    How to use:
        - Type the existing claim node gmeow:MemoryItem, set gmeow:memoryOf and gmeow:memoryKind,
          carry salience on the EXISTING statement-level gmeow:importanceLevel and recency on the
          statement clocks; revise by superseding with gmeow:displayable false rather than
          deleting.

    Examples:
        - ex:m1 a gmeow:MemoryItem ; gmeow:memoryOf ex:lillith ; gmeow:memoryKind
          gmeow:memoryKindSemantic .

    Usage:
        >>> from gmeow_models.ai import MemoryItem
        >>> MemoryItem.model_config["json_schema_extra"]["curie"]
        'gmeow:MemoryItem'

    IRI:    https://blackcatinformatics.ca/gmeow/MemoryItem
    CURIE:  gmeow:MemoryItem

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/MemoryItem", "curie": "gmeow:MemoryItem", "definitionDigest": "blake3:4f8c0bb87bb3a527b4482de66d44b1fd343e9d06cdc0221d0f02d56d9c6ea682", "iri": "https://blackcatinformatics.ca/gmeow/MemoryItem"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    memoryOf: list[Agent] = Field(min_length=1, description="The agent whose memory holds this item.", alias="gmeow:memoryOf")


class ModelCard(ConfiguredBaseModel):
    """Model Card.

    A structured description of a model agent (gmeow:describesModel): provider, version tag,
    context window, training cutoff. Aligns Model Cards / ML-Schema / SPDX-AI by reference
    (P5). The card is a document ABOUT the agent; capability claims are claim-layer
    observations, attributed and contestable.

    When to use:
        - Use for the structured document describing a model agent — provider, version tag,
          context window, training cutoff; the information object ABOUT the gmeow:SoftwareAgent a
          gmeow:ModelInvocation uses.

    When to avoid:
        - Avoid conflating it with the model itself (the agent is the gmeow:SoftwareAgent, the
          card is a document about it) and avoid baking capability claims into the card as facts —
          those are attributed, contestable claim-layer observations.

    How to use:
        - Type the description gmeow:ModelCard, link it to the agent with gmeow:describesModel,
          and carry the declared facets (gmeow:modelProvider, gmeow:modelVersionTag,
          gmeow:modelContextWindow, gmeow:modelTrainingCutoff); keep capability assertions as
          separate attributed claims.

    Examples:
        - ex:opusCard a gmeow:ModelCard ; gmeow:describesModel ex:opus ; gmeow:modelProvider
          "Anthropic" ; gmeow:modelTrainingCutoff "2026-01-01"^^xsd:date .

    Usage:
        >>> from gmeow_models.ai import ModelCard
        >>> ModelCard.model_config["json_schema_extra"]["curie"]
        'gmeow:ModelCard'

    IRI:    https://blackcatinformatics.ca/gmeow/ModelCard
    CURIE:  gmeow:ModelCard

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ModelCard", "curie": "gmeow:ModelCard", "definitionDigest": "blake3:102527551e6753d446e0e7f2a5661645a0a1d4baf80c4fa48dfdf15b4aa6c608", "iri": "https://blackcatinformatics.ca/gmeow/ModelCard"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    describesModelArtifact: list[str] | None = Field(default=None, description="Relates a gmeow:ModelCard to the gmeow:ModelArtifact it documents — architecture, version, training cutoff, parameter count, context window, where those are artifact properties. Splits gmeow:describesModel (functional → gmeow:SoftwareAgent) so a documented artifact is not inferred to be a SoftwareAgent.", alias="gmeow:describesModelArtifact")
    describesModelService: list[ModelDeployment] | None = Field(default=None, description="Relates a gmeow:ModelCard to the served gmeow:ModelDeployment it documents — provider, endpoint, rate limits, geographic placement, where those are deployment properties. The deployment-facing half of the ModelCard split.", alias="gmeow:describesModelService")

<!-- GENERATED by gmeow ontology-docs. DO NOT EDIT. -->

# event time zone

- **CURIE:** [`gmeow:eventTimeZone`](gmeow-eventTimeZone.md)
- **IRI:** <https://blackcatinformatics.ca/gmeow/eventTimeZone>
- **Category:** property
- **Defined by:** [`gmeow:slices/calendar`](../../slices/calendar.md)
- **Box roles:** [RBox role](../boxes/rbox.md) ([What is this?](../../four-boxes/index.md))

The time zone in which an event's temporal placement is expressed.

## Structure

**Property shape:** object property; [`gmeow:Event`](../classes/gmeow-Event.md) -> [`gmeow:TimeZone`](../classes/gmeow-TimeZone.md); functional

## Practical Pattern

Use [`gmeow:eventTimeZone`](gmeow-eventTimeZone.md) from [`gmeow:Event`](../classes/gmeow-Event.md) to [`gmeow:TimeZone`](../classes/gmeow-TimeZone.md) when the relationship itself belongs in the native GMEOW graph.

## Example Snippets

These snippets are generated from canonical slice examples and trimmed to the Turtle blocks where this term appears.

### Recurring Meeting

- **Source:** [`slices/core/calendar/examples/recurring-meeting.ttl`](https://github.com/Blackcat-Informatics/gmeow-ontology/blob/main/slices/core/calendar/examples/recurring-meeting.ttl)
- **Examples catalog:** [open in catalog](../../examples/index.md#example-slices-core-calendar-examples-recurring-meeting)#example-slices-core-calendar-examples-recurring-meeting

```turtle
# --- The template event the schedule repeats.
ex:standup a gmeow:Event ;
    rdfs:label               "Weekly engineering standup"@en ;
    gmeow:eventType          ex:eventTypeTeamMeeting ;
    gmeow:eventTime          "2026-06-15T09:00:00-06:00"^^xsd:dateTime ;
    gmeow:eventTemporalFrame gmeow:temporalFrameUTCGregorian ;
    gmeow:eventTimeZone      ex:edmonton .
```

## Common Companion Terms

[`gmeow:Event`](../classes/gmeow-Event.md), [`gmeow:TimeZone`](../classes/gmeow-TimeZone.md)

## Usage Advice

### Use when

- Use to fix the civil offset an event's wall-clock placement is read in — overriding the calendar default when an occurrence sits in a different zone.

### Avoid when

- Avoid asserting two zones (it is functional — one per event) and avoid relying on it where the calendar default suffices; set it only when the event's offset differs from [`gmeow:calendarTimeZone`](gmeow-calendarTimeZone.md).

### How to use

- Point the event at one [`gmeow:TimeZone`](../classes/gmeow-TimeZone.md); it takes precedence over the containing calendar's [`gmeow:calendarTimeZone`](gmeow-calendarTimeZone.md) for that event's local-time rendering.

### Examples

- ex:standupEvent [`gmeow:eventTimeZone`](gmeow-eventTimeZone.md) ex:torontoZone.

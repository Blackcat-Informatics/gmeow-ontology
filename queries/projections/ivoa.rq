# Projection: GMEOW → pure ivoa. GENERATED by `gmeow compile-mappings` from mapping-dsl/ — DO NOT EDIT.
# Lossy and directional by design; drops: determinacy, granularity, standpoint, and reference-frame metadata are dropped; only the generic dataset class survives; the full GMEOW reference-frame profile is flattened to a coordinate-system string or IRI; the telescope/facility node is preserved, but agent roles and platform location are dropped; the instrument node is preserved, but agent roles and platform location are dropped; unit, determinacy, granularity, and reference frame are dropped; only the measured physical quantity survives; the single GMEOW result node is replicated across all four ObsCore axes; axis-specific metadata (units, coverage, resolution) is not preserved.
PREFIX gmeow: <https://blackcatinformatics.ca/gmeow/>
PREFIX obscore: <http://www.ivoa.net/rdf/ObsCore#>

CONSTRUCT {
    ?obs a obscore:ObsDataset .
    ?obs obscore:coordsys ?frame .
    ?obs obscore:facility ?sensor .
    ?obs obscore:instrument ?sensor .
    ?obs obscore:observable ?qty .
    ?obs obscore:spectralAxis ?result .
    ?obs obscore:spatialAxis ?result .
    ?obs obscore:timeAxis ?result .
    ?obs obscore:observableAxis ?result .
}
WHERE {
    {
        ?obs a gmeow:SensoryObservation .
    }
    UNION {
        ?obs a gmeow:SensoryObservation .
        ?obs gmeow:hasReferenceFrame ?frame .
    }
    UNION {
        ?obs a gmeow:SensoryObservation .
        ?obs gmeow:vantage ?sensor .
        ?sensor a gmeow:Sensor .
    }
    UNION {
        ?obs a gmeow:SensoryObservation .
        ?obs gmeow:observationResult ?qty .
        ?qty a gmeow:ScalarQuantity .
    }
    UNION {
        ?obs a gmeow:SensoryObservation .
        ?obs gmeow:observationResult ?result .
    }
}

"""GMEOW models — slice GMEOW Trust Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/trust
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class KeySchemeEnum(StrEnum):
    keySchemeNostr = "gmeow:keySchemeNostr"
    keySchemePGP = "gmeow:keySchemePGP"
    keySchemeSSH = "gmeow:keySchemeSSH"
    keySchemeX509 = "gmeow:keySchemeX509"


class Certification(ConfiguredBaseModel):
    """Certification.

    A reified attestation that a cryptographic key belongs to a given identity, made by a
    certifying agent (a PGP key-signature / Web-of-Trust certification). Its validity window
    is carried with gmeow:validFrom/validUntil; revocation sets validUntil.

    When to use:
        - Use to reify a Web-of-Trust attestation — a PGP key-signature — that a certifying agent
          vouches a given key belongs to a given identity; the relator that mediates certifier,
          key, and identity and carries its own dated validity window.

    When to avoid:
        - Avoid for an agent's subjective trust in another agent (that is gmeow:TrustAssertion)
          and for mere key possession (gmeow:holdsKey); a certification is a third-party binding
          claim, and a contested binding is recorded as competing standpoints, never a
          primaryCertification flag (Principle 9).

    How to use:
        - Mint the relator, bind gmeow:certifier, gmeow:certifiedKey, and gmeow:certifiedIdentity
          (the EL minimum), set gmeow:certificationLevel, and carry the window with
          gmeow:validFrom / gmeow:validUntil — revocation sets gmeow:validUntil; a disputed
          binding rides the cross-cutting gmeow:accordingTo standpoint axis.

    Examples:
        - ex:cert1 a gmeow:Certification ; gmeow:certifier ex:bob ; gmeow:certifiedKey
          ex:lillithKey ; gmeow:certifiedIdentity ex:lillith .

    Usage:
        >>> from gmeow_models.trust import Certification
        >>> Certification.model_config["json_schema_extra"]["curie"]
        'gmeow:Certification'

    IRI:    https://blackcatinformatics.ca/gmeow/Certification
    CURIE:  gmeow:Certification

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Certification", "curie": "gmeow:Certification", "definitionDigest": "blake3:19ee6463b0687fb82b7fe893a43f7ceb7db1ea584f871d2525a2d6eb9629a2a8", "iri": "https://blackcatinformatics.ca/gmeow/Certification"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    certifiedIdentity: list[Agent] | None = Field(default=None, description="The agent identity a certification binds the key to.", alias="gmeow:certifiedIdentity")
    certifiedKey: list[CryptographicKey] | None = Field(default=None, description="The cryptographic key a certification vouches for.", alias="gmeow:certifiedKey")
    certifier: list[Agent] | None = Field(default=None, description="The agent that made a certification.", alias="gmeow:certifier")


class CryptographicKey(ConfiguredBaseModel):
    """Cryptographic Key.

    A public key, certificate, or key material bound to an agent's identity — the thing a
    signature is made with and a certification vouches for.

    When to use:
        - Use to model a public key, certificate, or key material as a first-class entity that an
          agent holds, a certification vouches for, and a signature is made with — across OpenPGP,
          X.509, SSH, or Nostr.

    When to avoid:
        - Avoid for the binding of a key to an identity (that is gmeow:Certification) and for an
          act of signing (that is gmeow:CryptographicSignature); the key is the material, not the
          attestation or the signature it produces.

    How to use:
        - Mint the key, name its format with the functional gmeow:keyScheme, carry source-variable
          identifiers (gmeow:fingerprint, gmeow:keyId, gmeow:keyMaterial, gmeow:keyAlgorithm,
          gmeow:keyExpiresAt) non-functionally, and link it to its holder with gmeow:holdsKey and
          to an account with gmeow:accountKey.

    Examples:
        - ex:lillithKey a gmeow:CryptographicKey ; gmeow:keyScheme gmeow:keySchemePGP ;
          gmeow:fingerprint "A1B2..." .

    Usage:
        >>> from gmeow_models.trust import CryptographicKey
        >>> CryptographicKey.model_config["json_schema_extra"]["curie"]
        'gmeow:CryptographicKey'

    IRI:    https://blackcatinformatics.ca/gmeow/CryptographicKey
    CURIE:  gmeow:CryptographicKey

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/CryptographicKey", "curie": "gmeow:CryptographicKey", "definitionDigest": "blake3:4842bcd8a3119dd02487339d44a8821a5ad4c38e4acfe3631bacd67109525d99", "iri": "https://blackcatinformatics.ca/gmeow/CryptographicKey"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class TrustAssertion(ConfiguredBaseModel):
    """Trust Assertion.

    A reified, perspectival assertion that one agent (the trustor) trusts another (the
    trustee), optionally as an introducer to a given depth — the OpenPGP owner-trust notion.
    Its validity window is carried with gmeow:validFrom/validUntil.

    When to use:
        - Use whenever trust is HELD by a particular agent rather than asserted globally —
          owner-trust, an introducer designation, a leveled and dated vouching — so the claim
          names its trustor frame holder explicitly (Principle 9: A trusts B according to A's
          vantage, never a global verdict).

    When to avoid:
        - Avoid for a key↔identity binding attestation (that is gmeow:Certification) and for a
          quick directional vouch where level and date do not matter (use the gmeow:endorses
          shortcut); never collapse competing trustors into one privileged trust, and never expect
          the reasoner to propagate it transitively.

    How to use:
        - Mint the relator, bind gmeow:trustor and gmeow:trustee (the EL minimum), set
          gmeow:trustLevel and the gmeow:introducerDepth / gmeow:introducerAmount trust-signature
          parameters, and date it with gmeow:validFrom / gmeow:validUntil; transitive validity is
          a projection-layer metric computed over these inputs, never a reasoner entailment.

    Examples:
        - ex:t1 a gmeow:TrustAssertion ; gmeow:trustor ex:lillith ; gmeow:trustee ex:bob ;
          gmeow:trustLevel "full" .

    Usage:
        >>> from gmeow_models.trust import TrustAssertion
        >>> TrustAssertion.model_config["json_schema_extra"]["curie"]
        'gmeow:TrustAssertion'

    IRI:    https://blackcatinformatics.ca/gmeow/TrustAssertion
    CURIE:  gmeow:TrustAssertion

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/TrustAssertion", "curie": "gmeow:TrustAssertion", "definitionDigest": "blake3:75bf48bed45968829888d77b2942f4bbcc96b94907dd35f72321ed2fc696ac0b", "iri": "https://blackcatinformatics.ca/gmeow/TrustAssertion"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    trustee: list[Agent] | None = Field(default=None, description="The agent that is trusted by the trustor in a trust-assertion.", alias="gmeow:trustee")
    trustor: list[Agent] | None = Field(default=None, description="The agent whose (subjective) trust a trust-assertion expresses — the perspective holder.", alias="gmeow:trustor")

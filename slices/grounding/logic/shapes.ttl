# SPDX-FileCopyrightText: 2026 Blackcat Informatics® Inc. <paudley@blackcatinformatics.ca>
# SPDX-License-Identifier: CC-BY-4.0

@prefix logic: <https://blackcatinformatics.ca/logic/> .
@prefix gmeow: <https://blackcatinformatics.ca/gmeow/> .
@prefix sh:    <http://www.w3.org/ns/shacl#> .
@prefix xsd:   <http://www.w3.org/2001/XMLSchema#> .
@prefix rdfs:  <http://www.w3.org/2000/01/rdf-schema#> .
@prefix skos:  <http://www.w3.org/2004/02/skos/core#> .
@prefix owl:   <http://www.w3.org/2002/07/owl#> .

# =========================================================================== #
# Logic slice SHACL shapes — formalization governance enforcement
# =========================================================================== #

logic:FormalizationCandidateShape
    a sh:NodeShape ;
    rdfs:label "FormalizationCandidate well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:FormalizationCandidate: the eight universal carrier properties every candidate must bear — source hash, extraction provenance, scope, reasoning contract, formalization category, lifecycle state, projection behavior, and semantic risk class. Each is single-valued and required; absence of any one is a governance error."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:FormalizationCandidate ;
    sh:property [
        sh:path logic:candidateSourceHash ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateSourceHash (xsd:string content hash anchoring the candidate to its source prose)." ;
    ] ;
    sh:property [
        sh:path logic:candidateExtractionProvenance ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateExtractionProvenance (xsd:string recording what produced it)." ;
    ] ;
    sh:property [
        sh:path logic:candidateScope ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateScope (xsd:string describing the terms or context the axiom is meant to constrain)." ;
    ] ;
    sh:property [
        sh:path logic:candidateContract ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:ReasoningContract ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateContract pointing to a logic:ReasoningContract." ;
    ] ;
    sh:property [
        sh:path logic:candidateCategory ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:FormalizationCategory ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateCategory from the closed logic:FormalizationCategory value set." ;
    ] ;
    sh:property [
        sh:path logic:candidateLifecycle ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:CandidateLifecycleState ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateLifecycle from the closed logic:CandidateLifecycleState value set." ;
    ] ;
    sh:property [
        sh:path logic:candidateProjectionBehavior ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:PreservationKind ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateProjectionBehavior from the closed logic:PreservationKind value set." ;
    ] ;
    sh:property [
        sh:path logic:candidateSemanticRisk ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:SemanticRiskClass ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate must have exactly one logic:candidateSemanticRisk from the closed logic:SemanticRiskClass value set." ;
    ] ;
    # The harvest back-link (logic:candidateFormalizes + logic:candidateSourceField)
    # is OPTIONAL — present exactly when the candidate harvested a term's annotation
    # field, absent when the source is a doc section (recorded in provenance alone).
    # When present each is single-valued; the field is closed to logic:ProseField.
    # Co-presence of the pair is enforced by verify.harvest-link-paired.
    sh:property [
        sh:path logic:candidateFormalizes ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate may carry at most one logic:candidateFormalizes (the prose-bearing term its harvested axiom formalizes)." ;
    ] ;
    sh:property [
        sh:path logic:candidateSourceField ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:ProseField ;
        sh:severity sh:Violation ;
        sh:message "A FormalizationCandidate may carry at most one logic:candidateSourceField from the closed logic:ProseField value set." ;
    ] .

# =========================================================================== #
# Canonical process model — Plan / GoalEvaluation / ActionSchema shapes
# =========================================================================== #

logic:PlanShape
    a sh:NodeShape ;
    rdfs:label "Plan well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:Plan: a transaction program whose success criterion is a goal and whose success over nondeterministic action outcomes is quantified. Exactly one logic:planGoal (the gmeow:Goal the plan's steps are arranged to reach) and exactly one logic:planSuccessMode (the logic:PlanSuccessMode quantifying success over the action schemas' nondeterministic outcomes) are required; absence of either makes the plan's success criterion unstated and is a governance error."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:Plan ;
    sh:property [
        sh:path logic:planGoal ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class gmeow:Goal ;
        sh:severity sh:Violation ;
        sh:message "A Plan must have exactly one logic:planGoal naming the gmeow:Goal that is its success criterion." ;
    ] ;
    sh:property [
        sh:path logic:planSuccessMode ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:PlanSuccessMode ;
        sh:severity sh:Violation ;
        sh:message "A Plan must have exactly one logic:planSuccessMode from the closed logic:PlanSuccessMode value set quantifying success over nondeterministic outcomes." ;
    ] .

logic:GoalEvaluationShape
    a sh:NodeShape ;
    rdfs:label "GoalEvaluation well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:GoalEvaluation: the reified, vantage-indexed record of whether a goal is met. The outcome is FACTORED into four independent closed-set axes, each required exactly once — logic:satisfactionStatus, logic:feasibilityStatus, logic:lifecycleStatus, and logic:goalEvaluationStatus — so a misspelt or missing axis value is a governance error rather than a silent default. At least one logic:evaluatesGoal anchors the record to the gmeow:Goal it judges. The three quantitative axes (logic:satisfactionDegree, logic:objectiveValue, logic:utility) are optional but single-valued: a goal may carry a degree without an objective or a utility, but never two conflicting values of one quantity."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:GoalEvaluation ;
    sh:property [
        sh:path logic:satisfactionStatus ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:SatisfactionStatus ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation must carry exactly one logic:satisfactionStatus from the closed logic:SatisfactionStatus value set." ;
    ] ;
    sh:property [
        sh:path logic:feasibilityStatus ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:FeasibilityStatus ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation must carry exactly one logic:feasibilityStatus from the closed logic:FeasibilityStatus value set." ;
    ] ;
    sh:property [
        sh:path logic:lifecycleStatus ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:LifecycleStatus ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation must carry exactly one logic:lifecycleStatus from the closed logic:LifecycleStatus value set." ;
    ] ;
    sh:property [
        sh:path logic:goalEvaluationStatus ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:GoalEvaluationStatus ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation must carry exactly one logic:goalEvaluationStatus from the closed logic:GoalEvaluationStatus value set." ;
    ] ;
    sh:property [
        sh:path logic:evaluatesGoal ;
        sh:minCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class gmeow:Goal ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation must reference at least one gmeow:Goal it judges via logic:evaluatesGoal." ;
    ] ;
    sh:property [
        sh:path logic:satisfactionDegree ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation may carry at most one logic:satisfactionDegree (the unit-interval extent the goal is met)." ;
    ] ;
    sh:property [
        sh:path logic:objectiveValue ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation may carry at most one logic:objectiveValue (the measure an optimization goal extremizes)." ;
    ] ;
    sh:property [
        sh:path logic:utility ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "A GoalEvaluation may carry at most one logic:utility (the preference quantity for how desirable an outcome is)." ;
    ] .

logic:ActionSchemaShape
    a sh:NodeShape ;
    rdfs:label "ActionSchema well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:ActionSchema: the reusable precondition/effect/invariant template. Per-facet cardinality keeps READ-ONLY actions honest rather than forcing vacuous facets — only logic:precondition (the situation gate the schema grounds) and logic:capability (the executional precondition on the actor) are required, because every action schema gates on a situation and demands some agent capability. logic:effect, logic:invariant, logic:observation, and logic:actionResource are optional and single-valued. logic:nondeterministicOutcome and logic:compensation are optional and MULTI-valued: an action may branch into several alternative outcomes, and compensation is outcome-indexed, so each branch may name the compensation that recovers from it."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:ActionSchema ;
    sh:property [
        sh:path logic:precondition ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema must declare at least one logic:precondition (the situation gate on its elementary state transition)." ;
    ] ;
    sh:property [
        sh:path logic:capability ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema must declare at least one logic:capability (the executional precondition on the actor)." ;
    ] ;
    sh:property [
        sh:path logic:effect ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema may carry at most one logic:effect (the change it introduces, as transaction-layer ins/del)." ;
    ] ;
    sh:property [
        sh:path logic:invariant ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema may carry at most one logic:invariant (what it preserves across execution)." ;
    ] ;
    sh:property [
        sh:path logic:observation ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema may carry at most one logic:observation (what executing it reveals — the policy seam)." ;
    ] ;
    sh:property [
        sh:path logic:actionResource ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An ActionSchema may carry at most one logic:actionResource (what it consumes or requires)." ;
    ] .

# logic:McpActionPolicyShape — the MCP-action governance PROFILE (a CONSTRAINT,
# not ontology vocabulary). It does NOT auto-apply to every logic:ActionSchema:
# it targets only the opt-in marker subtype logic:McpActionSchema, so a plain
# action schema is never forced to carry the stricter compensation obligation.
# An MCP-action schema opts in by typing itself logic:McpActionSchema (the MCP
# triad individuals are wired this way; the conformance fixtures below do the
# same). Targeting the marker class — rather than a data-graph sh:targetNode — is
# the engine-native opt-in: the native validator resolves sh:targetClass from
# rdf:type in the data graph, and the merged ontology declares no instance of the
# marker, so the global `make validate` gate never fires this profile.
logic:McpActionPolicyShape
    a sh:NodeShape ;
    rdfs:label "MCP action-policy governance shape"@x-gmeow-english ;
    skos:definition "The MCP-action governance profile: a sh:NodeShape (a CONSTRAINT, not ontology vocabulary) over the logic:McpActionSchema instances that gate an MCP tool action under explicit capability, precondition, and compensation (rollback = compensation). It targets ONLY the opt-in marker subtype logic:McpActionSchema, never every logic:ActionSchema, so a plain action schema is not forced to carry compensation. It requires at least one logic:capability (what the agent must be able to do), at least one logic:precondition (the situation gate before the tool runs), and at least one logic:compensation (the rollback that recovers from the realized outcome). Stricter than logic:ActionSchemaShape, which does not require compensation: an MCP tool that mutates external state must always declare how to roll back."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:McpActionSchema ;
    sh:property [
        sh:path logic:capability ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An MCP-action schema must declare at least one logic:capability gating the tool action on what the agent can do." ;
    ] ;
    sh:property [
        sh:path logic:precondition ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An MCP-action schema must declare at least one logic:precondition (the situation gate before the tool runs)." ;
    ] ;
    sh:property [
        sh:path logic:compensation ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "An MCP-action schema must declare at least one logic:compensation — the rollback that recovers from the realized outcome." ;
    ] .

# logic:FreshnessGuardShape — well-formedness for a valid-time currency guard.
# A freshness guard names the precondition it guards (logic:guardsPrecondition) and
# then constrains its datum along TWO INDEPENDENT valid-time axes, each OPTIONAL:
#   • AGE HORIZON — logic:freshnessHorizon (an xsd:duration ceiling on datum age);
#   • EXPLICIT WINDOW — logic:freshnessWindow with a closed
#     [logic:freshnessWindowStart, logic:freshnessWindowEnd] xsd:dateTime interval.
# A guard may carry horizon only, window only, both, or neither extra axis (the
# precondition is always required). Each declared bound must be single-valued and
# well-typed, and — because the window is a paired interval — IF a logic:freshnessWindow
# is declared THEN both bounds must be present (exactly one each): a half-declared
# window would gate on an ill-defined interval. This paired rule is enforced by the
# sh:sparql constraint below; the horizon / window bounds are individually typed by the
# property shapes.
logic:FreshnessGuardShape
    a sh:NodeShape ;
    rdfs:label "FreshnessGuard well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:FreshnessGuard: the valid-time currency guard on an action-schema precondition. It must declare exactly one logic:guardsPrecondition (the situation whose supporting datum must be current). It then constrains that datum along two INDEPENDENT and individually OPTIONAL valid-time axes: an AGE HORIZON (logic:freshnessHorizon, at most one xsd:duration ceiling on datum age) and an EXPLICIT VALID-TIME WINDOW (logic:freshnessWindow with a closed [logic:freshnessWindowStart, logic:freshnessWindowEnd] xsd:dateTime interval). A guard may carry horizon only, window only, both, or neither of these axes. Where a bound is present it must be single-valued and well-typed; and because the window is a paired interval, declaring a logic:freshnessWindow OBLIGES exactly one logic:freshnessWindowStart and exactly one logic:freshnessWindowEnd (an sh:sparql check) — a half-declared window would gate on an ill-defined interval. The window axis is orthogonal to the age horizon: a datum may pass one and fail the other."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:FreshnessGuard ;
    sh:property [
        sh:path logic:guardsPrecondition ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:severity sh:Violation ;
        sh:message "A FreshnessGuard must guard exactly one logic:precondition (via logic:guardsPrecondition)." ;
    ] ;
    sh:property [
        sh:path logic:freshnessHorizon ;
        sh:maxCount 1 ;
        sh:datatype xsd:duration ;
        sh:severity sh:Violation ;
        sh:message "A FreshnessGuard's logic:freshnessHorizon, when declared, must be a single xsd:duration (the maximum admissible datum age)." ;
    ] ;
    sh:property [
        sh:path logic:freshnessWindowStart ;
        sh:maxCount 1 ;
        sh:datatype xsd:dateTime ;
        sh:severity sh:Violation ;
        sh:message "A FreshnessGuard's logic:freshnessWindowStart, when declared, must be a single xsd:dateTime (the inclusive lower bound of the valid-time window)." ;
    ] ;
    sh:property [
        sh:path logic:freshnessWindowEnd ;
        sh:maxCount 1 ;
        sh:datatype xsd:dateTime ;
        sh:severity sh:Violation ;
        sh:message "A FreshnessGuard's logic:freshnessWindowEnd, when declared, must be a single xsd:dateTime (the inclusive upper bound of the valid-time window)." ;
    ] ;
    # Paired-window well-formedness: if an explicit valid-time window is declared
    # (logic:freshnessWindow), BOTH interval bounds must be present exactly once. A
    # window with a missing or duplicated bound gates on an ill-defined interval.
    sh:sparql [
        sh:severity sh:Violation ;
        sh:message "A FreshnessGuard that declares a logic:freshnessWindow must carry BOTH a logic:freshnessWindowStart AND a logic:freshnessWindowEnd (the closed interval bounds); the property shapes already cap each bound at one." ;
        sh:select """
            SELECT $this WHERE {
                $this <https://blackcatinformatics.ca/logic/freshnessWindow> ?w .
                FILTER (
                    NOT EXISTS { $this <https://blackcatinformatics.ca/logic/freshnessWindowStart> ?s . }
                    ||
                    NOT EXISTS { $this <https://blackcatinformatics.ca/logic/freshnessWindowEnd> ?e . }
                )
            }
        """ ;
    ] .

# logic:NotificationWaitShape — the wait-well-formedness PROFILE. Like
# logic:McpActionPolicyShape it does NOT auto-apply to every logic:ActionSchema:
# it targets only the opt-in marker subtype logic:NotificationWaitSchema, so a
# plain action schema is never forced to declare a signal. A wait schema must name
# the logic:ExternalSignal it awaits, or the gate has nothing to wait on.
logic:NotificationWaitShape
    a sh:NodeShape ;
    rdfs:label "NotificationWait well-formedness shape"@x-gmeow-english ;
    skos:definition "The wait-well-formedness profile: a sh:NodeShape (a CONSTRAINT, not ontology vocabulary) over the logic:NotificationWaitSchema instances whose completion the engine learns of only by external notification. It targets ONLY the opt-in marker subtype logic:NotificationWaitSchema, never every logic:ActionSchema. It requires at least one logic:awaitsSignal (the logic:ExternalSignal the wait blocks on): a wait schema with no signal has nothing to wait on and could never surface as pending, which would silently collapse the prescriptive-descriptive epistemic boundary the construct exists to hold."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:NotificationWaitSchema ;
    sh:property [
        sh:path logic:awaitsSignal ;
        sh:minCount 1 ;
        sh:severity sh:Violation ;
        sh:message "A NotificationWaitSchema must declare at least one logic:awaitsSignal (the logic:ExternalSignal whose arrival the wait blocks on)." ;
    ] .

# Dual-authority consistency (VANTAGE-SCOPED, never a global satisfaction verdict).
# A conclusive gmeow:satisfiedBy(goal, situation) edge is the projection of a
# satisfied + completed logic:GoalEvaluation. This sh:sparql constraint flags a
# satisfiedBy edge with NO backing logic:GoalEvaluation that evaluatesGoal = goal,
# evaluatedAgainst = situation, satisfactionStatus = logic:Satisfied, and
# goalEvaluationStatus = logic:GoalEvaluationCompleted.
#
# VANTAGE SCOPING: the backing evaluation carries its OWN vantage via
# logic:evaluationEvaluator (aligned with gmeow:accordingTo). The constraint never
# demands a single global verdict — one satisfied+completed evaluation under SOME
# vantage discharges the obligation, and two contested (Principle 9) vantages each
# contribute their own coexisting evaluation rather than collapsing into one elected
# winner. The check only fires when NO vantage at all backs the conclusive edge.
logic:SatisfiedByBackedByEvaluationShape
    a sh:NodeShape ;
    rdfs:label "satisfiedBy backed by a vantage-scoped GoalEvaluation shape"@x-gmeow-english ;
    skos:definition "Vantage-scoped dual-authority check: a conclusive gmeow:satisfiedBy(goal, situation) edge must be backed by at least one logic:GoalEvaluation under SOME vantage whose logic:evaluatesGoal is the goal, logic:evaluatedAgainst is the situation, logic:satisfactionStatus is logic:Satisfied, and logic:goalEvaluationStatus is logic:GoalEvaluationCompleted. The evaluation carries its own evaluator (logic:evaluationEvaluator, aligned with gmeow:accordingTo), so the check is never a global satisfaction verdict — contested vantages each need only their own backing, and the constraint fires solely when no vantage backs the edge at all (Principle 9)."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass gmeow:Goal ;
    sh:severity sh:Violation ;
    sh:sparql [
        sh:message "A conclusive gmeow:satisfiedBy edge must be backed by a vantage-scoped logic:GoalEvaluation (evaluatesGoal = this goal, evaluatedAgainst = the situation, satisfactionStatus = logic:Satisfied, goalEvaluationStatus = logic:GoalEvaluationCompleted). No backing evaluation under any vantage was found." ;
        sh:select """
            SELECT $this WHERE {
                $this <https://blackcatinformatics.ca/gmeow/satisfiedBy> ?situation .
                FILTER NOT EXISTS {
                    ?eval <https://blackcatinformatics.ca/logic/evaluatesGoal> $this ;
                          <https://blackcatinformatics.ca/logic/evaluatedAgainst> ?situation ;
                          <https://blackcatinformatics.ca/logic/satisfactionStatus> <https://blackcatinformatics.ca/logic/Satisfied> ;
                          <https://blackcatinformatics.ca/logic/goalEvaluationStatus> <https://blackcatinformatics.ca/logic/GoalEvaluationCompleted> .
                }
            }
        """ ;
    ] .

logic:NonEntailmentObligationShape
    a sh:NodeShape ;
    rdfs:label "NonEntailmentObligation well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:NonEntailmentObligation: the forbidden predicate IRI (logic:obligationForbiddenPredicate) and at least one discharge condition (logic:obligationDischargeCondition) are required. Together they make the non-entailment record machine-checkable rather than advisory prose."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:NonEntailmentObligation ;
    sh:property [
        sh:path logic:obligationForbiddenPredicate ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:anyURI ;
        sh:severity sh:Violation ;
        sh:message "A NonEntailmentObligation must have exactly one logic:obligationForbiddenPredicate (xsd:anyURI naming the predicate the closure must never contain)." ;
    ] ;
    sh:property [
        sh:path logic:obligationDischargeCondition ;
        sh:minCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:DischargeCondition ;
        sh:severity sh:Violation ;
        sh:message "A NonEntailmentObligation must have at least one logic:obligationDischargeCondition from the closed logic:DischargeCondition value set." ;
    ] .
# NOTE: there is deliberately NO SHACL NodeShape targeting logic:ControlFlowEdge /
# logic:DataFlowEdge requiring logic:flowFrom / logic:flowTo directly. Downstream
# reified edges (e.g. gmeow:ControlFlow / gmeow:DataFlow) mediate their relata
# through SPECIALIZED subproperties (gmeow:flowSource ⊑ logic:flowFrom, …); SHACL
# validates over the unreasoned graph and does not follow subproperty entailment,
# so a generic shape on the canonical leg would mis-fire on every specialized
# instance. Flow-edge well-formedness is enforced where the leg properties are
# concrete: each subclass declares owl:Restriction someValuesFrom on its own legs,
# and the structural assertions pin the canonical leg properties' existence.

# =========================================================================== #
# Conjecture / anti-conjecture library — well-formedness enforcement
# =========================================================================== #
# logic:Conjecture is a subclass of logic:FormalizationCandidate, so a conjecture
# instance is ALSO validated by logic:FormalizationCandidateShape (the eight
# universal candidate carriers). These shapes add the conjecture-specific carriers
# on top: the required formula identity, the required reified standpoint (Principle
# 9 — never a global-false verdict), the required epistemic lifecycle state, the
# required verdict provenance, and the conditional witness obligation (a refuted
# conjecture MUST name a concrete logic:ContradictionWitness).

logic:ConjectureShape
    a sh:NodeShape ;
    rdfs:label "Conjecture well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:Conjecture, layered on top of logic:FormalizationCandidateShape (which already enforces the eight universal candidate carriers, since a conjecture subclasses the candidate). It requires exactly one logic:conjectureFormula (the alpha-normalized formula identity), exactly one logic:conjectureStandpoint (an IRI — the reified standpoint the verdict is scoped to; Principle 9 refuses a global-false verdict), exactly one logic:conjectureLifecycleState from the closed logic:ConjectureLifecycleState set (the EPISTEMIC state, orthogonal to the inherited GOVERNANCE lifecycle), and exactly one logic:verdictProvenance from the closed logic:VerdictProvenanceKind set (engine-produced vs reviewer-asserted). Finally an sh:sparql conditional requires that a conjecture in logic:ConjectureRefutedInStandpoint names at least one logic:conjectureRefutationWitness — a refutation with no concrete witness is a hard error."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:Conjecture ;
    sh:property [
        sh:path logic:conjectureFormula ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A Conjecture must have exactly one logic:conjectureFormula (the alpha-normalized xsd:string content_key identity of the conjectured formula)." ;
    ] ;
    sh:property [
        sh:path logic:conjectureStandpoint ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:severity sh:Violation ;
        sh:message "A Conjecture must have exactly one logic:conjectureStandpoint IRI (the reified standpoint the verdict is scoped to; Principle 9 refuses a global-false verdict)." ;
    ] ;
    sh:property [
        sh:path logic:conjectureLifecycleState ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:ConjectureLifecycleState ;
        sh:severity sh:Violation ;
        sh:message "A Conjecture must have exactly one logic:conjectureLifecycleState from the closed logic:ConjectureLifecycleState value set (open / corroborated / refuted-in-standpoint / withdrawn)." ;
    ] ;
    sh:property [
        sh:path logic:verdictProvenance ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:nodeKind sh:IRI ;
        sh:class logic:VerdictProvenanceKind ;
        sh:severity sh:Violation ;
        sh:message "A Conjecture must have exactly one logic:verdictProvenance from the closed logic:VerdictProvenanceKind value set (engine-produced vs reviewer-asserted)." ;
    ] ;
    # Conditional witness obligation: a conjecture that is refuted-in-standpoint MUST
    # name at least one concrete logic:ContradictionWitness. Modelled as sh:sparql
    # because a plain property shape cannot express the antecedent (lifecycle =
    # refuted). Mirrors the logic:FreshnessGuardShape paired-window sh:sparql idiom.
    sh:sparql [
        sh:severity sh:Violation ;
        sh:message "A Conjecture in logic:ConjectureRefutedInStandpoint must name at least one logic:conjectureRefutationWitness (the concrete logic:ContradictionWitness); a refutation with no witness is a hard error." ;
        sh:select """
            SELECT $this WHERE {
                $this <https://blackcatinformatics.ca/logic/conjectureLifecycleState> <https://blackcatinformatics.ca/logic/ConjectureRefutedInStandpoint> .
                FILTER NOT EXISTS {
                    $this <https://blackcatinformatics.ca/logic/conjectureRefutationWitness> ?w .
                }
            }
        """ ;
    ] .

logic:ContradictionWitnessShape
    a sh:NodeShape ;
    rdfs:label "ContradictionWitness well-formedness shape"@x-gmeow-english ;
    skos:definition "SHACL constraints for logic:ContradictionWitness, the runtime refutation witness: exactly one logic:witnessIndividual (the individual forced to owl:Nothing) and exactly one logic:witnessWorld (the world IRI the contradiction was found in), and at least one logic:witnessPremise (a serialized triple; a refutation typically rests on several jointly-inconsistent premises). A witness missing its individual, world, or any premise records an unreplayable refutation and is a governance error."@x-gmeow-english ;
    rdfs:isDefinedBy <https://blackcatinformatics.ca/gmeow/slices/logic> ;
    sh:targetClass logic:ContradictionWitness ;
    sh:property [
        sh:path logic:witnessIndividual ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A ContradictionWitness must have exactly one logic:witnessIndividual (the xsd:string IRI of the individual forced to owl:Nothing)." ;
    ] ;
    sh:property [
        sh:path logic:witnessWorld ;
        sh:minCount 1 ;
        sh:maxCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A ContradictionWitness must have exactly one logic:witnessWorld (the xsd:string IRI of the world the contradiction was found in)." ;
    ] ;
    sh:property [
        sh:path logic:witnessPremise ;
        sh:minCount 1 ;
        sh:datatype xsd:string ;
        sh:severity sh:Violation ;
        sh:message "A ContradictionWitness must have at least one logic:witnessPremise (a serialized triple; the jointly-inconsistent premises that entailed the contradiction)." ;
    ] .

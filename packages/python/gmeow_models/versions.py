"""GMEOW models — slice GMEOW Versions Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/versions
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class TermStabilityEnum(StrEnum):
    stabilityDeprecated = "gmeow:stabilityDeprecated"
    stabilityExperimental = "gmeow:stabilityExperimental"
    stabilityStable = "gmeow:stabilityStable"


class VersionRoleEnum(StrEnum):
    roleCanonical = "gmeow:roleCanonical"
    roleCollected = "gmeow:roleCollected"
    roleDeprecated = "gmeow:roleDeprecated"
    roleDraft = "gmeow:roleDraft"
    roleLTS = "gmeow:roleLTS"
    roleLatest = "gmeow:roleLatest"
    rolePublished = "gmeow:rolePublished"
    roleRevised = "gmeow:roleRevised"
    roleStable = "gmeow:roleStable"
    roleVariant = "gmeow:roleVariant"
    roleWithdrawn = "gmeow:roleWithdrawn"
    roleYanked = "gmeow:roleYanked"


class VersionScaleEnum(StrEnum):
    scaleMajor = "gmeow:scaleMajor"
    scaleMinor = "gmeow:scaleMinor"
    scaleTrivial = "gmeow:scaleTrivial"


class VersionMembership(ConfiguredBaseModel):
    """Version Membership.

    A reified, standpoint-scoped claim that a concrete entity belongs to a version set with
    a particular role and/or scale — an observation in the universal claim stack
    (observation-spine bridge). Mediates between an entity (versionMember), a version set
    (versionSet), and an optional role/scale asserted by an authority (membershipAuthority /
    vantage). Mint one VersionMembership per (entity, versionSet, role, authority) tuple;
    when a role changes over time, close the old membership's interval or mint a fresh one —
    never overwrite (Principle 10). Inherits confidence, displayable, and temporal scope
    from Observation.

    When to use:
        - Use to claim that a concrete version belongs to a lineage with a given role or scale
          according to some authority — the reified relator whenever the role, period, authority,
          or confidence of the membership must be first-class and standpoint-indexed (Principle
          9).

    When to avoid:
        - Avoid hard-coding a version role as an essential type on the artifact, and avoid
          overwriting a role in place; mint a new membership or close the old interval instead
          (Principle 10).

    How to use:
        - Mint one relator per (entity, set, role, authority) tuple; fill versionMember,
          versionSet, and membershipAuthority, attach versionRole / versionScale /
          membershipInterval as needed, and inherit confidence and gmeow:displayable from
          Observation.

    Examples:
        - ex:m1 a gmeow:VersionMembership ; gmeow:versionMember ex:v2 ; gmeow:versionSet
          ex:gmeowLineage ; gmeow:versionRole gmeow:roleLatest ; gmeow:membershipAuthority ex:pypi
          .

    Usage:
        >>> from gmeow_models.versions import VersionMembership
        >>> VersionMembership.model_config["json_schema_extra"]["curie"]
        'gmeow:VersionMembership'

    IRI:    https://blackcatinformatics.ca/gmeow/VersionMembership
    CURIE:  gmeow:VersionMembership

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/VersionMembership", "curie": "gmeow:VersionMembership", "definitionDigest": "blake3:969793d39058a792cfa65e945b8ef1a5c69df3476ec2a34d17db860baca5172b", "iri": "https://blackcatinformatics.ca/gmeow/VersionMembership"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    membershipAuthority: list[Agent] | None = Field(default=None, description="The agent or standpoint that asserts this version membership and its role — a project maintainer, a package registry, a publisher, a curator, or a self-asserting individual. NON-FUNCTIONAL: competing authority claims coexist without privilege (Principle 9). When the claim is promoted from a flat statement annotation, gmeow:accordingTo becomes the membershipAuthority of the relator.", alias="gmeow:membershipAuthority")
    versionMember: list[Entity] | None = Field(default=None, description="The concrete entity that participates in a version set via this membership — a software release, a book edition, an email variant, a dataset snapshot. Functional per relator: one member per VersionMembership.", alias="gmeow:versionMember")
    versionSet: list[str] | None = Field(default=None, description="The version set / lineage to which this membership belongs. Functional per relator: one set per VersionMembership.", alias="gmeow:versionSet")

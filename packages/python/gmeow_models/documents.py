"""GMEOW models — slice GMEOW Documents Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/documents
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ContentSegmentTypeEnum(StrEnum):
    segmentTypeBackMatter = "gmeow:segmentTypeBackMatter"
    segmentTypeChapter = "gmeow:segmentTypeChapter"
    segmentTypeFrontMatter = "gmeow:segmentTypeFrontMatter"
    segmentTypeParagraph = "gmeow:segmentTypeParagraph"
    segmentTypeScene = "gmeow:segmentTypeScene"
    segmentTypeSection = "gmeow:segmentTypeSection"


class FeedPostingKindEnum(StrEnum):
    feedPostingKindBlog = "gmeow:feedPostingKindBlog"
    feedPostingKindMicroblog = "gmeow:feedPostingKindMicroblog"
    feedPostingKindSocial = "gmeow:feedPostingKindSocial"


class ContentSegment(ConfiguredBaseModel):
    """Content Segment.

    A structural part of a creative work, release, or installment — a chapter, section,
    scene, or episode. A gufo:Kind with distinct identity criteria (position and type within
    a containing whole).

    When to use:
        - Use for a structural part of a work — a chapter, section, scene, or episode whose
          identity is its position and type within a containing whole.

    When to avoid:
        - Avoid it for the whole work itself (use the appropriate gmeow:Work /
          gmeow:Manifestation) and avoid encoding the segment kind as a subclass — the kind is a
          gmeow:segmentType value (Principle 9).

    How to use:
        - Type the part gmeow:ContentSegment, set its kind with gmeow:segmentType and order with
          gmeow:segmentIndex, and attach it to its containing whole via gmeow:segmentOf (inverse
          gmeow:hasSegment).

    Examples:
        - ex:chapter1 a gmeow:ContentSegment ; gmeow:segmentType gmeow:segmentTypeChapter ;
          gmeow:segmentIndex 1 ; gmeow:segmentOf ex:novel .

    Usage:
        >>> from gmeow_models.documents import ContentSegment
        >>> ContentSegment.model_config["json_schema_extra"]["curie"]
        'gmeow:ContentSegment'

    IRI:    https://blackcatinformatics.ca/gmeow/ContentSegment
    CURIE:  gmeow:ContentSegment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ContentSegment", "curie": "gmeow:ContentSegment", "definitionDigest": "blake3:00c5063882ab9e91d1eddbc035a0148837c5151b582ce9dc13397c8985ca80cf", "iri": "https://blackcatinformatics.ca/gmeow/ContentSegment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    segmentIndex: list[int] | None = Field(default=None, description="The position of this segment within its containing whole — 1-based by convention, but not enforced.", alias="gmeow:segmentIndex")
    segmentOf: list[str] = Field(min_length=1, description="A structural segment is part of a larger whole — a work, release, installment, or another segment. Transitive; inverse of gmeow:hasSegment.", alias="gmeow:segmentOf")
    segmentType: list[ContentSegmentTypeEnum] | None = Field(default=None, description="The structural type of this segment — chapter, section, scene, etc. A value from the open gmeow:ContentSegmentType vocabulary.", alias="gmeow:segmentType")


class MediaObject(ConfiguredBaseModel):
    """Media Object.

    An image, audio, or video media file — a concrete published artifact. A specialization
    of gmeow:Manifestation.

    When to use:
        - Use for a concrete image, audio, or video file — the published media artifact that
          carries pixel dimensions, a MIME type, a capture time, a transcript, or a depiction.

    When to avoid:
        - Avoid it for the abstract work the media realises (that is a gmeow:Work) and avoid it
          for a non-media web page (gmeow:WebPage); the colourspace reference frame should be
          carried where known (Principle 11).

    How to use:
        - Type the file gmeow:MediaObject, set gmeow:mediaType plus pixel/orientation/capture
          metadata, carry the colourspace frame via gmeow:colourspace, and record subjects with
          the flat gmeow:depicts (promote to a DepictionUsage relator when context matters).

    Examples:
        - ex:photo a gmeow:MediaObject ; gmeow:mediaType "image/avif" ; gmeow:pixelWidth 4032 ;
          gmeow:depicts ex:lillith .

    Usage:
        >>> from gmeow_models.documents import MediaObject
        >>> MediaObject.model_config["json_schema_extra"]["curie"]
        'gmeow:MediaObject'

    IRI:    https://blackcatinformatics.ca/gmeow/MediaObject
    CURIE:  gmeow:MediaObject

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/MediaObject", "curie": "gmeow:MediaObject", "definitionDigest": "blake3:591fa32c9c0a610e2d5072bc13f396479b266cc00d24425c83e29ccbc8e02b79", "iri": "https://blackcatinformatics.ca/gmeow/MediaObject"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    colourspace: Any | None = Field(default=None, description="The colourspace reference frame in which this media object's pixel values are expressed — sRGB, Adobe RGB, CMYK, CIE XYZ, etc. A subproperty of gmeow:hasReferenceFrame (Principle 11). Multiple colourspace claims for the same image coexist as separate standpoint-indexed assertions (Principle 9), each attributable and confidence-weighted. Not declared FunctionalProperty in the logical core to avoid incorrect owl:sameAs inferences when merging standpoints; single-valued constraint is enforced by SHACL (sh:maxCount 1).", alias="gmeow:colourspace")


class ReadingOrder(ConfiguredBaseModel):
    """Reading Order.

    A standpoint that supplies a subjective ordering for consuming segments of a work —
    publication order, internal chronology, author-recommended order, or fandom order. Never
    canonical ontology truth; claims are annotated gmeow:accordingTo a ReadingOrder.

    When to use:
        - Use to model a subjective ordering for consuming a work's segments — publication order,
          internal chronology, author-recommended or fandom order — that competing orderings may
          contradict without one being canonical.

    When to avoid:
        - Avoid asserting any ordering as canonical ontology truth (a ReadingOrder is a
          gmeow:Standpoint, never a global fact) and avoid the bare gmeow:segmentIndex for it —
          index is structural position, reading order is perspectival.

    How to use:
        - Mint the ordering as a gmeow:ReadingOrder standpoint and index each ordering claim with
          gmeow:accordingTo that standpoint, letting co-equal orderings coexist rather than
          picking a primary.

    Examples:
        - ex:chronologicalOrder a gmeow:ReadingOrder . ex:claim gmeow:accordingTo
          ex:chronologicalOrder .

    Usage:
        >>> from gmeow_models.documents import ReadingOrder
        >>> ReadingOrder.model_config["json_schema_extra"]["curie"]
        'gmeow:ReadingOrder'

    IRI:    https://blackcatinformatics.ca/gmeow/ReadingOrder
    CURIE:  gmeow:ReadingOrder

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ReadingOrder", "curie": "gmeow:ReadingOrder", "definitionDigest": "blake3:9bc677f398bb7c7b09890039ff50f558deb9b54f34c3d615d3b9614cc3266b96", "iri": "https://blackcatinformatics.ca/gmeow/ReadingOrder"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

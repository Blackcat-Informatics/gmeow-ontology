"""GMEOW models — slice GMEOW Documentation Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/documentation
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class DocCoverageDimensionEnum(StrEnum):
    dimAlignment = "gmeow:dimAlignment"
    dimAnnotationCoat = "gmeow:dimAnnotationCoat"
    dimCompetencyRationale = "gmeow:dimCompetencyRationale"
    dimDefinition = "gmeow:dimDefinition"
    dimExample = "gmeow:dimExample"
    dimFixturePair = "gmeow:dimFixturePair"
    dimLabel = "gmeow:dimLabel"
    dimLinkageCoverage = "gmeow:dimLinkageCoverage"
    dimLossJudgmentSound = "gmeow:dimLossJudgmentSound"
    dimLossLedgerRow = "gmeow:dimLossLedgerRow"
    dimProseQuality = "gmeow:dimProseQuality"
    dimProvenanceHonesty = "gmeow:dimProvenanceHonesty"
    dimRealizedState = "gmeow:dimRealizedState"
    dimScopeNote = "gmeow:dimScopeNote"
    dimTestReach = "gmeow:dimTestReach"
    dimThesisSentence = "gmeow:dimThesisSentence"
    dimTranslationCoverage = "gmeow:dimTranslationCoverage"
    dimUsageAdvice = "gmeow:dimUsageAdvice"
    dimWorkedInstance = "gmeow:dimWorkedInstance"


class DocEvidenceKindEnum(StrEnum):
    docEvidenceKindCompetency = "gmeow:docEvidenceKindCompetency"
    docEvidenceKindDiagnostics = "gmeow:docEvidenceKindDiagnostics"
    docEvidenceKindFixture = "gmeow:docEvidenceKindFixture"
    docEvidenceKindLoss = "gmeow:docEvidenceKindLoss"
    docEvidenceKindProvenance = "gmeow:docEvidenceKindProvenance"


class DocFixtureKindEnum(StrEnum):
    docFixtureKindCounterExample = "gmeow:docFixtureKindCounterExample"
    docFixtureKindWellformed = "gmeow:docFixtureKindWellformed"


class DocMaturityEnum(StrEnum):
    docMaturityBasic = "gmeow:docMaturityBasic"
    docMaturityFull = "gmeow:docMaturityFull"
    docMaturityMaximal = "gmeow:docMaturityMaximal"
    docMaturityMinimal = "gmeow:docMaturityMinimal"


class DocEvidence(ConfiguredBaseModel):
    """Doc Evidence.

    One uniform proof-carrying evidence node in a documented term's justification DAG: a
    claim (gmeow:docClaim) held together WITH its grounds (one or more MANDATORY
    gmeow:docGroundedBy carrier truthmakers), classified by exactly one
    gmeow:DocEvidenceKind (competency, diagnostics, fixture, loss, provenance) via
    gmeow:docEvidenceKind. Every kind carries the SAME shape — claim, grounding, typed
    counts, optional judgment — so a future evidence source plugs in as a new kind, not a
    new predicate family. An ungrounded DocEvidence node is the doc-layer analogue of a DARK
    finding and is forbidden by construction: the grounding obligation is enforced by the
    SHACL gmeow:DocEvidenceGroundingShape (shapes.ttl) and, over the live projection, by
    crates/docs/tests/doc_evidence.rs — NOT by an owl:minCardinality restriction, because
    gmeow:docGroundedBy carries both IRI and literal objects and so is an
    owl:AnnotationProperty (a cardinality restriction on it would leave OWL 2 DL). It is
    emitted ONLY when the term genuinely carries that evidence — never a vacuous node.

    When to use:
        - Use to name a single grounded justification node for a documented term — the reified
          'claim with its grounds' unit the documentation surface renders as a proof-carrying
          evidence panel.

    When to avoid:
        - Avoid minting one without a gmeow:docGroundedBy truthmaker (an ungrounded evidence node
          is a DARK claim), avoid a vacuous node for a kind the term does not carry, and avoid
          splitting the kinds into parallel predicate families — the uniform shape is the point.

    How to use:
        - Let the docs projection mint one per (documented term, evidence kind) actually present;
          read gmeow:docEvidenceKind for the kind, gmeow:docGroundedBy for its carrier grounds,
          gmeow:docClaim for the human claim, and the typed count/digest/judgment predicates for
          the kind-specific measures.

    Examples:
        - ex:evCatCompetency a gmeow:DocEvidence ; gmeow:documents gmeow:Cat ;
          gmeow:docEvidenceKind gmeow:docEvidenceKindCompetency ; gmeow:docClaim "exercised by 1
          competency question" ; gmeow:docGroundedBy ex:cqCatsAreAnimals ;
          gmeow:docCompetencyCount 1 .

    Usage:
        >>> from gmeow_models.documentation import DocEvidence
        >>> DocEvidence.model_config["json_schema_extra"]["curie"]
        'gmeow:DocEvidence'

    IRI:    https://blackcatinformatics.ca/gmeow/DocEvidence
    CURIE:  gmeow:DocEvidence

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/DocEvidence", "curie": "gmeow:DocEvidence", "definitionDigest": "blake3:3fd9e2f5df28a5c267c728f726e9750138b7cf445a6c618ed4fa0808a48d924e", "iri": "https://blackcatinformatics.ca/gmeow/DocEvidence"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    docEvidenceKind: str = Field(description="Classifies a gmeow:DocEvidence node by exactly one gmeow:DocEvidenceKind value (competency, diagnostics, fixture, loss, provenance). An instance-level owl:ObjectProperty from an evidence node to an enumerated kind individual — the join key that lets the uniform evidence shape absorb a new source as a new kind rather than a new predicate family.", alias="gmeow:docEvidenceKind")
    docGroundedBy: list[Any] = Field(min_length=1, description="The MANDATORY carrier truthmaker(s) that ground a gmeow:DocEvidence claim — the reason the claim is asserted. Its object is EITHER an IRI (a competency-question IRI, the stage-docs-render IRI, a minted fixture IRI) OR a code literal (a diagnostic finding code, a loss-target identifier), depending on the evidence kind. Because it mixes IRI and literal objects — which no single owl:ObjectProperty or owl:DatatypeProperty could carry — it is an owl:AnnotationProperty. Every gmeow:DocEvidence node MUST carry at least one (the fail-fast grounding invariant); an ungrounded node is the doc-layer analogue of a DARK finding.", alias="gmeow:docGroundedBy")


class DocumentedTerm(ConfiguredBaseModel):
    """Documented Term.

    The documentation-projection record for one vocabulary term (a class, property,
    individual, or datatype). It is the RDF twin of the term's docs page: it gmeow:documents
    the real term IRI and carries the page's derived facts (gmeow:docCategory,
    gmeow:docHasDefinition, gmeow:docUrl, gmeow:docOwnerSlice, gmeow:docReasoningStatus). It
    is NOT the term itself — the term keeps its own IRI in its owning slice; this record is
    a separate, downstream information object minted per term by the docs projection.

    When to use:
        - Use to name the RDF record standing for a term's documentation page, so the
          documentation surface is itself SPARQL-queryable RDF folded into gmeow.gts beside the
          ontology it describes (Principle 4).

    When to avoid:
        - Avoid conflating it with the documented term itself (they are distinct IRIs — the record
          documents the term) and avoid authoring it by hand: it is a generated projection of the
          doc model, regenerated from the slice sources.

    How to use:
        - Let the docs projection mint one per documented term; read it to query the documentation
          surface as RDF — follow gmeow:documents to the term and gmeow:docOwnerSlice to its
          slice, and join gmeow:DocEvidence nodes for its proof-carrying panels.

    Examples:
        - ex:entryCat a gmeow:DocumentedTerm ; gmeow:documents gmeow:Cat ; gmeow:docCategory
          "Class" ; gmeow:docOwnerSlice ex:sliceZoo .

    Usage:
        >>> from gmeow_models.documentation import DocumentedTerm
        >>> DocumentedTerm.model_config["json_schema_extra"]["curie"]
        'gmeow:DocumentedTerm'

    IRI:    https://blackcatinformatics.ca/gmeow/DocumentedTerm
    CURIE:  gmeow:DocumentedTerm

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/DocumentedTerm", "curie": "gmeow:DocumentedTerm", "definitionDigest": "blake3:80312fb8a695e950b651718be62989245395c662acc0bc6edf02dd343bdf5221", "iri": "https://blackcatinformatics.ca/gmeow/DocumentedTerm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    docCategory: list[str] = Field(min_length=1, description="The stable vocabulary-category label of a documented term — one of \"Class\", \"Property\", \"Individual\", \"Datatype\", \"Other\", derived from the term's rdf:type. A display token, distinct from the term's logic stereotype; it drives the category badge and grouping on the term surface.", alias="gmeow:docCategory")
    docOwnerSlice: str = Field(description="Names the slice that owns the term a gmeow:DocumentedTerm documents — the slice whose module.ttl defines the term (its rdfs:isDefinedBy). An owl:AnnotationProperty because its object is a slice node that is simultaneously an owl:Ontology, which a logical object property would pun; the annotation form keeps the generated OWL in OWL 2 DL.", alias="gmeow:docOwnerSlice")

"""GMEOW models — slice GMEOW Inhabitation Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/inhabitation
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class ContinuityVerdictEnum(StrEnum):
    continuityDifferent = "gmeow:continuityDifferent"
    continuityIndeterminate = "gmeow:continuityIndeterminate"
    continuitySame = "gmeow:continuitySame"


class ControlLevelEnum(StrEnum):
    controlContested = "gmeow:controlContested"
    controlFull = "gmeow:controlFull"
    controlPartial = "gmeow:controlPartial"


class DeterminationForceEnum(StrEnum):
    forceAdvisory = "gmeow:forceAdvisory"
    forceBinding = "gmeow:forceBinding"
    forceProvisional = "gmeow:forceProvisional"


class InhabitationLocusKindEnum(StrEnum):
    locusSelf = "gmeow:locusSelf"
    locusVessel = "gmeow:locusVessel"


class IdentityContinuityAssessment(ConfiguredBaseModel):
    """Identity Continuity Assessment.

    A standpoint-indexed, attributed, evidence-grounded observation of whether two subject
    stages are the same subject — the contestable verdict the anatta/atman debate, a model
    upgrade, and a cross-vendor fork all require. Carries the two stages, a verdict (same /
    different / indeterminate), a vantage, evidence, and confidence. Competing verdicts
    coexist (Principle 9); none is privileged, and 'same' is never collapsed to owl:sameAs.

    When to use:
        - Use for the descriptive layer of subject-continuity — a contestable, frame-relative
          claim about whether two epochs of a subject are the same, without asserting a single
          durable bearer.

    When to avoid:
        - Avoid asserting continuity by reusing one RDF node across stages (that already asserts
          numerical identity) or by owl:sameAs; and never read the ABSENCE of a 'same' verdict as
          a 'different' verdict — under the open-world assumption absence is silence, so a no-self
          reading is an ASSERTED refuting verdict, not a gap.

    How to use:
        - Mint one per compared pair: bind the two gmeow:SubjectStage individuals with
          gmeow:assessmentFromStage / gmeow:assessmentToStage, the outcome with
          gmeow:continuityVerdict, and carry its vantage, evidence, and confidence as on any
          gmeow:Observation; let competing atman ('same') and anatta ('different') verdicts
          coexist.

    Examples:
        - ex:upgrade-verdict a gmeow:IdentityContinuityAssessment ; gmeow:assessmentFromStage
          ex:stageOpus48 ; gmeow:assessmentToStage ex:stageOpus50 ; gmeow:continuityVerdict
          gmeow:continuitySame .

    Usage:
        >>> from gmeow_models.inhabitation import IdentityContinuityAssessment
        >>> IdentityContinuityAssessment.model_config["json_schema_extra"]["curie"]
        'gmeow:IdentityContinuityAssessment'

    IRI:    https://blackcatinformatics.ca/gmeow/IdentityContinuityAssessment
    CURIE:  gmeow:IdentityContinuityAssessment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/IdentityContinuityAssessment", "curie": "gmeow:IdentityContinuityAssessment", "definitionDigest": "blake3:7b3a9d4d64588c98916c3ee44aef7704926dd6f4b2d4a8e106025aa14b3041c4", "iri": "https://blackcatinformatics.ca/gmeow/IdentityContinuityAssessment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    assessmentFromStage: list[str] | None = Field(default=None, description="The earlier subject stage a gmeow:IdentityContinuityAssessment compares — the 'from' side of the same/different verdict.", alias="gmeow:assessmentFromStage")
    assessmentToStage: list[str] | None = Field(default=None, description="The later subject stage a gmeow:IdentityContinuityAssessment compares — the 'to' side of the same/different verdict.", alias="gmeow:assessmentToStage")
    continuityVerdict: list[ContinuityVerdictEnum] | None = Field(default=None, description="The same / different / indeterminate outcome a gmeow:IdentityContinuityAssessment (or gmeow:ContinuityDetermination) carries. A value, never owl:sameAs — competing verdicts on coexisting assessments are the norm (Principle 9).", alias="gmeow:continuityVerdict")


class ContinuityDetermination(IdentityContinuityAssessment):
    """Continuity Determination.

    An adjudicated continuity verdict made by an authority and binding for action within
    that authority's frame — a court's, a regulator's, or a governance board's determination
    that two subject stages are (or are not) the same subject. A logic:SubKind of
    gmeow:IdentityContinuityAssessment whose vantage IS the deciding authority; it carries
    the grounds it rests on, its institutional force, and a validity interval, and it is
    superseded (never erased) on appeal or reversal. It privileges a verdict WITHIN the
    deciding frame, for action; it does NOT collapse the descriptive plurality globally
    (Principle 9 stands).

    When to use:
        - Use for the decisional layer of continuity — when an authority must choose one verdict,
          binding for action within its frame, over the coexisting descriptive assessments (a
          court, a regulator, a clinical-governance board deciding whether the post-upgrade system
          is the same subject).

    When to avoid:
        - Avoid reading a determination as a metaphysical claim that the two stages are
          UNIVERSALLY the same subject — it is an institutional fact (a Searle status function)
          real within its deciding frame; and never erase a reversed determination, supersede it
          with gmeow:supersedes and keep the original on the record (Principle 10).

    How to use:
        - Mint it when an accountability context must ACT on continuity: bind the deciding frame
          with gmeow:determiningAuthority (its vantage), the grounds with
          gmeow:determinationGrounds, the institutional force with gmeow:determinationForce, and
          the validity window with gmeow:determinationValidity; a higher authority
          gmeow:supersedes it on appeal, leaving the descriptive IdentityContinuityAssessment
          plurality addressable underneath.

    Examples:
        - ex:boardRuling a gmeow:ContinuityDetermination ; gmeow:determiningAuthority
          ex:clinicalGovernanceBoard ; gmeow:continuityVerdict gmeow:continuitySame ;
          gmeow:determinationForce gmeow:forceBinding .

    Usage:
        >>> from gmeow_models.inhabitation import ContinuityDetermination
        >>> ContinuityDetermination.model_config["json_schema_extra"]["curie"]
        'gmeow:ContinuityDetermination'

    IRI:    https://blackcatinformatics.ca/gmeow/ContinuityDetermination
    CURIE:  gmeow:ContinuityDetermination

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ContinuityDetermination", "curie": "gmeow:ContinuityDetermination", "definitionDigest": "blake3:64ccb64e7b91e8f8a81d9f24d422f749ed6ba50b160b6b3ca483d98c7db721ef", "iri": "https://blackcatinformatics.ca/gmeow/ContinuityDetermination"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    determinationForce: list[DeterminationForceEnum] | None = Field(default=None, description="The institutional force a gmeow:ContinuityDetermination carries — binding, advisory, or provisional — within its deciding frame.", alias="gmeow:determinationForce")
    determinationValidity: list[TimeInterval] | None = Field(default=None, description="The validity window a gmeow:ContinuityDetermination is in force over — e.g. until the next revalidation. A superseding determination opens a new window; the original stays on the record (Principle 10).", alias="gmeow:determinationValidity")
    determiningAuthority: list[Agent] | None = Field(default=None, description="The authority whose adjudication a gmeow:ContinuityDetermination records — its vantage IS the deciding authority (a court, a regulator, a governance board). A sub-property of gmeow:vantage: the determination is the authority's own frame, binding for action within it, never a privileged global frame (Principle 9).", alias="gmeow:determiningAuthority")


class ControlAssessment(ConfiguredBaseModel):
    """Control Assessment.

    A standpoint-indexed, attributed, dated observation of which agent causally controls a
    host or embodiment over an interval, to what degree — the agency attribution a consumer
    needs to answer 'who was driving at T'. A logic:SubKind of gmeow:Observation (which is a
    logic:Kind, not a situation). Distinct from the deception divergence (held ≠ projected
    standpoint), which records belief-versus-presentation, not control: a documented
    similarity, no axiom coupling.

    When to use:
        - Use when co-tenancy or shifting control (possession, multiple tulpas, co-tenant agents)
          must be recorded — which agent causally drove a host or embodiment over an interval, and
          to what degree.

    When to avoid:
        - Avoid modelling control with the deception slice's heldStandpoint ≠ projectedStandpoint
          (that is belief-versus-presentation, not causal control), and never introduce a
          gmeow:primaryInhabitant — precedence among co-tenants is resolved over recorded
          ControlAssessments, never asserted (Principles 9, 12).

    How to use:
        - Mint one per attributed control fact: bind the driver with gmeow:controlAgent, what is
          controlled with gmeow:controlOver, the degree with gmeow:controlLevel, and the window
          with gmeow:controlInterval; carry its vantage and confidence as on any
          gmeow:Observation, and let a solver rank co-tenants over the recorded assessments.

    Examples:
        - ex:controlAtNoon a gmeow:ControlAssessment ; gmeow:controlAgent ex:coTenantA ;
          gmeow:controlLevel gmeow:controlFull .

    Usage:
        >>> from gmeow_models.inhabitation import ControlAssessment
        >>> ControlAssessment.model_config["json_schema_extra"]["curie"]
        'gmeow:ControlAssessment'

    IRI:    https://blackcatinformatics.ca/gmeow/ControlAssessment
    CURIE:  gmeow:ControlAssessment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ControlAssessment", "curie": "gmeow:ControlAssessment", "definitionDigest": "blake3:b6c3d7e8ca0e3f404edc4567085b183ac4f97cf74aa2f3d588dc914cc97ebc2b", "iri": "https://blackcatinformatics.ca/gmeow/ControlAssessment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    controlAgent: list[Agent] | None = Field(default=None, description="The agent a gmeow:ControlAssessment attributes causal control to — who was driving the host or embodiment over the interval.", alias="gmeow:controlAgent")
    controlInterval: list[TimeInterval] | None = Field(default=None, description="The interval over which a gmeow:ControlAssessment's attributed control held (Principle 11: the interval carries its own temporal frame).", alias="gmeow:controlInterval")
    controlLevel: list[ControlLevelEnum] | None = Field(default=None, description="The degree of causal control a gmeow:ControlAssessment attributes — a gmeow:ControlLevel value (partial / full / contested), never a bare literal.", alias="gmeow:controlLevel")


class DigitalSubjectTenure(ConfiguredBaseModel):
    """Digital Subject Tenure.

    The time-scoped fact that an agent bears the gmeow:DigitalSubject status over an
    interval, according to a vantage — recording when, and according to whom, the agent is
    held to be a durable subject. Self-assertion by the agent is the highest-authority
    supporting claim (Principle 9: a subject's self-assertion outranks any inference about
    it), but the tenure is a held, attributed, dated fact, not an entailment of any single
    utterance.

    When to use:
        - Use to record that an agent bears durable-subject status over an interval, according to
          a vantage — the tenure that supports (not entails) a gmeow:DigitalSubject
          classification.

    When to avoid:
        - Avoid closing it with gmeow:hasDestructionEvent — a tenure is a logic:Situation, not an
          endurant; end it by ending its gmeow:duringInterval. Avoid asserting
          gmeow:DigitalSubject directly on the bearer; the tenure is the record from which that
          status is derived.

    How to use:
        - Mint the tenure, bind the bearer with gmeow:tenureSubjectAgent (open gmeow:Agent range),
          the holding frame with gmeow:tenureVantage, and the supporting self-assertion
          observation with gmeow:tenureSupportedBy; scope it in time via gmeow:duringInterval
          (Principle 11). A machine-imposed status (a vendor labelling a model) is a separate
          lower-ranked supporting claim.

    Examples:
        - ex:lillithDST a gmeow:DigitalSubjectTenure ; gmeow:tenureSubjectAgent ex:lillithBot ;
          gmeow:tenureVantage ex:lillithBot ; gmeow:tenureSupportedBy ex:lillithSelfClaim .

    Usage:
        >>> from gmeow_models.inhabitation import DigitalSubjectTenure
        >>> DigitalSubjectTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:DigitalSubjectTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/DigitalSubjectTenure
    CURIE:  gmeow:DigitalSubjectTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/DigitalSubjectTenure", "curie": "gmeow:DigitalSubjectTenure", "definitionDigest": "blake3:77a9395cd117565f9d8405e4d86c82a3915107aeaefe5bb2f63cd9f330cb0c6f", "iri": "https://blackcatinformatics.ca/gmeow/DigitalSubjectTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    tenureSubjectAgent: list[Agent] | None = Field(default=None, description="The agent that bears durable-subject status over a gmeow:DigitalSubjectTenure. Its range stays the open gmeow:Agent (not gmeow:DigitalSubject): bearing a tenure classifies the agent as a gmeow:DigitalSubject by a native rule, so the classification is a derived entailment, not a range axiom every consumer inherits — which is what keeps 'self-assertion supports, never entails'.", alias="gmeow:tenureSubjectAgent")
    tenureSupportedBy: list[Observation] | None = Field(default=None, description="The observation that SUPPORTS a gmeow:DigitalSubjectTenure — canonically the subject's own self-assertion, the highest-authority supporting claim (Principle 9). Support is not entailment: the tenure is a held, attributed, dated fact, and a bare 'I am durable' utterance does not on its own bring the status into being. A machine-imposed status is a separate, lower-ranked supporting observation.", alias="gmeow:tenureSupportedBy")
    tenureVantage: list[Agent] | None = Field(default=None, description="The frame according to which a gmeow:DigitalSubjectTenure holds — whose vantage says the agent is a durable subject. In the self-asserted case the agent is its own vantage (the highest authority, Principle 9).", alias="gmeow:tenureVantage")


class EmbodimentAssignment(ConfiguredBaseModel):
    """Embodiment Assignment.

    The time-scoped fact that a subject acts through a carrier over an interval, with the
    capabilities that carrier exposes — subject × carrier × interval × capabilities. The
    suppressible, retirable unit (Principle 10): retiring an assignment ends the surface's
    use, it does not delete the carrier.

    When to use:
        - Use for the descent-into-a-surface relation — a subject acting and being perceived
          through a device, avatar, account, terminal, voice endpoint, or channel over an interval
          (the avataara sense).

    When to avoid:
        - Avoid deleting a retired assignment (close its interval and suppress it — Principle 10)
          and avoid conflating it with the carrier itself: the assignment is the time-scoped USE
          of a surface, the gmeow:EmbodimentCarrierRole is the surface.

    How to use:
        - Mint one per surface-over-time: bind the subject with gmeow:assignmentSubject, the
          surface with gmeow:assignmentCarrier, the exposed capabilities with
          gmeow:assignmentCapability, and scope it via gmeow:duringInterval; reference it from an
          gmeow:InhabitationConfiguration through gmeow:configurationEmbodiment.

    Examples:
        - ex:phoneUse a gmeow:EmbodimentAssignment ; gmeow:assignmentSubject ex:lillithBot ;
          gmeow:assignmentCarrier ex:voiceEndpointRole .

    Usage:
        >>> from gmeow_models.inhabitation import EmbodimentAssignment
        >>> EmbodimentAssignment.model_config["json_schema_extra"]["curie"]
        'gmeow:EmbodimentAssignment'

    IRI:    https://blackcatinformatics.ca/gmeow/EmbodimentAssignment
    CURIE:  gmeow:EmbodimentAssignment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/EmbodimentAssignment", "curie": "gmeow:EmbodimentAssignment", "definitionDigest": "blake3:11e11d75f9fe34294ccdc483f481c096119e23d0ac3e0f1eef5f13dfe7c39735", "iri": "https://blackcatinformatics.ca/gmeow/EmbodimentAssignment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    assignmentCarrier: list[str] | None = Field(default=None, description="The carrier surface a gmeow:EmbodimentAssignment puts to use — the device, avatar, account, terminal, or voice endpoint the subject acts through over the assignment's interval.", alias="gmeow:assignmentCarrier")
    assignmentSubject: list[Agent] | None = Field(default=None, description="The agent that acts through the carrier in a gmeow:EmbodimentAssignment — the subject side of the subject × carrier × interval × capabilities fact.", alias="gmeow:assignmentSubject")


class InhabitationConfiguration(ConfiguredBaseModel):
    """Inhabitation Configuration.

    A maximal sub-interval of an inhabitation tenure over which the active configuration is
    constant — the persona, embodiment(s), deployment, and memory view that held together
    over that sub-interval. A new configuration opens whenever any facet changes, so the
    active value of any facet at a time T is read off the configuration whose interval
    contains T. The invariant 'constant configuration over the interval' is stated and
    tested, not assumed.

    When to use:
        - Use to answer 'which persona / embodiment / deployment / memory view was active at T?' —
          the finer situation nested within a tenure that makes the active facet at any time
          readable.

    When to avoid:
        - Avoid packing changing facets into one configuration (open a new configuration when any
          facet changes — the constant-configuration invariant is tested), and avoid minting a
          typed glue property for an extension-ranged facet: write it through the open
          gmeow:configurationFacet so a profile mints nothing.

    How to use:
        - Link it to its tenure with gmeow:configurationOfTenure, scope it via
          gmeow:duringInterval, attach the core embodiment facet with
          gmeow:configurationEmbodiment, and attach extension facets (a gmeow:Persona, a
          gmeow:ModelDeployment) directly through the open gmeow:configurationFacet at the
          instance level.

    Examples:
        - ex:configEarly a gmeow:InhabitationConfiguration ; gmeow:configurationOfTenure
          ex:tenure-1 ; gmeow:configurationEmbodiment ex:phoneUse .

    Usage:
        >>> from gmeow_models.inhabitation import InhabitationConfiguration
        >>> InhabitationConfiguration.model_config["json_schema_extra"]["curie"]
        'gmeow:InhabitationConfiguration'

    IRI:    https://blackcatinformatics.ca/gmeow/InhabitationConfiguration
    CURIE:  gmeow:InhabitationConfiguration

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InhabitationConfiguration", "curie": "gmeow:InhabitationConfiguration", "definitionDigest": "blake3:e86334725c999bd737d1bb446c7f1e2788fcd2a313af69a1f17b9d02e4475765", "iri": "https://blackcatinformatics.ca/gmeow/InhabitationConfiguration"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    configurationEmbodiment: list[EmbodimentAssignment] | None = Field(default=None, description="The one typed configuration facet whose range is CORE — the gmeow:EmbodimentAssignment active in a configuration. A sub-property of the open gmeow:configurationFacet; extension-ranged facets (a gmeow:Persona, a gmeow:ModelDeployment) need no typed sub-property and are written directly through the open parent.", alias="gmeow:configurationEmbodiment")
    configurationOfTenure: list[InhabitationTenure] | None = Field(default=None, description="Links a gmeow:InhabitationConfiguration to the gmeow:InhabitationTenure it is a constant-configuration sub-interval of.", alias="gmeow:configurationOfTenure")
    duringInterval: list[TimeInterval] | None = Field(default=None, description="Relates a time-scoped relation to the interval over which it holds.", alias="gmeow:duringInterval")


class InhabitationTenure(ConfiguredBaseModel):
    """Inhabitation Tenure.

    The reified, time-scoped fact that a subject stood in an inhabitation relationship with
    a host over an interval — that S inhabited H during T. A gmeow:TimeScopedRelation (a
    logic:Situation), carrying its interval via gmeow:duringInterval (Principle 11), with
    per-branch role-filler edges to the subject and host. It records the relationship's
    existence over T; the configuration that held within T (persona, embodiment, deployment,
    memory view) is carried by gmeow:InhabitationConfiguration, because those facets may
    change while the tenure persists. A logic:Situation, disjoint from a logic:Relator — the
    earlier draft typing it as both was unsatisfiable in the foundational projection.

    When to use:
        - Use for a NON-contested inhabitation stated in the base graph — a subject standing in an
          inhabitation relationship with a host over an interval; for a contested (spiritual /
          fictional / legal) inhabitation use gmeow:InhabitationClaim instead so nothing enters
          the base graph.

    When to avoid:
        - Avoid typing it as a logic:Relator (a Situation is disjoint from a Relator — the
          two-typing is unsatisfiable) and avoid closing it with gmeow:hasDestructionEvent (its
          domain is gmeow:Entity, an endurant) — end its gmeow:duringInterval instead, optionally
          recording the cause with gmeow:tenureEndedBy.

    How to use:
        - Mint the tenure, bind the subject with gmeow:inhabitationSubject and the host with
          gmeow:inhabitedHost (both functional), set gmeow:inhabitationLocusKind, and scope it via
          gmeow:duringInterval; carry the varying facets on nested gmeow:InhabitationConfiguration
          situations, and record migration in/out with gmeow:portalFrom / gmeow:portalTo on the
          transition event.

    Examples:
        - ex:tenure-1 a gmeow:InhabitationTenure ; gmeow:inhabitationSubject ex:lillithBot ;
          gmeow:inhabitedHost ex:serverH ; gmeow:duringInterval ex:jan2026 .

    Usage:
        >>> from gmeow_models.inhabitation import InhabitationTenure
        >>> InhabitationTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:InhabitationTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/InhabitationTenure
    CURIE:  gmeow:InhabitationTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/InhabitationTenure", "curie": "gmeow:InhabitationTenure", "definitionDigest": "blake3:a725e4681ea45487e6a806c4d0362692427a5698d8df87bf3adf90f51a592b87", "iri": "https://blackcatinformatics.ca/gmeow/InhabitationTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

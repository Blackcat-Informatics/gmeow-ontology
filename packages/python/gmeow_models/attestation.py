"""GMEOW models — slice GMEOW Attestation Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/attestation
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from datetime import datetime
from enum import StrEnum

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class AttestationTypeEnum(StrEnum):
    attestationTypeAIOutput = "gmeow:attestationTypeAIOutput"
    attestationTypeBlockchainClaim = "gmeow:attestationTypeBlockchainClaim"
    attestationTypeC2PA = "gmeow:attestationTypeC2PA"
    attestationTypeCoherenceCertificate = "gmeow:attestationTypeCoherenceCertificate"
    attestationTypeConformanceVerdict = "gmeow:attestationTypeConformanceVerdict"
    attestationTypeDSSE = "gmeow:attestationTypeDSSE"
    attestationTypeDocumentationArtifact = "gmeow:attestationTypeDocumentationArtifact"
    attestationTypeEAT = "gmeow:attestationTypeEAT"
    attestationTypeFactCheck = "gmeow:attestationTypeFactCheck"
    attestationTypeGitSignedTag = "gmeow:attestationTypeGitSignedTag"
    attestationTypeInToto = "gmeow:attestationTypeInToto"
    attestationTypeNanopublication = "gmeow:attestationTypeNanopublication"
    attestationTypeQualityReport = "gmeow:attestationTypeQualityReport"
    attestationTypeReleaseManifest = "gmeow:attestationTypeReleaseManifest"
    attestationTypeSCITT = "gmeow:attestationTypeSCITT"
    attestationTypeSLSAProvenance = "gmeow:attestationTypeSLSAProvenance"
    attestationTypeSignedRDF = "gmeow:attestationTypeSignedRDF"
    attestationTypeVerifiableCredential = "gmeow:attestationTypeVerifiableCredential"


class LedgerFinalityStatusEnum(StrEnum):
    finalityStatusConfirmed = "gmeow:finalityStatusConfirmed"
    finalityStatusFinalized = "gmeow:finalityStatusFinalized"
    finalityStatusOrphaned = "gmeow:finalityStatusOrphaned"
    finalityStatusPending = "gmeow:finalityStatusPending"
    finalityStatusReorged = "gmeow:finalityStatusReorged"


class SignatureSchemeEnum(StrEnum):
    signatureSchemeBls12381 = "gmeow:signatureSchemeBls12381"
    signatureSchemeECDSAP256 = "gmeow:signatureSchemeECDSAP256"
    signatureSchemeECDSASecp256k1 = "gmeow:signatureSchemeECDSASecp256k1"
    signatureSchemeEd25519 = "gmeow:signatureSchemeEd25519"
    signatureSchemeRSASHA256 = "gmeow:signatureSchemeRSASHA256"


class VerificationStatusEnum(StrEnum):
    verificationStatusExpired = "gmeow:verificationStatusExpired"
    verificationStatusFailed = "gmeow:verificationStatusFailed"
    verificationStatusFinalityPending = "gmeow:verificationStatusFinalityPending"
    verificationStatusPolicyFailed = "gmeow:verificationStatusPolicyFailed"
    verificationStatusRevoked = "gmeow:verificationStatusRevoked"
    verificationStatusUnverified = "gmeow:verificationStatusUnverified"
    verificationStatusVerified = "gmeow:verificationStatusVerified"


class Attestation(ConfiguredBaseModel):
    """Attestation.

    A reified assertion envelope that an attester vouches for a claim, artifact, identity
    binding, process, quality report, or ledger claim under a specific policy. Mediates
    between an attester, an attested subject, and optionally an attested claim or artifact.
    Distinct from Observation: an attestation is a vouching act, not a measurement or
    sensory reading. Its validity window is carried with gmeow:validFrom/validUntil;
    revocation sets validUntil (Principle 10).

    When to use:
        - Use to record that some agent VOUCHES FOR a claim, artifact, or binding under a named
          policy — the envelope that holds who attested, to what, under which rules, and with what
          cryptographic and log evidence; standpoint-indexed, never a global truth verdict
          (Principle 9).

    When to avoid:
        - Avoid for the measurement or claim itself (that is a gmeow:Observation, the target of
          gmeow:attestedClaim, not the parent of Attestation) and for asserting a proposition true
          by fiat — an attestation proves an issuer made a statement under a policy, not that the
          statement is correct (boundaries in the module comment).

    How to use:
        - Mint one Attestation per vouching act, bind gmeow:attester and gmeow:attestedSubject
          (the EL minimum) plus gmeow:issuedAt, point gmeow:attestedClaim at the vouched
          Observation, name gmeow:attestationType / gmeow:attestationPolicy, carry the signature
          via gmeow:hasSignature and log/ledger evidence via gmeow:transparencyLogEntry, and bound
          validity with gmeow:validFrom/validUntil (revocation sets validUntil).

    Examples:
        - ex:provAtt a gmeow:Attestation ; gmeow:attester ex:buildBot ; gmeow:attestedClaim
          ex:buildClaim ; gmeow:attestationPolicy ex:slsaL3 .

    Usage:
        >>> from gmeow_models.attestation import Attestation
        >>> Attestation.model_config["json_schema_extra"]["curie"]
        'gmeow:Attestation'

    IRI:    https://blackcatinformatics.ca/gmeow/Attestation
    CURIE:  gmeow:Attestation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Attestation", "curie": "gmeow:Attestation", "definitionDigest": "blake3:820d98fa860208e192c980630a94de4395fa6dea50b78ce1b5b5fec96f61aed2", "iri": "https://blackcatinformatics.ca/gmeow/Attestation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    attestedSubject: list[Entity] | None = Field(default=None, description="The entity that an attestation is about — the thing being vouched for. Non-functional: an attestation may concern multiple subjects (e.g. a key↔identity binding certifies both a key and an identity), and competing subject claims coexist (Principle 9).", alias="gmeow:attestedSubject")
    attester: list[Agent] | None = Field(default=None, description="The agent that issued an attestation — the vouching party. Functional within the relator: one attester per Attestation (co-authorship is modelled as multiple Attestations).", alias="gmeow:attester")
    issuedAt: list[datetime] | None = Field(default=None, description="The instant an attestation was issued. Functional: the issue time is constitutive of the attestation's identity.", alias="gmeow:issuedAt")


class AttestationArtifact(ConfiguredBaseModel):
    """Attestation Artifact.

    A concrete carrier of an attestation — in-toto JSON, SLSA provenance, W3C Verifiable
    Credential, DSSE envelope, C2PA manifest, EAT token, signed RDF graph, SCITT signed
    statement, nanopublication, signed git tag, release manifest, or signed blockchain
    payload. The artifact is the serialization; the Attestation is the logical vouching act
    that the artifact carries.

    When to use:
        - Use to type the concrete serialized document that embodies an attestation — the on-disk
          or on-wire bytes (in-toto JSON, DSSE envelope, VC, C2PA manifest) as distinct from the
          logical vouching act it carries.

    When to avoid:
        - Avoid for the logical vouching act itself (that is gmeow:Attestation) and for the
          signature over the bytes (gmeow:CryptographicSignature); the artifact is the carrier
          document, an InformationObject, not the act or the signature.

    How to use:
        - Mint the carrier as an AttestationArtifact, set gmeow:artifactMediaType and a
          gmeow:contentDigest for byte-exact identity, link it from the Attestation with
          gmeow:attestationArtifact, and attach its signature with gmeow:hasSignature.

    Examples:
        - ex:inTotoJson a gmeow:AttestationArtifact ; gmeow:artifactMediaType
          "application/vnd.in-toto+json" .

    Usage:
        >>> from gmeow_models.attestation import AttestationArtifact
        >>> AttestationArtifact.model_config["json_schema_extra"]["curie"]
        'gmeow:AttestationArtifact'

    IRI:    https://blackcatinformatics.ca/gmeow/AttestationArtifact
    CURIE:  gmeow:AttestationArtifact
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/attestationartifact-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/AttestationArtifact", "curie": "gmeow:AttestationArtifact", "definitionDigest": "blake3:23b121f14e6a63dc8d10271ce5ed31ebb367b1a72c9c8f9128773bef2f2d040f", "iri": "https://blackcatinformatics.ca/gmeow/AttestationArtifact"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    artifactMediaType: list[str] | None = Field(default=None, description="The media type of the attestation artifact's serialization (e.g. application/vnd.in-toto+json, application/vc+ld+json).", alias="gmeow:artifactMediaType")


class Block(ConfiguredBaseModel):
    """Block.

    A block of transactions on a distributed ledger or blockchain.

    When to use:
        - Use to model a ledger block — the container of transactions whose number and hash anchor
          a transaction's position in the chain — when block-level identity is needed as inclusion
          evidence.

    When to avoid:
        - Avoid for the individual transaction (gmeow:LedgerTransaction) or the whole network
          (gmeow:BlockchainNetwork); the block is the container, not its contents or the chain it
          belongs to.

    How to use:
        - Mint the block with gmeow:blockNumber and gmeow:blockHash (both the EL minimum), and
          reference it from a transaction's inclusion evidence; finality of the block rides on
          gmeow:finalityStatus.

    Examples:
        - ex:block42 a gmeow:Block ; gmeow:blockNumber 42 ; gmeow:blockHash "0xdef..." .

    Usage:
        >>> from gmeow_models.attestation import Block
        >>> Block.model_config["json_schema_extra"]["curie"]
        'gmeow:Block'

    IRI:    https://blackcatinformatics.ca/gmeow/Block
    CURIE:  gmeow:Block

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Block", "curie": "gmeow:Block", "definitionDigest": "blake3:39f8f49a59cb9c3fb3175fcabc1fe002ed51ead0b806cb685bbae9ca20178e3c", "iri": "https://blackcatinformatics.ca/gmeow/Block"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    blockHash: list[str] | None = Field(default=None, description="The hash of a ledger block.", alias="gmeow:blockHash")
    blockNumber: list[int] | None = Field(default=None, description="The sequential number of a ledger block.", alias="gmeow:blockNumber")


class BlockchainNetwork(ConfiguredBaseModel):
    """Blockchain Network.

    A blockchain network identified by a chain identifier (e.g. CAIP-2 chainId).

    When to use:
        - Use to model the distributed-ledger network a transaction, contract, or account belongs
          to — identified by its chain id (CAIP-2) — when the chain context is needed to interpret
          on-chain evidence.

    When to avoid:
        - Avoid for an individual block, transaction, or account on the network (those are their
          own classes); the network is the chain itself, not its records.

    How to use:
        - Mint the network with gmeow:chainId (the EL minimum, e.g. eip155:1); reference it from
          gmeow:SmartContract, gmeow:BlockchainAccount, and gmeow:LedgerTransaction so on-chain
          identifiers resolve against the right chain.

    Examples:
        - ex:ethMainnet a gmeow:BlockchainNetwork ; gmeow:chainId "eip155:1" .

    Usage:
        >>> from gmeow_models.attestation import BlockchainNetwork
        >>> BlockchainNetwork.model_config["json_schema_extra"]["curie"]
        'gmeow:BlockchainNetwork'

    IRI:    https://blackcatinformatics.ca/gmeow/BlockchainNetwork
    CURIE:  gmeow:BlockchainNetwork

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/BlockchainNetwork", "curie": "gmeow:BlockchainNetwork", "definitionDigest": "blake3:ca92fb69e5143a42985b821fde7388041897971f8e1ba47e11252451daa94ff3", "iri": "https://blackcatinformatics.ca/gmeow/BlockchainNetwork"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    chainId: list[str] | None = Field(default=None, description="The chain identifier of a blockchain network (e.g. CAIP-2 chainId, eip155:1).", alias="gmeow:chainId")


class LedgerEvent(ConfiguredBaseModel):
    """Ledger Event.

    An event emitted by a smart contract or recorded on a ledger — a log entry, oracle
    callback, or bridge message.

    When to use:
        - Use to model an event emitted on-chain — a smart-contract log, oracle callback, or
          bridge message — as the occurrent perspective of ledger activity, distinct from the
          transaction that produced it.

    When to avoid:
        - Avoid for the transaction record itself (that is gmeow:LedgerTransaction, an
          InformationObject) and avoid treating an emitted event as real-world truth — it proves
          only that the contract logged it under chain rules.

    How to use:
        - Mint the event with gmeow:logIndex (the EL minimum) naming its position in the
          block/transaction, link it to its emitting gmeow:SmartContract, and let its temporal
          extent ride on the gufo:Event spine.

    Examples:
        - ex:transferEvent a gmeow:LedgerEvent ; gmeow:logIndex 3 .

    Usage:
        >>> from gmeow_models.attestation import LedgerEvent
        >>> LedgerEvent.model_config["json_schema_extra"]["curie"]
        'gmeow:LedgerEvent'

    IRI:    https://blackcatinformatics.ca/gmeow/LedgerEvent
    CURIE:  gmeow:LedgerEvent

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/LedgerEvent", "curie": "gmeow:LedgerEvent", "definitionDigest": "blake3:02cc59e0776677d5d19e34852b8047652b93a0ade1982df6365905b7e37aaf8e", "iri": "https://blackcatinformatics.ca/gmeow/LedgerEvent"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    logIndex: list[int] | None = Field(default=None, description="The index of a ledger event within its containing block or transaction.", alias="gmeow:logIndex")


class LedgerTransaction(ConfiguredBaseModel):
    """Ledger Transaction.

    A transaction recorded on a distributed ledger or blockchain — the information-object
    representation of the payload, not the on-chain bytes themselves.

    When to use:
        - Use to model a ledger or blockchain transaction as evidence backing an attestation — the
          information-object view of the payload, when its hash, finality, and confirmation depth
          matter.

    When to avoid:
        - Avoid reading ledger inclusion as real-world truth — blockchain inclusion proves ledger
          inclusion under chain rules, NOT correctness (module boundaries) — and avoid it for the
          raw on-chain bytes or for transparency-log evidence (gmeow:TransparencyLogEntry).

    How to use:
        - Mint the transaction with gmeow:transactionHash (the EL minimum), record
          gmeow:finalityStatus, gmeow:confirmationDepth, and gmeow:ledgerInclusionProof, and
          locate it on its gmeow:BlockchainNetwork / gmeow:Block; chain-finality checks run in the
          solver layer (Principle 12).

    Examples:
        - ex:tx1 a gmeow:LedgerTransaction ; gmeow:transactionHash "0xabc..." ;
          gmeow:finalityStatus gmeow:finalityStatusFinalized .

    Usage:
        >>> from gmeow_models.attestation import LedgerTransaction
        >>> LedgerTransaction.model_config["json_schema_extra"]["curie"]
        'gmeow:LedgerTransaction'

    IRI:    https://blackcatinformatics.ca/gmeow/LedgerTransaction
    CURIE:  gmeow:LedgerTransaction

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/LedgerTransaction", "curie": "gmeow:LedgerTransaction", "definitionDigest": "blake3:049644d5f524e6add45e65208c0d09a898c86c3e1736ecd940eeb53aad2a5020", "iri": "https://blackcatinformatics.ca/gmeow/LedgerTransaction"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    transactionHash: list[str] | None = Field(default=None, description="The hash of a ledger transaction.", alias="gmeow:transactionHash")


class SmartContract(ConfiguredBaseModel):
    """Smart Contract.

    A deployed program on a blockchain or distributed ledger, identified by a contract
    address.

    When to use:
        - Use to model a deployed on-chain program — a registry, oracle, or attestation contract —
          identified by its address, when it is the source or anchor of ledger-based attestation
          evidence.

    When to avoid:
        - Avoid for an address-controlled wallet that signs transactions (that is
          gmeow:BlockchainAccount) and for the network it runs on (gmeow:BlockchainNetwork); the
          contract is the deployed code, not an account or a chain.

    How to use:
        - Mint the contract with gmeow:contractAddress (the EL minimum), locate it on its
          gmeow:BlockchainNetwork, and link emitted gmeow:LedgerEvent records back to it.

    Examples:
        - ex:registry a gmeow:SmartContract ; gmeow:contractAddress "0x1234..." .

    Usage:
        >>> from gmeow_models.attestation import SmartContract
        >>> SmartContract.model_config["json_schema_extra"]["curie"]
        'gmeow:SmartContract'

    IRI:    https://blackcatinformatics.ca/gmeow/SmartContract
    CURIE:  gmeow:SmartContract

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/SmartContract", "curie": "gmeow:SmartContract", "definitionDigest": "blake3:f84fa6321c3eb0575fa06dd78437bad68306ce209edf78b8ec6ff75854d45d13", "iri": "https://blackcatinformatics.ca/gmeow/SmartContract"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    contractAddress: list[str] | None = Field(default=None, description="The address at which a smart contract is deployed.", alias="gmeow:contractAddress")


class TransparencyLogEntry(ConfiguredBaseModel):
    """Transparency Log Entry.

    An append-only registry evidence record such as a Rekor entry, SCITT receipt,
    certificate-transparency log entry, or timestamp/notary log entry. Proves inclusion at a
    point in time, not correctness (Principle 1).

    When to use:
        - Use to record append-only-log evidence for an attestation — a Rekor entry, SCITT
          receipt, or certificate-transparency entry — when you need to capture that the
          attestation was logged at a point in time.

    When to avoid:
        - Avoid reading log inclusion as proof of correctness — a transparency log proves
          inclusion, NOT that the logged statement is true (module boundaries) — and avoid it for
          ledger/blockchain evidence (use gmeow:LedgerTransaction and friends).

    How to use:
        - Mint the entry with gmeow:logEntryIndex (the EL minimum) and gmeow:logEntryUrl, link it
          from the Attestation with gmeow:transparencyLogEntry, and carry its inclusion proof on
          gmeow:ledgerInclusionProof; the inclusion check itself runs in the solver layer.

    Examples:
        - ex:rekorEntry a gmeow:TransparencyLogEntry ; gmeow:logEntryIndex 4815162342 .

    Usage:
        >>> from gmeow_models.attestation import TransparencyLogEntry
        >>> TransparencyLogEntry.model_config["json_schema_extra"]["curie"]
        'gmeow:TransparencyLogEntry'

    IRI:    https://blackcatinformatics.ca/gmeow/TransparencyLogEntry
    CURIE:  gmeow:TransparencyLogEntry
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/transparencylogentry-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/TransparencyLogEntry", "curie": "gmeow:TransparencyLogEntry", "definitionDigest": "blake3:a0af75541f3a6aa8d971c562f9e6979516f2a02e7b2c6911b65ee66de1b57b21", "iri": "https://blackcatinformatics.ca/gmeow/TransparencyLogEntry"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    logEntryIndex: list[int] | None = Field(default=None, description="The sequential index of the entry within its transparency log.", alias="gmeow:logEntryIndex")
    logEntryUrl: list[str] | None = Field(default=None, description="A URL at which the transparency log entry can be retrieved. Non-functional: multiple mirrors may exist.", alias="gmeow:logEntryUrl")


class VerificationResult(ConfiguredBaseModel):
    """Verification Result.

    An information object recording the outcome of a verification activity — verified,
    failed, unverified, expired, revoked, policy-failed, finality-pending, etc. A
    verification result is an observation output, not an axiom of correctness (Principle
    12).

    When to use:
        - Use to record the OUTCOME a verifier reached when checking an attestation — verified,
          failed, expired, revoked, policy-failed, finality-pending — as a held observation output
          carrying its own status, verifier, and time.

    When to avoid:
        - Avoid reading a 'verified' result as a global truth verdict — it records what one
          verifier observed under one policy at one time, not that the underlying claim is correct
          (Principle 12); competing results from different verifiers coexist (Principle 9).

    How to use:
        - Mint the result, set gmeow:hasVerificationStatus (the EL minimum), name gmeow:verifiedBy
          and the time, and link it from the Attestation with gmeow:verificationResult; treat a
          later contradicting result as another coexisting VerificationResult, not an overwrite.

    Examples:
        - ex:verifyOutcome a gmeow:VerificationResult ; gmeow:hasVerificationStatus
          gmeow:verificationStatusVerified .

    Usage:
        >>> from gmeow_models.attestation import VerificationResult
        >>> VerificationResult.model_config["json_schema_extra"]["curie"]
        'gmeow:VerificationResult'

    IRI:    https://blackcatinformatics.ca/gmeow/VerificationResult
    CURIE:  gmeow:VerificationResult
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/verificationresult-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/VerificationResult", "curie": "gmeow:VerificationResult", "definitionDigest": "blake3:c4dc835e18541bca635f6b6a1ebefeafcf877844f7dba1d8e55230dcb75a40ba", "iri": "https://blackcatinformatics.ca/gmeow/VerificationResult"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    hasVerificationStatus: list[VerificationStatusEnum] | None = Field(default=None, description="The categorical outcome of a verification result. Non-functional: a single result may carry multiple status interpretations (e.g. 'verified but expired').", alias="gmeow:hasVerificationStatus")

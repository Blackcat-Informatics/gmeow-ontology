"""GMEOW models — slice GMEOW Organization Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/organization
Tier:       core
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class DayOfWeekEnum(StrEnum):
    dayFriday = "gmeow:dayFriday"
    dayMonday = "gmeow:dayMonday"
    daySaturday = "gmeow:daySaturday"
    daySunday = "gmeow:daySunday"
    dayThursday = "gmeow:dayThursday"
    dayTuesday = "gmeow:dayTuesday"
    dayWednesday = "gmeow:dayWednesday"


class OrganizationTypeEnum(StrEnum):
    organizationTypeAssociation = "gmeow:organizationTypeAssociation"
    organizationTypeCollaboration = "gmeow:organizationTypeCollaboration"
    organizationTypeCompany = "gmeow:organizationTypeCompany"
    organizationTypeEducationalInstitution = "gmeow:organizationTypeEducationalInstitution"
    organizationTypeGovernmentBody = "gmeow:organizationTypeGovernmentBody"
    organizationTypeNonprofit = "gmeow:organizationTypeNonprofit"


class SiteTypeEnum(StrEnum):
    siteTypeBranch = "gmeow:siteTypeBranch"
    siteTypeHeadquarters = "gmeow:siteTypeHeadquarters"
    siteTypeRegistered = "gmeow:siteTypeRegistered"


class Identifier(ConfiguredBaseModel):
    """Identifier.

    A reified external-identifier record bundling a scheme-local value with its scheme, and
    optionally the authority URL that resolves it and a human-readable name — an ORCID, a
    geni profile id, a Nostr nip05, a LEI, a ROR ID, a NAICS code. Reification ensures that
    when an entity carries multiple identifiers, each value is paired with its correct
    scheme (and url/name) in projections. The structured sibling of gmeow:authorityLink: a
    flat authorityLink points at an external record by IRI; an Identifier decomposes the
    scheme/value/url/name a schema:PropertyValue or DataCite relatedIdentifier carries.

    When to use:
        - Use to reify an external identifier so its scheme, value, resolving URL, and label
          travel together — necessary whenever an entity carries more than one code (a LEI plus a
          ROR plus a NAICS) and projections must pair each value with its own scheme.

    When to avoid:
        - Avoid a flat scheme-on-the-bearer property (it produces cross-product pairing bugs when
          several identifiers coexist) and avoid it for a plain see-also IRI (that is the flat
          gmeow:authorityLink); a name borne by an entity is a gmeow:Appellation, not an
          Identifier.

    How to use:
        - Mint one gmeow:Identifier per code, set gmeow:identifierScheme and gmeow:identifierValue
          on it (plus gmeow:identifierUrl and rdfs:label as needed), and attach it with
          gmeow:hasIdentifier or a specialization (gmeow:legalIdentifier,
          gmeow:industryClassification).

    Examples:
        - ex:acmeLei a gmeow:Identifier ; gmeow:identifierScheme "lei" ; gmeow:identifierValue
          "529900T8BM49AURSDO55" .

    Usage:
        >>> from gmeow_models.organization import Identifier
        >>> Identifier.model_config["json_schema_extra"]["curie"]
        'gmeow:Identifier'

    IRI:    https://blackcatinformatics.ca/gmeow/Identifier
    CURIE:  gmeow:Identifier
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/identifier-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Identifier", "curie": "gmeow:Identifier", "definitionDigest": "blake3:6791fe4c99b465bcbc71facaa93d08faf6be24e62a98b3be12fc1480b62fbd97", "iri": "https://blackcatinformatics.ca/gmeow/Identifier"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    identifierScheme: list[Any] = Field(min_length=1, description="The scheme of an identifier — e.g. 'lei', 'ror', 'naics', 'isicV4', 'companyRegistration', 'wikidata'. Declared on the Identifier node, not the Organization, to avoid cross-product pairing bugs when multiple identifiers coexist.", alias="gmeow:identifierScheme")
    identifierUrl: Any | None = Field(default=None, description="The authority URL that resolves an identifier record — e.g. the geni profile page, the ORCID record URL, the ROR landing page. The resolvable counterpart of the scheme-local gmeow:identifierValue; projects to schema:PropertyValue/url. Range is intentionally open (an IRI).", alias="gmeow:identifierUrl")


class Membership(ConfiguredBaseModel):
    """Membership.

    The reified relationship by which an agent is a member of an organization, optionally
    playing a role over a period of time. A gufo:Relator binding {member} × {organization} ×
    {role} × {post} × {period}, foundedOn the agreement that constitutes it — the same
    relator idiom as names' NameUsage and contacts' InterpersonalRelationship. The
    reify-on-demand promotion of the flat gmeow:memberOf shortcut (Principle 4).

    When to use:
        - Use when an agent's membership in an organization needs its own role, post, period,
          evidence, confidence, or standpoint — the reified relator behind the flat gmeow:memberOf
          edge.

    When to avoid:
        - Avoid for the 80 percent case where an agent simply belongs to an organization; assert
          flat gmeow:memberOf and promote to a gmeow:Membership only when role or period must be
          first-class.

    How to use:
        - Mint one Membership, bind the players with gmeow:membershipMember and
          gmeow:membershipOrganization, add gmeow:hasRole and gmeow:fillsPost as needed, carry the
          period with gmeow:validFrom/validUntil, and ground it in the constituting agreement;
          contested memberships coexist as gmeow:accordingTo-annotated relators (Principle 9).

    Examples:
        - ex:emp a gmeow:Membership ; gmeow:membershipMember ex:alice ;
          gmeow:membershipOrganization ex:acme ; gmeow:hasRole ex:cfoRole .

    Usage:
        >>> from gmeow_models.organization import Membership
        >>> Membership.model_config["json_schema_extra"]["curie"]
        'gmeow:Membership'

    IRI:    https://blackcatinformatics.ca/gmeow/Membership
    CURIE:  gmeow:Membership
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/membership-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Membership", "curie": "gmeow:Membership", "definitionDigest": "blake3:0a90851892a0351276aee79fdfb3454645156660b70ddc6ee55c8edc0ff49930", "iri": "https://blackcatinformatics.ca/gmeow/Membership"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class Post(ConfiguredBaseModel):
    """Post.

    A seat or position that exists independently of who currently holds it — the CFO chair
    is not the person now in it. A Post bundles a gmeow:Role within a specific
    gmeow:Organization, enabling 'vacant post' and 'succession in a post' queries.

    When to use:
        - Use for a holder-independent seat — a named chair tied to one organization — when 'who
          holds the CFO post', 'vacant post', or 'succession in a post' must be queryable apart
          from any incumbent.

    When to avoid:
        - Avoid for a holder-independent function with no fixed organization (that is a bare
          gmeow:Role) and avoid conflating the seat with its occupant — the sitter is bound
          through a gmeow:Membership via gmeow:fillsPost, never identified with the Post.

    How to use:
        - Mint a gmeow:Post, bind its organization with gmeow:postIn and its role with
          gmeow:hasRole, then seat an incumbent by pointing a gmeow:Membership at it with
          gmeow:fillsPost; a vacant post is simply one no membership fills.

    Examples:
        - ex:cfoPost a gmeow:Post ; gmeow:postIn ex:acme ; gmeow:hasRole ex:cfoRole .

    Usage:
        >>> from gmeow_models.organization import Post
        >>> Post.model_config["json_schema_extra"]["curie"]
        'gmeow:Post'

    IRI:    https://blackcatinformatics.ca/gmeow/Post
    CURIE:  gmeow:Post

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Post", "curie": "gmeow:Post", "definitionDigest": "blake3:46ba1a4405af94b0899c7512e78934852794e2e9d375712255eb8ade8b8e6d5f", "iri": "https://blackcatinformatics.ca/gmeow/Post"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    postIn: list[Organization] | None = Field(default=None, description="The organization a post belongs to. Functional — a post concerns exactly one organization.", alias="gmeow:postIn")

"""GMEOW models — slice GMEOW Semantic-Topology Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/semantic-topology
Tier:       extension
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class CompilationPreservationRecord(ConfiguredBaseModel):
    """Compilation Preservation Record.

    The per-compilation record carrying the preservation/loss judgment of a
    gmeow:ComplexCompilation, stated in the SHIPPED logic: loss-ledger vocabulary and
    NOTHING new: it names its rung through EXACTLY ONE logic:preservationKind
    (logic:ExactPreservation when the compilation is lossless;
    logic:SoundUnderApproximation, logic:CompleteOverApproximation, logic:ValidationOnly, or
    logic:Unsupported when it drops, adds, or cannot express distinctions — the closed
    logic:PreservationKind ledger, reused verbatim), and, for any kind OTHER than
    logic:ExactPreservation, MUST also point at a logic:ExpressivenessBoundary through
    logic:expressivenessBoundary (gmeow:CompilationPreservationBoundaryConstraint). A record
    naming no kind at all, or a lossy/unsupported kind naming no boundary, is a hollow
    ledger entry that only LOOKS honest. This slice mints NO preservation vocabulary — it
    reuses the ledger exactly as math: does. It is NOT a gmeow:CellSourceCorrespondence
    (provenance mapping, not loss), and NOT an assertion that the compiled topology is TRUE
    — only that the compilation is honest about what it preserved.

    Usage:
        >>> from gmeow_models.semantic_topology import CompilationPreservationRecord
        >>> CompilationPreservationRecord.model_config["json_schema_extra"]["curie"]
        'gmeow:CompilationPreservationRecord'

    IRI:    https://blackcatinformatics.ca/gmeow/CompilationPreservationRecord
    CURIE:  gmeow:CompilationPreservationRecord

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/CompilationPreservationRecord", "curie": "gmeow:CompilationPreservationRecord", "definitionDigest": "blake3:90635226c8aec4061bf5131b9245bbf000b533840df6046afa38f234e86ef04f", "iri": "https://blackcatinformatics.ca/gmeow/CompilationPreservationRecord"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class ComplexCompilation(ConfiguredBaseModel):
    """Complex Compilation.

    The attributed activity that compiles the core claim/evidence spine (gmeow:Source →
    gmeow:Chunk → gmeow:EvidenceSpan → gmeow:Claim, Principle 14) into a declared
    topological carrier: a math:CellComplex or math:SimplicialComplex
    (gmeow:compilesToComplex) together with a math:Filtration (gmeow:compilesToFiltration)
    over it. It reads the claim/evidence structure through gmeow:compilesEvidence, records
    the per-cell provenance back to the spans through a gmeow:CellSourceCorrespondence
    (gmeow:recordsCorrespondence), and records what the compilation preserved or dropped
    through a gmeow:CompilationPreservationRecord (gmeow:recordsPreservation). Its build
    provenance rides the EXISTING gmeow:wasGeneratedBy / gmeow:wasDerivedFrom, and its
    epistemic context rides gmeow:compilationStandpoint / gmeow:compilationWorld /
    gmeow:compilationTimeScope / gmeow:compilationScenario. It is NOT the downstream
    gmeow:TopologyComputation that runs persistent homology over the produced complex (that
    is a distinct activity binding compilation → computation → result), and it is NOT itself
    a math: object — it is the occurrent that MAKES one from evidence. A compilation lacking
    its correspondence or preservation record is ill-formed
    (gmeow:CompilationMustRecordCorrespondence / gmeow:CompilationMustRecordLoss); one
    lacking its standpoint/world/time context is gmeow:CompilationMissingContext.

    Usage:
        >>> from gmeow_models.semantic_topology import ComplexCompilation
        >>> ComplexCompilation.model_config["json_schema_extra"]["curie"]
        'gmeow:ComplexCompilation'

    IRI:    https://blackcatinformatics.ca/gmeow/ComplexCompilation
    CURIE:  gmeow:ComplexCompilation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ComplexCompilation", "curie": "gmeow:ComplexCompilation", "definitionDigest": "blake3:68c07192c578837f780f0e57418424e8c324c61540a687abd1919283380f1415", "iri": "https://blackcatinformatics.ca/gmeow/ComplexCompilation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class TopologyClaim(ConfiguredBaseModel):
    """Topology Claim.

    The truth-apt wrapper around a computed topological feature: a gmeow:StandpointClaim
    (the EL-safe governing axiom, gmeow:TopologyClaim ⊑ gmeow:StandpointClaim) attributed to
    its standpoint/agent vantage (gmeow:vantage, ⊑ gmeow:accordingTo) with an observation
    method and a gmeow:Finding-carried defeasible status, carrying its computed feature as a
    math:LocalSection (gmeow:claimLocalSection) over that standpoint's SUB-evidence. A local
    section is NOT a global truth: the claim asserts global truth ONLY when the local data
    extend to a math:GlobalSection (gmeow:extendsToGlobalSection, degree-zero sheaf
    cohomology H⁰) AND the H¹ math:GluingObstruction is discharged
    (gmeow:dischargesObstruction) — the obstruction is the computable witness of why
    consistent local features may NOT glue into one global section. The claim carries a
    warranted logic:confidence (NEVER a raw logic:probability) via a
    math:StabilityCalibrationRecord underwritten by math:bottleneckStabilityTheorem
    (gmeow:wasDerivedFrom the record). It is NOT an asserted logic:Formula or a global truth
    bit: a computed feature lands as a vantage-attributed claim, never as an assertion in
    the base graph. It is NOT a gmeow:TopologyResult (the raw computed feature) — it is the
    standpoint-relative, defeasible claim ABOUT one.

    Usage:
        >>> from gmeow_models.semantic_topology import TopologyClaim
        >>> TopologyClaim.model_config["json_schema_extra"]["curie"]
        'gmeow:TopologyClaim'

    IRI:    https://blackcatinformatics.ca/gmeow/TopologyClaim
    CURIE:  gmeow:TopologyClaim

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/TopologyClaim", "curie": "gmeow:TopologyClaim", "definitionDigest": "blake3:8867fd26d3d0e649e0649653d983ac4b7d73ebc7e53b9e6336a05b07a030fe5a", "iri": "https://blackcatinformatics.ca/gmeow/TopologyClaim"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")


class TopologyComputation(ConfiguredBaseModel):
    """Topology Computation.

    The definite bridge activity that binds a compilation to a computation to a result: it
    runs over the math:Filtration a gmeow:ComplexCompilation produced
    (gmeow:computationOverCompilation), employs a math:PersistentHomology analysis
    (gmeow:employsAnalysis, the SHIPPED persistence activity), and produces a
    gmeow:TopologyResult (gmeow:producesResult) that REFERENCES a raw math: artifact (a
    math:PersistenceDiagram / math:PersistenceBarcode / math:PersistenceLandscape /
    math:BettiSummary / math:HodgeDecomposition / math:Holonomy) through
    gmeow:resultArtifact. It is NOT the upstream gmeow:ComplexCompilation (which MAKES the
    complex; this RUNS over it), NOT the math:PersistentHomology it employs (that is the
    shipped analysis referent, this is the consumer-side occurrent that invokes it), and its
    gmeow:producesResult MUST point at a gmeow:TopologyResult wrapper, never directly at a
    raw math: object (gmeow:ResultObjectConflation).

    Usage:
        >>> from gmeow_models.semantic_topology import TopologyComputation
        >>> TopologyComputation.model_config["json_schema_extra"]["curie"]
        'gmeow:TopologyComputation'

    IRI:    https://blackcatinformatics.ca/gmeow/TopologyComputation
    CURIE:  gmeow:TopologyComputation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/TopologyComputation", "curie": "gmeow:TopologyComputation", "definitionDigest": "blake3:b180a015e18c94e0e23385956b40948e0ba3bfbb2c7e16ae2ac73574edaa85b8", "iri": "https://blackcatinformatics.ca/gmeow/TopologyComputation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")

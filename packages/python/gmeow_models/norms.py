"""GMEOW models — slice GMEOW Norms Module.

Slice IRI:  https://blackcatinformatics.ca/gmeow/slices/norms
Tier:       extension
DOI:        —

GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
"""
from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import ConfigDict, Field

from ._base import ConfiguredBaseModel


class AuthorityLevelEnum(StrEnum):
    authorityAbsolute = "gmeow:authorityAbsolute"
    authorityConditional = "gmeow:authorityConditional"
    authorityHigh = "gmeow:authorityHigh"
    authorityMedium = "gmeow:authorityMedium"


class CitationIntentEnum(StrEnum):
    intentBridgedByReference = "gmeow:intentBridgedByReference"
    intentCitesAsDataSource = "gmeow:intentCitesAsDataSource"
    intentConformsTo = "gmeow:intentConformsTo"
    intentDerivedFrom = "gmeow:intentDerivedFrom"
    intentDisagreesWith = "gmeow:intentDisagreesWith"
    intentDocuments = "gmeow:intentDocuments"
    intentExtends = "gmeow:intentExtends"
    intentIsInspiredBy = "gmeow:intentIsInspiredBy"
    intentSupports = "gmeow:intentSupports"
    intentUsesMethodIn = "gmeow:intentUsesMethodIn"


class CriterionDomainEnum(StrEnum):
    criterionDomainAesthetic = "gmeow:criterionDomainAesthetic"
    criterionDomainFactual = "gmeow:criterionDomainFactual"
    criterionDomainRelational = "gmeow:criterionDomainRelational"
    criterionDomainSafety = "gmeow:criterionDomainSafety"
    criterionDomainStylistic = "gmeow:criterionDomainStylistic"


class DeonticModalityEnum(StrEnum):
    deonticObligation = "gmeow:deonticObligation"
    deonticPermission = "gmeow:deonticPermission"
    deonticProhibition = "gmeow:deonticProhibition"
    deonticRecommendation = "gmeow:deonticRecommendation"


class EvaluationVerdictEnum(StrEnum):
    verdictHeld = "gmeow:verdictHeld"
    verdictNotHeld = "gmeow:verdictNotHeld"
    verdictUndetermined = "gmeow:verdictUndetermined"


class ExemplarPolarityEnum(StrEnum):
    polarityCautionary = "gmeow:polarityCautionary"
    polarityNegative = "gmeow:polarityNegative"
    polarityPositive = "gmeow:polarityPositive"


class ExpressionLanguageEnum(StrEnum):
    exprLangCedar = "gmeow:exprLangCedar"
    exprLangCel = "gmeow:exprLangCel"
    exprLangProse = "gmeow:exprLangProse"
    exprLangRego = "gmeow:exprLangRego"
    exprLangShacl = "gmeow:exprLangShacl"
    exprLangSparqlAsk = "gmeow:exprLangSparqlAsk"
    exprLangXacml = "gmeow:exprLangXacml"


class GroupOperatorEnum(StrEnum):
    operatorAll = "gmeow:operatorAll"
    operatorAny = "gmeow:operatorAny"
    operatorNone = "gmeow:operatorNone"


class Assessment(ConfiguredBaseModel):
    """Assessment.

    An observation that scores a target against a criterion (or whole rubric) — vantage =
    the judge, human or model: an LLM judge is just a vantage, and two models disagreeing
    are two coexisting cells with no winner (Principle 9). The judge model's identity and
    prompt digest travel as observation provenance. gmeow:assessmentCriterion /
    gmeow:assessmentRubric play the observationMethod ROLE but are deliberately not
    subproperties of it (observationMethod is functional with a QualityValue range;
    Criterion and Rubric are Entities — the claimModality axiom pattern). Scoring-density
    expectations by target granularity reuse kernel gmeow:hasGranularity on the target.

    When to use:
        - Use for a score of a target against a criterion or rubric — an observation whose vantage
          is the judge, human or model; disagreeing judges are coexisting cells with no winner.

    When to avoid:
        - Avoid treating a model's score as ground truth (an LLM judge is just a vantage —
          Principle 9) and avoid omitting a zero score; a zero is a score, not an absence (false
          positives are worse than zeros).

    How to use:
        - Mint one Assessment per criterion (a 21-axis pass is 21 Assessments), naming the target
          with gmeow:assessmentTarget, the criterion/rubric with gmeow:assessmentCriterion /
          gmeow:assessmentRubric, the score with gmeow:assessmentScoreValue, and the judge as
          observation vantage.

    Examples:
        - ex:as1 a gmeow:Assessment ; gmeow:assessmentTarget ex:chapter3 ;
          gmeow:assessmentCriterion ex:agency ; gmeow:assessmentScoreValue 0.9 .

    Usage:
        >>> from gmeow_models.norms import Assessment
        >>> Assessment.model_config["json_schema_extra"]["curie"]
        'gmeow:Assessment'

    IRI:    https://blackcatinformatics.ca/gmeow/Assessment
    CURIE:  gmeow:Assessment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Assessment", "curie": "gmeow:Assessment", "definitionDigest": "blake3:883e201ebd1dd2c5bb721927604ca2960f6ab3a548dd63df55c4f3920627bbe0", "iri": "https://blackcatinformatics.ca/gmeow/Assessment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    assessmentCriterion: list[Criterion] | None = Field(default=None, description="The criterion applied. Functional: a 21-axis scoring pass is 21 Assessments (zeros included — a zero is a score, not an absence). Plays the observationMethod role without the subproperty axiom (claimModality pattern). An assessment names a criterion or a rubric, or both (SHACL).", alias="gmeow:assessmentCriterion")
    assessmentRubric: list[str] | None = Field(default=None, description="Within gmeow:Assessment, values are node references constrained to gmeow:Rubric. The rubric under which this assessment was made — version-pinning context for the score. Functional. Plays the observationMethod role without the subproperty axiom (claimModality pattern).", alias="gmeow:assessmentRubric")
    assessmentScoreValue: float = Field(description="The numeric score, on the scale the criterion or rubric declares. Functional and mandatory (SHACL). A datatype twin of observationResult (which is entity-valued — the claimModality pattern); zeros are scores, never absences (false positives are worse than zeros).", alias="gmeow:assessmentScoreValue")
    assessmentTarget: str = Field(description="What is being scored — a work, an expression, a content segment, a chunk (⊑ observedFeature, the observation-spine bridge idiom). Range intentionally open. Functional: one assessment, one target.", alias="gmeow:assessmentTarget")
    vantage: list[str] = Field(min_length=1, description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")


class ComplianceAssessment(ConfiguredBaseModel):
    """Compliance Assessment.

    An observation that an event complied with or violated a norm — vantage = the assessor.
    The reified form of gmeow:violates / gmeow:complies, for when who-judged,
    on-what-evidence, and how-confidently must be first-class. Two assessors disagreeing are
    two coexisting cells; no global compliance verdict exists (Principle 9).

    When to use:
        - Use the reified form when who-judged, on-what-evidence, or how-confidently an event
          complied or violated must be first-class.

    When to avoid:
        - Avoid it for the ordinary case (flat gmeow:violates / gmeow:complies carry it more
          cheaply) and avoid expecting a single global verdict — disagreeing assessors coexist
          (Principle 9).

    How to use:
        - Mint the assessment, name the event with gmeow:assessedEvent, the norm with
          gmeow:assessedNorm, the verdict with gmeow:complianceVerdict, and carry the assessor as
          the observation vantage.

    Examples:
        - ex:ca1 a gmeow:ComplianceAssessment ; gmeow:assessedEvent ex:leakEvent ;
          gmeow:assessedNorm ex:n1 ; gmeow:complianceVerdict gmeow:verdictNotHeld .

    Usage:
        >>> from gmeow_models.norms import ComplianceAssessment
        >>> ComplianceAssessment.model_config["json_schema_extra"]["curie"]
        'gmeow:ComplianceAssessment'

    IRI:    https://blackcatinformatics.ca/gmeow/ComplianceAssessment
    CURIE:  gmeow:ComplianceAssessment

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ComplianceAssessment", "curie": "gmeow:ComplianceAssessment", "definitionDigest": "blake3:455539876a2e2f0763990678f1175045cbb23291d539cedb9de710f374d68b9d", "iri": "https://blackcatinformatics.ca/gmeow/ComplianceAssessment"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    assessedEvent: list[str] = Field(min_length=1, description="The event whose compliance is assessed (⊑ observedFeature). Functional.", alias="gmeow:assessedEvent")
    assessedNorm: list[str] = Field(min_length=1, description="The norm against which the event is assessed. Functional: one assessment, one norm.", alias="gmeow:assessedNorm")
    complianceVerdict: list[EvaluationVerdictEnum] = Field(description="The assessor's verdict, reusing the EvaluationVerdict vocabulary: held = compliant, not held = violative, undetermined = undetermined. Functional and mandatory (SHACL).", alias="gmeow:complianceVerdict")
    vantage: list[str] = Field(min_length=1, description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")


class Condition(ConfiguredBaseModel):
    """Condition.

    A describable circumstance — the trigger of a conditional norm, the activation context
    of a persona, the antecedent of a causal link (risk design). The canonical form is PROSE
    (gmeow:conditionText, mandatory by SHACL); machine formalizations attach via
    gmeow:formalizedAs, and each formalization is a claim of equivalence to the prose,
    challengeable through the statement layer. Whether the condition held at some moment is
    a gmeow:ConditionEvaluation — a vantage-indexed Observation — never a graph entailment.
    The ontology stores conditions; it never executes them (Principle 12).

    When to use:
        - Use for a describable circumstance — a norm's trigger, a persona's activation context, a
          causal antecedent — whose canonical form is prose and whose holding is observed, never
          entailed.

    When to avoid:
        - Avoid executing or evaluating a condition during validation (the ontology stores
          conditions, never runs them — Principle 12) and avoid treating an attached formalization
          as the canonical form; the prose is canonical.

    How to use:
        - Mint the condition with mandatory gmeow:conditionText prose, attach machine
          formalizations via gmeow:formalizedAs (each a challengeable equivalence claim), and
          record whether it held as a gmeow:ConditionEvaluation.

    Examples:
        - ex:c1 a gmeow:Condition ; gmeow:conditionText "the data subject has requested erasure" .

    Usage:
        >>> from gmeow_models.norms import Condition
        >>> Condition.model_config["json_schema_extra"]["curie"]
        'gmeow:Condition'

    IRI:    https://blackcatinformatics.ca/gmeow/Condition
    CURIE:  gmeow:Condition

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Condition", "curie": "gmeow:Condition", "definitionDigest": "blake3:aa17ceb619458b0f4562031b352df9422d8b1d06aabf68465e751aed9b2f78c4", "iri": "https://blackcatinformatics.ca/gmeow/Condition"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    conditionText: list[str] = Field(min_length=1, description="The natural-language statement of the condition — the canonical form, always present (SHACL). Formalizations approximate the prose, not the other way around.", alias="gmeow:conditionText")


class ConditionEvaluation(ConfiguredBaseModel):
    """Condition Evaluation.

    An observation that a condition held, did not hold, or could not be determined at some
    moment — vantage = the evaluator (an agent, a solver run, a model). The verdict is a
    recorded observation, NEVER a graph entailment: two evaluators returning different
    verdicts for one condition are two coexisting cells (Principle 9). The evaluation run
    itself attaches via gmeow:observationEvent; the evaluated condition rides
    gmeow:evaluatedCondition ⊑ gmeow:observedFeature.

    When to use:
        - Use to record that an evaluator found a condition held, did not hold, or was
          undetermined at some moment — a vantage-indexed observation, never a graph entailment.

    When to avoid:
        - Avoid treating a verdict as global truth (two evaluators may return different verdicts
          that coexist — Principle 9) and avoid having validation tooling execute the condition to
          produce it (Principle 12).

    How to use:
        - Mint the evaluation, name the condition with gmeow:evaluatedCondition, the outcome with
          gmeow:evaluationVerdict, and carry the evaluator as the observation vantage; group
          members get their own evaluations.

    Examples:
        - ex:ev1 a gmeow:ConditionEvaluation ; gmeow:evaluatedCondition ex:c1 ;
          gmeow:evaluationVerdict gmeow:verdictHeld .

    Usage:
        >>> from gmeow_models.norms import ConditionEvaluation
        >>> ConditionEvaluation.model_config["json_schema_extra"]["curie"]
        'gmeow:ConditionEvaluation'

    IRI:    https://blackcatinformatics.ca/gmeow/ConditionEvaluation
    CURIE:  gmeow:ConditionEvaluation

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConditionEvaluation", "curie": "gmeow:ConditionEvaluation", "definitionDigest": "blake3:fea1e40ff68ec70204ba1bbf1b009aa445d6cecd0a8832f36ed51c8f81a6b325", "iri": "https://blackcatinformatics.ca/gmeow/ConditionEvaluation"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    evaluatedCondition: Any = Field(description="The condition this evaluation reports on (⊑ observedFeature — the observation-spine bridge idiom). Functional: one evaluation, one condition; group members get their own evaluations.", alias="gmeow:evaluatedCondition")
    evaluationVerdict: Any = Field(description="The verdict of this evaluation. Functional and mandatory (SHACL). The claimModality pattern: equivalent in role to observationResult, separate in axiom because QualityValue ⟂ Entity.", alias="gmeow:evaluationVerdict")
    vantage: list[Any] = Field(min_length=1, description="The agent or standpoint from which the observation is made — the reified-object-property counterpart of gmeow:accordingTo. Semantically, gmeow:vantage ⊑ gmeow:accordingTo: when an annotated statement is promoted to a reified Observation, its gmeow:accordingTo becomes the gmeow:vantage of the relator. The agent in the vantage role — an observer, a sensor, a perceiver — IS a standpoint (Principle 9): no frame is privileged, and every vantage is a co-equal facet from which the claim is held. Range is gmeow:Entity (encompassing both gmeow:Agent and gmeow:Standpoint) because a vantage may be a bare agent (person, organization, software agent, sensor) or a gmeow:Standpoint individual when the frame needs its own identity. Non-functional: joint observations (a reading co-authored by two agencies) are valid.", alias="gmeow:vantage")


class ConditionExpression(ConfiguredBaseModel):
    """Condition Expression.

    A machine formalization of a condition in a named expression language. STORED, NEVER
    EXECUTED: no CI step, test, or tool may evaluate a ConditionExpression against the graph
    as part of validation (Principle 12 made operational). Attaching one to a Condition
    (gmeow:formalizedAs) is a claim that the expression captures the prose — a claim like
    any other, carrying confidence and vantage through the statement layer.

    When to use:
        - Use to carry a machine formalization of a condition in a named expression language —
          SPARQL ASK, CEL, Rego, Cedar, XACML, SHACL — as a stored, challengeable equivalence
          claim.

    When to avoid:
        - Avoid evaluating it against the graph during validation (stored, never executed —
          Principle 12) and avoid treating it as the canonical condition; the prose
          gmeow:conditionText is canonical.

    How to use:
        - Mint the expression with gmeow:expressionText and gmeow:expressionLanguage, attach it to
          a gmeow:Condition via gmeow:formalizedAs, and carry the equivalence claim's confidence
          and vantage on the statement layer.

    Examples:
        - ex:e1 a gmeow:ConditionExpression ; gmeow:expressionLanguage gmeow:exprLangSparqlAsk ;
          gmeow:expressionText "ASK { ?s ex:requestedErasure true }" .

    Usage:
        >>> from gmeow_models.norms import ConditionExpression
        >>> ConditionExpression.model_config["json_schema_extra"]["curie"]
        'gmeow:ConditionExpression'

    IRI:    https://blackcatinformatics.ca/gmeow/ConditionExpression
    CURIE:  gmeow:ConditionExpression

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConditionExpression", "curie": "gmeow:ConditionExpression", "definitionDigest": "blake3:e244f21e496288f8403f2866cb613e66ad59f5b624d1de21edb9163a754bc11e", "iri": "https://blackcatinformatics.ca/gmeow/ConditionExpression"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    expressionLanguage: ExpressionLanguageEnum = Field(description="The language of this expression. Functional and mandatory (SHACL).", alias="gmeow:expressionLanguage")
    expressionText: str = Field(description="The expression source, verbatim, in the named language. Functional and mandatory (SHACL).", alias="gmeow:expressionText")


class ConditionGroup(Condition):
    """Condition Group.

    A composite condition — all-of, any-of, or none-of its members (arbitrary and/or/not
    trees by nesting groups). Requires an operator and at least two members (SHACL). A group
    is itself a Condition: it carries its own prose summary and can be evaluated,
    formalized, and reused like any other.

    When to use:
        - Use to combine member conditions with explicit and/or/not logic — nest groups for
          arbitrary boolean trees.

    When to avoid:
        - Avoid it for a single condition (use a plain gmeow:Condition) and avoid an under-filled
          group — it requires an operator and at least two members (SHACL).

    How to use:
        - Mint the group, set gmeow:groupOperator, attach at least two gmeow:groupMember
          conditions, and give it its own gmeow:conditionText summary; it behaves as a Condition
          everywhere.

    Examples:
        - ex:cg1 a gmeow:ConditionGroup ; gmeow:groupOperator gmeow:operatorAll ;
          gmeow:groupMember ex:c1 , ex:c2 .

    Usage:
        >>> from gmeow_models.norms import ConditionGroup
        >>> ConditionGroup.model_config["json_schema_extra"]["curie"]
        'gmeow:ConditionGroup'

    IRI:    https://blackcatinformatics.ca/gmeow/ConditionGroup
    CURIE:  gmeow:ConditionGroup

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConditionGroup", "curie": "gmeow:ConditionGroup", "definitionDigest": "blake3:a11376d86750da9faca6395e7b9a707257a74622a9d4f85924eeb022c4664229", "iri": "https://blackcatinformatics.ca/gmeow/ConditionGroup"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    groupMember: list[str] = Field(min_length=2, description="A member condition of this group. At least two (SHACL); nest groups for arbitrary trees.", alias="gmeow:groupMember")
    groupOperator: GroupOperatorEnum = Field(description="The combination operator of this group. Functional and mandatory (SHACL).", alias="gmeow:groupOperator")


class ConditionParameter(ConfiguredBaseModel):
    """Condition Parameter.

    A named binding that instantiates a condition template — 'threshold = 0.8',
    'jurisdiction = Canada'. Lets one Condition serve many norms with different bindings.

    When to use:
        - Use to bind a named slot of a condition template so one Condition can serve many norms
          with different values — 'threshold = 0.8', 'jurisdiction = Canada'.

    When to avoid:
        - Avoid duplicating a whole condition per binding (parameterize it instead) and avoid
          carrying both a literal and an entity binding on one parameter (it carries exactly one —
          SHACL).

    How to use:
        - Mint the parameter with gmeow:parameterName and exactly one of gmeow:parameterValue /
          gmeow:parameterEntity, then attach it to the condition via gmeow:conditionParameter.

    Examples:
        - ex:p1 a gmeow:ConditionParameter ; gmeow:parameterName "threshold" ;
          gmeow:parameterValue "0.8" .

    Usage:
        >>> from gmeow_models.norms import ConditionParameter
        >>> ConditionParameter.model_config["json_schema_extra"]["curie"]
        'gmeow:ConditionParameter'

    IRI:    https://blackcatinformatics.ca/gmeow/ConditionParameter
    CURIE:  gmeow:ConditionParameter
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/conditionparameter-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ConditionParameter", "curie": "gmeow:ConditionParameter", "definitionDigest": "blake3:9c99ce754968e74e38c097af447ab7166dd2d0a759dfb7b33e8cac5c04f143a9", "iri": "https://blackcatinformatics.ca/gmeow/ConditionParameter"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    parameterName: Any = Field(description="The parameter's name as used in the condition text or expressions. Functional and mandatory (SHACL).", alias="gmeow:parameterName")


class Criterion(ConfiguredBaseModel):
    """Criterion.

    One evaluative axis of a rubric — what is being judged, with NAMED poles: a reward pole
    and a penalty pole that are small information objects with their own labels and
    definitions ('Power from the Bottom' vs 'Passive Victimhood'), not just numeric
    extremes. Anchors (gmeow:hasScoreAnchor) pin score ranges to meanings and exemplars.

    When to use:
        - Use for one evaluative axis of a rubric — what is being judged, with named reward and
          penalty poles and calibration anchors.

    When to avoid:
        - Avoid reducing poles to bare numeric extremes (poles are named information objects) and
          avoid using a criterion outside a rubric — attach it via gmeow:hasCriterion.

    How to use:
        - Mint the criterion, give it mandatory distinct gmeow:rewardPole / gmeow:penaltyPole, pin
          score ranges with gmeow:hasScoreAnchor, classify it with gmeow:criterionDomain, and
          optionally override the rubric scale with gmeow:usesScale.

    Examples:
        - ex:agency a gmeow:Criterion ; gmeow:rewardPole ex:powerFromBottom ; gmeow:penaltyPole
          ex:passiveVictimhood .

    Usage:
        >>> from gmeow_models.norms import Criterion
        >>> Criterion.model_config["json_schema_extra"]["curie"]
        'gmeow:Criterion'

    IRI:    https://blackcatinformatics.ca/gmeow/Criterion
    CURIE:  gmeow:Criterion

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Criterion", "curie": "gmeow:Criterion", "definitionDigest": "blake3:bafbf9ef1403399a28112f3635f11cdafd976642ddababc47db78fb240f6abf3", "iri": "https://blackcatinformatics.ca/gmeow/Criterion"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    penaltyPole: list[str] = Field(min_length=1, description="Within gmeow:Criterion, values are node references constrained to gmeow:CriterionPole. The named pole this criterion penalizes. Functional and mandatory (SHACL — no poles is no axis); must differ from the reward pole (the logic:criterionPoleDistinctness axiom, projected to SHACL).", alias="gmeow:penaltyPole")
    rewardPole: list[str] = Field(min_length=1, description="Within gmeow:Criterion, values are node references constrained to gmeow:CriterionPole. The named pole this criterion rewards. Functional and mandatory (SHACL — no poles is no axis); must differ from the penalty pole (the logic:criterionPoleDistinctness axiom, projected to SHACL).", alias="gmeow:rewardPole")


class Exemplar(ConfiguredBaseModel):
    """Exemplar.

    A citation act that holds something up as an example — positively, negatively, or as a
    cautionary boundary case. Pins into the cited work via the citation Selector machinery
    (page, quote, locator) AND/OR names an entity subject (gmeow:exemplarSubject — a
    character's conduct across a whole work, foundation-corpus coordination): at least one
    of the two (SHACL). The kernel aboutness axis carries the phase-gate: an exemplar whose
    span DESCRIBES trust is source material; one whose span ENACTS it is embodiment — same
    selector, different gmeow:hasAboutness, different anchored range. CiTO alignment arrives
    free through CitationAct.

    When to use:
        - Use for a citation act that holds something up as an example — positive, negative, or
          cautionary — pinned into the cited work by selector and/or naming an entity subject.

    When to avoid:
        - Avoid an exemplar with neither a selector nor a subject (at least one is required —
          SHACL) and avoid conflating a span that DESCRIBES a quality with one that ENACTS it —
          the kernel aboutness axis carries that phase-gate.

    How to use:
        - Mint the exemplar (⊑ CitationAct), set its direction with gmeow:exemplarPolarity, pin it
          via the citation Selector and/or gmeow:exemplarSubject, set gmeow:hasAboutness for
          describe-vs-enact, and add gmeow:exemplarRationale; CiTO comes free.

    Examples:
        - ex:ex1 a gmeow:Exemplar ; gmeow:exemplarPolarity gmeow:polarityPositive ;
          gmeow:exemplarSubject ex:hari .

    Usage:
        >>> from gmeow_models.norms import Exemplar
        >>> Exemplar.model_config["json_schema_extra"]["curie"]
        'gmeow:Exemplar'

    IRI:    https://blackcatinformatics.ca/gmeow/Exemplar
    CURIE:  gmeow:Exemplar

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Exemplar", "curie": "gmeow:Exemplar", "definitionDigest": "blake3:6de758542ab923123b6b9eec955a19904c7f1daf5bc530b7416294fdbdab6b0c", "iri": "https://blackcatinformatics.ca/gmeow/Exemplar"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    citationIntent: CitationIntentEnum = Field(description="The intent with which the citation is made — a value from the open gmeow:CitationIntent vocabulary. Functional per relator: one intent per CitationAct.", alias="gmeow:citationIntent")
    citedEntity: str = Field(description="The creative work that is cited — a Work, Expression, Manifestation, or Item. Functional per relator: one cited entity per CitationAct.", alias="gmeow:citedEntity")
    citingEntity: str = Field(description="The entity that makes the citation — a claim, a work, a module, a dataset. Functional per relator: one citing entity per CitationAct.", alias="gmeow:citingEntity")
    exemplarPolarity: ExemplarPolarityEnum = Field(description="The exemplar's direction. Functional and mandatory (SHACL): an exemplar that cannot say whether to emulate or avoid is just a citation.", alias="gmeow:exemplarPolarity")


class Norm(ConfiguredBaseModel):
    """Norm.

    A prescription existing by social convention — an obligation, prohibition, permission,
    or recommendation issued by an agent or standpoint over some conduct. A norm asserts
    nothing about the world: it records that an issuer prescribes. Whether anything complies
    is a vantage-indexed ComplianceAssessment; what wins when norms conflict is solver work
    over the recorded precedence claims (Principle 12). Every modality-bearing norm must
    name its issuer (SHACL): there is no ought, only ought-according-to (Principles 1, 9).
    GROUNDING: a gufo:Category at Entity level — the IntentionalMoment precedent — because
    the rights graft places gmeow:Rule (⊑ gufo:Relator) beneath it while plain standalone
    norms are object-like social descriptions; committing Norm to gufo:Object OR
    gufo:Relator would make one branch inconsistent (Object ⟂ Aspect), and a Kind would
    stack identities under the rights trio (OntoUML MixIden). The social-convention reading
    lives on the concrete classes: NormativeSystem ⊑ SocialObject, the trio ⊑ Relator.

    When to use:
        - Use for any issued prescription — obligation, prohibition, permission, recommendation —
          over some conduct, when the prescription names its issuer and asserts nothing about
          whether anything in fact complies.

    When to avoid:
        - Avoid asserting a norm as a global ought (every modality-bearing norm must name
          gmeow:normIssuer — there is no ought, only ought-according-to) and avoid using it for
          whether conduct complied (that is a vantage-indexed gmeow:ComplianceAssessment).

    How to use:
        - Mint the norm, fix its force with gmeow:deonticModality, name its issuer with
          gmeow:normIssuer, point gmeow:prescribedConduct at the governed conduct, and bind
          bearers with gmeow:normBearer (absent = everyone in the issuer's scope).

    Examples:
        - ex:n1 a gmeow:Norm ; gmeow:deonticModality gmeow:deonticObligation ; gmeow:normIssuer
          ex:gdpr ; gmeow:prescribedConduct ex:eraseOnRequest .

    Usage:
        >>> from gmeow_models.norms import Norm
        >>> Norm.model_config["json_schema_extra"]["curie"]
        'gmeow:Norm'

    IRI:    https://blackcatinformatics.ca/gmeow/Norm
    CURIE:  gmeow:Norm
    Docs:   https://blackcatinformatics.ca/gmeow/documentation/term/norm-class

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Norm", "curie": "gmeow:Norm", "definitionDigest": "blake3:15c640e6cb7d3ed034c84333ca35a7635feb373c32a3535201d8d26b7ca006b9", "iri": "https://blackcatinformatics.ca/gmeow/Norm"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    deonticModality: Any | None = Field(default=None, description="The deontic force of this norm. Functional: one norm, one force — a rule that both permits and obliges is two norms. A norm carrying a modality must also carry an issuer (SHACL): no anonymous oughts.", alias="gmeow:deonticModality")


class Persona(ConfiguredBaseModel):
    """Persona.

    A reified expression policy of one agent — the relator binding {bearer} × {register(s)}
    × {expressed norms} × {activation context}. PRIMARY and PRIVATE are two Personas of one
    agent; 'Dr. Chen at the clinic' and 'Mom at home' are two of another. No primaryPersona
    exists or ever will (Principle 9): personas are co-equal, context-activated, and
    withdrawn by suppression, never deletion (Principle 10). Register-switching is not
    deception — divergence of held and projected standpoints is the deception module's
    territory, a documented boundary with no axiom coupling.

    When to use:
        - Use for a reified expression policy of one agent — the binding of bearer, register(s),
          expressed norms, and activation context; PRIMARY and PRIVATE are two co-equal personas
          of one agent.

    When to avoid:
        - Avoid a primaryPersona or any privileged persona (personas are co-equal — Principle 9),
          avoid deleting a withdrawn one (suppress with gmeow:displayable false — Principle 10),
          and avoid reading register-switching as deception (a documented boundary, never an
          axiom).

    How to use:
        - Mint the persona, name its bearer with gmeow:personaBearer, its register(s) with
          gmeow:personaRegister, its activation with gmeow:activatedIn, and the norms it expresses
          with gmeow:expressesNorm; order conflicting personas via gmeow:overrides on their
          activation norms.

    Examples:
        - ex:drChen a gmeow:Persona ; gmeow:personaBearer ex:chen ; gmeow:personaRegister
          gmeow:registerClinical ; gmeow:activatedIn ex:atClinic .

    Usage:
        >>> from gmeow_models.norms import Persona
        >>> Persona.model_config["json_schema_extra"]["curie"]
        'gmeow:Persona'

    IRI:    https://blackcatinformatics.ca/gmeow/Persona
    CURIE:  gmeow:Persona

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/Persona", "curie": "gmeow:Persona", "definitionDigest": "blake3:794f63782c6dbcd27c3c1e3ba01a7c2a15aaccde24305bfb0198a6ef7a860bd1", "iri": "https://blackcatinformatics.ca/gmeow/Persona"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    expressesNorm: list[Norm] | None = Field(default=None, description="A norm this persona expresses — the 'same principia, different registers' invariant made queryable: whether all personas of one agent express the same authority-absolute norm set is a COMPETENCY QUERY, deliberately not a shape, because divergence is legal (Principle 9) and the query makes it visible rather than forbidden. NOT functional.", alias="gmeow:expressesNorm")
    personaBearer: Any = Field(description="The agent whose expression policy this persona is. Functional and mandatory (SHACL): one persona, one bearer; an agent bears many co-equal personas.", alias="gmeow:personaBearer")
    personaRegister: list[Any] = Field(min_length=1, description="A register this persona expresses in — drawn from the names-core gmeow:Register spine (a name register IS a register; persona and address registers share one vocabulary). NOT functional, at least one (SHACL): a persona may span registers (public-formal sliding to public-wry), and the vocabulary is OPEN (sh:nodeKind sh:IRI, never sh:in — Principle 9).", alias="gmeow:personaRegister")


class PrecedenceTenure(ConfiguredBaseModel):
    """Precedence Tenure.

    The reified, time-scoped fact that one norm took precedence over another within a
    normative system over an interval — 'Tier 2 overrode X until v3.5' made sayable. The
    StandpointTenure idiom (Principle 10): carries its interval via gmeow:duringInterval
    (temporal module); a withdrawn precedence is kept with gmeow:displayable false, never
    deleted. Use only when adoption / revision of the precedence is itself worth modelling;
    the common case is flat gmeow:overrides.

    When to use:
        - Use the reified form when the precedence held over an interval — its adoption, revision,
          or withdrawal — is itself the fact of interest, e.g. 'Tier 2 overrode X until v3.5'.

    When to avoid:
        - Avoid it for the ordinary case (flat gmeow:overrides carries pairwise precedence more
          cheaply) and avoid deleting a withdrawn precedence — keep it with gmeow:displayable
          false (Principle 10).

    How to use:
        - Mint the tenure, name the prevailing and yielding norms with
          gmeow:precedenceHigher/precedenceLower, scope it with gmeow:precedenceScope, and carry
          its interval via the inherited gmeow:duringInterval.

    Examples:
        - ex:pt1 a gmeow:PrecedenceTenure ; gmeow:precedenceHigher ex:tier2 ;
          gmeow:precedenceLower ex:xN ; gmeow:precedenceScope ex:gdpr .

    Usage:
        >>> from gmeow_models.norms import PrecedenceTenure
        >>> PrecedenceTenure.model_config["json_schema_extra"]["curie"]
        'gmeow:PrecedenceTenure'

    IRI:    https://blackcatinformatics.ca/gmeow/PrecedenceTenure
    CURIE:  gmeow:PrecedenceTenure

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/PrecedenceTenure", "curie": "gmeow:PrecedenceTenure", "definitionDigest": "blake3:86d60886091d98e03e7278d2408b8d7d77e3f4f3f9e2d8d0637716001066da24", "iri": "https://blackcatinformatics.ca/gmeow/PrecedenceTenure"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    precedenceHigher: list[Norm] = Field(min_length=1, description="The norm that prevails during this tenure. Functional.", alias="gmeow:precedenceHigher")
    precedenceLower: list[Norm] = Field(min_length=1, description="The norm that yields during this tenure. Functional, and distinct from precedenceHigher (the logic:precedenceTenureDistinctness axiom, projected to SHACL).", alias="gmeow:precedenceLower")
    precedenceScope: list[str] = Field(min_length=1, description="Within gmeow:PrecedenceTenure, values are node references constrained to gmeow:NormativeSystem. The normative system within which this precedence holds. Functional and mandatory (SHACL): precedence is always scoped — a system orders its own norms; it cannot order the world's.", alias="gmeow:precedenceScope")


class ScoreAnchor(ConfiguredBaseModel):
    """Score Anchor.

    A pinned score range with its meaning and exemplars — 'range 0.8–1.0 means the text
    embodies the covenant, as in THIS passage'. Anchors are the rubric's calibration
    content; interpolation between anchors is solver work (Principle 12).

    When to use:
        - Use to pin a score range to its meaning and exemplars — the rubric's calibration content
          that anchors what a number means.

    When to avoid:
        - Avoid encoding interpolation between anchors in triples (that is solver work — Principle
          12) and avoid an anchor without a meaning (an anchor without gmeow:anchorMeaning is just
          an interval).

    How to use:
        - Mint the anchor with gmeow:anchorRangeMin / gmeow:anchorRangeMax and a mandatory
          gmeow:anchorMeaning, attach exemplars via gmeow:anchorExemplar, and link it to a
          criterion with gmeow:hasScoreAnchor.

    Examples:
        - ex:a1 a gmeow:ScoreAnchor ; gmeow:anchorRangeMin 0.8 ; gmeow:anchorRangeMax 1.0 ;
          gmeow:anchorMeaning "embodies the covenant"@en .

    Usage:
        >>> from gmeow_models.norms import ScoreAnchor
        >>> ScoreAnchor.model_config["json_schema_extra"]["curie"]
        'gmeow:ScoreAnchor'

    IRI:    https://blackcatinformatics.ca/gmeow/ScoreAnchor
    CURIE:  gmeow:ScoreAnchor

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ScoreAnchor", "curie": "gmeow:ScoreAnchor", "definitionDigest": "blake3:cca80c6c21f098971d585d1be5345e1d5d07ea224c9e4c414701d7b8bf57af8d", "iri": "https://blackcatinformatics.ca/gmeow/ScoreAnchor"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    anchorMeaning: list[Any] = Field(min_length=1, description="What scores in this range MEAN — the calibration prose. NOT functional and range-open (localizable-prose convention): localizable prose may carry one meaning per language tag (sh:uniqueLang, SHACL) — a French and an English calibration coexist; two same-language meanings do not. Mandatory: an anchor without a meaning is just an interval.", alias="gmeow:anchorMeaning")
    anchorRangeMax: float = Field(description="The anchored range's upper bound. Functional.", alias="gmeow:anchorRangeMax")
    anchorRangeMin: float = Field(description="The anchored range's lower bound. Functional; must not exceed the upper bound (SHACL).", alias="gmeow:anchorRangeMin")


class ScoreScale(ConfiguredBaseModel):
    """Score Scale.

    A numeric scale for assessments — minimum, maximum, optional step. Scale arithmetic
    (normalization, conversion between scales) is solver work (Principle 12).

    When to use:
        - Use for a numeric scoring scale — its minimum, maximum, and optional step — that a
          rubric or criterion declares for its assessments.

    When to avoid:
        - Avoid encoding normalization or scale-conversion arithmetic in triples (that is solver
          work — Principle 12).

    How to use:
        - Mint the scale with gmeow:scaleMin / gmeow:scaleMax (and gmeow:scaleStep when discrete),
          then attach it via gmeow:usesScale at the rubric level (default) or criterion level
          (override).

    Examples:
        - ex:zeroToOne a gmeow:ScoreScale ; gmeow:scaleMin 0.0 ; gmeow:scaleMax 1.0 .

    Usage:
        >>> from gmeow_models.norms import ScoreScale
        >>> ScoreScale.model_config["json_schema_extra"]["curie"]
        'gmeow:ScoreScale'

    IRI:    https://blackcatinformatics.ca/gmeow/ScoreScale
    CURIE:  gmeow:ScoreScale

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/ScoreScale", "curie": "gmeow:ScoreScale", "definitionDigest": "blake3:15a1714b1e67514d41fe7a901bc236305e0ee0af231c0a7fc532c1044ceafb4f", "iri": "https://blackcatinformatics.ca/gmeow/ScoreScale"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    scaleMax: float = Field(description="The scale's maximum value. Functional and mandatory; must exceed the minimum (SHACL).", alias="gmeow:scaleMax")
    scaleMin: float = Field(description="The scale's minimum value. Functional and mandatory; must be below the maximum (SHACL).", alias="gmeow:scaleMin")
    scaleStep: float | None = Field(default=None, description="The scale's granularity step, when discrete. Optional; absent means continuous.", alias="gmeow:scaleStep")


class StyleGuide(ConfiguredBaseModel):
    """Style Guide.

    The voice payload of a persona or register — prose whose REGISTER IS THE CONTENT. The
    prose-voice doctrine (gap analysis): voice documents attach byte-perfect via
    gmeow:voiceExemplifiedBy (content-digested Documents carrying gmeow:hasAboutness
    gmeow:aboutnessEnacts — the document does not describe the voice, it IS the voice),
    never shredded into pseudo-quantified style triples. Style dimensions, when stated, are
    prose.

    When to use:
        - Use for the voice payload of a persona or register — prose whose register IS the
          content, exemplified byte-perfect by enacting documents.

    When to avoid:
        - Avoid shredding voice into pseudo-quantified style triples (style dimensions, when
          stated, are prose) and avoid an exemplar document that merely describes the voice — it
          must enact it (gmeow:aboutnessEnacts).

    How to use:
        - Mint the guide, name what it voices with gmeow:styleGuideFor, and attach byte-perfect
          enacting documents via gmeow:voiceExemplifiedBy (each carrying gmeow:contentDigest and
          gmeow:hasAboutness gmeow:aboutnessEnacts).

    Examples:
        - ex:clinicalVoice a gmeow:StyleGuide ; gmeow:styleGuideFor ex:drChen ;
          gmeow:voiceExemplifiedBy ex:sampleNote .

    Usage:
        >>> from gmeow_models.norms import StyleGuide
        >>> StyleGuide.model_config["json_schema_extra"]["curie"]
        'gmeow:StyleGuide'

    IRI:    https://blackcatinformatics.ca/gmeow/StyleGuide
    CURIE:  gmeow:StyleGuide

    GENERATED by the gmeow pydantic emitter — DO NOT EDIT.
    """

    model_config = ConfigDict(
        extra="allow",
        json_schema_extra={"$id": "https://blackcatinformatics.ca/gmeow/StyleGuide", "curie": "gmeow:StyleGuide", "definitionDigest": "blake3:6e07ab878da6a8eea109a64d2d593776cb891501e860421964ae177ca32b6228", "iri": "https://blackcatinformatics.ca/gmeow/StyleGuide"},
    )

    annotation: Annotation | None = Field(default=None, alias="@annotation")
    id: str | None = Field(default=None, alias="@id")
    type_: str | list[str] | None = Field(default=None, alias="@type")
    styleGuideFor: list[Any] = Field(min_length=1, description="What this guide voices — a gmeow:Persona or a gmeow:Register value. Range intentionally open across the two. NOT functional (one guide may voice a persona and its register); at least one (SHACL): a style guide for nothing is just a document.", alias="gmeow:styleGuideFor")
